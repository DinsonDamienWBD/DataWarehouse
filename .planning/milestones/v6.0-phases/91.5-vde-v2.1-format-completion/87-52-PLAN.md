---
phase: 91.5-vde-v2.1-format-completion
plan: 87-52
type: execute
wave: 4
depends_on: ["87-65"]
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSModuleField.cs
  - DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSPolicyRecord.cs
  - DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSClass.cs
  - DataWarehouse.SDK/VirtualDiskEngine/QoS/ExtentQoSFlags.cs
autonomous: true

must_haves:
  truths:
    - "QoS class (3-bit, 8 levels) stored in QOS module 4-byte inode field in Module Overflow Block per spec"
    - "QoS Policy Records are 64 bytes each with TenantId, QoSClass, IOPS limits, bandwidth caps, burst budget per spec"
    - "Extent flags bits 6-7 carry 2-bit per-extent NVMe hardware priority class (Background/Normal/High/RealTime)"
    - "Extent flags bits 19-22 carry 4-bit DeadlineTier (0=inherited, 1=10us..0xF=best-effort) gated by QOS module bit 34"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSClass.cs"
      provides: "8-level QoS class enum matching spec (Background=0 through Realtime=7)"
      contains: "enum QoSClass"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSModuleField.cs"
      provides: "4-byte QOS module inode field (TenantId + QoSClass + Reserved) in Module Overflow Block"
      contains: "struct QoSModuleField"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSPolicyRecord.cs"
      provides: "64-byte Policy Vault QoS record with IOPS/bandwidth/burst/quota per spec"
      contains: "struct QoSPolicyRecord"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/QoS/ExtentQoSFlags.cs"
      provides: "Helpers for extent flags bits 6-7 (QoS_Class) and bits 19-22 (DeadlineTier)"
      contains: "class ExtentQoSFlags"
  key_links:
    - from: "QoSModuleField.cs"
      to: "QoSClass"
      via: "Byte 2 of 4-byte field stores QoSClass enum value"
      pattern: "QoSClass"
    - from: "QoSPolicyRecord.cs"
      to: "QoSClass"
      via: "QoSClass byte at offset +0x02 matches module field"
      pattern: "QoSClass"
    - from: "ExtentQoSFlags.cs"
      to: "InodeExtent"
      via: "Extracts/sets bits 6-7 and 19-22 from uint Flags field"
      pattern: "ExtentFlags"
---

<objective>
Implement format-native QoS structures (VOPT-71-73). QoS uses the QOS module (bit 34) with a 4-byte inode field in the Module Overflow Block, plus 64-byte Policy Vault records (type tag 0x0003), plus per-extent flags (bits 6-7 for NVMe priority, bits 19-22 for DeadlineTier).

Purpose: Enable hardware-native I/O scheduling via NVMe SQE ioprio passthrough (3-bit QoS class maps to io_uring ioprio), per-tenant IOPS/bandwidth limits via Policy Vault, and per-extent deadline hints for mixed-criticality workloads.
Output: Four files in `DataWarehouse.SDK/VirtualDiskEngine/QoS/`.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v6.0-VDE-FORMAT-v2.0-SPEC.md (lines 3414-3540: QoS spec)
@DataWarehouse.SDK/VirtualDiskEngine/Format/InodeExtent.cs
@DataWarehouse.SDK/VirtualDiskEngine/Format/ExtentFlags (enum in InodeExtent.cs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: QoS class, module field, policy record, and extent flag helpers</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSClass.cs
    DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSModuleField.cs
    DataWarehouse.SDK/VirtualDiskEngine/QoS/QoSPolicyRecord.cs
    DataWarehouse.SDK/VirtualDiskEngine/QoS/ExtentQoSFlags.cs
  </files>
  <action>
Create directory `DataWarehouse.SDK/VirtualDiskEngine/QoS/` if it does not exist.

**QoSClass.cs** -- enum with `[SdkCompatibility("6.0.0")]`:
```
public enum QoSClass : byte
{
    Background = 0,    // lowest priority, best-effort, throttled
    Idle = 1,          // runs during idle time only
    BelowNormal = 2,   // below default, tolerates latency
    Normal = 3,        // default interactive workload
    AboveNormal = 4,   // latency-sensitive, e.g. web serving
    High = 5,          // time-critical, e.g. database commits
    Critical = 6,      // near-real-time, e.g. OLTP hot path
    Realtime = 7,      // hard real-time, never throttled
}
```
- Also include `NvmeQoSPriority` enum for extent flags bits 6-7: `Background = 0, Normal = 1, High = 2, RealTime = 3`
- Namespace: `DataWarehouse.SDK.VirtualDiskEngine.QoS`

**QoSModuleField.cs** -- readonly struct with `[SdkCompatibility("6.0.0")]`:
- `public const int Size = 4` (matches QOS module's 4-byte inode field in Module Overflow Block)
- Properties: `ushort TenantId`, `QoSClass QoSClass`, `byte Reserved` (always 0)
- Layout per spec: Bytes 0-1 = TenantId (uint16 LE), Byte 2 = QoSClass (uint8), Byte 3 = Reserved (zero)
- `void WriteTo(Span<byte> buffer)` -- writes 4 bytes using BinaryPrimitives
- `static QoSModuleField ReadFrom(ReadOnlySpan<byte> buffer)` -- reads 4 bytes
- IEquatable<QoSModuleField> implementation

**QoSPolicyRecord.cs** -- readonly struct with `[SdkCompatibility("6.0.0")]`:
- `public const int Size = 64` (per spec)
- `public const uint PolicyVaultTypeTag = 0x0003`
- Properties matching spec at offsets:
  - `ushort TenantId` (+0x00)
  - `QoSClass QoSClass` (+0x02)
  - `byte Flags` (+0x03): Bit 0 = ENABLED, Bit 1 = HARD_LIMIT
  - `uint MaxIops` (+0x04, 0=unlimited)
  - `uint MinGuaranteedIops` (+0x08, 0=best-effort)
  - `uint MaxBandwidthMBps` (+0x0C, 0=unlimited)
  - `uint MinGuaranteedBandMBps` (+0x10, 0=best-effort)
  - `uint LatencyDeadlineUs` (+0x14, 0=none)
  - `uint BurstIops` (+0x18)
  - `ushort BurstDurationMs` (+0x1C)
  - `ushort PriorityWeight` (+0x1E)
  - `ulong BytesQuota` (+0x20, 0=unlimited)
  - `ulong BytesUsed` (+0x28)
  // +0x30 16 bytes reserved (zero)
- `bool IsEnabled => (Flags & 0x01) != 0`
- `bool IsHardLimit => (Flags & 0x02) != 0`
- `void WriteTo(Span<byte> buffer)` -- writes exactly 64 bytes using BinaryPrimitives
- `static QoSPolicyRecord ReadFrom(ReadOnlySpan<byte> buffer)` -- reads 64 bytes
- IEquatable<QoSPolicyRecord> implementation

**ExtentQoSFlags.cs** -- static class with `[SdkCompatibility("6.0.0")]`:
- Helper methods for reading/writing QoS-related bits from the 32-bit extent Flags field:
- `static NvmeQoSPriority GetQoSClass(uint flags)` -- extracts bits 6-7: `(NvmeQoSPriority)((flags >> 6) & 0x03)`
- `static uint SetQoSClass(uint flags, NvmeQoSPriority priority)` -- sets bits 6-7: `(flags & ~(0x03u << 6)) | ((uint)priority << 6)`
- `static byte GetDeadlineTier(uint flags)` -- extracts bits 19-22: `(byte)((flags >> 19) & 0x0F)`
- `static uint SetDeadlineTier(uint flags, byte tier)` -- sets bits 19-22: `(flags & ~(0x0Fu << 19)) | ((uint)(tier & 0x0F) << 19)`. Validate tier <= 0x0F.
- `static TimeSpan? GetDeadlineFromTier(byte tier)` -- maps tier to deadline: 0=null (inherited), 1=10us, 2=50us, 3=100us, 4=500us, 5=1ms, 6=5ms, 7=10ms, 8=50ms, 9=100ms, 0xA=500ms, 0xB=1s, 0xC-0xE=null (reserved), 0xF=null (best-effort)
- Namespace: `DataWarehouse.SDK.VirtualDiskEngine.QoS`

Use `System.Buffers.Binary.BinaryPrimitives` for all serialization.
  </action>
  <verify>Build compiles: `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5`</verify>
  <done>QoS module field (4 bytes in Module Overflow Block), 64-byte Policy Vault records, and extent flag bit helpers are fully spec-compliant with correct offsets and bit positions.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore` succeeds with zero errors
- QoSModuleField serializes to exactly 4 bytes (TenantId + QoSClass + Reserved)
- QoSPolicyRecord serializes to exactly 64 bytes per spec layout
- ExtentQoSFlags correctly extracts/sets bits 6-7 and bits 19-22
</verification>

<success_criteria>
- VOPT-71-73 satisfied: Format-native QoS with QOS module inode field, Policy Vault records, extent-level NVMe priority hints, and I/O deadline tiers
</success_criteria>

<output>
After completion, create `.planning/phases/91.5-vde-v2.1-format-completion/87-52-SUMMARY.md`
</output>
