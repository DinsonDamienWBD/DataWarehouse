---
phase: 91.5-vde-v2.1-format-completion
plan: 48
type: execute
wave: 6
depends_on: ["87-45"]
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/Preamble/NativeAotPublishSpec.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Preamble/NativeAotScriptGenerator.cs
autonomous: true

must_haves:
  truths:
    - "NativeAotPublishSpec defines publish configuration targeting <25MB single binary"
    - "NativeAotScriptGenerator produces dotnet publish commands for NativeAOT single-binary output"
    - "Generated scripts support plugin subsetting via composition profiles"
    - "This is a C# helper that generates build scripts, NOT an actual build pipeline"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Preamble/NativeAotPublishSpec.cs"
      provides: "NativeAOT publish specification for DW Runtime preamble binary"
      contains: "class NativeAotPublishSpec"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Preamble/NativeAotScriptGenerator.cs"
      provides: "Generates dotnet publish scripts for NativeAOT single binary"
      contains: "class NativeAotScriptGenerator"
  key_links:
    - from: "NativeAotScriptGenerator.cs"
      to: "NativeAotPublishSpec.cs"
      via: "reads spec to emit dotnet publish command with NativeAOT flags"
      pattern: "NativeAotPublishSpec"
    - from: "NativeAotPublishSpec.cs"
      to: "PreambleHeader"
      via: "output binary size validated against uint32 RuntimeSize limit"
      pattern: "uint.MaxValue"
---

<objective>
Implement NativeAOT DW Runtime packaging helper classes (VOPT-65): C# classes that generate build scripts for a single-binary NativeAOT publish of the DW Runtime <25MB.

Purpose: The preamble bundles the DW Runtime (kernel + selected plugins + CLI) as a NativeAOT single binary for bare-metal boot. These helper classes generate the publish scripts and composition configuration.
Output: 2 files in `DataWarehouse.SDK/VirtualDiskEngine/Preamble/`.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v6.0-VDE-FORMAT-v2.0-SPEC.md
@DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleHeader.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: NativeAotPublishSpec and NativeAotScriptGenerator</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/Preamble/NativeAotPublishSpec.cs
    DataWarehouse.SDK/VirtualDiskEngine/Preamble/NativeAotScriptGenerator.cs
  </files>
  <action>
1. Create `NativeAotPublishSpec.cs` with `[SdkCompatibility("6.0.0")]`:
   - Namespace: `DataWarehouse.SDK.VirtualDiskEngine.Preamble`
   - `sealed class NativeAotPublishSpec`
   - Properties:
     - `string RuntimeIdentifier { get; init; } = "linux-x64"` — target RID
     - `TargetArchitecture Architecture { get; init; } = TargetArchitecture.X86_64`
     - `long MaxBinarySizeBytes { get; init; } = 25 * 1024 * 1024` — 25MB target
     - `string ProjectPath { get; init; }` — path to the DW Runtime .csproj
     - `bool EnableTrimming { get; init; } = true` — IL trimming for size
     - `bool EnableSingleFile { get; init; } = true` — single-file publish
     - `bool EnableCompression { get; init; } = true` — compress embedded assemblies
     - `bool IncludeCli { get; init; } = true` — include CLI commands
     - `bool IncludeGui { get; init; } = false` — exclude GUI for headless boot (default)
     - `IReadOnlyList<string> IncludedPlugins { get; init; }` — explicit plugin list for composition
     - `IReadOnlyList<string> ExcludedPlugins { get; init; }` — plugins to exclude
     - `IReadOnlyDictionary<string, string> AdditionalMsBuildProperties { get; init; }` — extra MSBuild props
   - Static factory methods (matching spec composition profiles):
     - `static NativeAotPublishSpec ServerFull()` — all plugins, CLI, no GUI, linux-x64
     - `static NativeAotPublishSpec EmbeddedMinimal()` — core plugins only (Storage, Encryption, RAID, Filesystem), CLI, no GUI, linux-arm64
     - `static NativeAotPublishSpec RecoveryReadonly()` — read-only subset (Storage, Filesystem, Integrity), CLI only
   - `void Validate()`:
     - Throws if ProjectPath is null/empty
     - Throws if Architecture doesn't match RuntimeIdentifier (x86_64 must be linux-x64, aarch64 must be linux-arm64)
     - Throws if both IncludedPlugins and ExcludedPlugins have entries (mutually exclusive)

2. Create `NativeAotScriptGenerator.cs` with `[SdkCompatibility("6.0.0")]`:
   - `sealed class NativeAotScriptGenerator`
   - `string GeneratePublishScript(NativeAotPublishSpec spec)`:
     Returns a bash script that:
     a. Sets environment variables for NativeAOT cross-compilation if needed
     b. Runs `dotnet publish` with flags:
        - `-r {spec.RuntimeIdentifier}`
        - `-c Release`
        - `-p:PublishAot=true`
        - `-p:PublishSingleFile={spec.EnableSingleFile}`
        - `-p:PublishTrimmed={spec.EnableTrimming}`
        - `-p:EnableCompressionInSingleFile={spec.EnableCompression}`
        - `-p:InvariantGlobalization=true` (reduce size)
        - `-p:DebugType=none` (no PDB in binary)
        - `-p:StripSymbols=true` (strip native symbols)
        - Any AdditionalMsBuildProperties as `-p:Key=Value`
     c. Checks output binary size against MaxBinarySizeBytes, errors if exceeded
     d. Prints SHA256 of output binary
     e. Prints final binary size in MB
   - `string GeneratePluginCompositionFile(NativeAotPublishSpec spec)`:
     Returns an XML string for a Directory.Build.props that conditionally includes/excludes plugin ProjectReferences based on IncludedPlugins/ExcludedPlugins. Uses MSBuild conditions with DefineConstants.
   - `string GenerateDockerPublishScript(NativeAotPublishSpec spec)`:
     Returns a Dockerfile for reproducible NativeAOT builds:
     a. FROM mcr.microsoft.com/dotnet/sdk:9.0 (or latest)
     b. Installs NativeAOT prerequisites (clang, zlib1g-dev)
     c. COPY solution
     d. RUN dotnet publish with all NativeAOT flags
     e. Multi-stage: FROM scratch, COPY only the single binary
   - `IReadOnlyList<string> EstimateSizeBreakdown(NativeAotPublishSpec spec)`:
     Returns estimated size contributions as informational strings:
     - "CoreCLR runtime: ~5-8MB"
     - "SDK core: ~3-5MB"
     - "Per plugin (avg): ~200KB-1MB"
     - "CLI layer: ~1-2MB"
     - "Estimated total: {computed}MB"
  </action>
  <verify>Build compiles: `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5`</verify>
  <done>NativeAotPublishSpec defines composition profiles for server/embedded/recovery. NativeAotScriptGenerator produces dotnet publish scripts, Docker builds, and plugin composition files targeting <25MB single binary.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore` succeeds with zero errors
- Three factory profiles produce distinct publish configurations
- Generated scripts include all NativeAOT MSBuild properties
</verification>

<success_criteria>
- VOPT-65 satisfied: C# helper classes that generate NativeAOT publish scripts for DW Runtime preamble binary
</success_criteria>

<output>
After completion, create `.planning/phases/91.5-vde-v2.1-format-completion/87-48-SUMMARY.md`
</output>
