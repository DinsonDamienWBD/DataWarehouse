---
phase: 91.5-vde-v2.1-format-completion
plan: 87-16
type: execute
wave: 2
depends_on: ["87-65"]
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/Journal/ShardedWriteAheadLog.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Journal/WalShard.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Journal/ShardCommitBarrier.cs
autonomous: true

must_haves:
  truths:
    - "N WAL shards exist for N CPU cores, each with an independent lock-free ring buffer"
    - "Shard 0 acts as the commit barrier referencing all shard LSNs"
    - "Each shard flushes independently to its own region on disk"
    - "Multi-shard recovery replays all shards and reconciles at the barrier"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Journal/WalShard.cs"
      provides: "Single WAL shard with lock-free ring buffer and per-shard flush"
      contains: "class WalShard"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Journal/ShardedWriteAheadLog.cs"
      provides: "IWriteAheadLog implementation distributing entries across N shards by thread affinity"
      contains: "class ShardedWriteAheadLog"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Journal/ShardCommitBarrier.cs"
      provides: "Commit barrier on Shard 0 that references max LSN from all shards"
      contains: "class ShardCommitBarrier"
  key_links:
    - from: "ShardedWriteAheadLog.cs"
      to: "IWriteAheadLog"
      via: "implements IWriteAheadLog distributing to WalShard[] by Environment.CurrentManagedThreadId"
      pattern: "IWriteAheadLog"
    - from: "WalShard.cs"
      to: "IBlockDevice"
      via: "each shard writes to its own region via IBlockDevice"
      pattern: "IBlockDevice"
    - from: "ShardCommitBarrier.cs"
      to: "WalShard"
      via: "barrier collects max LSN from all shards for commit point"
      pattern: "WalShard"
---

<objective>
Implement thread-affinity sharded WAL (VOPT-29) with N WAL shards for N CPU cores, per-core lock-free ring buffers mapped 1:1 to io_uring submission queues, parallel shard flush, and Shard 0 commit barrier referencing all shard LSNs.

Purpose: The single-threaded WAL is a contention bottleneck at high IOPS. Sharding by thread affinity eliminates cache-line bouncing and lock contention, enabling linear scaling with core count. Per spec: "N WAL shards for N CPU cores, lock-free ring buffers, io_uring 1:1 mapping. Commit barrier on Shard 0."
Output: Three files in `DataWarehouse.SDK/VirtualDiskEngine/Journal/`.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v6.0-VDE-FORMAT-v2.0-SPEC.md
@DataWarehouse.SDK/VirtualDiskEngine/Journal/IWriteAheadLog.cs
@DataWarehouse.SDK/VirtualDiskEngine/Journal/WriteAheadLog.cs
@DataWarehouse.SDK/VirtualDiskEngine/Journal/JournalEntry.cs
@DataWarehouse.SDK/VirtualDiskEngine/Journal/WalTransaction.cs
@DataWarehouse.SDK/VirtualDiskEngine/IBlockDevice.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WalShard and ShardCommitBarrier</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/Journal/WalShard.cs
    DataWarehouse.SDK/VirtualDiskEngine/Journal/ShardCommitBarrier.cs
  </files>
  <action>
Create `WalShard` class with `[SdkCompatibility("6.0.0")]`:
- Constructor: `int shardId, IBlockDevice device, long shardStartBlock, long shardBlockCount, int blockSize`
- Lock-free ring buffer: `JournalEntry[]` array (power-of-2 capacity, default 4096 entries)
- `_head` and `_tail` as `long` with `Interlocked.Increment` for lock-free append
- `void Append(JournalEntry entry)`: lock-free enqueue via Interlocked.CompareExchange on head; sets `entry.SequenceNumber` from per-shard monotonic counter; throws if ring full
- `Task FlushAsync(CancellationToken ct)`: serializes all entries between tail and head to shard's disk region; advances tail; uses same binary format as existing WriteAheadLog (JournalEntry.HeaderSize + data)
- `long CurrentLsn { get; }`: latest sequence number in this shard
- `int ShardId { get; }`
- `int PendingEntryCount`: head - tail
- `Task<IReadOnlyList<JournalEntry>> ReplayAsync(CancellationToken ct)`: reads all committed entries from disk for crash recovery

Create `ShardCommitBarrier` class with `[SdkCompatibility("6.0.0")]`:
- Constructor: `WalShard[] shards` (shard 0 is the barrier shard)
- `Task<long> CommitBarrierAsync(long transactionId, CancellationToken ct)`:
  1. Collect `CurrentLsn` from ALL shards
  2. Create barrier entry on Shard 0: `JournalEntryType.Checkpoint` with AfterImage containing packed shard LSNs (8 bytes per shard: `[ShardId:2][Lsn:6]` repeated N times)
  3. Flush Shard 0
  4. Return barrier LSN
- `BarrierRecord` readonly struct: `long BarrierLsn, long[] ShardLsns, long TransactionId`
- `Task<BarrierRecord?> FindLastBarrierAsync(CancellationToken ct)`: scans Shard 0 for latest barrier entry during recovery
- `Task ReconcileShardsAsync(BarrierRecord barrier, WalShard[] shards, CancellationToken ct)`: during recovery, truncates each shard to its barrier-recorded LSN, discarding any entries past the barrier point
  </action>
  <verify>Build compiles: `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5`</verify>
  <done>WalShard provides lock-free ring buffer append with per-shard flush; ShardCommitBarrier collects shard LSNs and writes barrier entry on Shard 0 for atomic multi-shard commit.</done>
</task>

<task type="auto">
  <name>Task 2: ShardedWriteAheadLog</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/Journal/ShardedWriteAheadLog.cs
  </files>
  <action>
Create `ShardedWriteAheadLog` class implementing `IWriteAheadLog, IAsyncDisposable` with `[SdkCompatibility("6.0.0")]`:
- Constructor: `IBlockDevice device, long walStartBlock, long walTotalBlocks, int blockSize, int shardCount = 0`
  - If shardCount == 0, auto-detect: `Math.Min(Environment.ProcessorCount, 32)` per spec recommendation
  - Divide walTotalBlocks evenly across shards (Shard 0 gets extra blocks for barrier entries)
  - Create `WalShard[shardCount]` and `ShardCommitBarrier`
- Thread affinity: `[ThreadStatic] private static int? _assignedShard;` — assign on first call via `Interlocked.Increment` on a global counter mod shardCount. This pins each thread to a shard for the session.
- `Task<WalTransaction> BeginTransactionAsync(CancellationToken ct)`: allocate transaction ID, return WalTransaction referencing this WAL
- `Task AppendEntryAsync(JournalEntry entry, CancellationToken ct)`: route to `_shards[GetShardForCurrentThread()].Append(entry)` — lock-free, no contention across threads
- `Task FlushAsync(CancellationToken ct)`: flush ALL shards in parallel (`Task.WhenAll`), then commit barrier on Shard 0
- `Task<IReadOnlyList<JournalEntry>> ReplayAsync(CancellationToken ct)`:
  1. Find last barrier via ShardCommitBarrier
  2. Reconcile all shards to barrier point
  3. Replay all shards, merge by sequence number
  4. Return merged ordered list
- `Task CheckpointAsync(CancellationToken ct)`: checkpoint all shards, write barrier
- `long CurrentSequenceNumber`: max across all shards
- `long WalSizeBlocks`: total across all shards
- `double WalUtilization`: weighted average across shards
- `bool NeedsRecovery`: true if any shard needs recovery
- `int ShardCount { get; }`: number of active shards
- `WalShardStats[] GetShardStats()`: per-shard utilization for monitoring

Private helper:
- `int GetShardForCurrentThread()`: returns `_assignedShard ??= Interlocked.Increment(ref _nextShardAssignment) % _shardCount`
  </action>
  <verify>Build compiles: `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5`</verify>
  <done>ShardedWriteAheadLog implements IWriteAheadLog with N shards, thread-affinity routing, parallel flush, barrier-based commit, and multi-shard crash recovery.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore` succeeds with zero errors
- ShardedWriteAheadLog implements IWriteAheadLog (drop-in replacement)
- Thread affinity pins each thread to exactly one shard
- Barrier on Shard 0 references LSNs from all shards
</verification>

<success_criteria>
- VOPT-29 satisfied: Thread-affinity sharded WAL with N shards, lock-free ring buffers, Shard 0 commit barrier, multi-shard recovery
</success_criteria>

<output>
After completion, create `.planning/phases/91.5-vde-v2.1-format-completion/87-16-SUMMARY.md`
</output>
