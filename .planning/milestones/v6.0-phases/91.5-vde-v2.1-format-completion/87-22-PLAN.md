---
phase: 91.5-vde-v2.1-format-completion
plan: 87-22
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/Journal/WalSubscriberCursorTable.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Journal/IWalSubscriber.cs
autonomous: true

must_haves:
  truths:
    - "WalSubscriberCursorTable stores 32B cursor entries (128 per 4KB block)"
    - "Each cursor tracks SubscriberID:8, LastEpoch:8, LastSequence:8, Flags:8"
    - "IWalSubscriber provides zero-copy read interface for WAL shard consumption"
    - "Per-subscriber ACL enforcement via flags field"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Journal/WalSubscriberCursorTable.cs"
      provides: "WALS region with subscriber cursor entries and serialization"
      contains: "class WalSubscriberCursorTable"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Journal/IWalSubscriber.cs"
      provides: "Contract for WAL stream subscription with cursor management"
      contains: "interface IWalSubscriber"
  key_links:
    - from: "WalSubscriberCursorTable.cs"
      to: "IWriteAheadLog"
      via: "Cursors reference WAL sequence numbers for stream position"
      pattern: "LastSequence"
    - from: "WalSubscriberCursorTable.cs"
      to: "BlockTypeTags"
      via: "Uses WALS block type tag (0x57414C53)"
      pattern: "BlockTypeTags"
---

<objective>
Implement WAL Streaming Pub/Sub (WALS, Module bit 21, VOPT-35): subscriber cursor table region and WAL subscription interface enabling filesystem-as-Kafka zero-copy event streaming.

Purpose: Expose the thread-sharded WAL to user-space subscribers via mmap/Span<T>. Subscribers read WAL mutations at zero-copy RAM speed, eliminating the need for Kafka/RabbitMQ for local microservices. Each subscriber maintains a cursor tracking position across WAL shards.
Output: Two files in `DataWarehouse.SDK/VirtualDiskEngine/Journal/`.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v6.0-VDE-FORMAT-v2.0-SPEC.md
@DataWarehouse.SDK/VirtualDiskEngine/Journal/IWriteAheadLog.cs
@DataWarehouse.SDK/VirtualDiskEngine/Journal/JournalEntry.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WalSubscriberCursorTable and IWalSubscriber</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/Journal/WalSubscriberCursorTable.cs
    DataWarehouse.SDK/VirtualDiskEngine/Journal/IWalSubscriber.cs
  </files>
  <action>
Create `WalSubscriberCursorTable` class with `[SdkCompatibility("6.0.0")]` in namespace `DataWarehouse.SDK.VirtualDiskEngine.Journal`:

Per spec: cursor entry is 32B: [SubscriberID:8][LastEpoch:8][LastSequence:8][Flags:8]

- `WalSubscriberCursor` readonly struct (32 bytes):
  - `ulong SubscriberId` -- unique subscriber identifier
  - `ulong LastEpoch` -- last consumed MVCC epoch
  - `ulong LastSequence` -- last consumed WAL sequence number
  - `WalSubscriberFlags Flags` -- [Flags] enum: None=0, Active=1, Paused=2, AclRestricted=4, ReadOnly=8, AutoAdvance=16
  - `const int CursorSize = 32`
  - `static void Serialize(in WalSubscriberCursor cursor, Span<byte> buffer)` -- 32B LE
  - `static WalSubscriberCursor Deserialize(ReadOnlySpan<byte> buffer)` -- 32B LE

- `WalSubscriberCursorTable` class:
  - Constructor: `long regionStartBlock, int blockSize`
  - `const uint BlockTypeTag = 0x57414C53` -- "WALS" matching spec
  - `const byte ModuleBitPosition = 21` -- WALS in ModuleManifest
  - `int MaxCursorsPerBlock` -- computed: (blockSize - 16) / 32 = 127 at 4KB
  - `const int MaxSubscribers = 128` -- per spec: 128 subscribers per 4KB cursor block
  - `Dictionary` of cursors keyed by SubscriberId
  - `WalSubscriberCursor? GetCursor(ulong subscriberId)` -- find cursor by ID
  - `bool RegisterSubscriber(ulong subscriberId, WalSubscriberFlags flags)` -- creates new cursor, returns false if at capacity
  - `bool UnregisterSubscriber(ulong subscriberId)` -- removes cursor
  - `void AdvanceCursor(ulong subscriberId, ulong epoch, ulong sequence)` -- updates LastEpoch/LastSequence
  - `IReadOnlyList<WalSubscriberCursor> GetActiveCursors()` -- all cursors with Active flag
  - `ulong GetOldestSubscriberEpoch()` -- minimum LastEpoch across active subscribers (for WAL retention)
  - `int ActiveSubscriberCount { get; }`
  - `int Serialize(Span<byte> buffer)` -- writes all cursors, returns bytes written
  - `static WalSubscriberCursorTable Deserialize(ReadOnlySpan<byte> buffer, long regionStartBlock, int blockSize)` -- reads cursors

Create `IWalSubscriber` interface with `[SdkCompatibility("6.0.0")]` in namespace `DataWarehouse.SDK.VirtualDiskEngine.Journal`:
- `ulong SubscriberId { get; }` -- this subscriber's unique ID
- `Task<IReadOnlyList<JournalEntry>> PollAsync(int maxEntries, CancellationToken ct)` -- reads entries after current cursor position
- `Task AdvanceAsync(ulong epoch, ulong sequence, CancellationToken ct)` -- commits cursor position
- `Task<WalSubscriberCursor> GetCursorAsync(CancellationToken ct)` -- returns current cursor state
- `Task PauseAsync(CancellationToken ct)` -- sets Paused flag
- `Task ResumeAsync(CancellationToken ct)` -- clears Paused flag
- `bool IsActive { get; }` -- true if not paused and not unregistered

Use `System.Buffers.Binary.BinaryPrimitives` for serialization. Use `DataWarehouse.SDK.Contracts` for SdkCompatibility.
  </action>
  <verify>Build compiles: `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5`</verify>
  <done>WalSubscriberCursorTable stores 32B cursor entries with WALS block type, IWalSubscriber provides poll/advance interface for zero-copy WAL consumption with per-subscriber ACLs.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore` succeeds with zero errors
- WalSubscriberCursor serializes to exactly 32 bytes per spec
- MaxSubscribers = 128 matches spec (128 subscribers per 4KB cursor block)
</verification>

<success_criteria>
- VOPT-35 satisfied: WALS region (bit 21), subscriber cursor table, zero-copy WAL subscription interface, per-subscriber ACLs, cursor management
</success_criteria>

<output>
After completion, create `.planning/phases/91.5-vde-v2.1-format-completion/87-22-SUMMARY.md`
</output>
