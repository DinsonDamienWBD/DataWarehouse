---
phase: 94-data-plugin-consolidation
plan: 05
type: execute
wave: 3
depends_on: ["94-01", "94-02", "94-03", "94-04"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Full solution builds with zero errors and zero new warnings"
    - "No private _lineage store exists in UltimateDataCatalog or UltimateDataLake -- all lineage goes through message bus to UltimateDataLineage"
    - "No private _catalog store exists in UltimateDataLake or UltimateDataManagement -- all catalog goes through message bus to UltimateDataCatalog"
    - "No plugin has a ProjectReference to another plugin -- only SDK references"
    - "Every cross-plugin message bus call goes through MessageBusDelegationHelper with circuit breaker"
    - "Message bus topic wiring is complete: datalake.lineage delegates to lineage.*, datalake.catalog delegates to catalog.*, catalog.lineage delegates to lineage.*"
  artifacts:
    - path: ".planning/phases/94-data-plugin-consolidation/94-05-VERIFICATION.md"
      provides: "Comprehensive verification report for the entire phase 94 consolidation"
  key_links:
    - from: "UltimateDataLakePlugin"
      to: "UltimateDataLineage (via message bus)"
      via: "lineage.track / lineage.upstream / lineage.downstream"
      pattern: "lineage\\."
    - from: "UltimateDataLakePlugin"
      to: "UltimateDataCatalog (via message bus)"
      via: "catalog.register / catalog.search"
      pattern: "catalog\\."
    - from: "UltimateDataCatalogPlugin"
      to: "UltimateDataLineage (via message bus)"
      via: "lineage.track / lineage.upstream / lineage.downstream"
      pattern: "lineage\\."
---

<objective>
Verify the entire Phase 94 consolidation is correct: build health, no duplicate private stores, plugin isolation intact, message bus wiring complete, circuit breaker degradation in place. This is an audit-and-fix plan -- if issues are found, fix them in-place.

Purpose: Final gate before Phase 94 is marked complete. Ensures zero regressions from the consolidation work.

Output: A verification report at `.planning/phases/94-data-plugin-consolidation/94-05-VERIFICATION.md` documenting all checks and their results.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/94-data-plugin-consolidation/94-01-SUMMARY.md
@.planning/phases/94-data-plugin-consolidation/94-02-SUMMARY.md
@.planning/phases/94-data-plugin-consolidation/94-03-SUMMARY.md
@.planning/phases/94-data-plugin-consolidation/94-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification and duplicate store audit</name>
  <files></files>
  <action>
Run the following verification checks and record results:

**1. Full build check:**
```bash
dotnet build --no-restore 2>&1 | tail -20
```
Must be 0 errors. Record warning count.

**2. Duplicate private store audit -- LINEAGE:**
Grep for `BoundedDictionary<string, DataLineageRecord>` and `BoundedDictionary<string, LineageEdge>` across ALL plugins EXCEPT UltimateDataLineage. These must NOT exist as primary stores (they may exist inside MessageBusDelegationHelper as fallback caches -- that is allowed, but they must be `BoundedCache` not `BoundedDictionary`).

Specific checks:
- `grep -r "_lineage" Plugins/DataWarehouse.Plugins.UltimateDataCatalog/` -- should only appear in Delegation/ folder
- `grep -r "_lineage" Plugins/DataWarehouse.Plugins.UltimateDataLake/` -- should only appear in Delegation/ folder
- If found outside Delegation/, the private store was not properly removed by 94-01. Fix it.

**3. Duplicate private store audit -- CATALOG:**
- `grep -r "_catalog" Plugins/DataWarehouse.Plugins.UltimateDataLake/` -- should only appear in Delegation/ folder
- `grep -r "DataCatalogEntry" Plugins/DataWarehouse.Plugins.UltimateDataLake/` -- record type should be removed or only in Delegation/
- If found outside Delegation/, the private store was not properly removed by 94-02. Fix it.

**4. Duplicate private store audit -- RELATIONSHIPS:**
- `grep -r "_relationships" Plugins/DataWarehouse.Plugins.UltimateDataCatalog/UltimateDataCatalogPlugin.cs` -- should not exist as a primary store
- `grep -r "CatalogRelationship" Plugins/DataWarehouse.Plugins.UltimateDataCatalog/UltimateDataCatalogPlugin.cs` -- record type should be removed from plugin-level stores

If ANY duplicate stores are found, remove them and update the handlers to delegate through MessageBusDelegationHelper. Rebuild after fixes.
  </action>
  <verify>
    dotnet build --no-restore 2>&1 | tail -5
    # Then grep commands above should return empty (or only Delegation/ hits)
  </verify>
  <done>
    Build passes with 0 errors. No private lineage stores outside UltimateDataLineage. No private catalog stores outside UltimateDataCatalog. All delegation goes through MessageBusDelegationHelper.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plugin isolation and message bus wiring audit</name>
  <files>
    .planning/phases/94-data-plugin-consolidation/94-05-VERIFICATION.md
  </files>
  <action>
Run the following verification checks and produce the verification report:

**1. Plugin isolation check:**
```bash
grep -r "ProjectReference.*Plugins" Plugins/DataWarehouse.Plugins.UltimateDataCatalog/*.csproj
grep -r "ProjectReference.*Plugins" Plugins/DataWarehouse.Plugins.UltimateDataLake/*.csproj
grep -r "ProjectReference.*Plugins" Plugins/DataWarehouse.Plugins.UltimateDataManagement/*.csproj
grep -r "ProjectReference.*Plugins" Plugins/DataWarehouse.Plugins.UltimateDataLineage/*.csproj
```
All must reference ONLY `DataWarehouse.SDK`. Any cross-plugin ProjectReference is a BLOCKER -- fix it.

**2. Message bus wiring audit:**
For each delegating plugin, verify the OnMessageAsync handler delegates correctly:

UltimateDataLake:
- `datalake.catalog` handler must delegate to `catalog.*` topics (NOT use local `_catalog`)
- `datalake.lineage` handler must delegate to `lineage.*` topics (NOT use local `_lineage`)

UltimateDataCatalog:
- `catalog.lineage` handler must delegate to `lineage.*` topics (NOT use local `_relationships`)

UltimateDataManagement:
- Fabric catalog strategies must delegate to `catalog.*` if applicable

**3. Circuit breaker wiring audit:**
```bash
grep -rn "InMemoryCircuitBreaker" Plugins/DataWarehouse.Plugins.UltimateDataCatalog/
grep -rn "InMemoryCircuitBreaker" Plugins/DataWarehouse.Plugins.UltimateDataLake/
grep -rn "InMemoryCircuitBreaker" Plugins/DataWarehouse.Plugins.UltimateDataManagement/
```
Each delegating plugin must have at least one InMemoryCircuitBreaker instance.

**4. Write verification report** to `.planning/phases/94-data-plugin-consolidation/94-05-VERIFICATION.md`:
```markdown
# Phase 94 Verification Report

## Build Status
- Errors: {N}
- Warnings: {N}

## Duplicate Store Audit
| Plugin | Private Lineage Store | Private Catalog Store | Private Relationships Store | Status |
|--------|----------------------|----------------------|---------------------------|--------|
| UltimateDataCatalog | ... | N/A (authoritative) | ... | PASS/FAIL |
| UltimateDataLake | ... | ... | N/A | PASS/FAIL |
| UltimateDataManagement | N/A | ... | N/A | PASS/FAIL |
| UltimateDataLineage | N/A (authoritative) | N/A | N/A | PASS |

## Plugin Isolation
| Plugin | References Only SDK | Status |
|--------|-------------------|--------|

## Message Bus Wiring
| Source Plugin | Handler | Delegates To | Via Helper | Status |
|--------------|---------|--------------|------------|--------|

## Circuit Breaker Coverage
| Plugin | Helpers | Targets | Status |
|--------|---------|---------|--------|

## Fixes Applied
(list any fixes made during this verification)

## Result
PASS / FAIL (with details)
```

If any check fails, fix it first, then re-verify, then write the report with the fix documented.
  </action>
  <verify>
    dotnet build --no-restore 2>&1 | tail -5
    cat .planning/phases/94-data-plugin-consolidation/94-05-VERIFICATION.md | head -5
  </verify>
  <done>
    Verification report exists documenting all checks. Build passes. No duplicate stores. Plugin isolation intact. Message bus wiring complete. Circuit breakers in place. Phase 94 is verified.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build --no-restore 2>&1 | tail -5` -- zero errors
2. `.planning/phases/94-data-plugin-consolidation/94-05-VERIFICATION.md` exists and shows PASS
3. No `BoundedDictionary<string, DataLineageRecord>` in UltimateDataLake or UltimateDataCatalog outside Delegation/
4. No `BoundedDictionary<string, DataCatalogEntry>` in UltimateDataLake outside Delegation/
5. No cross-plugin ProjectReferences in any .csproj
6. InMemoryCircuitBreaker present in all delegating plugins
</verification>

<success_criteria>
- Full solution builds with 0 errors
- Verification report at 94-05-VERIFICATION.md documents all checks passing
- Zero private duplicate stores for lineage or catalog outside authoritative plugins
- Plugin isolation confirmed (SDK-only references)
- Message bus wiring confirmed (all delegation paths use correct topics)
- Circuit breaker coverage confirmed (all delegation paths protected)
- Any issues found during audit are fixed and documented
</success_criteria>

<output>
After completion, create `.planning/phases/94-data-plugin-consolidation/94-05-SUMMARY.md`
</output>
