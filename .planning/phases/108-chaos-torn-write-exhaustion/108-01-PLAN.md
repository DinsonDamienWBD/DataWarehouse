---
phase: 108-chaos-torn-write-exhaustion
plan: 01
type: execute
wave: 1
depends_on: ["107-02"]
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/TornWrite/TornWriteRecoveryTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/TornWrite/CrashSimulator.cs
  - DataWarehouse.Hardening.Tests/Chaos/TornWrite/VdeTestHarness.cs
autonomous: true
requirements:
  - CHAOS-03

must_haves:
  truths:
    - "WAL recovers data to pre-crash consistent state after torn write"
    - "RAID parity rebuilds correctly after partial write failure"
    - "No silent data corruption — integrity checks detect and report any inconsistency"
    - "Recovery is automatic on next mount without manual intervention"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/TornWrite/TornWriteRecoveryTests.cs"
      provides: "Torn write recovery test cases"
      min_lines: 200
    - path: "DataWarehouse.Hardening.Tests/Chaos/TornWrite/CrashSimulator.cs"
      provides: "Coyote-controlled crash injection at VDE decorator chain points"
      min_lines: 80
    - path: "DataWarehouse.Hardening.Tests/Chaos/TornWrite/VdeTestHarness.cs"
      provides: "VDE pipeline test setup with injectable crash points"
      min_lines: 60
  key_links:
    - from: "TornWriteRecoveryTests.cs"
      to: "VDE decorator chain"
      via: "CrashSimulator injects CancellationToken at decorator boundaries"
      pattern: "CancellationToken|decorator.*crash"
    - from: "CrashSimulator.cs"
      to: "Coyote SystematicTestingEngine"
      via: "non-deterministic crash point selection"
      pattern: "Coyote|SystematicTesting"
---

<objective>
Torn-write recovery chaos testing — prove VDE data integrity survives mid-write crashes at any point in the decorator chain.

Purpose: Validate WAL and RAID parity recovery guarantees. A crash mid-write must never corrupt data.
Output: Test suite in DataWarehouse.Hardening.Tests/Chaos/TornWrite/ with report "Stage 3 - Steps 3-4 - Torn-Write Recovery"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create crash simulator and VDE test harness</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/TornWrite/CrashSimulator.cs,
    DataWarehouse.Hardening.Tests/Chaos/TornWrite/VdeTestHarness.cs
  </files>
  <action>
1. Read `docs/ai-maps/map-core.md` and relevant VDE source files to understand the decorator chain: Cache→Integrity→Compression→Dedup→Encryption→WAL→RAID→File.
2. Read WAL implementation to understand recovery protocol (redo/undo log, checkpoint mechanism).
3. Read RAID parity implementation to understand rebuild protocol.
4. Create `CrashSimulator.cs`:
   - Wraps a CancellationTokenSource that fires at a configurable point in the decorator chain
   - Supports Coyote non-deterministic selection of crash point (which decorator, before/after write)
   - Provides `InjectAt(DecoratorStage stage, CrashTiming timing)` where timing is BeforeWrite/DuringWrite/AfterWriteBeforeFlush
   - Tracks which crash point was actually hit for test assertions
5. Create `VdeTestHarness.cs`:
   - Boots a minimal VDE pipeline with real decorator chain (not mocks)
   - Provides methods to write known data patterns, crash mid-write, then remount and verify
   - Uses in-memory backing store (MemoryStream) that preserves state across "crash" and "remount"
   - Helper: `WriteAndCrash(data, crashPoint)` → `Remount()` → `VerifyRecovery()` pattern
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>CrashSimulator and VdeTestHarness compile, can inject crashes at configurable decorator chain points</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement torn-write recovery tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/TornWrite/TornWriteRecoveryTests.cs
  </files>
  <behavior>
    - Test: Crash during WAL write phase → remount recovers to pre-crash state (no partial data)
    - Test: Crash after WAL commit but before data flush → remount replays WAL, data matches expected
    - Test: Crash during RAID parity write → remount rebuilds parity from surviving data
    - Test: Crash at each decorator stage (8 stages) → all recover correctly
    - Test: Coyote explores 100+ crash schedules → no schedule produces data corruption
  </behavior>
  <action>
1. Create `TornWriteRecoveryTests.cs`:
   - `[Fact] WalRecovery_CrashDuringWalWrite_DataConsistent` — write 1MB data pattern, crash mid-WAL-write, remount, verify data either fully present or fully absent (atomic)
   - `[Fact] WalReplay_CrashAfterCommitBeforeFlush_DataRecovered` — write, commit WAL, crash before data flush, remount, verify WAL replays and data matches
   - `[Fact] RaidParity_CrashDuringParityWrite_RebuildSucceeds` — write with RAID enabled, crash during parity computation/write, remount, verify parity rebuild produces correct data
   - `[Theory] CrashAtEachDecorator_AllRecover` with `[InlineData]` for each of the 8 decorator stages — crash at each point, verify recovery
   - `[Fact] CoyoteExploration_100Schedules_NeverCorrupt` — use Coyote to explore 100+ non-deterministic crash schedules, assert zero data corruption across all
2. Each test writes a known data pattern (e.g., repeating 0xDEADBEEF), crashes, remounts, and verifies the pattern is either fully recovered or cleanly rolled back.
3. Integrity verification: after remount, read all data and verify checksums match expected values.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~TornWrite" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All torn-write recovery tests pass. WAL recovery proven for crash at any decorator stage. RAID parity rebuild verified. Coyote finds no corruption across 100+ schedules.</done>
</task>

</tasks>

<verification>
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~TornWrite" -c Release -v normal
```
</verification>

<success_criteria>
- 5+ torn-write tests passing including Coyote exploration
- WAL recovery proven at every decorator chain stage
- RAID parity rebuild verified
- Zero data corruption across all crash schedules
- Report: "Stage 3 - Steps 3-4 - Torn-Write Recovery"
</success_criteria>

<output>
After completion, create `.planning/phases/108-chaos-torn-write-exhaustion/108-01-SUMMARY.md`
</output>
