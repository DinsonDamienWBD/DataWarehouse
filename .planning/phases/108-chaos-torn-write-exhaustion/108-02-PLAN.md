---
phase: 108-chaos-torn-write-exhaustion
plan: 02
type: execute
wave: 2
depends_on: ["108-01"]
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/ResourceExhaustion/ResourceExhaustionTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/ResourceExhaustion/ResourceThrottler.cs
autonomous: true
requirements:
  - CHAOS-04

must_haves:
  truths:
    - "ThreadPool starvation causes clean failure, not deadlock"
    - "MemoryPool exhaustion returns error, no OOM crash"
    - "Disk-full during VDE write fails with IOException, no corruption"
    - "System recovers to normal operation when resources become available"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/ResourceExhaustion/ResourceExhaustionTests.cs"
      provides: "Resource exhaustion chaos test cases"
      min_lines: 180
    - path: "DataWarehouse.Hardening.Tests/Chaos/ResourceExhaustion/ResourceThrottler.cs"
      provides: "Utility to simulate resource starvation"
      min_lines: 80
  key_links:
    - from: "ResourceExhaustionTests.cs"
      to: "VDE write path"
      via: "writes during resource starvation"
      pattern: "ThreadPool|MemoryPool|disk.*full|IOException"
    - from: "ResourceThrottler.cs"
      to: "System.Threading.ThreadPool"
      via: "saturates thread pool with blocking work"
      pattern: "ThreadPool\\.SetMinThreads|QueueUserWorkItem"
---

<objective>
Resource exhaustion chaos testing — prove graceful degradation under ThreadPool starvation, MemoryPool depletion, and disk-full conditions.

Purpose: Validate the system never hangs or corrupts data under resource starvation. Operations fail cleanly and recover.
Output: Test suite in DataWarehouse.Hardening.Tests/Chaos/ResourceExhaustion/ with report "Stage 3 - Steps 3-4 - Resource Exhaustion"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resource throttling infrastructure</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/ResourceExhaustion/ResourceThrottler.cs
  </files>
  <action>
1. Read VDE write path and memory pool usage patterns from SDK/Kernel source.
2. Create `ResourceThrottler.cs` — a disposable utility class that simulates resource starvation:
   - `StarveThreadPool()` — queue blocking work items to saturate all ThreadPool threads. Use ManualResetEventSlim to hold threads. Returns IDisposable that releases on dispose.
   - `ExhaustMemoryPool(IMemoryOwner pool)` — rent all available buffers from the target MemoryPool until allocation fails. Returns IDisposable that returns buffers.
   - `SimulateDiskFull(Stream backingStream)` — wrap a stream that throws IOException("No space left on device") after N bytes written. Configurable threshold.
   - All methods are IDisposable — cleanup releases resources so tests don't poison each other.
   - Thread-safe with proper cleanup even if test throws.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>ResourceThrottler compiles, provides ThreadPool starvation, MemoryPool exhaustion, and disk-full simulation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resource exhaustion tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/ResourceExhaustion/ResourceExhaustionTests.cs
  </files>
  <behavior>
    - Test: ThreadPool starved → VDE write times out cleanly (no deadlock), timeout < 30s
    - Test: MemoryPool exhausted → VDE write returns allocation failure, no OOM crash
    - Test: Disk full mid-write → IOException propagated, VDE state not corrupted, next write succeeds after space freed
    - Test: All three starvation modes simultaneously → system degrades but does not crash
    - Test: Recovery — after resource starvation released, operations succeed normally
  </behavior>
  <action>
1. Create `ResourceExhaustionTests.cs`:
   - `[Fact] ThreadPoolStarvation_VdeWrite_TimesOutCleanly` — starve ThreadPool, attempt VDE write, assert it either completes (thread pool has reserved threads) or throws TimeoutException/TaskCanceledException within 30s. NO deadlock.
   - `[Fact] MemoryPoolExhaustion_VdeWrite_CleanFailure` — exhaust memory pool, attempt VDE write, assert InsufficientMemoryException or similar. No OOM crash.
   - `[Fact] DiskFull_VdeWrite_IoExceptionNoCorruption` — wrap backing store with disk-full simulator, write data, assert IOException thrown. Remount and verify pre-write data intact.
   - `[Fact] CombinedStarvation_NoCrash` — starve ThreadPool + MemoryPool + disk simultaneously, attempt operations, assert the process survives (no unhandled exceptions, no crash).
   - `[Fact] Recovery_AfterResourcesFreed_OperationsSucceed` — starve resources, release them, verify normal operations resume successfully.
2. Each test uses `ResourceThrottler` with `using` statements for proper cleanup.
3. All tests have `[Timeout(60000)]` to catch deadlocks.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~ResourceExhaustion" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 5+ resource exhaustion tests pass. No deadlocks, no OOM crashes, no data corruption under resource starvation. Clean recovery after resources freed.</done>
</task>

</tasks>

<verification>
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~ResourceExhaustion" -c Release -v normal
```
</verification>

<success_criteria>
- 5+ resource exhaustion tests passing
- ThreadPool starvation never causes deadlock
- MemoryPool exhaustion never causes OOM crash
- Disk-full never causes data corruption
- System recovers when resources available
- Report: "Stage 3 - Steps 3-4 - Resource Exhaustion"
</success_criteria>

<output>
After completion, create `.planning/phases/108-chaos-torn-write-exhaustion/108-02-SUMMARY.md`
</output>
