---
phase: 111-cicd-fortress
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - DataWarehouse.Hardening.Tests/CiCd/CoyoteGateVerificationTests.cs
  - DataWarehouse.Hardening.Tests/CiCd/BenchmarkGateVerificationTests.cs
  - DataWarehouse.Hardening.Tests/CiCd/StrykerGateVerificationTests.cs
autonomous: true
requirements:
  - CICD-02
  - CICD-03
  - CICD-04

must_haves:
  truths:
    - "Intentional concurrency bug is caught by Coyote gate"
    - "Intentional Gen2 allocation on zero-alloc path is caught by BenchmarkDotNet gate"
    - "Intentional mutation score drop is caught by Stryker gate"
    - "Each gate produces actionable error message identifying the failure"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/CiCd/CoyoteGateVerificationTests.cs"
      provides: "Test that Coyote catches intentional concurrency bug"
      min_lines: 40
    - path: "DataWarehouse.Hardening.Tests/CiCd/BenchmarkGateVerificationTests.cs"
      provides: "Test that BenchmarkDotNet catches Gen2 allocation"
      min_lines: 40
    - path: "DataWarehouse.Hardening.Tests/CiCd/StrykerGateVerificationTests.cs"
      provides: "Test with intentionally weak assertion for Stryker to catch"
      min_lines: 40
  key_links:
    - from: "CoyoteGateVerificationTests.cs"
      to: "Coyote SystematicTestingEngine"
      via: "intentional data race"
      pattern: "race|concurrent.*modify"
    - from: "BenchmarkGateVerificationTests.cs"
      to: "BenchmarkDotNet"
      via: "ZeroAlloc category with Gen2 allocation"
      pattern: "\\[ZeroAlloc\\]|Gen2|GcMode"
---

<objective>
Create verification tests that prove each CI/CD gate catches regressions — intentional bugs that gates must block.

Purpose: Prove the gates actually work by demonstrating they catch known-bad code.
Output: Gate verification tests. Report: "Stage 4 - Gate Verification"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-cicd-fortress/111-01-SUMMARY.md
@.github/workflows/audit.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Coyote gate verification test</name>
  <files>
    DataWarehouse.Hardening.Tests/CiCd/CoyoteGateVerificationTests.cs
  </files>
  <action>
1. Create `CoyoteGateVerificationTests.cs` with an intentional concurrency bug that Coyote MUST find:
   - Create a class with a shared `int counter` (no lock, no Interlocked)
   - Two tasks both read counter, increment, write back (classic race condition)
   - Assert counter == 2 (will sometimes fail under Coyote scheduling)
   - Mark with `[Fact]` and appropriate Coyote attributes
   - Add XML comment: "INTENTIONAL BUG — this test verifies the Coyote gate catches data races. If Coyote does NOT find this bug, the gate is misconfigured."
2. Run Coyote locally to verify it finds the bug:
   ```
   dotnet build DataWarehouse.Hardening.Tests/ -c Release
   # Verify Coyote catches it (should report bug found)
   ```
3. This test is EXPECTED TO FAIL under Coyote — that's the point. In normal `dotnet test` it may pass (race is probabilistic). The CI gate verification is that Coyote finds the race.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>CoyoteGateVerificationTests.cs compiles, contains intentional data race that Coyote systematic testing will find</done>
</task>

<task type="auto">
  <name>Task 2: Create BenchmarkDotNet and Stryker gate verification tests</name>
  <files>
    DataWarehouse.Hardening.Tests/CiCd/BenchmarkGateVerificationTests.cs,
    DataWarehouse.Hardening.Tests/CiCd/StrykerGateVerificationTests.cs
  </files>
  <action>
1. Create `BenchmarkGateVerificationTests.cs`:
   - Create a benchmark class with `[MemoryDiagnoser]` and a `[ZeroAlloc]` category
   - Method: `ZeroAllocPath_WithIntentionalLeak` — allocates a `new byte[1024]` array (intentional Gen2 candidate under pressure)
   - The BenchmarkDotNet Gen2 gate in audit.yml should catch this allocation
   - Add XML comment: "INTENTIONAL ALLOCATION — verifies BenchmarkDotNet Gen2 gate catches allocations on zero-alloc paths"
   - This is a benchmark, not a test — it runs via `dotnet run` not `dotnet test`

2. Create `StrykerGateVerificationTests.cs`:
   - Create a simple function: `IsPositive(int n) => n > 0`
   - Test only the happy path: `Assert.True(IsPositive(5))` — intentionally missing boundary test for `n == 0`
   - Stryker will mutate `n > 0` to `n >= 0` and the test will still pass → surviving mutant → score drop
   - Add XML comment: "INTENTIONAL WEAK TEST — verifies Stryker gate catches inadequate test coverage. Stryker should report a surviving mutant for the boundary condition."

3. Both files should compile and be runnable in their respective toolchains.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>BenchmarkGateVerificationTests and StrykerGateVerificationTests compile. Each contains intentional weakness that its respective gate must catch.</done>
</task>

</tasks>

<verification>
```bash
dotnet build DataWarehouse.Hardening.Tests/ -c Release
# Verify all gate verification files exist
ls DataWarehouse.Hardening.Tests/CiCd/
```
</verification>

<success_criteria>
- 3 gate verification test files created and compiling
- Coyote test contains intentional data race
- Benchmark test contains intentional Gen2 allocation on zero-alloc path
- Stryker test contains intentionally weak assertion
- Each file documents WHY the intentional bug exists
- Report: "Stage 4 - Gate Verification"
</success_criteria>

<output>
After completion, create `.planning/phases/111-cicd-fortress/111-02-SUMMARY.md`
</output>
