---
phase: 111-cicd-fortress
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/audit.yml
autonomous: true
requirements:
  - CICD-01

must_haves:
  truths:
    - "audit.yml has all gates active: build, Coyote, InspectCode, dupFinder, dotCover, BenchmarkDotNet, Stryker"
    - "All jobs have timeout guards"
    - "Performance gate blocks merge on Gen2 allocation in zero-alloc paths"
    - "InspectCode and dupFinder have baseline thresholds"
    - "audit-summary job aggregates all results and is the required status check"
  artifacts:
    - path: ".github/workflows/audit.yml"
      provides: "Complete CI/CD fortress workflow"
      min_lines: 300
  key_links:
    - from: ".github/workflows/audit.yml"
      to: "GitHub branch protection"
      via: "audit-summary required status check"
      pattern: "audit-summary|needs:.*build-and-test"
---

<objective>
Finalize the CI/CD fortress workflow — harden audit.yml with baseline thresholds, proper gate logic, and branch protection.

Purpose: Lock down the pipeline so no PR can degrade the hardened state.
Output: Production-ready .github/workflows/audit.yml. Report: "Stage 4 - CI/CD Pipeline Finalization"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/audit.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and harden the existing workflow</name>
  <files>.github/workflows/audit.yml</files>
  <action>
1. Read the existing `.github/workflows/audit.yml` in full.
2. Verify every job has a `timeout-minutes` guard. Add if missing.
3. Harden the static-analysis job:
   - After InspectCode runs, parse `inspectcode-results.xml` and count WARNING+ issues
   - Add a threshold check: if issue count exceeds baseline (store baseline as env var `INSPECTCODE_BASELINE`), fail the job
   - After dupFinder runs, parse `dupfinder-results.xml` and count duplicates with cost >= 100
   - Add a threshold check: if duplicate count exceeds baseline `DUPFINDER_BASELINE`, fail the job
   - Set initial baselines to 0 (will be calibrated during gate verification in Plan 02)
4. Harden the performance-check job:
   - Add BenchmarkDotNet `--filter '*ZeroAlloc*'` to focus on zero-allocation benchmarks
   - Make Gen2 check a hard gate (exit 1 on any Gen2 allocation, not just warning)
5. Harden the audit-summary job:
   - Add `performance-check` to the failure check (currently only checks build-and-test, coyote, mutation)
   - Add static-analysis to the failure check when running in gate mode
   - Add dotCover coverage threshold check (if coverage report includes percentage)
6. Ensure concurrency group properly cancels in-progress runs.
7. Add a comment block at top documenting: what each gate blocks, expected run times, how to update baselines.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/audit.yml'))" 2>&1 && echo "YAML valid"</automated>
  </verify>
  <done>audit.yml is valid YAML, all jobs have timeout guards, InspectCode/dupFinder have baseline threshold gates, performance gate is hard-fail, audit-summary checks all required jobs</done>
</task>

<task type="auto">
  <name>Task 2: Add branch protection configuration documentation</name>
  <files>.github/workflows/audit.yml</files>
  <action>
1. Add a `# Branch Protection Setup` comment section at the end of audit.yml documenting:
   - Required status check: `audit-summary`
   - Recommended settings: Require branches to be up to date, Require conversation resolution, Dismiss stale PR reviews
   - How to configure: Settings → Branches → Branch protection rules → main/master
2. Verify the `audit-summary` job uses `if: always()` so it runs even when upstream jobs fail (already present — confirm).
3. Verify the summary job properly checks ALL upstream jobs in its failure condition.
4. Add a `workflow_call` trigger so the workflow can be reused by other workflows if needed.
5. Ensure artifact retention is set to 30 days for all uploads.
6. Validate the entire YAML is syntactically correct.
  </action>
  <verify>
    <automated>python3 -c "import yaml; data=yaml.safe_load(open('.github/workflows/audit.yml')); jobs=list(data.get('jobs',{}).keys()); assert 'audit-summary' in jobs, f'Missing audit-summary, got {jobs}'; print(f'Jobs: {jobs}')"</automated>
  </verify>
  <done>audit.yml has branch protection documentation, audit-summary checks all gates, workflow_call trigger added, all artifacts have 30-day retention</done>
</task>

</tasks>

<verification>
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/audit.yml'))" && echo "Valid YAML"
grep -c "timeout-minutes" .github/workflows/audit.yml  # Should match job count
grep "audit-summary" .github/workflows/audit.yml
```
</verification>

<success_criteria>
- audit.yml is valid YAML with all gates active
- Every job has timeout guard
- InspectCode/dupFinder have baseline thresholds
- BenchmarkDotNet Gen2 gate is hard-fail
- audit-summary aggregates all results
- Branch protection setup documented
- Report: "Stage 4 - CI/CD Pipeline Finalization"
</success_criteria>

<output>
After completion, create `.planning/phases/111-cicd-fortress/111-01-SUMMARY.md`
</output>
