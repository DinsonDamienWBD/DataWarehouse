---
phase: 109-chaos-message-bus-federation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/MessageBus/MessageBusDisruptionTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/MessageBus/ChaosMessageBusProxy.cs
autonomous: true
requirements:
  - CHAOS-05

must_haves:
  truths:
    - "Duplicate messages produce identical final state as single delivery (idempotency)"
    - "Lost messages are detected and operations fail cleanly rather than silently corrupt"
    - "Reordered messages produce correct final state"
    - "No data corruption from any message bus disruption pattern"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/MessageBus/MessageBusDisruptionTests.cs"
      provides: "Message bus chaos test cases"
      min_lines: 180
    - path: "DataWarehouse.Hardening.Tests/Chaos/MessageBus/ChaosMessageBusProxy.cs"
      provides: "Proxy that injects loss, duplication, reorder"
      min_lines: 100
  key_links:
    - from: "ChaosMessageBusProxy.cs"
      to: "SDK IMessageBus"
      via: "decorates real message bus with chaos injection"
      pattern: "IMessageBus|Publish|Subscribe"
    - from: "MessageBusDisruptionTests.cs"
      to: "Plugin operations"
      via: "plugins operating while message bus is disrupted"
      pattern: "idempoten|duplicate|reorder|loss"
---

<objective>
Message bus disruption chaos testing — prove the system handles message loss, duplication, and reordering without data corruption.

Purpose: Validate idempotency guarantees and fault tolerance in the message bus communication layer.
Output: Test suite in DataWarehouse.Hardening.Tests/Chaos/MessageBus/ with report "Stage 3 - Steps 5-6 - Message Bus Disruption"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chaos message bus proxy</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/MessageBus/ChaosMessageBusProxy.cs
  </files>
  <action>
1. Read `docs/ai-maps/map-core.md` to find IMessageBus interface and its implementation.
2. Read the actual message bus implementation to understand Publish/Subscribe/Unsubscribe contracts.
3. Create `ChaosMessageBusProxy.cs` — a decorator around the real IMessageBus:
   - `ChaosMode` enum: Normal, DropMessages, DuplicateMessages, ReorderMessages, Combined
   - `DropRate` (0.0-1.0): probability of dropping a message in DropMessages mode
   - `DuplicateRate` (0.0-1.0): probability of duplicating a message
   - `ReorderBufferSize` (int): collect N messages then shuffle before delivery
   - Implements same IMessageBus interface as the real bus
   - On Publish: based on mode, either drop, duplicate, buffer-and-reorder, or pass through
   - Tracks stats: messages published, dropped, duplicated, reordered for test assertions
   - Thread-safe — multiple publishers and subscribers concurrently
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>ChaosMessageBusProxy compiles, decorates IMessageBus with configurable disruption modes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement message bus disruption tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/MessageBus/MessageBusDisruptionTests.cs
  </files>
  <behavior>
    - Test: 50% message drop rate → operations detect missing responses and fail cleanly (timeout/retry)
    - Test: 100% message duplication → final state identical to single-delivery state (idempotency)
    - Test: Reorder buffer of 10 → operations with ordering dependencies produce correct result
    - Test: Combined chaos (drop + dup + reorder) → no data corruption
    - Test: Chaos during multi-plugin workflow → plugins handle disruption without crash
  </behavior>
  <action>
1. Create `MessageBusDisruptionTests.cs`:
   - `[Fact] MessageLoss_50Percent_OperationsFailCleanly` — configure 50% drop rate, run plugin operations that depend on message responses, assert operations timeout or retry rather than producing corrupt results
   - `[Fact] MessageDuplication_100Percent_IdempotentResult` — duplicate every message, run a series of operations, compare final state to a clean (no-chaos) run. States must match.
   - `[Fact] MessageReorder_Buffer10_CorrectFinalState` — reorder with buffer of 10, run operations with ordering dependencies, verify final state correct
   - `[Fact] CombinedChaos_NoDataCorruption` — enable all three modes simultaneously (20% drop, 50% dup, reorder buffer 5), run operations, verify no data corruption via integrity checks
   - `[Fact] MultiPluginChaos_PluginsSurviveDisruption` — boot Kernel with multiple plugins, inject combined chaos, verify all plugins remain operational (may have degraded throughput but no crashes)
2. Each test establishes a "golden" state from a clean run, then compares chaos run against it.
3. Integrity verification uses checksums and state comparison, not just "no exception thrown."
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~MessageBus" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 5+ message bus disruption tests pass. Idempotency proven under duplication. Clean failure under loss. Correct ordering under reorder. No corruption under combined chaos.</done>
</task>

</tasks>

<verification>
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~MessageBus" -c Release -v normal
```
</verification>

<success_criteria>
- 5+ message bus chaos tests passing
- Idempotency verified under message duplication
- Clean failure (not silent corruption) under message loss
- Correct state under message reordering
- Report: "Stage 3 - Steps 5-6 - Message Bus Disruption"
</success_criteria>

<output>
After completion, create `.planning/phases/109-chaos-message-bus-federation/109-01-SUMMARY.md`
</output>
