---
phase: 109-chaos-message-bus-federation
plan: 02
type: execute
wave: 2
depends_on: ["109-01"]
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/Federation/FederationPartitionTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/Federation/PartitionSimulator.cs
autonomous: true
requirements:
  - CHAOS-06

must_haves:
  truths:
    - "CRDT state converges after network partition heals"
    - "No data loss during partition — operations either succeed locally or fail explicitly"
    - "All replicas reach identical state within bounded time after partition heal"
    - "Partition detection triggers appropriate failover behavior"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/Federation/FederationPartitionTests.cs"
      provides: "Federation partition chaos test cases"
      min_lines: 180
    - path: "DataWarehouse.Hardening.Tests/Chaos/Federation/PartitionSimulator.cs"
      provides: "Network partition injection for federation"
      min_lines: 80
  key_links:
    - from: "PartitionSimulator.cs"
      to: "Federation/Replication layer"
      via: "blocks network calls between replicas"
      pattern: "partition|network|replica"
    - from: "FederationPartitionTests.cs"
      to: "CRDT implementation"
      via: "verifies convergence after merge"
      pattern: "CRDT|convergence|merge|replicas"
---

<objective>
Federation network partition chaos testing — prove CRDT convergence after partition heals and no data loss during partitioned operation.

Purpose: Validate federation replication guarantees under network failure conditions.
Output: Test suite in DataWarehouse.Hardening.Tests/Chaos/Federation/ with report "Stage 3 - Steps 5-6 - Federation Partition"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create partition simulator</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/Federation/PartitionSimulator.cs
  </files>
  <action>
1. Read `docs/ai-maps/map-core.md` and federation/replication plugin maps to find CRDT implementation and replication protocol interfaces.
2. Read CRDT WAL dual-ring implementation (Ring A crash + Ring B replication) and merge protocol.
3. Create `PartitionSimulator.cs`:
   - Models a cluster of N replicas with configurable network topology
   - `Partition(replicaA, replicaB)` — blocks all communication between two replicas (both directions)
   - `PartitionGroup(group1, group2)` — splits cluster into two isolated groups
   - `Heal(replicaA, replicaB)` — restores communication
   - `HealAll()` — restores full connectivity
   - Tracks: messages blocked during partition, messages queued for delivery on heal
   - Option: `dropDuringPartition` (true = messages lost) vs `queueDuringPartition` (true = messages delayed)
   - Works by intercepting the replication transport layer (wrap IReplicationTransport or equivalent)
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>PartitionSimulator compiles, can partition and heal replica communication</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement federation partition tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/Federation/FederationPartitionTests.cs
  </files>
  <behavior>
    - Test: 3-replica cluster, partition 1 replica, writes on both sides, heal → CRDT converges to merged state
    - Test: Partition during active replication mid-stream → no partial/corrupt state on either side
    - Test: Long partition (1000+ operations on each side) → heal → convergence within bounded time
    - Test: Multiple sequential partitions → state eventually converges
    - Test: Asymmetric partition (A→B blocked, B→A open) → system detects and handles
  </behavior>
  <action>
1. Create `FederationPartitionTests.cs`:
   - `[Fact] ThreeReplicas_PartitionOne_HealConverges` — setup 3 replicas, partition replica C, write to A and B, heal, assert all 3 converge to identical state containing all writes
   - `[Fact] PartitionMidReplication_NoCorruption` — start replication, partition mid-stream, assert both sides have consistent (not partial) state
   - `[Fact] LongPartition_1000Ops_ConvergesAfterHeal` — partition, perform 1000 operations on each side, heal, assert convergence within configurable timeout (e.g., 30s)
   - `[Fact] SequentialPartitions_EventualConvergence` — partition→heal→partition→heal 5 times with operations between, assert final convergence
   - `[Fact] AsymmetricPartition_Detected` — block only one direction, verify system detects asymmetric partition and enters appropriate failover mode
2. Convergence verification: after heal, poll all replicas until state matches or timeout. Compare via CRDT state hash or full state equality.
3. Use Coyote scheduling where applicable for non-deterministic partition timing.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Federation" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 5+ federation partition tests pass. CRDT convergence proven after partition heal. No data loss or corruption during partitioned operation.</done>
</task>

</tasks>

<verification>
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Federation" -c Release -v normal
```
</verification>

<success_criteria>
- 5+ federation partition tests passing
- CRDT convergence verified after partition heal
- No data loss during partitioned operation
- Convergence within bounded time
- Report: "Stage 3 - Steps 5-6 - Federation Partition"
</success_criteria>

<output>
After completion, create `.planning/phases/109-chaos-message-bus-federation/109-02-SUMMARY.md`
</output>
