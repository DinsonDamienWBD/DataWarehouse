---
phase: 126-microsecond-benchmarks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Benchmarks/DashboardExporter.cs
  - DataWarehouse.Benchmarks/BenchmarkTrendAnalyzer.cs
autonomous: true
requirements: [PER-04]
must_haves:
  truths:
    - "BenchmarkDotNet results export to Dashboard-compatible format"
    - "Historical trend data accumulates for regression visualization"
    - "Regression alerts published via message bus when thresholds exceeded"
  artifacts:
    - path: "DataWarehouse.Benchmarks/DashboardExporter.cs"
      provides: "Exports benchmark results to Dashboard REST API format"
      min_lines: 60
    - path: "DataWarehouse.Benchmarks/BenchmarkTrendAnalyzer.cs"
      provides: "Analyzes historical benchmark trends, detects regressions"
      min_lines: 50
  key_links:
    - from: "DataWarehouse.Benchmarks/DashboardExporter.cs"
      to: "Metadata/benchmark-baseline-v8.0.json"
      via: "Reads results and pushes to dashboard"
      pattern: "benchmark.*json|HttpClient"
    - from: "DataWarehouse.Benchmarks/BenchmarkTrendAnalyzer.cs"
      to: "DashboardExporter"
      via: "Feeds trend data for visualization"
      pattern: "TrendAnalyzer.*Export"
---

<objective>
Build continuous performance dashboard integration with trend visualization and regression alerts.

Purpose: Enable ongoing performance monitoring by exporting benchmark results to the Dashboard and analyzing trends.
Output: Dashboard exporter and trend analyzer that detect performance regressions over time.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.Benchmarks/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Exporter</name>
  <files>DataWarehouse.Benchmarks/DashboardExporter.cs</files>
  <action>
Create DashboardExporter.cs that:
- Reads BenchmarkDotNet JSON output from BenchmarkDotNet.Artifacts/results/
- Transforms to Dashboard REST API payload format: { "source": "benchmarks", "timestamp": "...", "metrics": [ { "name": "VdeBenchmarks.Random4KBRead_Memory", "p50_us": N, "p90_us": N, "p99_us": N, "p999_us": N, "ops_per_sec": N, "allocated_bytes": N } ] }
- Posts to Dashboard REST endpoint (configurable via BENCHMARK_DASHBOARD_URL env var, default http://localhost:5000/api/metrics/benchmarks)
- Falls back to appending to local file Metadata/benchmark-history.jsonl if Dashboard unreachable
- Handles connection failures gracefully (log warning, don't fail CI)
- Uses System.Text.Json for serialization (not Newtonsoft -- consistent with SDK)
- Supports --export-dashboard CLI argument in Program.cs
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj -c Release --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>DashboardExporter compiles, transforms BenchmarkDotNet output to Dashboard API format, handles offline fallback</done>
</task>

<task type="auto">
  <name>Task 2: Trend Analyzer with Regression Alerts</name>
  <files>DataWarehouse.Benchmarks/BenchmarkTrendAnalyzer.cs</files>
  <action>
Create BenchmarkTrendAnalyzer.cs that:
- Reads historical benchmark data from Metadata/benchmark-history.jsonl
- Computes rolling average (last 10 runs) for each benchmark
- Detects trends: improving (>5% faster), stable, degrading (>5% slower), regression (>10% slower)
- Publishes regression alerts via SDK message bus (BenchmarkRegressionDetected message type)
- Generates summary report: { "analyzed_runs": N, "trends": { "benchmark_name": { "direction": "stable|improving|degrading|regression", "change_pct": N, "last_10_avg_p99_us": N } } }
- Supports --analyze-trends CLI argument in Program.cs
- Uses simple linear regression (slope of P99 over last 10 runs) for trend detection
- Does NOT require external charting library -- outputs JSON for Dashboard to render
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj -c Release --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>BenchmarkTrendAnalyzer compiles, detects regression trends, publishes alerts via message bus</done>
</task>

</tasks>

<verification>
dotnet build DataWarehouse.Benchmarks -c Release succeeds.
DashboardExporter and BenchmarkTrendAnalyzer classes exist with all described functionality.
Program.cs supports --export-dashboard and --analyze-trends CLI arguments.
</verification>

<success_criteria>
- Dashboard exporter transforms BenchmarkDotNet output to REST API payload
- Offline fallback to local JSONL file works
- Trend analyzer computes rolling averages and detects regression/improvement
- Message bus alert fires on regression detection
- All integrated into Program.cs CLI
</success_criteria>

<output>
After completion, create `.planning/phases/126-microsecond-benchmarks/126-03-SUMMARY.md`
</output>
