---
phase: 126-microsecond-benchmarks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Benchmarks/BenchmarkRunner.cs
  - Metadata/benchmark-baseline-v8.0.json
  - DataWarehouse.Benchmarks/RegressionGate.cs
autonomous: true
requirements: [PER-04]
must_haves:
  truths:
    - "P50/P90/P99/P999 latency numbers published for every benchmark operation"
    - "Baseline stored in Metadata/benchmark-baseline-v8.0.json as machine-readable JSON"
    - "CI regression gate blocks merge when P99 degrades more than 10%"
  artifacts:
    - path: "Metadata/benchmark-baseline-v8.0.json"
      provides: "Published baseline P50/P90/P99/P999 numbers per operation"
      min_lines: 50
    - path: "DataWarehouse.Benchmarks/BenchmarkRunner.cs"
      provides: "Automated runner that exports percentile results to JSON"
      min_lines: 40
    - path: "DataWarehouse.Benchmarks/RegressionGate.cs"
      provides: "CI gate that compares current run vs baseline"
      min_lines: 60
  key_links:
    - from: "DataWarehouse.Benchmarks/RegressionGate.cs"
      to: "Metadata/benchmark-baseline-v8.0.json"
      via: "JSON deserialization of baseline"
      pattern: "benchmark-baseline-v8\\.0\\.json"
    - from: "DataWarehouse.Benchmarks/BenchmarkRunner.cs"
      to: "BenchmarkDotNet"
      via: "BenchmarkRunner.Run with JSON exporter"
      pattern: "JsonExporter|BenchmarkRunner"
---

<objective>
Publish baseline P50/P90/P99/P999 latency numbers and implement CI regression gate.

Purpose: Provide published performance baseline for all critical operations, with automated regression detection.
Output: Baseline JSON file with percentile data and regression gate that blocks >10% P99 degradation.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.Benchmarks/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Benchmark Runner and Baseline Generator</name>
  <files>DataWarehouse.Benchmarks/BenchmarkRunner.cs, Metadata/benchmark-baseline-v8.0.json</files>
  <action>
Create BenchmarkRunner.cs that:
- Runs all benchmark classes using BenchmarkDotNet's BenchmarkSwitcher
- Extracts P50/P90/P99/P999 from BenchmarkDotNet's Statistics property
- Exports results to JSON format: { "version": "8.0.0", "timestamp": "...", "hardware": { "cpu": "...", "ram": "...", "os": "..." }, "benchmarks": { "VdeBenchmarks.Random4KBRead_Memory": { "p50_us": N, "p90_us": N, "p99_us": N, "p999_us": N, "ops_per_sec": N }, ... } }
- Uses BenchmarkDotNet's built-in StatisticColumn for percentile calculation
- Write to Metadata/benchmark-baseline-v8.0.json

Create initial Metadata/benchmark-baseline-v8.0.json with placeholder structure matching the schema. Include all benchmark method names from VdeBenchmarks, PipelineBenchmarks, FederationBenchmarks, MessageBusBenchmarks with initial values set to 0 (populated on first real run).

Reference hardware spec: Document that baseline should be run on a reference machine (e.g., 8-core, 32GB RAM, NVMe SSD) and the spec recorded in the JSON.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj -c Release --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>BenchmarkRunner compiles, baseline JSON file exists with correct schema</done>
</task>

<task type="auto">
  <name>Task 2: CI Regression Gate</name>
  <files>DataWarehouse.Benchmarks/RegressionGate.cs</files>
  <action>
Create RegressionGate.cs that:
- Loads baseline from Metadata/benchmark-baseline-v8.0.json
- Loads current run results from BenchmarkDotNet output (BenchmarkDotNet.Artifacts/)
- Compares P99 of each benchmark: if current P99 > baseline P99 * 1.10 (10% threshold), flag as regression
- Configurable threshold via environment variable BENCHMARK_REGRESSION_THRESHOLD (default 0.10 = 10%)
- Returns exit code 0 (pass) or 1 (fail) for CI integration
- Outputs detailed report: which benchmarks regressed, by how much, threshold used
- Skips comparison for benchmarks not in baseline (new benchmarks always pass)
- Static Main entry point for CLI usage: `dotnet run -- --regression-gate`

Add command-line argument parsing in Program.cs: if args contain "--regression-gate", run RegressionGate instead of benchmarks.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj -c Release --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>RegressionGate compiles, compares P99 vs baseline with configurable threshold, returns exit code for CI</done>
</task>

</tasks>

<verification>
dotnet build DataWarehouse.Benchmarks -c Release succeeds.
Metadata/benchmark-baseline-v8.0.json exists with valid JSON schema.
RegressionGate.cs has Main-compatible entry with exit code 0/1.
</verification>

<success_criteria>
- Baseline JSON published at Metadata/benchmark-baseline-v8.0.json with P50/P90/P99/P999 fields per operation
- Regression gate compares current P99 vs baseline, threshold 10% default
- Gate returns exit code 1 on regression for CI blocking
- Program.cs supports --regression-gate CLI argument
</success_criteria>

<output>
After completion, create `.planning/phases/126-microsecond-benchmarks/126-02-SUMMARY.md`
</output>
