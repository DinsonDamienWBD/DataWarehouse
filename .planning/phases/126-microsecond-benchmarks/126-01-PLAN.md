---
phase: 126-microsecond-benchmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Benchmarks/VdeBenchmarks.cs
  - DataWarehouse.Benchmarks/PipelineBenchmarks.cs
  - DataWarehouse.Benchmarks/FederationBenchmarks.cs
  - DataWarehouse.Benchmarks/MessageBusBenchmarks.cs
autonomous: true
requirements: [PER-04]
must_haves:
  truths:
    - "BenchmarkDotNet suite covers VDE read/write at each block device tier"
    - "Pipeline latency benchmark measures compress-encrypt-WAL-write chain"
    - "Federation cross-node latency and message bus pub/sub benchmarks exist"
    - "All benchmarks use OperationsPerInvoke for sub-microsecond accuracy"
  artifacts:
    - path: "DataWarehouse.Benchmarks/VdeBenchmarks.cs"
      provides: "VDE tier benchmarks (memory, file, SPDK, S3)"
      min_lines: 80
    - path: "DataWarehouse.Benchmarks/PipelineBenchmarks.cs"
      provides: "Pipeline stage latency benchmarks"
      min_lines: 60
    - path: "DataWarehouse.Benchmarks/FederationBenchmarks.cs"
      provides: "Federation cross-node query latency"
      min_lines: 50
    - path: "DataWarehouse.Benchmarks/MessageBusBenchmarks.cs"
      provides: "Message bus pub/sub latency"
      min_lines: 50
  key_links:
    - from: "DataWarehouse.Benchmarks/VdeBenchmarks.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine"
      via: "VDE block device read/write"
      pattern: "IBlockDevice.*Read|Write"
    - from: "DataWarehouse.Benchmarks/PipelineBenchmarks.cs"
      to: "DataWarehouse.SDK/Contracts/Pipeline"
      via: "Pipeline stage execution"
      pattern: "Compress.*Encrypt.*WAL.*Write"
---

<objective>
Build comprehensive microsecond-level benchmark suite covering all critical DataWarehouse operations.

Purpose: Establish the benchmark infrastructure needed to publish P99/P999 latency numbers for every critical path.
Output: Four benchmark classes covering VDE I/O, pipeline latency, federation, and message bus operations.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.Benchmarks/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: VDE and Pipeline Benchmarks</name>
  <files>DataWarehouse.Benchmarks/VdeBenchmarks.cs, DataWarehouse.Benchmarks/PipelineBenchmarks.cs</files>
  <action>
Create VdeBenchmarks.cs with BenchmarkDotNet benchmarks for:
- Random 4KB read/write per block device tier (MemoryBlockDevice, FileBlockDevice, CompoundBlockDevice)
- Sequential 1MB read/write per tier
- Mixed 70/30 read/write workload
- Use [OperationsPerInvoke(1000)] for sub-microsecond accuracy
- Use [Params(4096, 65536, 1048576)] for block sizes
- Add [MemoryDiagnoser] and [RankColumn] attributes

Create PipelineBenchmarks.cs measuring end-to-end pipeline latency:
- Compress stage (LZ4, Zstd, Brotli)
- Encrypt stage (AES-256-GCM, ChaCha20)
- WAL append stage
- Full pipeline (compress -> encrypt -> WAL -> write)
- Use [OperationsPerInvoke] for accurate sub-microsecond measurement

Reference existing StorageBenchmarks in Program.cs for config patterns. Use the same DefaultConfig setup with MemoryDiagnoser and MarkdownExporter.GitHub. Create test data in [GlobalSetup] methods.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj -c Release --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>VdeBenchmarks and PipelineBenchmarks classes compile with all benchmark methods annotated</done>
</task>

<task type="auto">
  <name>Task 2: Federation and Message Bus Benchmarks</name>
  <files>DataWarehouse.Benchmarks/FederationBenchmarks.cs, DataWarehouse.Benchmarks/MessageBusBenchmarks.cs</files>
  <action>
Create FederationBenchmarks.cs:
- Cross-shard query routing latency (single shard, fan-out to 10/100 shards)
- Shard catalog lookup latency
- Federation namespace walk latency
- Use [OperationsPerInvoke] for sub-microsecond accuracy
- Simulate federated topology in [GlobalSetup] using in-memory shard catalog

Create MessageBusBenchmarks.cs:
- Single message publish latency
- Batch publish (100, 1000 messages) latency
- Subscribe + receive roundtrip latency
- Topic routing overhead (1 topic vs 100 topics)
- Use kernel's MessageBus types from DataWarehouse.SDK

Both classes: [MemoryDiagnoser], [RankColumn], [OperationsPerInvoke] on all methods.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj -c Release --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Federation and MessageBus benchmark classes compile, covering cross-shard query, namespace walk, pub/sub latency</done>
</task>

</tasks>

<verification>
dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj -c Release succeeds with zero errors.
All four benchmark files exist with [Benchmark] annotated methods and [OperationsPerInvoke] attributes.
</verification>

<success_criteria>
- VdeBenchmarks covers 3+ block device tiers with read/write at multiple block sizes
- PipelineBenchmarks covers individual stages and full pipeline
- FederationBenchmarks covers cross-shard query fan-out
- MessageBusBenchmarks covers pub/sub latency
- All use [OperationsPerInvoke] for sub-microsecond accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/126-microsecond-benchmarks/126-01-SUMMARY.md`
</output>
