---
phase: 120-multi-level-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Security/MLS/SecurityLabel.cs
  - DataWarehouse.SDK/Security/MLS/SecurityClassification.cs
  - DataWarehouse.Hardening.Tests/Security/MLS/SecurityLabelTests.cs
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "SecurityLabel stores classification level + compartment set in 8 bytes"
    - "Classification lattice has Unclassified < Confidential < Secret < TopSecret ordering"
    - "Compartments are bit-flag encoded for efficient storage and comparison"
    - "Dominance relation (label A dominates label B) is correct for lattice-based MLS"
  artifacts:
    - path: "DataWarehouse.SDK/Security/MLS/SecurityLabel.cs"
      provides: "8-byte per-inode security label with classification + compartments"
      exports: ["SecurityLabel", "SecurityLabelSerializer"]
    - path: "DataWarehouse.SDK/Security/MLS/SecurityClassification.cs"
      provides: "Classification enum and compartment definitions"
      exports: ["SecurityClassification", "SecurityCompartment", "ClassificationLattice"]
    - path: "DataWarehouse.Hardening.Tests/Security/MLS/SecurityLabelTests.cs"
      provides: "Tests for label serialization, dominance, and lattice ordering"
  key_links:
    - from: "DataWarehouse.SDK/Security/MLS/SecurityLabel.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine/Format/ModuleDefinitions.cs"
      via: "ModuleId.CapabilityMacaroon (MACR) inode extension"
      pattern: "ModuleId\\.CapabilityMacaroon|InodeFieldBytes"
---

<objective>
Create per-inode SecurityLabel with classification lattice and compartments, stored as 8-byte field in VDE inode MACR module.

Purpose: Multi-Level Security (MLS) requires every data object to carry a formal security classification label. This is the foundation for Bell-LaPadula and Biba enforcement.
Output: SecurityLabel struct, SecurityClassification enum, lattice dominance logic, and tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/VirtualDiskEngine/Format/ModuleDefinitions.cs
@DataWarehouse.SDK/VirtualDiskEngine/Pipeline/Stages/AccessControlStage.cs

<interfaces>
<!-- Key types the executor needs -->

From DataWarehouse.SDK/VirtualDiskEngine/Format/ModuleDefinitions.cs:
```csharp
public enum ModuleId : byte
{
    Security = 0,
    CapabilityMacaroon = 37,  // MACR module — target for SecurityLabel storage
    // ...
}

public readonly record struct VdeModule
{
    public ModuleId Id { get; init; }
    public string Name { get; init; }
    public int InodeFieldBytes { get; init; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SecurityClassification enum and lattice</name>
  <files>DataWarehouse.SDK/Security/MLS/SecurityClassification.cs</files>
  <behavior>
    - SecurityClassification: Unclassified=0, Confidential=1, Secret=2, TopSecret=3
    - SecurityCompartment flags: None=0, SCI=1, SAP=2, NATO=4, FVEY=8, NoForn=16, RelTo=32 (6 compartments, fits in 2 bytes)
    - ClassificationLattice.Dominates(a, b) returns true if a.Level >= b.Level AND a.Compartments is superset of b.Compartments
    - ClassificationLattice.LeastUpperBound(a, b) returns label with max(level) and union(compartments)
    - ClassificationLattice.GreatestLowerBound(a, b) returns label with min(level) and intersection(compartments)
  </behavior>
  <action>
    Create `DataWarehouse.SDK/Security/MLS/SecurityClassification.cs`:
    1. `SecurityClassification` enum: `Unclassified = 0`, `Confidential = 1`, `Secret = 2`, `TopSecret = 3`
    2. `[Flags] SecurityCompartment` enum: `None = 0`, `SCI = 1`, `SAP = 2`, `NATO = 4`, `FVEY = 8`, `NoForn = 16`, `RelTo = 32`
    3. `ClassificationLattice` static class:
       - `Dominates(SecurityLabel subject, SecurityLabel obj)` — subject level >= object level AND subject compartments contain all object compartments
       - `LeastUpperBound(SecurityLabel a, SecurityLabel b)` — max level, union compartments
       - `GreatestLowerBound(SecurityLabel a, SecurityLabel b)` — min level, intersection compartments
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 120: MLS classification lattice")]`.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>SecurityClassification enum, SecurityCompartment flags, and ClassificationLattice with dominance/LUB/GLB operations exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SecurityLabel 8-byte struct + serialization + tests</name>
  <files>DataWarehouse.SDK/Security/MLS/SecurityLabel.cs, DataWarehouse.Hardening.Tests/Security/MLS/SecurityLabelTests.cs</files>
  <behavior>
    - SecurityLabel is exactly 8 bytes: 2 bytes classification, 2 bytes compartments, 4 bytes reserved
    - SecurityLabel.Serialize() produces exactly 8 bytes
    - SecurityLabel.Deserialize(ReadOnlySpan of byte) reconstructs from 8 bytes
    - Dominates, LUB, GLB operations work correctly via ClassificationLattice
    - TopSecret+SCI dominates Secret+SCI (higher level, same compartments)
    - Secret+SCI does NOT dominate Secret+NATO (different compartments)
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/MLS/SecurityLabel.cs`:
       - `readonly record struct SecurityLabel` with `SecurityClassification Level`, `SecurityCompartment Compartments`
       - Layout: bytes 0-1 = classification (ushort), bytes 2-3 = compartments (ushort flags), bytes 4-7 = reserved (zero)
       - `Serialize(Span<byte> destination)` — writes 8 bytes
       - `static Deserialize(ReadOnlySpan<byte> source)` — reads 8 bytes
       - `Dominates(SecurityLabel other)` — delegates to `ClassificationLattice.Dominates`
       - `static Unclassified`, `static Secret`, `static TopSecret` factory properties
       - `SecurityLabelSerializer` helper for inode integration
    2. Create tests in `DataWarehouse.Hardening.Tests/Security/MLS/SecurityLabelTests.cs`:
       - Test: Serialize/deserialize round-trip produces identical label
       - Test: Exactly 8 bytes written/read
       - Test: TopSecret+SCI dominates Secret+SCI
       - Test: Secret+SCI does NOT dominate Secret+NATO
       - Test: Unclassified+None does NOT dominate any higher label
       - Test: TopSecret+All dominates everything
       - Test: LUB of Confidential+SCI and Secret+NATO = Secret+(SCI|NATO)
       - Test: GLB of Secret+SCI|NATO and Secret+NATO = Secret+NATO
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 120: MLS security label")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~SecurityLabel" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>SecurityLabel is an 8-byte struct with serialize/deserialize, lattice dominance, LUB/GLB. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "SecurityLabel"` — all green
</verification>

<success_criteria>
- SecurityLabel stores classification + compartments in exactly 8 bytes
- Lattice dominance relation correct
- Round-trip serialization verified
</success_criteria>

<output>
After completion, create `.planning/phases/120-multi-level-security/120-01-SUMMARY.md`
</output>
