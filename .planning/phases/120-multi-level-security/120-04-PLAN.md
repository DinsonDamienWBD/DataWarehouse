---
phase: 120-multi-level-security
plan: 04
type: execute
wave: 2
depends_on: [120-01, 120-02]
files_modified:
  - DataWarehouse.SDK/Security/MLS/MlsFederationPropagation.cs
  - DataWarehouse.SDK/Security/MLS/DeclassificationGate.cs
  - DataWarehouse.Hardening.Tests/Security/MLS/MlsFederationTests.cs
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "Security labels propagate across federated VDE namespaces"
    - "Cross-classification-level data flow requires explicit declassification"
    - "Declassification produces audit trail entry with approver and justification"
    - "Label propagation preserves lattice dominance invariants"
  artifacts:
    - path: "DataWarehouse.SDK/Security/MLS/MlsFederationPropagation.cs"
      provides: "Security label propagation for federated VDE namespaces"
      exports: ["MlsFederationPropagator", "LabelPropagationPolicy"]
    - path: "DataWarehouse.SDK/Security/MLS/DeclassificationGate.cs"
      provides: "Explicit declassification gate with audit trail"
      exports: ["DeclassificationGate", "DeclassificationRequest", "DeclassificationAuditEntry"]
    - path: "DataWarehouse.Hardening.Tests/Security/MLS/MlsFederationTests.cs"
      provides: "Tests for label propagation and declassification"
  key_links:
    - from: "DataWarehouse.SDK/Security/MLS/MlsFederationPropagation.cs"
      to: "DataWarehouse.SDK/Security/MLS/SecurityLabel.cs"
      via: "SecurityLabel serialization for cross-node transfer"
      pattern: "SecurityLabel|Serialize|Deserialize"
    - from: "DataWarehouse.SDK/Security/MLS/DeclassificationGate.cs"
      to: "DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs"
      via: "SubjectClearance for approver verification"
      pattern: "SubjectClearance|MlsAccessDecision"
---

<objective>
Implement security label propagation across federated VDE namespaces with explicit declassification gate and audit trail.

Purpose: Federated deployments must propagate classification labels and prevent unauthorized cross-level data flows. Declassification requires explicit human approval with full audit.
Output: MlsFederationPropagator, DeclassificationGate, and tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Security/MLS/SecurityLabel.cs
@DataWarehouse.SDK/Security/MLS/SecurityClassification.cs
@DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs

<interfaces>
From DataWarehouse.SDK/Security/MLS/SecurityLabel.cs (from plan 120-01):
```csharp
public readonly record struct SecurityLabel
{
    public SecurityClassification Level { get; init; }
    public SecurityCompartment Compartments { get; init; }
    public bool Dominates(SecurityLabel other);
    public void Serialize(Span<byte> destination);
    public static SecurityLabel Deserialize(ReadOnlySpan<byte> source);
}
```

From DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs (from plan 120-02):
```csharp
public record SubjectClearance { public string SubjectId; public SecurityLabel ClearanceLabel; public bool IsSystemAdmin; }
public record MlsAccessDecision { public bool IsGranted; public string Reason; public string? ViolatedProperty; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DeclassificationGate with audit trail</name>
  <files>DataWarehouse.SDK/Security/MLS/DeclassificationGate.cs</files>
  <behavior>
    - DeclassificationRequest requires: approver SubjectClearance, source label, target label, justification
    - Approver must have clearance dominating the source label
    - Target label must be strictly lower than source label (actual downgrade)
    - Approved declassification produces DeclassificationAuditEntry with full context
    - Unapproved declassification throws UnauthorizedAccessException
  </behavior>
  <action>
    Create `DataWarehouse.SDK/Security/MLS/DeclassificationGate.cs`:
    1. `DeclassificationRequest` record: `SubjectClearance Approver`, `SecurityLabel SourceLabel`, `SecurityLabel TargetLabel`, `string Justification`, `string DataObjectId`, `DateTimeOffset RequestedAt`
    2. `DeclassificationAuditEntry` record: `string RequestId` (GUID), `DeclassificationRequest Request`, `bool Approved`, `string Reason`, `DateTimeOffset ProcessedAt`
    3. `DeclassificationGate` class:
       - `ProcessRequest(DeclassificationRequest request)` -> `DeclassificationAuditEntry`
       - Validation: approver clearance must dominate source label
       - Validation: target must be strictly lower than source (not same or higher)
       - Validation: justification must not be null/empty
       - On approval: return audit entry with Approved=true
       - On denial: throw `UnauthorizedAccessException` with denial reason
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 120: MLS declassification gate")]`.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>DeclassificationGate validates approver clearance, enforces downgrade-only, and produces audit entries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MlsFederationPropagator + federation tests</name>
  <files>DataWarehouse.SDK/Security/MLS/MlsFederationPropagation.cs, DataWarehouse.Hardening.Tests/Security/MLS/MlsFederationTests.cs</files>
  <behavior>
    - PropagateLabel(sourceLabel, targetNamespace) preserves or upgrades label (never downgrades without declassification)
    - LabelPropagationPolicy.MaxClassification limits what a namespace can receive
    - Data flowing to lower-max-classification namespace requires DeclassificationGate approval
    - Cross-namespace label serialization uses SecurityLabel.Serialize/Deserialize
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/MLS/MlsFederationPropagation.cs`:
       - `LabelPropagationPolicy` record: `string NamespaceId`, `SecurityClassification MaxClassification`, `SecurityCompartment AllowedCompartments`, `bool RequireDeclassification`
       - `MlsFederationPropagator` class:
         - `PropagateLabel(SecurityLabel sourceLabel, LabelPropagationPolicy targetPolicy)` -> `PropagationResult`
         - If sourceLabel.Level > targetPolicy.MaxClassification -> `PropagationResult.RequiresDeclassification`
         - If sourceLabel.Compartments has bits not in targetPolicy.AllowedCompartments -> `PropagationResult.CompartmentMismatch`
         - Otherwise -> `PropagationResult.Allowed` with propagated label
         - `SerializeLabelForTransfer(SecurityLabel label)` -> `byte[]` (8 bytes for cross-node wire format)
         - `DeserializeLabelFromTransfer(byte[] data)` -> `SecurityLabel`
       - `PropagationResult` record: `bool Allowed`, `bool RequiresDeclassification`, `SecurityLabel? PropagatedLabel`, `string? Reason`
    2. Create tests:
       - Test: Same-level propagation succeeds
       - Test: Lower-to-higher propagation succeeds (Confidential -> Secret namespace OK)
       - Test: Higher-to-lower propagation requires declassification
       - Test: Compartment mismatch blocked (SCI data -> non-SCI namespace)
       - Test: Label serialization for wire transfer round-trips correctly
       - Test: MaxClassification=Unclassified namespace rejects all classified data
       - Test: DeclassificationGate approver with insufficient clearance denied
       - Test: DeclassificationGate produces audit entry on approval
       - Test: DeclassificationGate rejects same-level (not a downgrade)
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 120: MLS federation label propagation")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~MlsFederation" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>MlsFederationPropagator handles cross-namespace label propagation with declassification gate integration. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "MlsFederation"` — all green
</verification>

<success_criteria>
- Labels propagate across federated namespaces
- Cross-classification flow requires declassification
- Declassification produces audit trail
- Wire format serialization correct
</success_criteria>

<output>
After completion, create `.planning/phases/120-multi-level-security/120-04-SUMMARY.md`
</output>
