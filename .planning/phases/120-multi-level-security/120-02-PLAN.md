---
phase: 120-multi-level-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs
  - DataWarehouse.SDK/Security/MLS/SubjectClearance.cs
  - DataWarehouse.Hardening.Tests/Security/MLS/BellLaPadulaEnforcerTests.cs
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "No read up: subject cannot read objects with higher classification than subject clearance"
    - "No write down: subject cannot write to objects with lower classification than subject clearance"
    - "Subject clearance derived from authenticated session context"
    - "Enforcement integrates with VDE AccessControlStage pipeline"
  artifacts:
    - path: "DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs"
      provides: "Bell-LaPadula confidentiality enforcer"
      exports: ["BellLaPadulaEnforcer", "IBellLaPadulaEnforcer", "MlsAccessDecision"]
    - path: "DataWarehouse.SDK/Security/MLS/SubjectClearance.cs"
      provides: "Subject clearance record derived from session"
      exports: ["SubjectClearance"]
    - path: "DataWarehouse.Hardening.Tests/Security/MLS/BellLaPadulaEnforcerTests.cs"
      provides: "Tests for BLP no-read-up, no-write-down"
  key_links:
    - from: "DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs"
      to: "DataWarehouse.SDK/Security/MLS/SecurityLabel.cs"
      via: "SecurityLabel.Dominates for lattice comparison"
      pattern: "SecurityLabel|Dominates"
    - from: "DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine/Pipeline/Stages/AccessControlStage.cs"
      via: "Integration point for VDE read/write pipeline"
      pattern: "AccessControlStage|IVdeReadStage"
---

<objective>
Implement Bell-LaPadula (BLP) confidentiality model enforcing "no read up, no write down" in VDE read/write pipeline.

Purpose: BLP is the formal confidentiality model for MLS systems. It prevents information flow from higher to lower classification levels.
Output: BellLaPadulaEnforcer, SubjectClearance, and tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Security/MLS/SecurityLabel.cs
@DataWarehouse.SDK/Security/MLS/SecurityClassification.cs
@DataWarehouse.SDK/VirtualDiskEngine/Pipeline/Stages/AccessControlStage.cs
@DataWarehouse.SDK/VirtualDiskEngine/Pipeline/IVdePipelineStage.cs

<interfaces>
From DataWarehouse.SDK/VirtualDiskEngine/Pipeline/Stages/AccessControlStage.cs:
```csharp
public sealed class AccessControlStage : IVdeReadStage
{
    public const string SecurityContextKey = "SecurityContext";
    public string StageName => "AccessControl";
    public ModuleId? ModuleGate => ModuleId.Security;
    public Task ExecuteAsync(VdePipelineContext context, CancellationToken ct);
}
```

From DataWarehouse.SDK/Security/MLS/SecurityLabel.cs (from plan 120-01):
```csharp
public readonly record struct SecurityLabel
{
    public SecurityClassification Level { get; init; }
    public SecurityCompartment Compartments { get; init; }
    public bool Dominates(SecurityLabel other);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SubjectClearance and BellLaPadulaEnforcer</name>
  <files>DataWarehouse.SDK/Security/MLS/SubjectClearance.cs, DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs, DataWarehouse.Hardening.Tests/Security/MLS/BellLaPadulaEnforcerTests.cs</files>
  <behavior>
    - SubjectClearance holds SecurityLabel for authenticated user session
    - BLP Read check: subject.Label.Dominates(object.Label) must be true (no read up)
    - BLP Write check: object.Label.Dominates(subject.Label) must be true (no write down)
    - Denied access returns MlsAccessDecision with reason string and violated property name
    - System subjects (IsSystemAdmin=true) bypass BLP checks with audit log
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/MLS/SubjectClearance.cs`:
       - `SubjectClearance` record: `string SubjectId`, `SecurityLabel ClearanceLabel`, `bool IsSystemAdmin`, `DateTimeOffset IssuedAt`, `DateTimeOffset? ExpiresAt`
       - `IsValid(DateTimeOffset? asOf = null)` — checks expiry
       - Factory: `FromSecurityContext(ISecurityContext ctx)` — maps roles to SecurityLabel (role "TopSecret" -> SecurityClassification.TopSecret, etc.)
    2. Create `DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs`:
       - `MlsAccessDecision` record: `bool IsGranted`, `string Reason`, `string? ViolatedProperty` ("SimpleSecurityProperty" for read-up, "StarProperty" for write-down)
       - `IBellLaPadulaEnforcer` interface:
         - `CheckRead(SubjectClearance subject, SecurityLabel objectLabel)` -> `MlsAccessDecision`
         - `CheckWrite(SubjectClearance subject, SecurityLabel objectLabel)` -> `MlsAccessDecision`
       - `BellLaPadulaEnforcer` implementation:
         - Read: if `!subject.ClearanceLabel.Dominates(objectLabel)` -> deny with "Simple Security Property violation: subject clearance {subject.Level} cannot read object at {object.Level}"
         - Write: if `!objectLabel.Dominates(subject.ClearanceLabel)` -> deny with "Star Property violation: subject clearance {subject.Level} cannot write down to {object.Level}"
         - System admin bypass: grant with reason "System admin bypass" and audit
    3. Create tests:
       - Test: Secret subject can read Confidential object (read down OK)
       - Test: Confidential subject CANNOT read Secret object (no read up)
       - Test: Secret subject can write to TopSecret object (write up OK)
       - Test: TopSecret subject CANNOT write to Confidential object (no write down)
       - Test: Same-level read and write both permitted
       - Test: Compartment mismatch denies access (Secret+SCI cannot read Secret+NATO)
       - Test: System admin bypasses all checks
       - Test: Expired clearance treated as invalid
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 120: Bell-LaPadula confidentiality enforcer")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~BellLaPadula" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>BellLaPadulaEnforcer correctly enforces no-read-up and no-write-down. Subject clearance maps from session context. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "BellLaPadula"` — all green
</verification>

<success_criteria>
- No read up enforced (Simple Security Property)
- No write down enforced (Star Property)
- System admin bypass with audit
- Compartment-aware dominance
</success_criteria>

<output>
After completion, create `.planning/phases/120-multi-level-security/120-02-SUMMARY.md`
</output>
