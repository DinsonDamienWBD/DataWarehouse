---
phase: 120-multi-level-security
plan: 03
type: execute
wave: 2
depends_on: [120-01]
files_modified:
  - DataWarehouse.SDK/Security/MLS/BibaIntegrityEnforcer.cs
  - DataWarehouse.Hardening.Tests/Security/MLS/BibaIntegrityEnforcerTests.cs
autonomous: true
requirements: [SEC-03]

must_haves:
  truths:
    - "No read down: subject cannot read objects with lower integrity than subject integrity level"
    - "No write up: subject cannot write to objects with higher integrity than subject integrity level"
    - "Biba operates on integrity labels independent of BLP confidentiality labels"
    - "Combined BLP+Biba enforcement possible via dual-label check"
  artifacts:
    - path: "DataWarehouse.SDK/Security/MLS/BibaIntegrityEnforcer.cs"
      provides: "Biba integrity model enforcer"
      exports: ["BibaIntegrityEnforcer", "IBibaIntegrityEnforcer", "IntegrityLabel", "IntegrityLevel"]
    - path: "DataWarehouse.Hardening.Tests/Security/MLS/BibaIntegrityEnforcerTests.cs"
      provides: "Tests for Biba no-read-down, no-write-up"
  key_links:
    - from: "DataWarehouse.SDK/Security/MLS/BibaIntegrityEnforcer.cs"
      to: "DataWarehouse.SDK/Security/MLS/SecurityLabel.cs"
      via: "Reuses lattice dominance pattern from SecurityLabel"
      pattern: "IntegrityLabel|Dominates"
---

<objective>
Implement Biba integrity model enforcing "no read down, no write up" as complement to Bell-LaPadula confidentiality.

Purpose: Biba prevents low-integrity processes from contaminating high-integrity data. Combined with BLP, it provides complete MLS enforcement.
Output: BibaIntegrityEnforcer, IntegrityLabel, and tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Security/MLS/SecurityLabel.cs
@DataWarehouse.SDK/Security/MLS/SecurityClassification.cs
@DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs

<interfaces>
From DataWarehouse.SDK/Security/MLS/SecurityClassification.cs (from plan 120-01):
```csharp
public enum SecurityClassification { Unclassified = 0, Confidential = 1, Secret = 2, TopSecret = 3 }
[Flags] public enum SecurityCompartment { None = 0, SCI = 1, SAP = 2, NATO = 4, FVEY = 8, NoForn = 16, RelTo = 32 }
```

From DataWarehouse.SDK/Security/MLS/BellLaPadulaEnforcer.cs (from plan 120-02):
```csharp
public record MlsAccessDecision { public bool IsGranted { get; init; } public string Reason { get; init; } public string? ViolatedProperty { get; init; } }
public record SubjectClearance { public string SubjectId { get; init; } public SecurityLabel ClearanceLabel { get; init; } public bool IsSystemAdmin { get; init; } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BibaIntegrityEnforcer with integrity labels</name>
  <files>DataWarehouse.SDK/Security/MLS/BibaIntegrityEnforcer.cs, DataWarehouse.Hardening.Tests/Security/MLS/BibaIntegrityEnforcerTests.cs</files>
  <behavior>
    - IntegrityLevel: Low=0, Medium=1, High=2, System=3
    - IntegrityLabel: IntegrityLevel + integrity compartments (reuse SecurityCompartment flags)
    - Biba Read check: object.IntegrityLabel.Dominates(subject.IntegrityLabel) — no read down
    - Biba Write check: subject.IntegrityLabel.Dominates(object.IntegrityLabel) — no write up
    - MlsAccessDecision reused with ViolatedProperty "BibaSimpleIntegrity" or "BibaStarIntegrity"
    - DualEnforcer.Check(subject, object, operation) runs both BLP and Biba
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/MLS/BibaIntegrityEnforcer.cs`:
       - `IntegrityLevel` enum: `Low = 0`, `Medium = 1`, `High = 2`, `System = 3`
       - `IntegrityLabel` readonly record struct: `IntegrityLevel Level`, `SecurityCompartment IntegrityCompartments`
         - `Dominates(IntegrityLabel other)` — level >= other.level AND compartments superset
         - `Serialize(Span<byte>)` / `Deserialize(ReadOnlySpan<byte>)` — 4 bytes (2 level, 2 compartments)
       - `IBibaIntegrityEnforcer` interface:
         - `CheckRead(IntegrityLabel subjectIntegrity, IntegrityLabel objectIntegrity)` -> MlsAccessDecision
         - `CheckWrite(IntegrityLabel subjectIntegrity, IntegrityLabel objectIntegrity)` -> MlsAccessDecision
       - `BibaIntegrityEnforcer` implementation:
         - Read: if `!objectIntegrity.Dominates(subjectIntegrity)` -> deny "Biba Simple Integrity Property: cannot read down"
         - Write: if `!subjectIntegrity.Dominates(objectIntegrity)` -> deny "Biba Star Integrity Property: cannot write up"
       - `MlsDualEnforcer` static class:
         - `CheckAccess(SubjectClearance subject, SecurityLabel objectConfidentiality, IntegrityLabel subjectIntegrity, IntegrityLabel objectIntegrity, MlsOperation operation)` — runs BLP + Biba, returns combined decision
         - `MlsOperation` enum: `Read`, `Write`, `Execute`, `Append`
    2. Create tests:
       - Test: High-integrity subject can read High-integrity object
       - Test: High-integrity subject CANNOT read Low-integrity object (no read down)
       - Test: Low-integrity subject can write to Low-integrity object
       - Test: Low-integrity subject CANNOT write to High-integrity object (no write up)
       - Test: DualEnforcer denies when BLP passes but Biba fails
       - Test: DualEnforcer denies when Biba passes but BLP fails
       - Test: DualEnforcer grants when both BLP and Biba pass
       - Test: IntegrityLabel serialize/deserialize round-trip
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 120: Biba integrity model enforcer")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~BibaIntegrity" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>BibaIntegrityEnforcer correctly enforces no-read-down and no-write-up. MlsDualEnforcer combines BLP+Biba. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "BibaIntegrity"` — all green
</verification>

<success_criteria>
- Biba no-read-down enforced (Simple Integrity Property)
- Biba no-write-up enforced (Star Integrity Property)
- Dual BLP+Biba enforcement via MlsDualEnforcer
</success_criteria>

<output>
After completion, create `.planning/phases/120-multi-level-security/120-03-SUMMARY.md`
</output>
