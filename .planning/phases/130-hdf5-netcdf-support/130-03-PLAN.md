---
phase: 130-hdf5-netcdf-support
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Infrastructure/Intelligence/ScientificStorageProfile.cs
  - Plugins/DataWarehouse.Plugins.UltimateDataFormat/Strategies/Scientific/Hdf5Strategy.cs
  - Plugins/DataWarehouse.Plugins.UltimateDataFormat/Strategies/Scientific/NetCdfStrategy.cs
  - DataWarehouse.Hardening.Tests/UltimateDataFormat/ScientificStorageProfileTests.cs
autonomous: true
requirements: [ADV-01]

must_haves:
  truths:
    - "Scientific workload profiles recommend delta encoding for climate time-series"
    - "SIMD-friendly data layout is configured for large homogeneous arrays"
    - "FormatStorageProfile integrates with HDF5 and NetCDF strategy Characteristics"
  artifacts:
    - path: "DataWarehouse.SDK/Infrastructure/Intelligence/ScientificStorageProfile.cs"
      provides: "HDF5/NetCDF-aware storage profile configuration"
      min_lines: 60
    - path: "DataWarehouse.Hardening.Tests/UltimateDataFormat/ScientificStorageProfileTests.cs"
      provides: "Tests for profile recommendations"
      min_lines: 40
  key_links:
    - from: "ScientificStorageProfile.cs"
      to: "Hdf5Strategy Characteristics"
      via: "strategy metadata"
      pattern: "Characteristics"
    - from: "ScientificStorageProfile.cs"
      to: "WorkloadAnalyzer"
      via: "workload profile integration"
      pattern: "WorkloadProfile"
---

<objective>
Create scientific workload storage profile that optimizes VDE storage for HDF5/NetCDF data patterns.

Purpose: Scientific data has distinct I/O patterns — large sequential reads, high compressibility via delta encoding, SIMD-friendly homogeneous arrays. The existing WorkloadProfile.ScientificImaging preset exists but lacks format-specific storage hints for HDF5/NetCDF.

Output: ScientificStorageProfile class, strategy Characteristics updates, passing tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DataWarehouse.SDK/Infrastructure/Intelligence/WorkloadAnalyzer.cs
@DataWarehouse.SDK/Infrastructure/Intelligence/PolicyAdvisor.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ScientificStorageProfile with format-specific hints</name>
  <files>
    DataWarehouse.SDK/Infrastructure/Intelligence/ScientificStorageProfile.cs,
    DataWarehouse.Hardening.Tests/UltimateDataFormat/ScientificStorageProfileTests.cs
  </files>
  <behavior>
    - Test: Profile for HDF5 recommends large read-ahead buffer (>=256KB)
    - Test: Profile for climate NetCDF recommends delta encoding compression
    - Test: Profile recommends SIMD-aligned layout for homogeneous float/double arrays
    - Test: Profile detects chunked vs contiguous dataset layout and adjusts VDE block size
  </behavior>
  <action>
1. Create `ScientificStorageProfile` in `DataWarehouse.SDK/Infrastructure/Intelligence/`:
   - Record type with properties: `PreferredBlockSize` (int), `ReadAheadBytes` (int), `CompressionHint` (enum: None/Delta/DeltaOfDelta/Gorilla), `LayoutHint` (enum: RowMajor/ColumnMajor/SimdAligned), `ChunkAlignment` (int)
   - Static factory `ForHdf5(DataFormatCapabilities caps, long datasetSize)`:
     - Large datasets (>10MB): ReadAhead=256KB, BlockSize=1MB, SimdAligned layout
     - Small datasets: ReadAhead=64KB, BlockSize=64KB, RowMajor layout
     - Chunked storage: align ChunkAlignment to HDF5 chunk size
   - Static factory `ForNetCdf(bool isTimeSeries, int variableCount)`:
     - Time-series: CompressionHint=Delta (temporal locality), ReadAhead=512KB
     - Multi-variable: ColumnMajor layout (variable-at-a-time access pattern)
   - Static factory `ForGenericScientific()` with balanced defaults

2. Update Hdf5Strategy `Characteristics` to include storage profile hints:
   - Add `"storage.readAhead"`, `"storage.preferredBlockSize"`, `"storage.compressionHint"`, `"storage.layoutHint"` keys

3. Update NetCdfStrategy `Characteristics` similarly.

4. Integrate with existing `PolicyAdvisor` pattern: when `WorkloadProfile` indicates scientific domain, the storage profile recommendations feed into VDE extent allocation decisions.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~ScientificStorageProfile" --no-restore -v minimal</automated>
  </verify>
  <done>
    - ScientificStorageProfile recommends delta encoding, SIMD layout, large read-ahead
    - Hdf5Strategy and NetCdfStrategy expose storage hints via Characteristics
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK` succeeds
- `dotnet build Plugins/DataWarehouse.Plugins.UltimateDataFormat` succeeds
- All ScientificStorageProfile tests pass
</verification>

<success_criteria>
Scientific workload storage profile provides format-aware optimization hints for HDF5/NetCDF data patterns — delta encoding for time-series, SIMD layout for arrays, appropriate block/chunk sizing.
</success_criteria>

<output>
After completion, create `.planning/phases/130-hdf5-netcdf-support/130-03-SUMMARY.md`
</output>
