---
phase: 136-cli-gui-production
plan: 06
type: execute
wave: 2
depends_on: [136-03]
files_modified:
  - DataWarehouse.GUI/Services/DynamicNavigationService.cs
  - DataWarehouse.GUI/Components/Shared/NavMenu.razor
  - DataWarehouse.GUI/MauiProgram.cs
autonomous: true
requirements: [APP-02]

must_haves:
  truths:
    - "GUI navigation menu auto-updates when plugins load/unload"
    - "Disabled pages show 'Plugin not loaded' with install instructions"
    - "Navigation reflects DynamicCommandRegistry state in real-time"
  artifacts:
    - path: "DataWarehouse.GUI/Services/DynamicNavigationService.cs"
      provides: "Bridge between DynamicCommandRegistry and GUI navigation"
      exports: ["DynamicNavigationService"]
  key_links:
    - from: "DataWarehouse.GUI/Services/DynamicNavigationService.cs"
      to: "DataWarehouse.Shared/DynamicCommandRegistry.cs"
      via: "Event subscription"
      pattern: "OnPluginLoaded|OnCapabilityChanged"
    - from: "DataWarehouse.GUI/Components/Shared/NavMenu.razor"
      to: "DataWarehouse.GUI/Services/DynamicNavigationService.cs"
      via: "Injected service"
      pattern: "@inject.*DynamicNavigationService"
---

<objective>
Wire GUI navigation to DynamicCommandRegistry so nav menu and pages reflect plugin availability.

Purpose: GUI must show only pages for loaded plugins. Unloaded plugin pages show "Plugin not loaded" with install instructions.
Output: DynamicNavigationService, updated NavMenu.razor, disabled-page handler.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.GUI/Components/Shared/NavMenu.razor
@DataWarehouse.GUI/MauiProgram.cs
@DataWarehouse.Shared/DynamicCommandRegistry.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DynamicNavigationService and update NavMenu</name>
  <files>
    DataWarehouse.GUI/Services/DynamicNavigationService.cs,
    DataWarehouse.GUI/Components/Shared/NavMenu.razor
  </files>
  <action>
DynamicNavigationService.cs:
1. Subscribes to DynamicCommandRegistry.OnPluginLoaded/Unloaded/CapabilityChanged
2. Maintains a Dictionary of page routes -> availability status
3. Exposes `IReadOnlyDictionary&lt;string, NavItem&gt; AvailablePages` (NavItem: route, label, icon, pluginId, isAvailable)
4. Fires `OnNavigationChanged` event for UI refresh
5. Maps plugin capabilities to page routes (e.g., "UltimatePolicyEngine" -> "/policy", "UltimateFederation" -> "/federation")

NavMenu.razor:
1. @inject DynamicNavigationService NavigationService
2. Subscribe to OnNavigationChanged -> StateHasChanged()
3. Render nav items from NavigationService.AvailablePages
4. Unavailable items: show grayed out with tooltip "Plugin {X} not loaded"
5. Group nav items by category (Storage, Security, Federation, AI, etc.)
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>NavMenu dynamically shows/hides pages based on plugin state. DynamicNavigationService tracks registry events.</done>
</task>

<task type="auto">
  <name>Task 2: Register DynamicNavigationService in DI and add disabled-page handler</name>
  <files>DataWarehouse.GUI/MauiProgram.cs</files>
  <action>
In MauiProgram.cs:
1. Register DynamicNavigationService as singleton in DI
2. Wire DynamicCommandRegistry from kernel into the navigation service
3. Add a catch-all route handler: when user navigates to a page for an unloaded plugin, show a "Plugin Not Loaded" page with: plugin name, install command (`dw plugin install {name}`), description of what the plugin provides
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>DynamicNavigationService registered in DI. Disabled pages show install instructions.</done>
</task>

</tasks>

<verification>
- DynamicNavigationService.cs subscribes to registry events
- NavMenu.razor renders dynamic nav items
- MauiProgram.cs registers the service
- `dotnet build DataWarehouse.GUI` succeeds
</verification>

<success_criteria>
GUI nav menu auto-updates as plugins load/unload. Unavailable pages show helpful "Plugin not loaded" message with install instructions. Navigation state tracks DynamicCommandRegistry in real-time.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-06-SUMMARY.md`
</output>
