---
phase: 136-cli-gui-production
plan: 10
type: execute
wave: 3
depends_on: [136-05, 136-06]
files_modified:
  - DataWarehouse.GUI/Services/KeyboardManager.cs
  - DataWarehouse.GUI/Components/Layout/MainLayout.razor
  - DataWarehouse.GUI/wwwroot/css/app.css
  - DataWarehouse.CLI/InteractiveMode.cs
autonomous: true
requirements: [APP-02]

must_haves:
  truths:
    - "GUI is fully keyboard-navigable without mouse"
    - "All interactive elements have screen reader labels"
    - "High-contrast theme available and toggleable"
    - "CLI InteractiveMode has configurable keybindings"
  artifacts:
    - path: "DataWarehouse.GUI/Services/KeyboardManager.cs"
      provides: "Keyboard shortcut management"
      contains: "KeyboardShortcut|RegisterShortcut"
    - path: "DataWarehouse.GUI/wwwroot/css/app.css"
      provides: "High-contrast theme styles"
      contains: "high-contrast"
  key_links:
    - from: "DataWarehouse.GUI/Components/Layout/MainLayout.razor"
      to: "DataWarehouse.GUI/Services/KeyboardManager.cs"
      via: "Keyboard event handler"
      pattern: "@onkeydown|KeyboardManager"
---

<objective>
Add full keyboard navigation, screen reader labels, high-contrast theme to GUI. Add configurable keybindings to CLI InteractiveMode.

Purpose: Production apps must be accessible. Keyboard-only operation and screen reader support are mandatory for enterprise/government deployments.
Output: Updated KeyboardManager, accessibility attributes on all pages, high-contrast CSS, CLI keybinding config.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.GUI/Services/KeyboardManager.cs
@DataWarehouse.GUI/Components/Layout/MainLayout.razor
@DataWarehouse.CLI/InteractiveMode.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: GUI keyboard navigation and accessibility</name>
  <files>
    DataWarehouse.GUI/Services/KeyboardManager.cs,
    DataWarehouse.GUI/Components/Layout/MainLayout.razor,
    DataWarehouse.GUI/wwwroot/css/app.css
  </files>
  <action>
KeyboardManager.cs updates:
1. Register global shortcuts: Ctrl+K (command palette), Ctrl+/ (help), Ctrl+1-9 (page navigation), Escape (close modal)
2. Per-page shortcuts: Ctrl+N (new item), Ctrl+S (save), Ctrl+D (delete with confirm), Ctrl+R (refresh)
3. Focus management: Tab order follows logical flow, Skip-to-content link, focus trap in modals
4. Shortcut cheat sheet overlay (Ctrl+?)

MainLayout.razor updates:
1. Add aria-label to all navigation elements
2. Add role="main" to content area, role="navigation" to nav
3. Add aria-live="polite" region for status updates
4. Add skip-to-content link as first focusable element

app.css additions:
1. High-contrast theme: `.high-contrast` class on body that overrides all colors to WCAG AAA contrast ratios
2. Focus-visible outlines (3px solid, high-contrast color)
3. Reduced-motion media query (prefers-reduced-motion)
4. Theme toggle persisted in localStorage
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>GUI is fully keyboard-navigable. Screen reader labels on all elements. High-contrast theme available.</done>
</task>

<task type="auto">
  <name>Task 2: CLI configurable keybindings</name>
  <files>DataWarehouse.CLI/InteractiveMode.cs</files>
  <action>
Update InteractiveMode:
1. Add keybinding configuration: read from `~/.dw/keybindings.json` (or default if missing)
2. Default bindings: Ctrl+R (reverse search), Ctrl+L (clear), Ctrl+D (exit), Tab (autocomplete), Up/Down (history), Ctrl+A/E (home/end)
3. Support rebinding any key combination to any action
4. Add `dw config keybindings show` and `dw config keybindings set <action> <key>` commands
5. Vi-mode toggle (`dw config keybindings mode vi/emacs`)
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>CLI InteractiveMode supports configurable keybindings with vi/emacs modes.</done>
</task>

</tasks>

<verification>
- KeyboardManager.cs has global and per-page shortcuts
- MainLayout.razor has ARIA attributes
- app.css has high-contrast theme
- InteractiveMode.cs supports configurable keybindings
- Both projects build successfully
</verification>

<success_criteria>
GUI is fully keyboard-navigable with screen reader labels and high-contrast theme. CLI InteractiveMode has configurable keybindings with vi/emacs modes.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-10-SUMMARY.md`
</output>
