---
phase: 136-cli-gui-production
plan: 08
type: execute
wave: 3
depends_on: [136-01, 136-02, 136-05, 136-07]
files_modified:
  - DataWarehouse.Hardening.Tests/CliGui/ParityVerificationTests.cs
autonomous: true
requirements: [APP-03]

must_haves:
  truths:
    - "Automated test enumerates all DynamicCommandRegistry entries"
    - "Test verifies CLI exposes equivalent functionality for each entry"
    - "Test verifies GUI exposes equivalent functionality for each entry"
    - "Zero parity gaps between CLI and GUI"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/CliGui/ParityVerificationTests.cs"
      provides: "Automated CLI/GUI parity verification"
      min_lines: 100
  key_links:
    - from: "DataWarehouse.Hardening.Tests/CliGui/ParityVerificationTests.cs"
      to: "DataWarehouse.Shared/DynamicCommandRegistry.cs"
      via: "Enumerate registered commands"
      pattern: "DynamicCommandRegistry"
---

<objective>
Create automated parity verification test that proves CLI and GUI expose equivalent functionality for every DynamicCommandRegistry entry.

Purpose: APP-03 requires 100% feature parity between CLI and GUI. Automated test catches parity gaps.
Output: Test file that enumerates registry, checks both CLI and GUI coverage, reports gaps.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.Shared/DynamicCommandRegistry.cs
@DataWarehouse.CLI/Program.cs
@DataWarehouse.GUI/MauiProgram.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CLI/GUI parity verification test</name>
  <files>DataWarehouse.Hardening.Tests/CliGui/ParityVerificationTests.cs</files>
  <behavior>
    - Test: All DynamicCommandRegistry command groups have a CLI handler
    - Test: All DynamicCommandRegistry command groups have a GUI page
    - Test: CLI command count matches GUI action count per group
    - Test: No orphan CLI commands without GUI equivalent
    - Test: No orphan GUI pages without CLI equivalent
  </behavior>
  <action>
Create ParityVerificationTests.cs in DataWarehouse.Hardening.Tests/CliGui/:

1. Test method: EnumerateAllDynamicCommands_BothCliAndGuiExpose
   - Instantiate DynamicCommandRegistry with all core commands registered
   - For each command group, verify a corresponding ICommandHandler exists in CLI assembly
   - For each command group, verify a corresponding .razor page exists in GUI assembly
   - Use reflection to scan CLI handlers and GUI pages
   - Assert zero gaps

2. Test method: CliCommandCount_MatchesGuiActionCount
   - For each command group, count CLI subcommands
   - For each command group, count GUI action buttons/forms
   - Assert counts match (or document intentional deviations)

3. Test method: NoOrphanCommands
   - Scan CLI for commands not in registry
   - Scan GUI for pages not in registry
   - Assert both sets are empty (or documented)

Use xUnit. Reference both DataWarehouse.CLI and DataWarehouse.GUI assemblies. Use reflection for discovery.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Parity test compiles. When run, it reports zero gaps between CLI and GUI command coverage.</done>
</task>

</tasks>

<verification>
- ParityVerificationTests.cs exists with 3+ test methods
- Tests use reflection to scan both assemblies
- `dotnet build DataWarehouse.Hardening.Tests` succeeds
</verification>

<success_criteria>
Automated parity test enumerates DynamicCommandRegistry, verifies both CLI and GUI cover every entry, reports zero parity gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-08-SUMMARY.md`
</output>
