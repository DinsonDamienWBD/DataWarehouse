---
phase: 136-cli-gui-production
plan: 05
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - DataWarehouse.GUI/Components/Pages/PolicyDashboard.razor
  - DataWarehouse.GUI/Components/Pages/AuthorityChain.razor
  - DataWarehouse.GUI/Components/Pages/ShardManager.razor
  - DataWarehouse.GUI/Components/Pages/AIAdvisorPanel.razor
  - DataWarehouse.GUI/Components/Pages/DeviceRaid.razor
  - DataWarehouse.GUI/Components/Pages/CrdtStatus.razor
  - DataWarehouse.GUI/Components/Pages/VdeDecorators.razor
autonomous: true
requirements: [APP-02]

must_haves:
  truths:
    - "PolicyDashboard page shows policy list, create/evaluate/cascade actions"
    - "AuthorityChain page shows quorum status, escalation, voting"
    - "ShardManager page shows shard list with split/merge/migrate actions"
    - "AIAdvisorPanel page shows recommendations with explain/audit"
    - "DeviceRaid page shows array status with create/rebuild"
    - "CrdtStatus page shows sync status and conflict resolution"
    - "VdeDecorators page shows decorator chain visualization"
  artifacts:
    - path: "DataWarehouse.GUI/Components/Pages/PolicyDashboard.razor"
      provides: "Policy management UI"
      min_lines: 80
    - path: "DataWarehouse.GUI/Components/Pages/ShardManager.razor"
      provides: "Shard lifecycle UI"
      min_lines: 80
    - path: "DataWarehouse.GUI/Components/Pages/VdeDecorators.razor"
      provides: "VDE decorator chain visualization"
      min_lines: 60
  key_links:
    - from: "DataWarehouse.GUI/Components/Pages/*.razor"
      to: "DataWarehouse.Kernel"
      via: "Injected kernel services"
      pattern: "@inject"
---

<objective>
Add 7 new GUI pages for v6.0 subsystems: PolicyDashboard, AuthorityChain, ShardManager, AIAdvisorPanel, DeviceRaid, CrdtStatus, VdeDecorators.

Purpose: GUI must expose all v6.0 capabilities visually. Currently missing pages for 7 subsystems.
Output: 7 new Razor pages with full CRUD/action UI, wired to kernel services.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.GUI/Components/Pages/Raid.razor
@DataWarehouse.GUI/Components/Pages/Storage.razor
@DataWarehouse.GUI/Components/Pages/FederationBrowser.razor
@DataWarehouse.GUI/_Imports.razor
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PolicyDashboard, AuthorityChain, and ShardManager pages</name>
  <files>
    DataWarehouse.GUI/Components/Pages/PolicyDashboard.razor,
    DataWarehouse.GUI/Components/Pages/AuthorityChain.razor,
    DataWarehouse.GUI/Components/Pages/ShardManager.razor
  </files>
  <action>
Follow existing page patterns (see Raid.razor, Storage.razor as templates). Each page uses @page directive, @inject for kernel services, @code block for logic.

PolicyDashboard.razor (@page "/policy"):
- Policy list table (Name, Status, LastEvaluated, Scope)
- Create policy form (name, rules JSON editor)
- Evaluate button per policy (shows pass/fail results)
- Cascade action with scope selector
- Dry-run toggle
- Status badges (Active/Draft/Archived)

AuthorityChain.razor (@page "/authority"):
- Chain visualization (hierarchical tree)
- Quorum status panel (required/current votes)
- Escalation button with level selector
- Vote panel for pending proposals (approve/reject/abstain)
- History log of past decisions

ShardManager.razor (@page "/shard"):
- Shard list with size, record count, node assignment
- Split button with strategy selector (range/hash/custom)
- Merge button for adjacent shards
- Migrate button with destination node picker
- Rebalance all button
- Migration progress indicator
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Three pages compile with full UI for policy, authority, and shard management.</done>
</task>

<task type="auto">
  <name>Task 2: Create AIAdvisorPanel, DeviceRaid, CrdtStatus, VdeDecorators pages</name>
  <files>
    DataWarehouse.GUI/Components/Pages/AIAdvisorPanel.razor,
    DataWarehouse.GUI/Components/Pages/DeviceRaid.razor,
    DataWarehouse.GUI/Components/Pages/CrdtStatus.razor,
    DataWarehouse.GUI/Components/Pages/VdeDecorators.razor
  </files>
  <action>
AIAdvisorPanel.razor (@page "/ai-advisor"):
- Recommendation cards (context, suggestion, confidence score)
- Explain button per recommendation (shows reasoning)
- Audit panel (select target, depth, run audit)
- Provider list with configuration
- History of past recommendations

DeviceRaid.razor (@page "/device-raid"):
- Array list (level, devices, status, health %)
- Create array form (level selector, device picker)
- Per-array: status detail, rebuild button, add/remove device
- Health heatmap for device status
- Rebuild progress indicator

CrdtStatus.razor (@page "/crdt"):
- Namespace list with sync status (synced/pending/conflict)
- Per-namespace: peer list, last sync time, conflict count
- Sync now button per peer
- Conflict resolution panel (auto/manual strategies)
- Sync history timeline

VdeDecorators.razor (@page "/vde-decorators"):
- Visual decorator chain: Cache->Integrity->Compression->Dedup->Encryption->WAL->RAID->File
- Per-decorator: enabled/disabled toggle, configuration panel
- Mount pipeline visualization (reads SuperblockV2 features)
- Performance metrics per decorator layer
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Four pages compile with full UI for AI advisor, device RAID, CRDT, and VDE decorators.</done>
</task>

</tasks>

<verification>
- All 7 new .razor files exist under Components/Pages/
- Each has @page route, @inject services, full UI
- `dotnet build DataWarehouse.GUI` succeeds
</verification>

<success_criteria>
7 new GUI pages render v6.0 subsystem data with full CRUD/action capabilities. All pages follow existing Blazor patterns. GUI project builds.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-05-SUMMARY.md`
</output>
