---
phase: 136-cli-gui-production
plan: 09
type: execute
wave: 3
depends_on: [136-01, 136-03]
files_modified:
  - DataWarehouse.CLI/ShellCompletions/ShellCompletionGenerator.cs
autonomous: true
requirements: [APP-01]

must_haves:
  truths:
    - "bash/zsh/fish/PowerShell completions generated for all commands including v6.0"
    - "Completions reflect DynamicCommandRegistry state"
    - "Completions update when plugins load/unload"
  artifacts:
    - path: "DataWarehouse.CLI/ShellCompletions/ShellCompletionGenerator.cs"
      provides: "Shell completion generation for all 4 shells"
      contains: "policy|federation|authority|shard|crdt|ai-advisor"
  key_links:
    - from: "DataWarehouse.CLI/ShellCompletions/ShellCompletionGenerator.cs"
      to: "DataWarehouse.Shared/DynamicCommandRegistry.cs"
      via: "Command enumeration"
      pattern: "DynamicCommandRegistry"
---

<objective>
Update ShellCompletionGenerator for all v6.0 commands and dynamic registry integration.

Purpose: Shell completions must cover all commands (including v6.0) and update dynamically as plugins load/unload.
Output: Updated ShellCompletionGenerator that reads from DynamicCommandRegistry.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.CLI/ShellCompletions/ShellCompletionGenerator.cs
@DataWarehouse.Shared/DynamicCommandRegistry.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ShellCompletionGenerator for v6.0 and dynamic registry</name>
  <files>DataWarehouse.CLI/ShellCompletions/ShellCompletionGenerator.cs</files>
  <action>
Update ShellCompletionGenerator to:
1. Read command tree from DynamicCommandRegistry instead of hardcoded list
2. Generate completions for all 4 shells: bash (_dw_completions function), zsh (_dw compdef), fish (complete -c dw), PowerShell (Register-ArgumentCompleter)
3. Include all v6.0 command groups: policy, federation, authority, shard, device-raid, ai-advisor, crdt
4. Include subcommand completions (e.g., `dw policy <TAB>` shows create, evaluate, cascade, dry-run, list, delete)
5. Include option completions (e.g., `dw policy create --<TAB>` shows --name, --rules-file)
6. Add `dw completions generate --shell {bash|zsh|fish|powershell}` command
7. Add `dw completions install` command that detects current shell and installs completion script
8. For dynamic mode (InteractiveMode), regenerate completions when OnCapabilityChanged fires
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>ShellCompletionGenerator reads from DynamicCommandRegistry. Generates completions for 4 shells including all v6.0 commands.</done>
</task>

</tasks>

<verification>
- ShellCompletionGenerator.cs reads from DynamicCommandRegistry
- Contains completion logic for bash, zsh, fish, PowerShell
- Includes all v6.0 command groups
- `dotnet build DataWarehouse.CLI` succeeds
</verification>

<success_criteria>
Shell completions cover all commands (including v6.0), are generated from DynamicCommandRegistry, and support all 4 shell types.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-09-SUMMARY.md`
</output>
