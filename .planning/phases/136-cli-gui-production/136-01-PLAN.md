---
phase: 136-cli-gui-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.CLI/Commands/PolicyCommands.cs
  - DataWarehouse.CLI/Commands/FederationCommands.cs
  - DataWarehouse.CLI/Commands/AuthorityCommands.cs
  - DataWarehouse.CLI/Commands/ShardCommands.cs
  - DataWarehouse.CLI/Commands/DeviceRaidCommands.cs
  - DataWarehouse.CLI/Commands/AIAdvisorCommands.cs
  - DataWarehouse.CLI/Commands/CrdtCommands.cs
  - DataWarehouse.CLI/Program.cs
autonomous: true
requirements: [APP-01]

must_haves:
  truths:
    - "User can run `dw policy create/evaluate/cascade/dry-run` commands"
    - "User can run `dw federation init/join/status/shard` commands"
    - "User can run `dw authority quorum/escalate/vote` commands"
    - "User can run `dw shard list/split/merge/migrate` commands"
    - "User can run `dw device-raid create/status/rebuild` commands"
    - "User can run `dw ai-advisor recommend/explain/audit` commands"
    - "User can run `dw crdt status/sync/conflict` commands"
  artifacts:
    - path: "DataWarehouse.CLI/Commands/PolicyCommands.cs"
      provides: "Policy engine CLI commands"
      exports: ["PolicyCommands"]
    - path: "DataWarehouse.CLI/Commands/FederationCommands.cs"
      provides: "Federation CLI commands"
      exports: ["FederationCommands"]
    - path: "DataWarehouse.CLI/Commands/AuthorityCommands.cs"
      provides: "Authority chain CLI commands"
      exports: ["AuthorityCommands"]
    - path: "DataWarehouse.CLI/Commands/ShardCommands.cs"
      provides: "Shard lifecycle CLI commands"
      exports: ["ShardCommands"]
    - path: "DataWarehouse.CLI/Commands/DeviceRaidCommands.cs"
      provides: "Device RAID CLI commands"
      exports: ["DeviceRaidCommands"]
    - path: "DataWarehouse.CLI/Commands/AIAdvisorCommands.cs"
      provides: "AI advisor CLI commands"
      exports: ["AIAdvisorCommands"]
    - path: "DataWarehouse.CLI/Commands/CrdtCommands.cs"
      provides: "CRDT CLI commands"
      exports: ["CrdtCommands"]
  key_links:
    - from: "DataWarehouse.CLI/Program.cs"
      to: "All new command classes"
      via: "System.CommandLine command registration"
      pattern: "rootCommand\\.Add"
---

<objective>
Add all missing v6.0 command groups to the CLI: policy, federation, authority, shard, device-raid, ai-advisor, crdt.

Purpose: CLI must expose every v6.0+ kernel capability. Currently missing 7 command groups for subsystems added in v6.0.
Output: 7 new command files registered in Program.cs, each with full CRUD/action subcommands.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@DataWarehouse.CLI/Program.cs
@DataWarehouse.CLI/Commands/RaidCommands.cs
@DataWarehouse.CLI/Commands/VdeCommands.cs
@DataWarehouse.Shared/DynamicCommandRegistry.cs

<interfaces>
<!-- Existing CLI command pattern (follow this for all new commands) -->
From DataWarehouse.CLI/Commands/RaidCommands.cs — static class with async methods:
```csharp
public static class RaidCommands
{
    public static async Task CreateArrayAsync(string level, string[] devices, ...);
    public static async Task ShowStatusAsync(string? arrayId);
    // ...
}
```

From DataWarehouse.Shared/DynamicCommandRegistry.cs:
```csharp
public void OnPluginLoaded(string pluginId, List<string> capabilities);
public void OnPluginUnloaded(string pluginId);
public void OnCapabilityChanged(string pluginId, string capability, bool available);
public bool IsCommandAvailable(string commandName);
public void RegisterCoreCommand(DynamicCommandDefinition def);
```

From DataWarehouse.CLI/Program.cs — System.CommandLine root command tree:
```csharp
var rootCommand = new RootCommand("DataWarehouse CLI");
// Add subcommands per group
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 7 v6.0 command group files</name>
  <files>
    DataWarehouse.CLI/Commands/PolicyCommands.cs,
    DataWarehouse.CLI/Commands/FederationCommands.cs,
    DataWarehouse.CLI/Commands/AuthorityCommands.cs,
    DataWarehouse.CLI/Commands/ShardCommands.cs,
    DataWarehouse.CLI/Commands/DeviceRaidCommands.cs,
    DataWarehouse.CLI/Commands/AIAdvisorCommands.cs,
    DataWarehouse.CLI/Commands/CrdtCommands.cs
  </files>
  <action>
Create 7 new static command classes following the existing pattern (see RaidCommands.cs, VdeCommands.cs as templates).

PolicyCommands.cs: `dw policy create --name --rules-file`, `dw policy evaluate --policy-id --target`, `dw policy cascade --policy-id --scope`, `dw policy dry-run --policy-id --target`, `dw policy list`, `dw policy delete --policy-id`.

FederationCommands.cs: `dw federation init --name --config-file`, `dw federation join --seed-address`, `dw federation status`, `dw federation shard --action --shard-id`, `dw federation leave`, `dw federation nodes`.

AuthorityCommands.cs: `dw authority quorum --action --chain-id`, `dw authority escalate --request-id --level`, `dw authority vote --proposal-id --decision`, `dw authority status`, `dw authority chain --chain-id`.

ShardCommands.cs: `dw shard list`, `dw shard split --shard-id --strategy`, `dw shard merge --source --target`, `dw shard migrate --shard-id --destination`, `dw shard status --shard-id`, `dw shard rebalance`.

DeviceRaidCommands.cs: `dw device-raid create --level --devices`, `dw device-raid status --array-id`, `dw device-raid rebuild --array-id --device`, `dw device-raid add --array-id --device`, `dw device-raid remove --array-id --device`.

AIAdvisorCommands.cs: `dw ai-advisor recommend --context --scope`, `dw ai-advisor explain --decision-id`, `dw ai-advisor audit --target --depth`, `dw ai-advisor providers`, `dw ai-advisor configure --provider --key`.

CrdtCommands.cs: `dw crdt status --namespace`, `dw crdt sync --peer --namespace`, `dw crdt conflict --namespace --resolve-strategy`, `dw crdt history --namespace --limit`, `dw crdt peers`.

Each command: use System.CommandLine with Option/Argument, produce CommandResult, render via ConsoleRenderer. Use production kernel access patterns (no stubs — connect to real kernel APIs via DataWarehouseKernel singleton or DI).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 command files compile, each with full subcommand tree matching the spec above.</done>
</task>

<task type="auto">
  <name>Task 2: Register all new command groups in Program.cs</name>
  <files>DataWarehouse.CLI/Program.cs</files>
  <action>
Wire all 7 new command groups into the CLI root command tree in Program.cs. Follow the existing pattern for how RaidCommands, VdeCommands, etc. are registered. Each group becomes a top-level subcommand under rootCommand (e.g., `rootCommand.Add(policyCommand)`). Ensure --help for each group lists all subcommands.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Running `dw --help` would list policy, federation, authority, shard, device-raid, ai-advisor, crdt as available command groups.</done>
</task>

</tasks>

<verification>
- All 7 new command files exist under DataWarehouse.CLI/Commands/
- Program.cs registers all 7 command groups
- `dotnet build DataWarehouse.CLI` succeeds with zero errors
</verification>

<success_criteria>
CLI project builds. 7 new command groups (policy, federation, authority, shard, device-raid, ai-advisor, crdt) are registered with full subcommand trees. Each command connects to kernel APIs — no stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-01-SUMMARY.md`
</output>
