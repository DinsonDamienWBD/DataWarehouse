---
phase: 136-cli-gui-production
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Shared/Services/NaturalLanguageProcessor.cs
  - DataWarehouse.CLI/InteractiveMode.cs
autonomous: true
requirements: [APP-01]

must_haves:
  truths:
    - "NLP understands v6.0 concepts: policy, federation, authority, shard, CRDT, VDE decorators"
    - "Natural language queries resolve to correct v6.0 commands"
    - "KnowledgeLake is queried for available strategy/capability info"
    - "Graceful degradation when Intelligence plugin not loaded"
  artifacts:
    - path: "DataWarehouse.Shared/Services/NaturalLanguageProcessor.cs"
      provides: "Updated NLP with v6.0 vocabulary"
      contains: "policy|federation|authority|shard|crdt"
  key_links:
    - from: "DataWarehouse.Shared/Services/NaturalLanguageProcessor.cs"
      to: "DataWarehouse.Kernel/Registry/KnowledgeLake.cs"
      via: "Query for available capabilities"
      pattern: "KnowledgeLake|IKnowledgeLake"
---

<objective>
Update NaturalLanguageProcessor and InteractiveMode for v6.0 concepts.

Purpose: Users can ask natural language questions about v6.0 features (policies, federation, shards, CRDT, AI advisor) and get correct command suggestions or knowledge answers.
Output: Updated NLP vocabulary, KnowledgeLake integration, graceful degradation.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.Shared/Services/NaturalLanguageProcessor.cs
@DataWarehouse.CLI/InteractiveMode.cs
@DataWarehouse.Kernel/Registry/KnowledgeLake.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update NLP vocabulary and KnowledgeLake integration</name>
  <files>DataWarehouse.Shared/Services/NaturalLanguageProcessor.cs</files>
  <action>
Update NaturalLanguageProcessor to:
1. Add v6.0 intent mappings: "create policy" -> `dw policy create`, "show federation status" -> `dw federation status`, "split shard" -> `dw shard split`, "CRDT conflicts" -> `dw crdt conflict`, "AI recommendations" -> `dw ai-advisor recommend`, "authority quorum" -> `dw authority quorum`, "device raid status" -> `dw device-raid status`, "VDE decorators" -> `dw vde decorators`
2. Add domain synonyms: "rule" -> policy, "cluster" -> federation, "partition" -> shard, "replicate" -> crdt sync, "smart suggest" -> ai-advisor
3. Integrate with KnowledgeLake: when query matches "what X are available?" pattern, query KnowledgeLake for loaded strategies/capabilities and return human-readable list
4. Handle "explain" queries: "explain policy cascade" -> look up concept in KnowledgeLake, format explanation
5. Graceful degradation: if KnowledgeLake or Intelligence plugin not available, fall back to static vocabulary matching with message "Install Intelligence plugin for AI-powered answers"
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Shared/DataWarehouse.Shared.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>NLP recognizes all v6.0 concepts, maps to correct commands, queries KnowledgeLake for dynamic info.</done>
</task>

<task type="auto">
  <name>Task 2: Update InteractiveMode for v6.0 features</name>
  <files>DataWarehouse.CLI/InteractiveMode.cs</files>
  <action>
Update InteractiveMode to:
1. Include v6.0 commands in autocomplete suggestions
2. Add context-aware help: when user types "dw policy" and pauses, show available subcommands
3. Add "?" shortcut that triggers NLP explain for the current command context
4. Show plugin availability status in the interactive prompt (e.g., "[12/52 plugins]" in prompt)
5. Handle NLP responses that include KnowledgeLake data — format as tables or bullet lists
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>InteractiveMode shows v6.0 commands in autocomplete, supports context-aware help, displays plugin status.</done>
</task>

</tasks>

<verification>
- NaturalLanguageProcessor.cs contains v6.0 vocabulary mappings
- InteractiveMode.cs includes v6.0 autocomplete
- Both projects build successfully
</verification>

<success_criteria>
NLP resolves v6.0 natural language queries to correct commands. KnowledgeLake provides dynamic capability info. Graceful fallback when Intelligence plugin absent. InteractiveMode autocompletes v6.0 commands.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-04-SUMMARY.md`
</output>
