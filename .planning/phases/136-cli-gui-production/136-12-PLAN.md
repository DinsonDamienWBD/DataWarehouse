---
phase: 136-cli-gui-production
plan: 12
type: execute
wave: 4
depends_on: [136-08, 136-09, 136-10]
files_modified:
  - DataWarehouse.CLI/ConsoleRenderer.cs
  - DataWarehouse.CLI/ErrorHandler.cs
  - DataWarehouse.GUI/Services/ErrorHandlingService.cs
  - DataWarehouse.GUI/Components/Shared/ErrorBoundary.razor
  - DataWarehouse.GUI/Components/Shared/ProgressIndicator.razor
autonomous: true
requirements: [APP-01, APP-02]

must_haves:
  truths:
    - "Consistent error messages across CLI and GUI"
    - "Progress bars shown for long operations (>2s)"
    - "Ctrl+C cancels current operation in CLI"
    - "Cancel button cancels current operation in GUI"
    - "Retry prompts for transient failures"
  artifacts:
    - path: "DataWarehouse.CLI/ErrorHandler.cs"
      provides: "Centralized CLI error handling"
      exports: ["ErrorHandler"]
    - path: "DataWarehouse.GUI/Services/ErrorHandlingService.cs"
      provides: "Centralized GUI error handling"
      exports: ["ErrorHandlingService"]
  key_links:
    - from: "DataWarehouse.CLI/ErrorHandler.cs"
      to: "DataWarehouse.GUI/Services/ErrorHandlingService.cs"
      via: "Shared error message format"
      pattern: "ErrorCode|ErrorMessage"
---

<objective>
Polish error handling and UX across CLI and GUI: consistent errors, progress bars, cancellation, retry prompts.

Purpose: Production apps must handle errors gracefully, show progress for long operations, support cancellation, and offer retry for transient failures.
Output: Shared error handling, progress indicators, cancellation support, retry logic.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.CLI/ConsoleRenderer.cs
@DataWarehouse.GUI/Services/DashboardFramework.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: CLI error handling and UX polish</name>
  <files>
    DataWarehouse.CLI/ErrorHandler.cs,
    DataWarehouse.CLI/ConsoleRenderer.cs
  </files>
  <action>
ErrorHandler.cs (new):
1. Centralized error handler: catches all unhandled exceptions from commands
2. Classifies errors: TransientError (retry-able), ValidationError (user fix needed), FatalError (bug report), AuthError (re-authenticate)
3. Transient errors: auto-retry up to 3 times with exponential backoff, then offer manual retry prompt
4. Validation errors: show specific field/parameter that failed with expected format
5. Fatal errors: show error code, stack trace (only with --verbose), offer `dw report-bug` command
6. All errors include error code (DW-XXXX format) for searchable documentation

ConsoleRenderer.cs updates:
1. Progress bars: automatic for any operation >2 seconds (spinner -> progress bar)
2. Ctrl+C handler: CancellationTokenSource propagated to all async operations, shows "Operation cancelled" on interrupt
3. Multi-step progress: numbered steps ("Step 2/5: Encrypting...") with elapsed time
4. Color-coded output: errors in red, warnings in yellow, success in green (respects NO_COLOR env var)
5. Output format support: --format table|json|csv|yaml for all command output
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>CLI has centralized error handling with retry logic, progress bars, cancellation, color-coded output.</done>
</task>

<task type="auto">
  <name>Task 2: GUI error handling and UX polish</name>
  <files>
    DataWarehouse.GUI/Services/ErrorHandlingService.cs,
    DataWarehouse.GUI/Components/Shared/ErrorBoundary.razor,
    DataWarehouse.GUI/Components/Shared/ProgressIndicator.razor
  </files>
  <action>
ErrorHandlingService.cs (new):
1. Mirror CLI error classification (TransientError, ValidationError, FatalError, AuthError)
2. Use same error codes (DW-XXXX) for consistency
3. Transient errors: show toast with retry button, auto-retry with backoff
4. Validation errors: highlight specific form fields with error messages
5. Fatal errors: show error dialog with error code, option to copy details
6. Log all errors to diagnostic service

ErrorBoundary.razor (new):
1. Wrap page content in Blazor ErrorBoundary
2. Show friendly error page with: what happened, error code, retry button, report button
3. Never show raw stack traces to users

ProgressIndicator.razor (new):
1. Reusable progress component: indeterminate (spinner) or determinate (bar with %)
2. Cancel button that fires CancellationTokenSource
3. Elapsed time display
4. Step indicator for multi-step operations
5. Use in all long-running GUI operations (backup, migration, audit, etc.)
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>GUI has centralized error handling, ErrorBoundary component, reusable ProgressIndicator, consistent error codes.</done>
</task>

</tasks>

<verification>
- ErrorHandler.cs in CLI with error classification
- ErrorHandlingService.cs in GUI with matching error codes
- ErrorBoundary.razor wraps page content
- ProgressIndicator.razor reusable component
- Both projects build successfully
</verification>

<success_criteria>
Consistent error messages (same DW-XXXX codes) across CLI and GUI. Progress bars for long operations. Cancellation support. Retry prompts for transient failures. Both projects build.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-12-SUMMARY.md`
</output>
