---
phase: 136-cli-gui-production
plan: 07
type: execute
wave: 2
depends_on: [136-05]
files_modified:
  - DataWarehouse.GUI/Services/IPolicyService.cs
  - DataWarehouse.GUI/Services/PolicyService.cs
  - DataWarehouse.GUI/Services/IFederationService.cs
  - DataWarehouse.GUI/Services/FederationService.cs
  - DataWarehouse.GUI/Services/IShardService.cs
  - DataWarehouse.GUI/Services/ShardService.cs
  - DataWarehouse.GUI/Services/IAIAdvisorService.cs
  - DataWarehouse.GUI/Services/AIAdvisorService.cs
  - DataWarehouse.GUI/Services/IRaidService.cs
  - DataWarehouse.GUI/Services/RaidService.cs
  - DataWarehouse.GUI/MauiProgram.cs
autonomous: true
requirements: [APP-02]

must_haves:
  truths:
    - "All v6.0 GUI pages use injected service interfaces, not inline kernel calls"
    - "Each service is independently testable with mock kernel"
    - "Services are registered in DI container"
  artifacts:
    - path: "DataWarehouse.GUI/Services/IPolicyService.cs"
      provides: "Policy service contract"
      exports: ["IPolicyService"]
    - path: "DataWarehouse.GUI/Services/IFederationService.cs"
      provides: "Federation service contract"
      exports: ["IFederationService"]
    - path: "DataWarehouse.GUI/Services/IShardService.cs"
      provides: "Shard service contract"
      exports: ["IShardService"]
  key_links:
    - from: "DataWarehouse.GUI/Components/Pages/PolicyDashboard.razor"
      to: "DataWarehouse.GUI/Services/IPolicyService.cs"
      via: "@inject IPolicyService"
      pattern: "@inject.*IPolicyService"
---

<objective>
Extract all inline Razor logic from v6.0 GUI pages to testable service classes with DI-registered interfaces.

Purpose: Inline kernel calls in Razor pages are untestable. Service extraction enables unit testing and clean separation of concerns.
Output: 5 service interfaces + implementations, DI registration, pages updated to use services.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.GUI/Services/DashboardFramework.cs
@DataWarehouse.GUI/Services/StorageManagementService.cs
@DataWarehouse.GUI/MauiProgram.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service interfaces and implementations</name>
  <files>
    DataWarehouse.GUI/Services/IPolicyService.cs,
    DataWarehouse.GUI/Services/PolicyService.cs,
    DataWarehouse.GUI/Services/IFederationService.cs,
    DataWarehouse.GUI/Services/FederationService.cs,
    DataWarehouse.GUI/Services/IShardService.cs,
    DataWarehouse.GUI/Services/ShardService.cs,
    DataWarehouse.GUI/Services/IAIAdvisorService.cs,
    DataWarehouse.GUI/Services/AIAdvisorService.cs,
    DataWarehouse.GUI/Services/IRaidService.cs,
    DataWarehouse.GUI/Services/RaidService.cs
  </files>
  <action>
Follow existing service patterns (see StorageManagementService.cs as template).

IPolicyService / PolicyService:
- GetPoliciesAsync(), GetPolicyAsync(id), CreatePolicyAsync(name, rules), EvaluatePolicyAsync(id, target), CascadePolicyAsync(id, scope), DryRunPolicyAsync(id, target), DeletePolicyAsync(id)

IFederationService / FederationService:
- GetStatusAsync(), GetNodesAsync(), InitFederationAsync(name, config), JoinFederationAsync(seed), LeaveFederationAsync()

IShardService / ShardService:
- GetShardsAsync(), GetShardAsync(id), SplitShardAsync(id, strategy), MergeShardsAsync(source, target), MigrateShardAsync(id, destination), RebalanceAsync()

IAIAdvisorService / AIAdvisorService:
- GetRecommendationsAsync(context, scope), ExplainDecisionAsync(id), RunAuditAsync(target, depth), GetProvidersAsync(), ConfigureProviderAsync(provider, key)

IRaidService / RaidService:
- GetArraysAsync(), GetArrayAsync(id), CreateArrayAsync(level, devices), RebuildArrayAsync(id, device), AddDeviceAsync(id, device), RemoveDeviceAsync(id, device)

Each implementation takes DataWarehouseKernel via constructor injection and delegates to the appropriate kernel subsystem. All methods async. No stubs — real kernel integration.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>5 service interfaces and 5 implementations exist, all compile, all connect to kernel APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Register services in DI and update pages</name>
  <files>DataWarehouse.GUI/MauiProgram.cs</files>
  <action>
1. Register all 5 service pairs in MauiProgram.cs: `builder.Services.AddTransient&lt;IPolicyService, PolicyService&gt;()` etc.
2. Update the 7 new pages from plan 136-05 to @inject the appropriate service interface instead of any inline kernel access
3. Verify existing pages also use service pattern (audit existing pages like Raid.razor — if they have inline logic, extract to service as well)
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.GUI/DataWarehouse.GUI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>All services registered in DI. All v6.0 pages use @inject services. No inline kernel calls in Razor code.</done>
</task>

</tasks>

<verification>
- 10 service files exist (5 interfaces + 5 implementations)
- MauiProgram.cs registers all services
- GUI pages use @inject services
- `dotnet build DataWarehouse.GUI` succeeds
</verification>

<success_criteria>
All v6.0 GUI pages delegate to testable service classes via DI. Each service connects to real kernel APIs. No inline kernel calls remain in Razor pages.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-07-SUMMARY.md`
</output>
