---
phase: 136-cli-gui-production
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.CLI/Handlers/ICommandHandler.cs
  - DataWarehouse.CLI/Handlers/AuditCommandHandler.cs
  - DataWarehouse.CLI/Handlers/BackupCommandHandler.cs
  - DataWarehouse.CLI/Handlers/BenchmarkCommandHandler.cs
  - DataWarehouse.CLI/Handlers/ComplianceCommandHandler.cs
  - DataWarehouse.CLI/Handlers/ConfigCommandHandler.cs
  - DataWarehouse.CLI/Handlers/HealthCommandHandler.cs
  - DataWarehouse.CLI/Handlers/PluginCommandHandler.cs
  - DataWarehouse.CLI/Handlers/StorageCommandHandler.cs
  - DataWarehouse.CLI/Handlers/VdeCommandHandler.cs
  - DataWarehouse.CLI/Handlers/RaidCommandHandler.cs
  - DataWarehouse.CLI/Program.cs
  - DataWarehouse.CLI/DataWarehouse.CLI.csproj
autonomous: true
requirements: [APP-01]

must_haves:
  truths:
    - "All CLI commands are resolved via DI, not static method calls"
    - "ICommandHandler interface defines the command handler contract"
    - "Each command handler is registered in DI container"
    - "Commands can be unit tested with mocked kernel dependencies"
  artifacts:
    - path: "DataWarehouse.CLI/Handlers/ICommandHandler.cs"
      provides: "Command handler contract"
      exports: ["ICommandHandler"]
    - path: "DataWarehouse.CLI/Program.cs"
      provides: "DI container setup for command handlers"
      contains: "services.AddTransient<ICommandHandler"
  key_links:
    - from: "DataWarehouse.CLI/Program.cs"
      to: "DataWarehouse.CLI/Handlers/*.cs"
      via: "Microsoft.Extensions.DependencyInjection"
      pattern: "services\\.Add"
---

<objective>
Migrate all existing static CLI command classes to DI-driven handlers with ICommandHandler interface.

Purpose: Static command classes cannot be unit tested and cannot receive injected kernel services. DI migration enables testability and proper kernel lifecycle management.
Output: ICommandHandler interface, handler implementations for all existing commands, DI registration in Program.cs.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DataWarehouse.CLI/Program.cs
@DataWarehouse.CLI/Commands/AuditCommands.cs
@DataWarehouse.CLI/Commands/StorageCommands.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ICommandHandler interface and handler implementations</name>
  <files>
    DataWarehouse.CLI/Handlers/ICommandHandler.cs,
    DataWarehouse.CLI/Handlers/AuditCommandHandler.cs,
    DataWarehouse.CLI/Handlers/BackupCommandHandler.cs,
    DataWarehouse.CLI/Handlers/BenchmarkCommandHandler.cs,
    DataWarehouse.CLI/Handlers/ComplianceCommandHandler.cs,
    DataWarehouse.CLI/Handlers/ConfigCommandHandler.cs,
    DataWarehouse.CLI/Handlers/HealthCommandHandler.cs,
    DataWarehouse.CLI/Handlers/PluginCommandHandler.cs,
    DataWarehouse.CLI/Handlers/StorageCommandHandler.cs,
    DataWarehouse.CLI/Handlers/VdeCommandHandler.cs,
    DataWarehouse.CLI/Handlers/RaidCommandHandler.cs
  </files>
  <action>
Create `ICommandHandler` interface:
```csharp
public interface ICommandHandler
{
    string GroupName { get; }
    Command BuildCommand();
}
```

For each existing static command class (Audit, Backup, Benchmark, Compliance, Config, Health, Plugin, Storage, VDE, RAID, Connect, Developer, Embedded, Install, Server), create a corresponding handler class that:
1. Implements ICommandHandler
2. Takes kernel dependencies via constructor injection (IServiceProvider, ILogger, DataWarehouseKernel)
3. Moves static method logic to instance methods
4. Preserves all existing command signatures and behavior exactly
5. The old static classes remain for backward compat but are marked [Obsolete]

Do NOT create handlers for the 7 new v6.0 commands from plan 136-01 — those will be created as handlers directly.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>ICommandHandler interface exists. All existing command groups have handler implementations. Static classes marked obsolete.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DI container in Program.cs</name>
  <files>DataWarehouse.CLI/Program.cs, DataWarehouse.CLI/DataWarehouse.CLI.csproj</files>
  <action>
Update Program.cs to:
1. Build IServiceCollection with all ICommandHandler registrations
2. Register kernel services (DataWarehouseKernel, ILogger, etc.)
3. Resolve handlers from DI to build the command tree: `foreach (var handler in services.GetServices&lt;ICommandHandler&gt;()) rootCommand.Add(handler.BuildCommand());`
4. Ensure Microsoft.Extensions.DependencyInjection is referenced in csproj if not already

Keep backward-compatible — if DI fails to resolve, fall back to static command registration.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.CLI/DataWarehouse.CLI.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Program.cs uses DI to resolve all command handlers. CLI builds and all commands still function.</done>
</task>

</tasks>

<verification>
- ICommandHandler.cs defines the contract
- All existing commands have handler implementations
- Program.cs builds command tree from DI
- `dotnet build DataWarehouse.CLI` succeeds
</verification>

<success_criteria>
All CLI commands are DI-driven. ICommandHandler interface exists. Each handler receives kernel services via constructor injection. CLI project builds with zero errors.
</success_criteria>

<output>
After completion, create `.planning/phases/136-cli-gui-production/136-02-SUMMARY.md`
</output>
