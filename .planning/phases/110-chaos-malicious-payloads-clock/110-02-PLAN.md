---
phase: 110-chaos-malicious-payloads-clock
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/ClockSkew/ClockSkewTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/ClockSkew/SkewableTimeProvider.cs
autonomous: true
requirements:
  - CHAOS-08

must_haves:
  truths:
    - "Clock skew of +24hrs does not bypass token expiry (expired tokens still rejected)"
    - "Clock skew of -24hrs does not cause premature token rejection (valid tokens accepted)"
    - "Policy evaluation produces correct results regardless of clock manipulation"
    - "Cache expiry respects monotonic time, not wall clock"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/ClockSkew/ClockSkewTests.cs"
      provides: "Clock skew chaos test cases"
      min_lines: 180
    - path: "DataWarehouse.Hardening.Tests/Chaos/ClockSkew/SkewableTimeProvider.cs"
      provides: "TimeProvider that can be skewed during test execution"
      min_lines: 50
  key_links:
    - from: "SkewableTimeProvider.cs"
      to: "System.TimeProvider"
      via: "extends TimeProvider with configurable offset"
      pattern: "TimeProvider|GetUtcNow|offset"
    - from: "ClockSkewTests.cs"
      to: "Auth/Policy/Cache subsystems"
      via: "exercises time-dependent logic under skewed clock"
      pattern: "token.*expir|policy.*evaluat|cache.*ttl"
---

<objective>
Clock skew chaos testing — prove time-dependent logic (auth tokens, policy evaluation, cache TTL) handles extreme clock manipulation without security bypass.

Purpose: Validate that no time-dependent security check can be bypassed by manipulating the system clock.
Output: Test suite in DataWarehouse.Hardening.Tests/Chaos/ClockSkew/ with report "Stage 3 - Steps 7-8 - Clock Skew"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skewable time provider</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/ClockSkew/SkewableTimeProvider.cs
  </files>
  <action>
1. Read SDK and Kernel source to find how TimeProvider is used — look for `TimeProvider`, `DateTimeOffset.UtcNow`, `DateTime.UtcNow` in auth, policy, and cache code.
2. Create `SkewableTimeProvider.cs` — extends `System.TimeProvider`:
   - Constructor takes a `TimeSpan offset` (positive = future, negative = past)
   - `SetOffset(TimeSpan)` — change skew at runtime (thread-safe)
   - `JumpForward(TimeSpan)` / `JumpBackward(TimeSpan)` — relative adjustments
   - Override `GetUtcNow()` to return `DateTimeOffset.UtcNow + _offset`
   - Override `GetTimestamp()` and `GetElapsedTime()` consistently
   - Provide a `CreateTimer()` override that respects the skewed time
   - Track all calls for test assertions (call count, max/min returned times)
3. If the codebase uses `ISystemClock` or a custom time abstraction instead of `TimeProvider`, adapt to match that contract.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>SkewableTimeProvider compiles, can skew time by arbitrary offset, thread-safe runtime adjustment</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement clock skew tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/ClockSkew/ClockSkewTests.cs
  </files>
  <behavior>
    - Test: Token issued at T=0, clock skewed +25hrs → token validation rejects (expired)
    - Test: Token issued at T=0, clock skewed -24hrs → token validation accepts (not yet expired by wall clock but issued in "future" from skewed perspective — depends on implementation; must not crash)
    - Test: Policy with time-based conditions evaluated under +24hr skew → produces correct result
    - Test: Cache with 1hr TTL, clock skewed +2hrs → cache miss (entry expired)
    - Test: Clock oscillates (jump +12hr, -12hr, +12hr) during active operations → no crash, no auth bypass
  </behavior>
  <action>
1. Read auth token creation/validation, policy evaluation, and cache implementation to understand time dependencies.
2. Create `ClockSkewTests.cs`:
   - `[Fact] TokenExpiry_ClockForward24hr_TokenRejected` — create token with 1hr expiry, skew clock +25hr, validate token, assert rejected as expired
   - `[Fact] TokenExpiry_ClockBackward24hr_NoBypass` — create token at current time, skew clock -24hr, validate, assert: either accepted (token was issued in "future" relative to skewed clock, reasonable behavior) OR rejected (conservative behavior) — but NEVER crash or throw unhandled exception
   - `[Fact] PolicyEvaluation_SkewedClock_CorrectResult` — create time-based policy (e.g., "allow access 9AM-5PM"), skew clock to outside window, assert policy denies access
   - `[Fact] CacheTtl_SkewedClock_ExpiredCorrectly` — set cache entry with 1hr TTL, skew clock +2hr, assert cache miss
   - `[Fact] ClockOscillation_NoAuthBypass` — oscillate clock +-12hr 10 times during active auth/policy operations, assert no unhandled exceptions and no expired tokens accepted
   - `[Theory] SkewRanges` with `[InlineData(-24, 0, 12, 24)]` hours — test various skew magnitudes
3. Inject SkewableTimeProvider via DI or constructor injection into auth/policy/cache components.
4. Verify no `DateTime.UtcNow` or `DateTimeOffset.UtcNow` direct calls bypass the provider (grep for these — flag if found).
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~ClockSkew" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 6+ clock skew tests pass. No auth bypass under any clock manipulation. Policy evaluation correct. Cache TTL respects time provider. No crashes under oscillation.</done>
</task>

</tasks>

<verification>
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~ClockSkew" -c Release -v normal
```
</verification>

<success_criteria>
- 6+ clock skew tests passing
- No auth bypass under +/-24hr clock skew
- Policy evaluation correct under all skew values
- Cache TTL works correctly with skewed time
- No crashes under clock oscillation
- Report: "Stage 3 - Steps 7-8 - Clock Skew"
</success_criteria>

<output>
After completion, create `.planning/phases/110-chaos-malicious-payloads-clock/110-02-SUMMARY.md`
</output>
