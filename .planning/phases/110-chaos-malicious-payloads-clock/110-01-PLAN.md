---
phase: 110-chaos-malicious-payloads-clock
plan: 01
type: execute
wave: 1
depends_on: ["109-02"]
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/MaliciousPayload/MaliciousPayloadTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/MaliciousPayload/PayloadGenerator.cs
autonomous: true
requirements:
  - CHAOS-07

must_haves:
  truths:
    - "Zip bombs rejected without OOM or thread hang"
    - "Malformed encryption IVs rejected without crash"
    - "Oversized headers rejected within bounded time and memory"
    - "Path traversal in metadata rejected without filesystem access"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/MaliciousPayload/MaliciousPayloadTests.cs"
      provides: "Malicious payload rejection test cases"
      min_lines: 200
    - path: "DataWarehouse.Hardening.Tests/Chaos/MaliciousPayload/PayloadGenerator.cs"
      provides: "Generator for various malicious payloads"
      min_lines: 100
  key_links:
    - from: "MaliciousPayloadTests.cs"
      to: "VDE input validation"
      via: "feeds hostile data through VDE pipeline"
      pattern: "zip.*bomb|malformed.*IV|oversized|path.*traversal"
    - from: "PayloadGenerator.cs"
      to: "Compression/Encryption decorators"
      via: "crafts payloads targeting specific decorator weaknesses"
      pattern: "GZip|AES|IV|header"
---

<objective>
Malicious payload chaos testing — prove the system instantly rejects hostile inputs without OOM, hangs, or security bypass.

Purpose: Validate input validation and resource bounding across the entire VDE pipeline and metadata processing.
Output: Test suite in DataWarehouse.Hardening.Tests/Chaos/MaliciousPayload/ with report "Stage 3 - Steps 7-8 - Malicious Payloads"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create malicious payload generator</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/MaliciousPayload/PayloadGenerator.cs
  </files>
  <action>
1. Read VDE decorator chain source — specifically Compression, Encryption, and metadata parsing to understand input validation boundaries.
2. Create `PayloadGenerator.cs` with static factory methods:
   - `ZipBomb(compressionRatio, uncompressedSize)` — creates a highly compressed payload that expands to `uncompressedSize` (e.g., 10GB). Use nested GZip streams for realistic ratios (1000:1+).
   - `MalformedIV(algorithm)` — creates encryption header with wrong IV length, null IV, all-zero IV, or truncated IV for AES-256-GCM, ChaCha20, etc.
   - `OversizedHeader(sizeBytes)` — creates a VDE header claiming impossibly large field sizes (e.g., 4GB filename, 2^63 block count)
   - `PathTraversal()` — creates metadata containing `../../../etc/passwd`, `..\\..\\Windows\\System32`, null bytes in paths, unicode normalization attacks
   - `MalformedSuperblock()` — invalid magic bytes, version overflow, corrupted feature flags
   - `IntegerOverflow()` — block count * block size = overflow, offset + length wraps around
   - Each returns `byte[]` or `Stream` ready to feed into VDE pipeline
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>PayloadGenerator compiles, can produce zip bombs, malformed IVs, oversized headers, path traversal, and overflow payloads</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement malicious payload rejection tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/MaliciousPayload/MaliciousPayloadTests.cs
  </files>
  <behavior>
    - Test: Zip bomb (1000:1 ratio, 10GB expanded) → rejected within 100ms, memory stays under 50MB
    - Test: Malformed AES IV (wrong length) → rejected with FormatException, no crash
    - Test: Oversized header (4GB filename claim) → rejected without allocating 4GB
    - Test: Path traversal in metadata → rejected, no filesystem access outside VDE volume
    - Test: Integer overflow in block math → detected, ArithmeticException or similar
    - Test: Malformed superblock → rejected at mount, no partial initialization
  </behavior>
  <action>
1. Create `MaliciousPayloadTests.cs`:
   - `[Fact] ZipBomb_RejectedWithinBounds` — feed zip bomb through decompression decorator, assert rejection within 100ms and memory allocation < 50MB (use GC.GetTotalMemory before/after)
   - `[Theory] MalformedIV_AllAlgorithms_Rejected` — test malformed IVs for each supported encryption algorithm, assert FormatException or equivalent
   - `[Fact] OversizedHeader_NoMassiveAllocation` — feed oversized header, assert rejection without large allocation (track GC.GetTotalMemory)
   - `[Theory] PathTraversal_AllVariants_Rejected` — test `../`, `..\\`, null bytes, unicode normalization — assert SecurityException or equivalent, verify no file I/O outside volume
   - `[Fact] IntegerOverflow_BlockMath_Detected` — craft payload where block_count * block_size overflows Int64, assert ArithmeticException/OverflowException
   - `[Fact] MalformedSuperblock_MountRejected` — feed corrupt superblock, assert mount fails cleanly
   - `[Fact] AllPayloads_NoHang` — run all malicious payloads with 10-second timeout, assert none hangs
2. Memory tracking: record GC.GetTotalMemory(true) before and after each test, assert delta within bounds.
3. Timing: use Stopwatch to verify rejection speed.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~MaliciousPayload" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 7+ malicious payload tests pass. Zip bombs, malformed IVs, path traversal, overflows all rejected instantly without OOM, hang, or security bypass.</done>
</task>

</tasks>

<verification>
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~MaliciousPayload" -c Release -v normal
```
</verification>

<success_criteria>
- 7+ malicious payload tests passing
- Zip bombs rejected within bounded time and memory
- Malformed IVs rejected for all algorithms
- Path traversal blocked with no filesystem access
- Integer overflows detected
- Report: "Stage 3 - Steps 7-8 - Malicious Payloads"
</success_criteria>

<output>
After completion, create `.planning/phases/110-chaos-malicious-payloads-clock/110-01-SUMMARY.md`
</output>
