---
phase: 105-integration-profiling
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - DataWarehouse.Hardening.Tests/Integration/IntegrationProfilingTests.cs
  - Metadata/integration-profile-report/integration-profiling-results.md
autonomous: true
requirements: [SOAK-02]

must_haves:
  truths:
    - "dotTrace identifies cross-boundary bottlenecks in integration test execution"
    - "No single cross-boundary bottleneck exceeds 5% of total execution time"
    - "dotMemory confirms working set stays bounded during streaming"
    - "Integration profiling report exported with quantified findings"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Integration/IntegrationProfilingTests.cs"
      provides: "Profiling wrapper around integration tests with dotTrace + dotMemory.Unit"
      min_lines: 50
    - path: "Metadata/integration-profile-report/integration-profiling-results.md"
      provides: "Stage 2 - Step 1 - Integration Profiling Results"
      contains: "Cross-Boundary Bottleneck"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Integration/IntegrationProfilingTests.cs"
      to: "DataWarehouse.Hardening.Tests/Integration/IntegrationTestFixture.cs"
      via: "uses shared integration fixture for profiled test runs"
      pattern: "IntegrationTestFixture"
---

<objective>
Run dotTrace + dotMemory.Unit profiling on the integration test harness from Plan 105-01 to detect cross-boundary bottlenecks and verify working set stays bounded.

Purpose: SOAK-02 requires no single cross-boundary bottleneck exceeding 5% of total execution time.
Output: Integration profiling tests + profiling results report in Metadata/integration-profile-report/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-integration-profiling/105-01-SUMMARY.md
@DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration profiling tests with dotTrace + dotMemory</name>
  <files>DataWarehouse.Hardening.Tests/Integration/IntegrationProfilingTests.cs</files>
  <action>
1. Create `DataWarehouse.Hardening.Tests/Integration/IntegrationProfilingTests.cs`:
   - Uses `IntegrationTestFixture` from Plan 105-01
   - Profiles the integration test execution to find cross-boundary bottlenecks

2. Cross-boundary profiling approach:
   - Instrument key boundary crossing points:
     a. Plugin-to-Kernel calls (strategy dispatch)
     b. VDE decorator chain transitions (Cache->Integrity->Compression->etc.)
     c. MessageBus publish/subscribe delivery
     d. Plugin-to-SDK calls
   - Use `Stopwatch` instrumentation at each boundary
   - Calculate percentage of total execution time per boundary

3. Test methods:
   a. `Test_CrossBoundaryBottleneck_Below5Percent()`:
      - Run the 1GB CI-fast streaming test (from 105-01)
      - Measure time at each decorator boundary in the VDE chain
      - Assert: no single boundary exceeds 5% of total wall-clock time
      - Report: per-boundary timing breakdown

   b. `Test_WorkingSetBounded_DuringStreaming()`:
      - Monitor `Process.GetCurrentProcess().WorkingSet64` at 100ms intervals during streaming
      - Use a background monitoring thread or Timer
      - Collect working set samples
      - Assert: working set stabilizes (last 50% of samples within 10% of median)
      - Assert: no monotonic growth trend (linear regression slope near zero)

   c. `Test_GCPressure_DuringIntegration()`:
      - Record GC counts before/after integration run
      - Use dotMemory.Unit assertions if available, else `GC.CollectionCount()`
      - Assert: Gen2 collections below threshold during streaming

4. All tests tagged `[Trait("Category", "IntegrationProfile")]`.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse &amp;&amp; dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj 2>&amp;1 | tail -5</automated>
  </verify>
  <done>IntegrationProfilingTests.cs compiles. Contains cross-boundary bottleneck detection (5% threshold), working set boundedness check, and GC pressure monitoring.</done>
</task>

<task type="auto">
  <name>Task 2: Run integration profiling and generate results report</name>
  <files>Metadata/integration-profile-report/integration-profiling-results.md</files>
  <action>
1. Create directory `Metadata/integration-profile-report/` if not exists.

2. Run integration profiling tests:
   ```
   dotnet test DataWarehouse.Hardening.Tests/ --filter "Category=IntegrationProfile" --logger "console;verbosity=detailed"
   ```

3. Generate `Metadata/integration-profile-report/integration-profiling-results.md`:
   ```markdown
   # Stage 2 - Step 1 - Integration Profiling Results

   ## Summary
   - Date: {date}
   - Payload size: 1GB (CI mode)
   - Kernel + plugins: {count} loaded
   - Result: PASS / FAIL

   ## Cross-Boundary Bottleneck Analysis
   | Boundary | Time (ms) | % of Total | Threshold (5%) | Status |
   |----------|-----------|------------|-----------------|--------|
   | Cache decorator | ... | ... | ... | OK/FAIL |
   | Integrity decorator | ... | ... | ... | OK/FAIL |
   | Compression decorator | ... | ... | ... | OK/FAIL |
   | Dedup decorator | ... | ... | ... | OK/FAIL |
   | Encryption decorator | ... | ... | ... | OK/FAIL |
   | WAL decorator | ... | ... | ... | OK/FAIL |
   | RAID decorator | ... | ... | ... | OK/FAIL |
   | File backend | ... | ... | ... | OK/FAIL |
   | MessageBus delivery | ... | ... | ... | OK/FAIL |

   ## Working Set Analysis
   - Samples collected: {count}
   - Initial: {MB}
   - Peak: {MB}
   - Final: {MB}
   - Median (last 50%): {MB}
   - Variance (last 50%): {MB}
   - Monotonic growth: No / Yes
   - Assessment: BOUNDED / UNBOUNDED

   ## GC Pressure During Integration
   | Generation | Collections | Rate (per GB) | Assessment |
   |------------|-------------|---------------|------------|
   | Gen0 | ... | ... | INFO |
   | Gen1 | ... | ... | INFO |
   | Gen2 | ... | ... | OK/WARN |

   ## Conclusion
   {Whether SOAK-01 and SOAK-02 are satisfied}
   ```
  </action>
  <verify>
    <automated>test -f "C:/Temp/DataWarehouse/DataWarehouse/Metadata/integration-profile-report/integration-profiling-results.md" &amp;&amp; echo "Report exists" || echo "MISSING"</automated>
  </verify>
  <done>Integration profiling report generated with cross-boundary bottleneck analysis (5% threshold), working set boundedness data, and GC pressure metrics. SOAK-01 and SOAK-02 assessment stated.</done>
</task>

</tasks>

<verification>
- Integration profiling tests compile and run
- Cross-boundary bottleneck analysis shows no boundary exceeding 5%
- Working set analysis confirms bounded memory during streaming
- Report exported to Metadata/integration-profile-report/
</verification>

<success_criteria>
- dotTrace-equivalent profiling ran on integration tests
- No cross-boundary bottleneck exceeds 5% of total time
- Working set stays bounded during streaming
- GC pressure within acceptable limits
- Report at Metadata/integration-profile-report/integration-profiling-results.md
</success_criteria>

<output>
After completion, create `.planning/phases/105-integration-profiling/105-02-SUMMARY.md`
</output>
