---
phase: 137-dashboard-production
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Dashboard/Hubs/DashboardHub.cs
  - DataWarehouse.Dashboard/Hubs/FederationHub.cs
  - DataWarehouse.Dashboard/Hubs/RaidHub.cs
  - DataWarehouse.Dashboard/Hubs/ShardHub.cs
  - DataWarehouse.Dashboard/Hubs/AIAdvisorHub.cs
  - DataWarehouse.Dashboard/Hubs/PolicyHub.cs
autonomous: true
requirements: [APP-04]

must_haves:
  truths:
    - "Federation status streams real-time to dashboard"
    - "RAID health streams real-time to dashboard"
    - "Shard migration progress streams real-time"
    - "AI advisor recommendations push in real-time"
    - "Policy evaluation results push in real-time"
  artifacts:
    - path: "DataWarehouse.Dashboard/Hubs/DashboardHub.cs"
      provides: "Main SignalR hub (updated)"
      contains: "SendAsync"
    - path: "DataWarehouse.Dashboard/Hubs/FederationHub.cs"
      provides: "Federation real-time channel"
      exports: ["FederationHub"]
  key_links:
    - from: "DataWarehouse.Dashboard/Hubs/*.cs"
      to: "DataWarehouse.Kernel/Messaging/MessageBus.cs"
      via: "Subscribe to kernel events"
      pattern: "Subscribe|OnMessage"
---

<objective>
Add SignalR real-time channels for v6.0 subsystems: federation status, RAID health, shard migration, AI advisor, policy evaluation.

Purpose: Dashboard must show real-time status for all v6.0 subsystems. Polling is insufficient for operational dashboards — SignalR provides push notifications.
Output: Updated DashboardHub and 5 specialized hubs with kernel event subscriptions.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.Dashboard/Hubs/DashboardHub.cs
@DataWarehouse.Kernel/Messaging/MessageBus.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create specialized SignalR hubs</name>
  <files>
    DataWarehouse.Dashboard/Hubs/FederationHub.cs,
    DataWarehouse.Dashboard/Hubs/RaidHub.cs,
    DataWarehouse.Dashboard/Hubs/ShardHub.cs,
    DataWarehouse.Dashboard/Hubs/AIAdvisorHub.cs,
    DataWarehouse.Dashboard/Hubs/PolicyHub.cs
  </files>
  <action>
Create 5 specialized SignalR hubs, each subscribing to kernel MessageBus for their domain events:

FederationHub: /hubs/federation — streams NodeJoined, NodeLeft, TopologyChanged, SyncProgress events. Client methods: SubscribeToCluster(clusterId), UnsubscribeFromCluster.

RaidHub: /hubs/raid — streams ArrayHealthChanged, RebuildProgress, DeviceStatusChanged, DegradedAlert events. Client methods: SubscribeToArray(arrayId), UnsubscribeFromArray.

ShardHub: /hubs/shard — streams MigrationStarted, MigrationProgress, MigrationCompleted, SplitCompleted, MergeCompleted, RebalanceProgress events. Client methods: SubscribeToShard(shardId), SubscribeToAllShards.

AIAdvisorHub: /hubs/ai-advisor — streams NewRecommendation, AuditCompleted, ProviderStatusChanged events. Client methods: Subscribe, Unsubscribe.

PolicyHub: /hubs/policy — streams EvaluationCompleted, PolicyViolation, CascadeProgress events. Client methods: SubscribeToPolicy(policyId), SubscribeToAllPolicies.

Each hub: inject kernel MessageBus, subscribe to relevant message types, forward to connected clients via SendAsync. Handle client disconnect (unsubscribe from kernel events). Use IHubContext for background pushes.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>5 specialized hubs compile, each with kernel event subscriptions and client push methods.</done>
</task>

<task type="auto">
  <name>Task 2: Update DashboardHub and register hub routes</name>
  <files>
    DataWarehouse.Dashboard/Hubs/DashboardHub.cs,
    DataWarehouse.Dashboard/Program.cs
  </files>
  <action>
DashboardHub.cs updates:
1. Add aggregated health stream that combines all subsystem statuses
2. Add system-wide notification channel for alerts across all subsystems
3. Add client methods: GetOverallHealth(), SubscribeToAlerts(), UnsubscribeFromAlerts()

Program.cs updates:
1. Map all hub routes: app.MapHub&lt;DashboardHub&gt;("/hubs/dashboard"), app.MapHub&lt;FederationHub&gt;("/hubs/federation"), etc.
2. Configure SignalR: message size limits, keepalive interval, handshake timeout
3. Enable MessagePack protocol for efficient binary serialization
4. Add CORS policy for hub endpoints
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>All hubs registered in Program.cs with proper configuration. DashboardHub provides aggregated health stream.</done>
</task>

</tasks>

<verification>
- 5 new hub files + updated DashboardHub
- All hubs registered in Program.cs
- SignalR configured with MessagePack
- `dotnet build DataWarehouse.Dashboard` succeeds
</verification>

<success_criteria>
Real-time SignalR channels for federation, RAID, shard, AI advisor, and policy subsystems. Each hub subscribes to kernel events and pushes to connected clients. Dashboard project builds.
</success_criteria>

<output>
After completion, create `.planning/phases/137-dashboard-production/137-02-SUMMARY.md`
</output>
