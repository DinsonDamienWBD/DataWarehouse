---
phase: 137-dashboard-production
plan: 06
type: execute
wave: 2
depends_on: [137-02, 137-03]
files_modified:
  - DataWarehouse.Dashboard/Services/ResponseCacheService.cs
  - DataWarehouse.Dashboard/Middleware/SignalRBatchingMiddleware.cs
  - DataWarehouse.Dashboard/Program.cs
  - DataWarehouse.Dashboard/Models/Pagination.cs
autonomous: true
requirements: [APP-04]

must_haves:
  truths:
    - "Response caching reduces API load for read-heavy endpoints"
    - "SignalR messages are batched to reduce network overhead"
    - "Pages lazy-load with loading indicators"
    - "All list endpoints support server-side pagination"
  artifacts:
    - path: "DataWarehouse.Dashboard/Services/ResponseCacheService.cs"
      provides: "Response caching for read endpoints"
      exports: ["ResponseCacheService"]
    - path: "DataWarehouse.Dashboard/Middleware/SignalRBatchingMiddleware.cs"
      provides: "SignalR message batching"
      exports: ["SignalRBatchingMiddleware"]
  key_links:
    - from: "DataWarehouse.Dashboard/Services/ResponseCacheService.cs"
      to: "DataWarehouse.Dashboard/Controllers/*.cs"
      via: "[ResponseCache] attribute"
      pattern: "\\[ResponseCache\\]|ICacheService"
---

<objective>
Optimize dashboard performance: response caching, SignalR batching, lazy-loaded pages, server-side pagination.

Purpose: Production dashboard must handle high-traffic scenarios. Caching, batching, and pagination reduce server load and improve responsiveness.
Output: Caching service, SignalR batching, pagination on all list endpoints, lazy-load setup.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.Dashboard/Models/Pagination.cs
@DataWarehouse.Dashboard/Middleware/RateLimitingMiddleware.cs
@DataWarehouse.Dashboard/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Response caching and pagination</name>
  <files>
    DataWarehouse.Dashboard/Services/ResponseCacheService.cs,
    DataWarehouse.Dashboard/Models/Pagination.cs,
    DataWarehouse.Dashboard/Program.cs
  </files>
  <action>
ResponseCacheService.cs:
1. In-memory response cache using IMemoryCache
2. Cache read-only endpoints: GET /policies, GET /shards, GET /federation/nodes, etc.
3. Cache duration: 30 seconds for status endpoints, 5 minutes for configuration
4. Cache invalidation: on any mutation (POST/PUT/DELETE), invalidate related cache entries
5. ETag support: return ETag header, honor If-None-Match for 304 responses
6. Add [CacheProfile("Short")] and [CacheProfile("Long")] attributes for controllers

Pagination.cs updates:
1. Generic PaginatedResponse&lt;T&gt;: Items, TotalCount, Page, PageSize, TotalPages, HasNext, HasPrevious
2. PaginationFilter: Page (default 1), PageSize (default 25, max 100), SortBy, SortDirection
3. Extension method: IQueryable&lt;T&gt;.ToPaginatedAsync(filter) for easy use in controllers
4. All list endpoints (GET /policies, GET /shards, etc.) accept PaginationFilter query params

Program.cs:
1. Register ResponseCacheService
2. Configure response caching middleware
3. Add cache profiles
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Response caching active. All list endpoints paginated. ETags supported.</done>
</task>

<task type="auto">
  <name>Task 2: SignalR batching and lazy-load pages</name>
  <files>
    DataWarehouse.Dashboard/Middleware/SignalRBatchingMiddleware.cs,
    DataWarehouse.Dashboard/Program.cs
  </files>
  <action>
SignalRBatchingMiddleware.cs:
1. Batch SignalR messages: collect messages in 100ms window, send as single batch
2. Configurable batch window (100ms-1000ms) and max batch size (50 messages)
3. Priority messages bypass batching (alerts, errors)
4. Compression: enable per-message compression for large payloads (>1KB)
5. Deduplication: if same data sent multiple times within batch window, send only latest

Program.cs updates for lazy loading:
1. Configure Blazor lazy loading for dashboard pages: each page group loads on demand
2. Add loading indicator component while page assemblies load
3. Preload critical pages (Index, Monitoring) on startup
4. Defer non-critical pages (Compliance, AI Insights) until navigation

Wire batching middleware into SignalR pipeline.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>SignalR batching reduces message overhead. Pages lazy-load with indicators. Dashboard builds.</done>
</task>

</tasks>

<verification>
- ResponseCacheService.cs with ETag support
- SignalRBatchingMiddleware.cs with configurable batching
- Pagination.cs with generic PaginatedResponse
- Program.cs registers all performance services
- `dotnet build DataWarehouse.Dashboard` succeeds
</verification>

<success_criteria>
Response caching with ETag support. SignalR message batching (100ms window). Server-side pagination on all list endpoints. Lazy-loaded pages. Dashboard builds.
</success_criteria>

<output>
After completion, create `.planning/phases/137-dashboard-production/137-06-SUMMARY.md`
</output>
