---
phase: 137-dashboard-production
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Dashboard/Program.cs
  - DataWarehouse.Dashboard/OpenApi/SwaggerConfig.cs
  - DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj
autonomous: true
requirements: [APP-04]

must_haves:
  truths:
    - "Every CLI command has a corresponding REST endpoint"
    - "OpenAPI/Swagger documentation auto-generated for all endpoints"
    - "API versioned under /api/v1/"
    - "Swagger UI accessible at /swagger"
  artifacts:
    - path: "DataWarehouse.Dashboard/OpenApi/SwaggerConfig.cs"
      provides: "Swagger/OpenAPI configuration"
      exports: ["SwaggerConfig"]
  key_links:
    - from: "DataWarehouse.Dashboard/Program.cs"
      to: "DataWarehouse.Dashboard/OpenApi/SwaggerConfig.cs"
      via: "Swagger middleware registration"
      pattern: "UseSwagger|AddSwaggerGen"
---

<objective>
Ensure full REST API parity with CLI commands and auto-generate OpenAPI/Swagger documentation.

Purpose: Dashboard REST API must be discoverable and documented. Every CLI operation must have a REST equivalent for programmatic access.
Output: Swagger configuration, API versioning, parity validation.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.Dashboard/Program.cs
@DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure OpenAPI/Swagger with API versioning</name>
  <files>
    DataWarehouse.Dashboard/OpenApi/SwaggerConfig.cs,
    DataWarehouse.Dashboard/Program.cs,
    DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj
  </files>
  <action>
SwaggerConfig.cs:
1. Configure Swashbuckle with XML comments from all controller assemblies
2. API versioning: v1 as current, support for future v2
3. Security definitions: Bearer token auth in Swagger UI
4. Group endpoints by controller (tag groups)
5. Add API descriptions for each operation
6. Example request/response bodies for complex endpoints
7. Configure Swagger UI at /swagger with "Try it out" enabled

Program.cs updates:
1. builder.Services.AddSwaggerGen(SwaggerConfig.Configure)
2. app.UseSwagger() and app.UseSwaggerUI()
3. API versioning middleware: builder.Services.AddApiVersioning()
4. All controllers use [ApiVersion("1.0")]

csproj updates:
1. Add Swashbuckle.AspNetCore package reference if not present
2. Add Microsoft.AspNetCore.Mvc.Versioning if not present
3. Enable XML documentation generation: &lt;GenerateDocumentationFile&gt;true&lt;/GenerateDocumentationFile&gt;
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Swagger UI configured at /swagger. All endpoints documented with OpenAPI spec. API versioned under /api/v1/.</done>
</task>

<task type="auto">
  <name>Task 2: Verify REST/CLI parity</name>
  <files>DataWarehouse.Dashboard/OpenApi/SwaggerConfig.cs</files>
  <action>
Add a startup validation that:
1. Enumerates all CLI command groups from DynamicCommandRegistry
2. Enumerates all REST controller actions via reflection
3. Logs any CLI commands without REST equivalents as warnings
4. This runs at startup in Development environment only
5. Output: "REST/CLI Parity: {matched}/{total} commands covered, {gaps} gaps"

Document any intentional gaps (e.g., `dw completions install` has no REST equivalent — it's shell-specific).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Dashboard/DataWarehouse.Dashboard.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Startup parity check logs REST/CLI coverage. All intentional gaps documented.</done>
</task>

</tasks>

<verification>
- SwaggerConfig.cs configures OpenAPI generation
- Program.cs registers Swagger middleware
- API versioning on all controllers
- `dotnet build DataWarehouse.Dashboard` succeeds
</verification>

<success_criteria>
Swagger UI at /swagger documents all endpoints. API versioned under /api/v1/. REST/CLI parity validated at startup. Dashboard project builds.
</success_criteria>

<output>
After completion, create `.planning/phases/137-dashboard-production/137-03-SUMMARY.md`
</output>
