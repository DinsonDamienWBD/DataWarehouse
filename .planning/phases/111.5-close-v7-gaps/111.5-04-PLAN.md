---
phase: 111.5-close-v7-gaps
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Soak/SoakTestHarness.cs
  - DataWarehouse.Hardening.Tests/Soak/SoakTestConfiguration.cs
autonomous: true
requirements: [GAP-03]

must_haves:
  truths:
    - "10-minute soak test runs with continuous read/write load"
    - "Gen2 collection rate stays below 2/min averaged over the full run"
    - "Working set plateaus after warm-up (no monotonic growth in final 5 minutes)"
    - "SOAK-04 requirement is satisfied"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Soak/SoakTestHarness.cs"
      provides: "10-minute soak test with Gen2 and working set assertions"
    - path: "DataWarehouse.Hardening.Tests/Soak/SoakTestConfiguration.cs"
      provides: "CI configuration preset for 10-minute soak"
  key_links:
    - from: "SoakTestHarness.cs"
      to: "GcEventMonitor"
      via: "EventPipe GC counter collection"
      pattern: "GcEventMonitor|Gen2.*rate"
---

<objective>
Execute the existing 10-minute soak test (Test_SoakCI_10Minutes) to validate Gen2 GC rate and working set stability under sustained load. The previous 2-minute run showed Gen2 rate 1.00/min (PASS) but working set grew 27.62% (attributed to warm-up, unvalidated).

Purpose: Satisfy SOAK-04 (GAP-03) -- prove the system has no memory leaks under sustained 10-minute load with Gen2 rate <2/min and working set plateau.
Output: Passing 10-minute soak test with documented GC metrics.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.Hardening.Tests/Soak/SoakTestHarness.cs
@DataWarehouse.Hardening.Tests/Soak/SoakTestConfiguration.cs
@DataWarehouse.Hardening.Tests/Soak/GcEventMonitor.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and execute the 10-minute soak test</name>
  <files>DataWarehouse.Hardening.Tests/Soak/SoakTestHarness.cs, DataWarehouse.Hardening.Tests/Soak/SoakTestConfiguration.cs</files>
  <action>
1. Read `SoakTestConfiguration.cs` to understand the CI preset:
   - Expected: `SoakTestConfiguration.CI` with ~10-minute duration
   - Verify it sets `SOAK_DURATION_MINUTES=10` (or equivalent DurationMinutes property)

2. Read `SoakTestHarness.cs` fully to understand:
   - The `Test_SoakCI_10Minutes` test method
   - The `RunSoakTest` method implementation
   - GC monitoring (Gen2 collection counting)
   - Working set monitoring (memory sampling)
   - Assertions at the end

3. Verify the test has these assertions (add if missing):
   - Gen2 collection rate < 2/min (averaged over full 10-minute run)
   - Working set plateau: in the final 5 minutes, working set variance is < 10% of median
   - No monotonic growth trend in final 5 minutes (linear regression slope near zero or negative)
   - No worker exceptions during the run

4. If the working set assertion is too lenient (e.g., just checks total growth), tighten it:
   ```csharp
   // Sample working set every 30 seconds
   // In final 5 minutes (10 samples), compute:
   // - Median working set
   // - Max deviation from median
   // Assert max deviation < 10% of median (plateau, not growth)
   ```

5. Build and run the test:
   ```
   dotnet build DataWarehouse.Hardening.Tests/
   dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Test_SoakCI_10Minutes" --verbosity normal --timeout 900000
   ```
   (15-minute timeout to allow 10 minutes of soak + setup/teardown)

6. If the test passes, capture and log:
   - Gen2 rate (collections/minute)
   - Working set: initial, peak, final, plateau range
   - Total operations completed
   - Any GC pauses > 100ms

7. If the test fails:
   - Gen2 rate too high: investigate allocation-heavy code paths in the soak workload, add pooling
   - Working set growth: investigate for memory leaks (event handlers not unsubscribed, growing collections, ArrayPool not returning)
   - Fix the issue in the soak harness or document if it's a known production issue requiring separate fix

8. Add output logging that clearly documents the SOAK-04 result:
   ```csharp
   _output.WriteLine($"[SOAK-04] Gen2 rate: {gen2Rate:F2}/min (threshold: <2/min) — {(gen2Rate < 2 ? "PASS" : "FAIL")}");
   _output.WriteLine($"[SOAK-04] Working set plateau variance: {variancePct:F1}% (threshold: <10%) — {(variancePct < 10 ? "PASS" : "FAIL")}");
   ```
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Test_SoakCI_10Minutes" --verbosity normal --timeout 900000</automated>
  </verify>
  <done>10-minute soak test passes. Gen2 rate < 2/min. Working set plateaus (< 10% variance in final 5 minutes). SOAK-04 satisfied.</done>
</task>

</tasks>

<verification>
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Test_SoakCI_10Minutes" --verbosity normal --timeout 900000
# Expected: 1 test passes with Gen2 < 2/min and working set plateau
</verification>

<success_criteria>
- Test_SoakCI_10Minutes executes for 10 minutes under continuous load
- Gen2 collection rate < 2/min averaged over the run
- Working set plateaus after warm-up (no monotonic growth in final 5 minutes)
- Test output clearly documents SOAK-04 pass/fail metrics
- SOAK-04 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/111.5-close-v7-gaps/111.5-04-SUMMARY.md`
</output>
