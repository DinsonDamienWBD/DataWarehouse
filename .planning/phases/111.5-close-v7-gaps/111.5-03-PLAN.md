---
phase: 111.5-close-v7-gaps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Integration/VdePipelineStreamingTests.cs
autonomous: true
requirements: [GAP-02]

must_haves:
  truths:
    - "1GB VDE streaming test is un-skipped and executes successfully"
    - "Full decorator chain write + readback completes without error"
    - "Checksum verification confirms data integrity after roundtrip"
    - "SOAK-01 requirement is satisfied"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Integration/VdePipelineStreamingTests.cs"
      provides: "Un-skipped 1GB streaming test with checksum verification"
  key_links:
    - from: "VdePipelineStreamingTests.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine/VdeCreator"
      via: "CreateVdeAsync + StreamingWrite"
      pattern: "VdeCreator.*CreateVdeAsync"
---

<objective>
Un-skip and execute the 1GB VDE streaming soak test that was Skip-annotated in Phase 105. The test streams 1GB through the full VDE decorator chain (Cache-Integrity-Compression-Dedup-Encryption-WAL-RAID-File), reads it back, and verifies checksum integrity.

Purpose: Satisfy SOAK-01 (GAP-02) -- prove the VDE pipeline handles sustained 1GB streaming writes without data corruption, deadlocks, or memory exhaustion.
Output: Passing 1GB streaming test. 100GB documented as manual-only certification step.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.Hardening.Tests/Integration/VdePipelineStreamingTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-skip 1GB VDE streaming test and execute</name>
  <files>DataWarehouse.Hardening.Tests/Integration/VdePipelineStreamingTests.cs</files>
  <action>
1. Open `DataWarehouse.Hardening.Tests/Integration/VdePipelineStreamingTests.cs`.

2. Find the test `Test_Stream1GB_ThroughVdePipeline_CIFast` (currently has `[Fact(Skip = "Medium-running: 1GB streaming test -- run in CI or integration environment")]`).

3. Change it from `[Fact(Skip = "...")]` to `[Fact]`. Keep the `[Trait("Category", "Integration")]` and `[Trait("Duration", "Medium")]` traits.

4. Review the test implementation:
   - It should create a VDE container via VdeCreator.CreateVdeAsync
   - Stream 1GB of deterministic data through the pipeline
   - Read the data back
   - Verify checksum matches (SHA-256 or XxHash64)
   - Clean up temp files

5. If the test lacks readback + checksum verification, ADD it:
   - After writing 1GB, read the data back from the VDE container
   - Compare SHA-256 hash of written data vs read data
   - Assert they match

6. Add a timeout attribute or CancellationToken timeout of 30 minutes (1GB should complete well within this on any modern machine).

7. Build and run the test:
   ```
   dotnet build DataWarehouse.Hardening.Tests/
   dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Test_Stream1GB_ThroughVdePipeline_CIFast" --verbosity normal
   ```

8. If the test passes, document the result (elapsed time, throughput).

9. If the test fails (deadlock, OOM, corruption), diagnose and fix:
   - Deadlock: check for missing async awaits, unbounded queues
   - OOM: check for non-streaming materialization (ToArray on large data)
   - Corruption: check decorator chain ordering, checksum calculation

10. Add a comment documenting that 100GB is a manual-only certification step:
    ```csharp
    // NOTE: 100GB streaming test is impractical in CI.
    // For full SOAK-01 certification, run manually with:
    //   SOAK_SIZE_GB=100 dotnet test --filter "Test_Stream100GB"
    ```

NOTE: This test will take several minutes to run. Use a 30-minute timeout. If the test infrastructure (VdeCreator, streaming pipeline) hits issues, fix the test to work with the actual API surface rather than changing production code.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Test_Stream1GB" --verbosity normal --timeout 1800000</automated>
  </verify>
  <done>1GB VDE streaming test passes with checksum verification. SOAK-01 satisfied. 100GB documented as manual-only step.</done>
</task>

</tasks>

<verification>
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Test_Stream1GB" --verbosity normal
# Expected: 1 test passes, checksum verified, no timeout/deadlock
</verification>

<success_criteria>
- Test_Stream1GB_ThroughVdePipeline_CIFast is no longer Skip-annotated
- Test passes: 1GB written, read back, checksums match
- No deadlocks, OOM, or data corruption
- 100GB documented as manual-only certification step
- SOAK-01 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/111.5-close-v7-gaps/111.5-03-SUMMARY.md`
</output>
