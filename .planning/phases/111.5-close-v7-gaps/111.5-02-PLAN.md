---
phase: 111.5-close-v7-gaps
plan: 02
type: execute
wave: 2
depends_on: ["111.5-01"]
files_modified:
  - DataWarehouse.Runtime.Tests/stryker-config.json
  - Metadata/stryker-report/surviving-mutants.md
autonomous: true
requirements: [GAP-01]

must_haves:
  truths:
    - "Stryker runs against DataWarehouse.Runtime.Tests and produces a mutation score"
    - "Mutation score is documented with target 95%+"
    - "All surviving mutants are documented with justification"
    - "MUTN-02 requirement is satisfied"
  artifacts:
    - path: "DataWarehouse.Runtime.Tests/stryker-config.json"
      provides: "Stryker configuration targeting SDK project"
    - path: "Metadata/stryker-report/surviving-mutants.md"
      provides: "Documented surviving mutants with justifications"
  key_links:
    - from: "DataWarehouse.Runtime.Tests/stryker-config.json"
      to: "DataWarehouse.SDK"
      via: "Stryker project config"
      pattern: "\"project\".*SDK"
---

<objective>
Run Stryker mutation testing against DataWarehouse.Runtime.Tests and document all surviving mutants. This satisfies the MUTN-02 requirement (GAP-01) that was deferred from v7.0 Phase 104.

Purpose: Prove test quality through mutation testing -- surviving mutants must be justified as equivalent, unreachable, or acceptable risk.
Output: Stryker report with mutation score and surviving-mutants.md documentation.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111.5-close-v7-gaps/111.5-01-SUMMARY.md

Existing Stryker config for reference:
@DataWarehouse.Hardening.Tests/stryker-config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure and run Stryker mutation testing</name>
  <files>DataWarehouse.Runtime.Tests/stryker-config.json</files>
  <action>
1. Install Stryker.NET tool if not already installed:
   `dotnet tool install -g dotnet-stryker` (or update if present)

2. Create `DataWarehouse.Runtime.Tests/stryker-config.json`:
   ```json
   {
     "stryker-config": {
       "project": "DataWarehouse.SDK.csproj",
       "reporters": ["html", "json", "markdown"],
       "verbosity": "info",
       "concurrency": 4,
       "thresholds": {
         "high": 95,
         "low": 80,
         "break": 0
       },
       "mutate": [
         "!**/obj/**",
         "!**/bin/**"
       ],
       "additional-timeout": 30000,
       "coverage-analysis": "perTest",
       "break-on-initial-test-failure": false
     }
   }
   ```

3. Run Stryker from the DataWarehouse.Runtime.Tests directory:
   `cd DataWarehouse.Runtime.Tests && dotnet stryker`

4. If Stryker takes too long on the full SDK, narrow the mutate scope to the classes covered by the runtime tests:
   ```
   "mutate": [
     "!**/obj/**",
     "!**/bin/**",
     "**/Contracts/Scaling/BoundedCache.cs",
     "**/Contracts/StrategyBase.cs",
     "**/Utilities/BoundedDictionary.cs",
     "**/VirtualDiskEngine/Format/Superblock*.cs",
     "**/VirtualDiskEngine/VdeCreator.cs",
     "**/PolicyEngine/**"
   ]
   ```

5. Capture the mutation score and list of surviving mutants from the Stryker output.

NOTE: If Stryker cannot run due to tooling issues on this environment, document the configuration and provide the stryker-config.json ready for execution, then create the surviving-mutants.md with a "pending execution" status and instructions for manual run.
  </action>
  <verify>
    <automated>dotnet stryker --version 2>/dev/null && echo "Stryker available" || echo "Stryker not installed"</automated>
  </verify>
  <done>Stryker configuration created and mutation testing executed (or documented as ready-to-run)</done>
</task>

<task type="auto">
  <name>Task 2: Document surviving mutants</name>
  <files>Metadata/stryker-report/surviving-mutants.md</files>
  <action>
1. Create `Metadata/stryker-report/` directory.

2. Create `Metadata/stryker-report/surviving-mutants.md` with:

   ```markdown
   # Stryker Mutation Testing Report — DataWarehouse.Runtime.Tests

   **Date:** [execution date]
   **Stryker Version:** [version]
   **Target Project:** DataWarehouse.SDK
   **Test Project:** DataWarehouse.Runtime.Tests
   **Mutation Score:** [X]%

   ## Summary

   | Metric | Count |
   |--------|-------|
   | Total mutants | N |
   | Killed | N |
   | Survived | N |
   | No Coverage | N |
   | Timeout | N |
   | Compile Error | N |

   ## Surviving Mutants

   For each surviving mutant, provide:
   - File and line number
   - Mutation type (e.g., arithmetic operator replacement, boolean substitution)
   - Original code vs mutated code
   - Justification: equivalent mutant / unreachable code / acceptable risk / test gap to address

   ### [Category 1: Equivalent Mutants]
   ...

   ### [Category 2: Acceptable Risk]
   ...

   ### [Category 3: Test Gaps to Address in Future]
   ...

   ## Conclusion

   [Statement on whether MUTN-02 is satisfied: mutation score >= 95% or all survivors justified]
   ```

3. If Stryker was executed, populate from actual results.
4. If Stryker could not run, document the configuration and provide "pending execution" status with exact commands to run.
  </action>
  <verify>
    <automated>test -f Metadata/stryker-report/surviving-mutants.md && echo "Report exists" || echo "MISSING"</automated>
  </verify>
  <done>surviving-mutants.md exists with mutation score, survivor justifications, and MUTN-02 satisfaction statement</done>
</task>

</tasks>

<verification>
test -f DataWarehouse.Runtime.Tests/stryker-config.json && echo "Config exists"
test -f Metadata/stryker-report/surviving-mutants.md && echo "Report exists"
grep -c "Mutation Score" Metadata/stryker-report/surviving-mutants.md
</verification>

<success_criteria>
- Stryker config file exists targeting SDK via Runtime.Tests
- Stryker executed (or documented ready-to-run with exact commands)
- surviving-mutants.md documents mutation score and justifies all survivors
- MUTN-02 requirement addressed with clear pass/fail statement
</success_criteria>

<output>
After completion, create `.planning/phases/111.5-close-v7-gaps/111.5-02-SUMMARY.md`
</output>
