---
phase: 111.5-close-v7-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Runtime.Tests/DataWarehouse.Runtime.Tests.csproj
  - DataWarehouse.Runtime.Tests/BoundedCacheTests.cs
  - DataWarehouse.Runtime.Tests/VdeDecoratorChainTests.cs
  - DataWarehouse.Runtime.Tests/PolicyEngineTests.cs
  - DataWarehouse.Runtime.Tests/MessageBusTests.cs
  - DataWarehouse.Runtime.Tests/StrategyBaseContractTests.cs
  - DataWarehouse.sln
autonomous: true
requirements: [GAP-01]

must_haves:
  truths:
    - "Runtime tests call production SDK/Kernel methods directly (not source-code analysis)"
    - "Tests exercise IL-level code paths so Stryker mutations are visible"
    - "BoundedCache LRU/ARC/TTL eviction paths are tested with assertions on behavior"
    - "VDE decorator chain methods are invoked and validated"
    - "Message bus publish/subscribe roundtrip is tested"
    - "Strategy base contract methods (init/execute/dispose) are tested"
  artifacts:
    - path: "DataWarehouse.Runtime.Tests/DataWarehouse.Runtime.Tests.csproj"
      provides: "Test project with xunit + SDK/Kernel references"
    - path: "DataWarehouse.Runtime.Tests/BoundedCacheTests.cs"
      provides: "Runtime tests for BoundedCache operations"
    - path: "DataWarehouse.Runtime.Tests/VdeDecoratorChainTests.cs"
      provides: "Runtime tests for VDE decorator chain"
    - path: "DataWarehouse.Runtime.Tests/PolicyEngineTests.cs"
      provides: "Runtime tests for policy engine resolution"
    - path: "DataWarehouse.Runtime.Tests/MessageBusTests.cs"
      provides: "Runtime tests for message bus pub/sub"
    - path: "DataWarehouse.Runtime.Tests/StrategyBaseContractTests.cs"
      provides: "Runtime tests for strategy base lifecycle"
  key_links:
    - from: "DataWarehouse.Runtime.Tests/*.cs"
      to: "DataWarehouse.SDK"
      via: "ProjectReference"
      pattern: "new BoundedCache|new AdvancedMessageBus"
---

<objective>
Create a runtime unit test project (DataWarehouse.Runtime.Tests) with tests that call production SDK and Kernel methods directly. Unlike the existing Hardening.Tests which do source-code analysis via regex, these tests instantiate real objects and exercise IL-level code paths so Stryker mutation testing can detect mutants.

Purpose: Enable meaningful Stryker mutation testing (GAP-01/MUTN-02) by providing tests that actually execute production code.
Output: DataWarehouse.Runtime.Tests/ project with 40+ runtime tests across 5 test classes.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v8.0-PLAN.md (lines 25-90 for Phase 111.5 detail)

Key SDK classes to test:
- DataWarehouse.SDK/Contracts/Scaling/BoundedCache.cs (LRU/ARC/TTL eviction, AutoSizeFromRam)
- DataWarehouse.SDK/Contracts/StrategyBase.cs (init/execute/dispose lifecycle)
- DataWarehouse.SDK/Utilities/BoundedDictionary.cs (legacy bounded dict)
- DataWarehouse.Kernel/Messaging/AdvancedMessageBus.cs (pub/sub, topic routing)
- DataWarehouse.SDK/VirtualDiskEngine/ (decorator chain, Superblock, VdeCreator)
- DataWarehouse.SDK/PolicyEngine/ (policy resolution)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataWarehouse.Runtime.Tests project and csproj</name>
  <files>DataWarehouse.Runtime.Tests/DataWarehouse.Runtime.Tests.csproj, DataWarehouse.sln</files>
  <action>
Create a new xunit test project `DataWarehouse.Runtime.Tests/`:

1. Create `DataWarehouse.Runtime.Tests.csproj` with:
   - TargetFramework: net10.0
   - ImplicitUsings: enable, Nullable: enable, IsPackable: false
   - PackageReferences: Microsoft.NET.Test.Sdk 17.14.1, xunit 2.9.3, xunit.runner.visualstudio 3.1.4, coverlet.collector 6.0.*
   - ProjectReferences to DataWarehouse.SDK and DataWarehouse.Kernel (these are the mutation targets)
   - Global using for Xunit

2. Add the project to `DataWarehouse.sln` via `dotnet sln add`.

3. Verify the project builds: `dotnet build DataWarehouse.Runtime.Tests/`.

CRITICAL: These tests must instantiate REAL production objects and call REAL methods. No mocking of the classes under test. No source-code analysis. No regex pattern matching. Every test must exercise IL-level code that Stryker can mutate.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Runtime.Tests/DataWarehouse.Runtime.Tests.csproj</automated>
  </verify>
  <done>Project created, added to solution, builds successfully with SDK+Kernel references</done>
</task>

<task type="auto">
  <name>Task 2: Write runtime tests for BoundedCache, VDE, PolicyEngine, MessageBus, StrategyBase</name>
  <files>DataWarehouse.Runtime.Tests/BoundedCacheTests.cs, DataWarehouse.Runtime.Tests/VdeDecoratorChainTests.cs, DataWarehouse.Runtime.Tests/PolicyEngineTests.cs, DataWarehouse.Runtime.Tests/MessageBusTests.cs, DataWarehouse.Runtime.Tests/StrategyBaseContractTests.cs</files>
  <action>
Create 5 test classes with 8-10 tests each (40-50 total). All tests MUST instantiate real objects and call real methods.

**BoundedCacheTests.cs** (target: BoundedCache in SDK/Contracts/Scaling/):
- Test LRU eviction: add maxEntries+1 items, verify oldest evicted
- Test ARC eviction: add items, access some to promote, verify frequency-based eviction
- Test TTL expiry: add item with short TTL, wait, verify expired
- Test GetOrAdd: verify factory called on miss, cached on hit
- Test TryGet: returns true for existing, false for missing
- Test Clear: clears all entries
- Test Count property accuracy after add/remove/evict
- Test concurrent add/get from multiple threads (no exceptions)
- Test AutoSizeFromRam option (constructor doesn't throw, MaxEntries > 0)

**VdeDecoratorChainTests.cs** (target: SDK/VirtualDiskEngine/):
- Test VdeCreator.CreateVdeAsync creates a valid VDE container on disk
- Test Superblock.Serialize/Deserialize roundtrip with known values
- Test SuperblockV2.Serialize/Deserialize roundtrip
- Test MagicSignature.Serialize/Deserialize roundtrip
- Test CompactInode64 creation and field access
- Test UniversalBlockTrailer checksum creation
- Test VdeDetector.Detect identifies a valid VDE file
- Test InodeExtent creation with valid block ranges
Read the actual class signatures before writing tests. Use `new` directly or static factory methods. Handle any required constructor dependencies with minimal real instances (not mocks).

**PolicyEngineTests.cs** (target: SDK/PolicyEngine/ or similar):
- Test policy creation and rule attachment
- Test policy resolution returns correct policy for context
- Test priority ordering when multiple policies match
- Test default policy when no rules match
- Test policy validation rejects invalid rules
Read the actual PolicyEngine classes to find the correct types and methods.

**MessageBusTests.cs** (target: Kernel/Messaging/AdvancedMessageBus):
- Test publish sends message to subscriber
- Test topic-based routing delivers only to matching subscribers
- Test unsubscribe stops delivery
- Test publish with no subscribers doesn't throw
- Test concurrent publish/subscribe from multiple threads
- Test message ordering (FIFO within single publisher)
Read AdvancedMessageBus constructor to understand required dependencies. Provide minimal real dependencies.

**StrategyBaseContractTests.cs** (target: SDK/Contracts/StrategyBase):
- Test InitializeAsync completes without error on concrete subclass
- Test ExecuteAsync on a simple strategy
- Test DisposeAsync releases resources
- Test StrategyApplicability property returns Both by default
- Test strategy Name and Description properties
Create a minimal concrete subclass of StrategyBase for testing (TestStrategy : StrategyBase).

IMPORTANT: If any class requires complex DI or infrastructure, create a minimal real implementation rather than a mock. The goal is IL-level code execution for Stryker visibility.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Runtime.Tests/ --no-build --verbosity normal</automated>
  </verify>
  <done>40+ runtime tests pass, all calling real production methods. `dotnet test` shows green for all tests.</done>
</task>

</tasks>

<verification>
dotnet test DataWarehouse.Runtime.Tests/ --verbosity normal
# Expected: 40+ tests pass, 0 failures
# Verify tests are runtime (not source-code analysis): grep for "new BoundedCache\|new AdvancedMessageBus\|Superblock.Deserialize\|StrategyBase" in test files
</verification>

<success_criteria>
- DataWarehouse.Runtime.Tests project exists, builds, and is in the solution
- 40+ tests that instantiate real SDK/Kernel objects and call real methods
- All tests pass with `dotnet test`
- No mocks of classes under test (mocks of infrastructure like ILogger are acceptable)
- Tests exercise code paths Stryker can mutate (branching logic, return values, arithmetic)
</success_criteria>

<output>
After completion, create `.planning/phases/111.5-close-v7-gaps/111.5-01-SUMMARY.md`
</output>
