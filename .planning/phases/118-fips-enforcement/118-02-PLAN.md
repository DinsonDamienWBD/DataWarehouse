---
phase: 118-fips-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Security/FipsApprovedAlgorithms.cs
  - DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs
  - DataWarehouse.Hardening.Tests/Security/FipsApprovedAlgorithmsTests.cs
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "FipsApprovedAlgorithms lists every NIST-approved algorithm by category"
    - "CryptographicAlgorithmRegistry validates selections against FipsApprovedAlgorithms when FIPS enabled"
    - "Non-FIPS hash algorithms rejected when FIPS mode active"
  artifacts:
    - path: "DataWarehouse.SDK/Security/FipsApprovedAlgorithms.cs"
      provides: "Static FIPS algorithm whitelist by category"
      exports: ["FipsApprovedAlgorithms"]
    - path: "DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs"
      provides: "Updated registry with FIPS validation gate"
      exports: ["ICryptographicAlgorithmRegistry", "DefaultCryptographicAlgorithmRegistry"]
    - path: "DataWarehouse.Hardening.Tests/Security/FipsApprovedAlgorithmsTests.cs"
      provides: "Tests for algorithm registry FIPS validation"
      min_lines: 60
  key_links:
    - from: "DataWarehouse.SDK/Security/FipsApprovedAlgorithms.cs"
      to: "DataWarehouse.SDK/Security/FipsModeEnforcer.cs"
      via: "Enforcer delegates to FipsApprovedAlgorithms for lookups"
      pattern: "FipsApprovedAlgorithms"
    - from: "DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs"
      to: "DataWarehouse.SDK/Security/FipsApprovedAlgorithms.cs"
      via: "Registry checks FIPS status via static class"
      pattern: "FipsApprovedAlgorithms\\.Is"
---

<objective>
Create the FIPS algorithm registry — a static class cataloging every NIST FIPS 140-2/3 approved algorithm by category, and extend the existing CryptographicAlgorithmRegistry to validate selections against this registry.

Purpose: Provides the authoritative lookup table for FipsModeEnforcer (Plan 01) and any future crypto validation. Extends the existing ICryptographicAlgorithmRegistry.IsFipsApproved() to cover symmetric ciphers, asymmetric, KDFs, and MACs beyond just hash algorithms.

Output: FipsApprovedAlgorithms static class, updated CryptographicAlgorithmRegistry, TDD tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing ICryptographicAlgorithmRegistry to extend -->

From DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs:
```csharp
public interface ICryptographicAlgorithmRegistry
{
    HashAlgorithmName DefaultHashAlgorithm { get; }
    HashAlgorithmName DefaultHmacAlgorithm { get; }
    IReadOnlyList<HashAlgorithmName> SupportedHashAlgorithms { get; }
    IReadOnlyList<HashAlgorithmName> SupportedHmacAlgorithms { get; }
    HashAlgorithm CreateHashAlgorithm(HashAlgorithmName algorithmName);
    HMAC CreateHmac(HashAlgorithmName algorithmName, byte[] key);
    byte[] ComputeHash(HashAlgorithmName algorithmName, ReadOnlySpan<byte> data);
    byte[] ComputeHmac(HashAlgorithmName algorithmName, ReadOnlySpan<byte> key, ReadOnlySpan<byte> data);
    bool IsFipsApproved(HashAlgorithmName algorithmName);
}

public class DefaultCryptographicAlgorithmRegistry : ICryptographicAlgorithmRegistry
{
    public bool IsFipsApproved(HashAlgorithmName algorithmName)
    {
        return algorithmName == HashAlgorithmName.SHA256
            || algorithmName == HashAlgorithmName.SHA384
            || algorithmName == HashAlgorithmName.SHA512;
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FipsApprovedAlgorithms static class</name>
  <files>DataWarehouse.SDK/Security/FipsApprovedAlgorithms.cs</files>
  <behavior>
    - FipsApprovedAlgorithms.IsApproved("AES-256-GCM") returns true
    - FipsApprovedAlgorithms.IsApproved("ChaCha20-Poly1305") returns false
    - FipsApprovedAlgorithms.GetCategory("AES-256-GCM") returns SymmetricCipher
    - FipsApprovedAlgorithms.GetApprovedByCategory(FipsAlgorithmCategory.Hash) returns SHA-256, SHA-384, SHA-512
    - FipsApprovedAlgorithms.GetNistReference("AES-256-GCM") returns "FIPS 197, SP 800-38D"
  </behavior>
  <action>
Create `DataWarehouse.SDK/Security/FipsApprovedAlgorithms.cs`:

1. `FipsAlgorithmCategory` enum: `SymmetricCipher`, `AsymmetricCipher`, `Hash`, `Mac`, `KeyDerivation`, `DigitalSignature`, `KeyAgreement`, `RandomNumberGeneration`

2. `FipsAlgorithmEntry` record: `string AlgorithmId`, `FipsAlgorithmCategory Category`, `string NistReference`, `int MinimumKeyBits` (0 if N/A), `string? Notes`

3. `FipsApprovedAlgorithms` static class with:
   - Private `Dictionary<string, FipsAlgorithmEntry>` (OrdinalIgnoreCase comparer)
   - Static constructor populates with NIST FIPS 140-3 approved algorithms:

   **Symmetric Ciphers (FIPS 197, SP 800-38A/D/E):**
   - AES-128, AES-192, AES-256, AES-128-GCM, AES-192-GCM, AES-256-GCM, AES-128-CBC, AES-256-CBC, AES-256-XTS, AES-128-CTR, AES-256-CTR, AES-256-CCM
   - 3DES (TDEA) — "FIPS 46-3 (deprecated, legacy only)"

   **Hash (FIPS 180-4):**
   - SHA-1 (legacy, signing only), SHA-224, SHA-256, SHA-384, SHA-512, SHA-512/256

   **MAC (FIPS 198-1, SP 800-38B):**
   - HMAC-SHA-1, HMAC-SHA-224, HMAC-SHA-256, HMAC-SHA-384, HMAC-SHA-512, AES-CMAC

   **Digital Signature (FIPS 186-5):**
   - RSA-2048, RSA-3072, RSA-4096, ECDSA-P256, ECDSA-P384, ECDSA-P521, EdDSA-Ed25519 (FIPS 186-5)

   **Key Derivation (SP 800-108, SP 800-132):**
   - PBKDF2-SHA256, PBKDF2-SHA384, PBKDF2-SHA512, HKDF-SHA256, HKDF-SHA384, HKDF-SHA512, SP800-108-HMAC

   **Key Agreement (SP 800-56A/B):**
   - ECDH-P256, ECDH-P384, ECDH-P521, DH-2048, DH-3072

   **RNG (SP 800-90A):**
   - CTR-DRBG, HMAC-DRBG, Hash-DRBG

   Public methods:
   - `static bool IsApproved(string algorithmId)` — case-insensitive lookup
   - `static FipsAlgorithmCategory? GetCategory(string algorithmId)` — returns null if not approved
   - `static string? GetNistReference(string algorithmId)`
   - `static IReadOnlyList<FipsAlgorithmEntry> GetApprovedByCategory(FipsAlgorithmCategory category)`
   - `static IReadOnlyList<FipsAlgorithmEntry> GetAllApproved()`
   - `static int GetMinimumKeyBits(string algorithmId)` — returns 0 if not found

   Include XML doc comments. Each entry must have accurate NIST SP/FIPS references.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>FipsApprovedAlgorithms static class compiles with complete NIST algorithm inventory. Each entry has category, NIST reference, and minimum key bits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend CryptographicAlgorithmRegistry + tests</name>
  <files>DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs, DataWarehouse.Hardening.Tests/Security/FipsApprovedAlgorithmsTests.cs</files>
  <behavior>
    - Existing IsFipsApproved(HashAlgorithmName) still works (backward compatible)
    - New IsFipsApprovedAlgorithm(string algorithmId) delegates to FipsApprovedAlgorithms
    - GetAllApproved returns 50+ entries across all categories
    - Each category has at least 2 approved algorithms
    - SHA3-256, BLAKE3, ChaCha20 are NOT in the approved list
  </behavior>
  <action>
1. Update `DefaultCryptographicAlgorithmRegistry` in `DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs`:
   - Add method `public bool IsFipsApprovedAlgorithm(string algorithmId) => FipsApprovedAlgorithms.IsApproved(algorithmId);`
   - Update existing `IsFipsApproved(HashAlgorithmName)` to delegate: `return FipsApprovedAlgorithms.IsApproved(algorithmName.Name ?? "");`
   - Add `IsFipsApprovedAlgorithm(string algorithmId)` to `ICryptographicAlgorithmRegistry` interface (default interface method for backward compat if needed, or just add to interface since all implementations are internal)

2. Create `DataWarehouse.Hardening.Tests/Security/FipsApprovedAlgorithmsTests.cs`:
   - `[Fact] AllCategories_HaveApprovedAlgorithms` — every FipsAlgorithmCategory has >= 2 entries
   - `[Theory] ApprovedSymmetricCiphers_ReturnTrue` — AES-128-GCM, AES-256-GCM, AES-256-XTS
   - `[Theory] NonFipsAlgorithms_ReturnFalse` — ChaCha20, ChaCha20-Poly1305, XChaCha20, BLAKE2, BLAKE3, Serpent-256-GCM, Serpent-256-XTS, Twofish, Camellia, Argon2id, SHA3-256, SHA3-512, ML-KEM-1024, Kyber1024-AES256
   - `[Fact] GetCategory_ReturnsCorrectCategory`
   - `[Fact] GetNistReference_ReturnsValidReference`
   - `[Fact] CaseInsensitive_Lookup`
   - `[Fact] Registry_IsFipsApprovedAlgorithm_DelegatesToStatic`
   - `[Fact] TotalApprovedCount_AtLeast50` — ensures comprehensive coverage
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~FipsApprovedAlgorithms" --no-restore -v q 2>&1 | tail -10</automated>
  </verify>
  <done>FipsApprovedAlgorithms has 50+ entries. All NIST categories populated. Registry delegates to static class. Non-FIPS algorithms (ChaCha20, BLAKE3, Serpent, Argon2id, SHA3, ML-KEM) correctly excluded. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test --filter FipsApprovedAlgorithms` — all tests pass
- FipsApprovedAlgorithms.IsApproved("AES-256-GCM") returns true
- FipsApprovedAlgorithms.IsApproved("ChaCha20-Poly1305") returns false
- Every FipsAlgorithmCategory has at least 2 entries
- Total approved algorithm count >= 50
</verification>

<success_criteria>
- FipsApprovedAlgorithms static class with complete NIST FIPS 140-2/3 inventory
- Each entry has category, NIST reference, minimum key bits
- CryptographicAlgorithmRegistry delegates IsFipsApproved to static class
- Non-FIPS algorithms definitively excluded
- Tests verify all categories, approved/blocked algorithms, case insensitivity
</success_criteria>

<output>
After completion, create `.planning/phases/118-fips-enforcement/118-02-SUMMARY.md`
</output>
