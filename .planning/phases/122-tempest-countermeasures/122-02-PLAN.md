---
phase: 122-tempest-countermeasures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Security/Tempest/SideChannelAudit.cs
  - DataWarehouse.Hardening.Tests/Security/Tempest/SideChannelAuditTests.cs
autonomous: true
requirements: [SEC-05]

must_haves:
  truths:
    - "All crypto strategies audited for timing leaks"
    - "FixedTimeEquals used everywhere comparisons involve secrets"
    - "Random delays added to prevent power analysis patterns"
    - "Residual hardware-dependent risks documented"
  artifacts:
    - path: "DataWarehouse.SDK/Security/Tempest/SideChannelAudit.cs"
      provides: "Side-channel hardening audit for crypto strategies"
      exports: ["SideChannelAudit", "SideChannelAuditResult", "TimingLeakPattern"]
    - path: "DataWarehouse.Hardening.Tests/Security/Tempest/SideChannelAuditTests.cs"
      provides: "Tests for side-channel audit patterns"
  key_links:
    - from: "DataWarehouse.SDK/Security/Tempest/SideChannelAudit.cs"
      to: "DataWarehouse.SDK/Security/Tempest/ConstantTimeOperations.cs"
      via: "Validates usage of constant-time operations"
      pattern: "ConstantTimeOperations|FixedTimeEquals"
    - from: "DataWarehouse.SDK/Security/Tempest/SideChannelAudit.cs"
      to: "DataWarehouse.SDK/Contracts/Encryption/EncryptionStrategy.cs"
      via: "Audits encryption strategy implementations"
      pattern: "EncryptionStrategy|IEncryptionStrategy"
---

<objective>
Audit all crypto strategies for timing leaks, ensure FixedTimeEquals usage, add random delay helpers, and document residual hardware risks.

Purpose: Side-channel attacks exploit timing variations, power consumption, and cache behavior. This audit ensures all crypto paths use constant-time operations and documents what software cannot mitigate.
Output: SideChannelAudit scanner, TimingLeakPattern detector, and tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Security/Tempest/ConstantTimeOperations.cs
@DataWarehouse.SDK/Contracts/Encryption/EncryptionStrategy.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SideChannelAudit scanner and timing leak patterns</name>
  <files>DataWarehouse.SDK/Security/Tempest/SideChannelAudit.cs, DataWarehouse.Hardening.Tests/Security/Tempest/SideChannelAuditTests.cs</files>
  <behavior>
    - SideChannelAudit.ScanAssembly(assembly) returns audit results for all types with crypto operations
    - TimingLeakPattern detects: early-return comparisons, data-dependent branching, non-constant-time array indexing
    - RandomDelayHelper.AddJitter(minMs, maxMs) adds cryptographically random delay
    - HardwareRiskDocumentation returns structured list of residual hardware risks
    - Audit flags methods using SequenceEqual (non-constant-time) for secret comparison
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/Tempest/SideChannelAudit.cs`:
       - `TimingLeakPattern` enum flags: `None = 0`, `EarlyReturnComparison = 1`, `DataDependentBranching = 2`, `NonConstantTimeIndexing = 4`, `UnsafeSequenceEqual = 8`, `VariableTimeCrypto = 16`
       - `SideChannelAuditEntry` record: `string TypeName`, `string MethodName`, `TimingLeakPattern DetectedPatterns`, `string Description`, `string Recommendation`
       - `SideChannelAuditResult` record: `IReadOnlyList<SideChannelAuditEntry> Findings`, `int TotalMethodsScanned`, `int MethodsWithIssues`, `IReadOnlyList<string> HardwareResidualRisks`
       - `SideChannelAudit` static class:
         - `ScanAssembly(Assembly assembly)` -> `SideChannelAuditResult`:
           - Scans all types for methods that: compare byte arrays (check if using FixedTimeEquals or SequenceEqual), have data-dependent branches in crypto context, use array indexing that leaks access patterns
           - Uses reflection to find methods with `byte[]` parameters or return types in security-related namespaces
         - `GetKnownTimingLeakPatterns()` -> `IReadOnlyList<TimingLeakPattern>` — reference list
         - `GenerateHardwareRiskReport()` -> `IReadOnlyList<string>`:
           - "CPU branch predictor leaks via Spectre/Meltdown variants"
           - "DRAM row buffer timing reveals access patterns"
           - "CPU cache hierarchy (L1/L2/L3) enables cache-timing attacks"
           - "Power supply rail voltage fluctuations correlate with computation"
           - "EM emanation from CPU/memory bus detectable at distance"
           - "Keyboard/display EM emanation (Van Eck phreaking)"
           - ".NET JIT optimization may reorder constant-time code"
           - ".NET GC stop-the-world pauses create timing signatures"
       - `RandomDelayHelper` static class:
         - `AddJitter(int minMs, int maxMs)` — `Thread.Sleep` for random duration via `RandomNumberGenerator.GetInt32(minMs, maxMs)`
         - `AddJitterAsync(int minMs, int maxMs, CancellationToken ct)` — async version with `Task.Delay`
    2. Create tests:
       - Test: ScanAssembly returns results (non-null, scanned count > 0)
       - Test: TimingLeakPattern flags are combinable (flags enum)
       - Test: GenerateHardwareRiskReport returns non-empty list
       - Test: RandomDelayHelper.AddJitter produces delay within range
       - Test: Known patterns include EarlyReturnComparison and UnsafeSequenceEqual
       - Test: SideChannelAuditEntry contains actionable recommendation
       - Test: Audit result includes hardware residual risks
       - Test: Assembly scan handles assemblies with no crypto types gracefully
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 122: Side-channel hardening audit")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~SideChannelAudit" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>SideChannelAudit scans assemblies for timing leak patterns. RandomDelayHelper provides jitter. Hardware residual risks documented. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "SideChannelAudit"` — all green
</verification>

<success_criteria>
- Assembly scanner detects timing leak patterns
- FixedTimeEquals usage validated
- Random delay helpers available
- Hardware residual risks documented with mitigations
</success_criteria>

<output>
After completion, create `.planning/phases/122-tempest-countermeasures/122-02-SUMMARY.md`
</output>
