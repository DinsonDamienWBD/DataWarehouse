---
phase: 139-test-coverage
plan: 05
type: execute
wave: 3
depends_on: [139-03, 139-04]
files_modified:
  - DataWarehouse.Hardening.Tests/Plugin/StrategyFunctionalTests.cs
  - DataWarehouse.Hardening.Tests/Plugin/TopStrategyTestData.cs
autonomous: true
requirements: [TST-02]

must_haves:
  truths:
    - "Top 20 most-used strategies per plugin have functional tests with real data"
    - "Functional tests verify actual input/output behavior, not just lifecycle"
    - "Edge cases and error paths tested for critical strategies"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Plugin/StrategyFunctionalTests.cs"
      provides: "Deep functional tests for top strategies"
      min_lines: 500
    - path: "DataWarehouse.Hardening.Tests/Plugin/TopStrategyTestData.cs"
      provides: "Test data generators for functional tests"
      min_lines: 200
  key_links:
    - from: "StrategyFunctionalTests.cs"
      to: "Plugins/DataWarehouse.Plugins.*"
      via: "Direct strategy instantiation and Execute()"
      pattern: "\\.Execute\\("
---

<objective>
Write deeper functional tests for the top 20 most-used strategies per plugin — real data in, real data out verification.

Purpose: Contract compliance (plan 04) proves lifecycle works. Functional tests prove strategies actually DO their job correctly with representative inputs.
Output: Functional test suite covering ~1,000 most-critical strategies across all plugins.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PLUGIN-CATALOG.md
@Metadata/plugin-strategy-map.json
@.planning/phases/139-test-coverage/139-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Top strategy functional test data and infrastructure</name>
  <files>DataWarehouse.Hardening.Tests/Plugin/TopStrategyTestData.cs</files>
  <behavior>
    - Test data provider identifies top 20 strategies per plugin by complexity/importance
    - Test data includes valid inputs, boundary inputs, and invalid inputs for each strategy domain
    - Data generators produce representative test payloads for each strategy category (encryption, compression, storage, networking, etc.)
  </behavior>
  <action>
    1. Read `Metadata/plugin-strategy-map.json` and `.planning/PLUGIN-CATALOG.md` to identify the most critical strategies per plugin.
    2. Create `TopStrategyTestData.cs` with:
       - Categorization of strategies by domain: encryption, compression, storage, filesystem, networking, compliance, data format, replication, etc.
       - Per-domain test data factories: `CreateEncryptionTestData()`, `CreateCompressionTestData()`, `CreateStorageTestData()`, etc.
       - Each factory returns test payloads appropriate for that domain (byte arrays for encryption/compression, file metadata for filesystem, config objects for compliance, etc.).
       - `GetTopStrategiesPerPlugin()` — returns top 20 from each of the 52 plugins (sorted by number of public methods as complexity proxy).
    3. Focus on strategies that process data (transforms, validators, formatters) over simple configuration holders.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -3</automated>
  </verify>
  <done>TopStrategyTestData compiles and provides test data generators for all strategy domains. Top strategies identified per plugin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Functional tests for top strategies across all plugins</name>
  <files>DataWarehouse.Hardening.Tests/Plugin/StrategyFunctionalTests.cs</files>
  <behavior>
    - Encryption strategies: encrypt/decrypt round-trip produces original data
    - Compression strategies: compress/decompress round-trip preserves data
    - Storage strategies: write/read returns same content
    - Validation strategies: accept valid input, reject invalid input
    - Transform strategies: output matches expected transformation
  </behavior>
  <action>
    1. Create `StrategyFunctionalTests.cs` with test methods grouped by domain.
    2. For each domain category, write `[Theory]` tests using data from `TopStrategyTestData`:
       - **Encryption**: `EncryptDecryptRoundTrip` — encrypt bytes, decrypt, assert equal to original.
       - **Compression**: `CompressDecompressRoundTrip` — compress, decompress, assert equal.
       - **Storage**: `WriteReadRoundTrip` — write data, read back, assert equal.
       - **Compliance**: `ValidInputAccepted` / `InvalidInputRejected`.
       - **DataFormat**: `SerializeDeserializeRoundTrip`.
       - **Replication**: `ReplicateAndVerify`.
       - **Networking**: `ConnectionLifecycle` (connect, send, receive, disconnect with mocks).
    3. For strategies that require external resources (network, disk, GPU), mock the resource layer and test the strategy logic.
    4. Each test should take <1 second. Use in-memory buffers, not actual I/O.
    5. Target: at least 500 individual test cases across all domains.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~StrategyFunctional" --no-build 2>&1 | tail -10</automated>
  </verify>
  <done>500+ functional test cases pass across all strategy domains. Top 20 strategies per plugin have real input/output verification.</done>
</task>

</tasks>

<verification>
- All functional tests pass
- At least 500 individual test cases
- Coverage of encryption, compression, storage, compliance, data format, and networking domains
</verification>

<success_criteria>
Top strategies across all 52 plugins have functional tests verifying actual behavior with real data. Round-trip tests confirm data integrity for transform strategies.
</success_criteria>

<output>
After completion, create `.planning/phases/139-test-coverage/139-05-SUMMARY.md`
</output>
