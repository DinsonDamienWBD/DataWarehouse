---
phase: 139-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/SDK/SdkCoverageTests.cs
  - DataWarehouse.Hardening.Tests/SDK/SdkBaseClassTests.cs
  - DataWarehouse.Hardening.Tests/SDK/SdkUtilityTests.cs
autonomous: true
requirements: [TST-01]

must_haves:
  truths:
    - "Every SDK base class has unit tests covering public API surface"
    - "SDK utility classes (serialization, hashing, config) are tested"
    - "SDK contract interfaces are validated via mock implementations"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/SDK/SdkBaseClassTests.cs"
      provides: "Tests for all 23 SDK base classes"
      min_lines: 200
    - path: "DataWarehouse.Hardening.Tests/SDK/SdkUtilityTests.cs"
      provides: "Tests for SDK utility/helper classes"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/SDK/SdkCoverageTests.cs"
      provides: "Tests for SDK configuration, attributes, contracts"
      min_lines: 100
  key_links:
    - from: "DataWarehouse.Hardening.Tests/SDK/"
      to: "DataWarehouse.SDK/"
      via: "ProjectReference"
      pattern: "using DataWarehouse\\.SDK"
---

<objective>
Write comprehensive unit tests for all DataWarehouse.SDK base classes, contracts, and utilities to achieve 100% line coverage.

Purpose: SDK is the foundation all 52 plugins build on. 100% SDK coverage ensures every base class contract is verified before plugin tests build on them.
Output: SDK test files achieving 100% coverage of DataWarehouse.SDK project.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SDK base class unit tests</name>
  <files>DataWarehouse.Hardening.Tests/SDK/SdkBaseClassTests.cs</files>
  <behavior>
    - Each of the 23 SDK base classes (PluginBase, StrategyBase, and domain-specific bases) can be instantiated via a test subclass
    - Initialize/Execute/Dispose lifecycle works correctly for each base
    - Virtual methods have default behavior that is verifiable
    - Abstract members are enforced (compilation check via test subclass)
    - StrategyApplicability enum (Both/DataOnly/DeviceOnly) defaults correctly
  </behavior>
  <action>
    1. Read `docs/ai-maps/map-core.md` and `memory/plugin-base-hierarchy.md` to get the complete list of SDK base classes.
    2. For each base class in the 4-level hierarchy (PluginBase > domain PluginBases > StrategyBase > domain StrategyBases):
       - Create a minimal test subclass that implements required abstract members
       - Test construction, initialization with mock IServiceProvider, execution with mock inputs, and disposal
       - Test that StrategyApplicability defaults to `Both`
       - Test any virtual methods for default return values
    3. Group tests by hierarchy level. Use `[Theory]` with `[MemberData]` where the same pattern applies to multiple base classes.
    4. Existing tests in `DataWarehouse.Hardening.Tests/SDK/` should be preserved. Check what exists first and only add missing coverage.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~SdkBaseClass" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All 23 SDK base classes have lifecycle tests (construct, init, execute, dispose). Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SDK utility and contract tests</name>
  <files>DataWarehouse.Hardening.Tests/SDK/SdkUtilityTests.cs, DataWarehouse.Hardening.Tests/SDK/SdkCoverageTests.cs</files>
  <behavior>
    - SDK Configuration classes serialize/deserialize correctly
    - SDK Attributes (e.g., StrategyAttribute) store and retrieve metadata
    - SDK Connectors/Interfaces have testable default implementations
    - SDK enums cover all expected values
    - Edge cases: null inputs, empty strings, boundary values
  </behavior>
  <action>
    1. Scan `DataWarehouse.SDK/` for all public classes not covered by Task 1 (utilities, configuration models, attributes, enums, interfaces with default implementations).
    2. Create `SdkUtilityTests.cs`: Test each utility class (serialization helpers, hashing, path utilities, etc.) with valid inputs, edge cases, and error cases.
    3. Create `SdkCoverageTests.cs`: Test configuration models (can construct, serialize to JSON, deserialize back), attributes (can apply, retrieve values), and any remaining SDK surface area.
    4. Use `[InlineData]` for parameterized boundary testing.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~SdkUtility|FullyQualifiedName~SdkCoverage" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>SDK utility and contract tests pass. Combined with Task 1, SDK project has near-100% line coverage.</done>
</task>

</tasks>

<verification>
- All SDK tests pass: `dotnet test --filter "FullyQualifiedName~Sdk" --no-build`
- SDK base class hierarchy fully tested (23 bases)
- No SDK public method left untested
</verification>

<success_criteria>
SDK project achieves 100% line coverage. All 23 base classes, utility classes, configuration models, and contracts have unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/139-test-coverage/139-02-SUMMARY.md`
</output>
