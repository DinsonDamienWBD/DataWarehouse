---
phase: 139-test-coverage
plan: 07
type: execute
wave: 3
depends_on: [139-03, 139-04]
files_modified:
  - DataWarehouse.Hardening.Tests/Integration/MessageBusE2ETests.cs
  - DataWarehouse.Hardening.Tests/Integration/VdeDecoratorChainTests.cs
  - DataWarehouse.Hardening.Tests/Integration/FederationRouterTests.cs
  - DataWarehouse.Hardening.Tests/Integration/PipelineE2ETests.cs
autonomous: true
requirements: [TST-03]

must_haves:
  truths:
    - "Message bus E2E: publish from plugin A, received by plugin B through kernel"
    - "VDE decorator chain: data passes through Cache-Integrity-Compression-Dedup-Encryption-WAL-RAID-File"
    - "Federation router: route request to correct shard, handle shard unavailability"
    - "Pipeline orchestration: multi-stage pipeline executes in correct order with data passing"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Integration/MessageBusE2ETests.cs"
      provides: "Message bus end-to-end integration tests"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/Integration/VdeDecoratorChainTests.cs"
      provides: "VDE decorator chain integration tests"
      min_lines: 150
    - path: "DataWarehouse.Hardening.Tests/Integration/FederationRouterTests.cs"
      provides: "Federation router integration tests"
      min_lines: 100
  key_links:
    - from: "VdeDecoratorChainTests.cs"
      to: "VdeMountPipeline"
      via: "SuperblockV2 feature flags build decorator chain"
      pattern: "VdeMountPipeline|DecoratorChain"
    - from: "MessageBusE2ETests.cs"
      to: "DataWarehouse.Kernel/MessageBus"
      via: "Kernel boot with real plugins"
      pattern: "MessageBus.*Publish|Subscribe"
---

<objective>
Write integration tests for cross-cutting system behaviors: message bus E2E, VDE decorator chain, federation router, and pipeline orchestration.

Purpose: Unit tests verify individual components. Integration tests verify the CONNECTIONS between components work correctly — the key links where systems break.
Output: Integration test suite covering the critical inter-component data paths.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Message bus E2E and pipeline orchestration integration tests</name>
  <files>DataWarehouse.Hardening.Tests/Integration/MessageBusE2ETests.cs, DataWarehouse.Hardening.Tests/Integration/PipelineE2ETests.cs</files>
  <behavior>
    - Plugin A publishes typed message, Plugin B subscriber receives it within timeout
    - Multiple subscribers receive same broadcast message
    - Pipeline with 3+ stages executes sequentially, data flows between stages
    - Pipeline cancellation stops mid-execution
    - Error in one stage propagates correctly
  </behavior>
  <action>
    1. Check existing integration tests in `DataWarehouse.Hardening.Tests/Integration/` — extend, don't replace.
    2. Create `MessageBusE2ETests.cs`:
       - Boot a minimal kernel with 2 test plugins (or use real lightweight plugins).
       - Plugin A publishes a message to topic "test.data".
       - Plugin B subscribes to "test.data" and captures received messages.
       - Assert message arrives within 5 seconds.
       - Test wildcard subscription ("test.*").
       - Test unsubscribe prevents delivery.
    3. Create `PipelineE2ETests.cs`:
       - Create a pipeline with 3 stages (transform, validate, store).
       - Execute with test data and assert each stage processes in order.
       - Test cancellation token stops execution mid-pipeline.
       - Test stage error handling (first stage fails, verify pipeline stops).
    4. Use `IClassFixture<IntegrationTestFixture>` if the existing fixture provides kernel boot.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~MessageBusE2E|FullyQualifiedName~PipelineE2E" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>Message bus E2E and pipeline orchestration integration tests pass. Cross-plugin communication verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VDE decorator chain and federation router integration tests</name>
  <files>DataWarehouse.Hardening.Tests/Integration/VdeDecoratorChainTests.cs, DataWarehouse.Hardening.Tests/Integration/FederationRouterTests.cs</files>
  <behavior>
    - VDE mount pipeline reads SuperblockV2 features and builds correct decorator chain
    - Data written through full chain (Cache-Integrity-Compression-Dedup-Encryption-WAL-RAID-File) can be read back
    - Disabling a decorator (e.g., no encryption) skips it in the chain
    - Federation router routes to correct shard by key hash
    - Federation handles shard offline gracefully (error, not crash)
  </behavior>
  <action>
    1. Create `VdeDecoratorChainTests.cs`:
       - Create a VDE volume with all decorators enabled.
       - Write 1KB test data through the full chain.
       - Read back through the chain and assert data matches.
       - Create a volume with encryption disabled — verify chain is shorter.
       - Test VdeMountPipeline builds chain from SuperblockV2 feature flags.
       - Use in-memory file backend to avoid disk I/O.
    2. Create `FederationRouterTests.cs`:
       - Create federation config with 3 mock shards.
       - Route request for key "user:123" — assert correct shard selected.
       - Mark shard 2 as offline — route request, assert error or fallback.
       - Test shard lifecycle: bootstrap, join, leave, rejoin.
    3. Keep test execution under 30 seconds total. Use small data sizes.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~VdeDecoratorChain|FullyQualifiedName~FederationRouter" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>VDE decorator chain round-trip verified. Federation router shard selection and failover tested.</done>
</task>

</tasks>

<verification>
- All integration tests pass
- Message bus E2E, VDE chain, federation router, and pipeline orchestration covered
- Tests complete within 60 seconds
</verification>

<success_criteria>
Integration tests verify all critical cross-component data paths: message bus pub/sub, VDE decorator chain read/write, federation routing, and pipeline stage execution.
</success_criteria>

<output>
After completion, create `.planning/phases/139-test-coverage/139-07-SUMMARY.md`
</output>
