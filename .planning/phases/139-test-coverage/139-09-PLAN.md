---
phase: 139-test-coverage
plan: 09
type: execute
wave: 4
depends_on: [139-05, 139-06, 139-07, 139-08]
files_modified:
  - DataWarehouse.Hardening.Tests/Fuzz/VdeParserFuzzHarness.cs
  - DataWarehouse.Hardening.Tests/Fuzz/S3ParserFuzzHarness.cs
  - DataWarehouse.Hardening.Tests/Fuzz/SqlParserFuzzHarness.cs
  - DataWarehouse.Hardening.Tests/Fuzz/CompressionFuzzHarness.cs
  - DataWarehouse.Hardening.Tests/Fuzz/EncryptionFuzzHarness.cs
autonomous: true
requirements: [TST-05]

must_haves:
  truths:
    - "SharpFuzz harnesses exist for VDE parser, S3 parser, SQL parser, compression, encryption"
    - "Each harness accepts fuzzed byte[] input and exercises the target parser/codec"
    - "No unhandled exceptions from 10,000+ iterations of random input"
    - "Crash logs (if any) are written to Metadata/fuzz-results/"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Fuzz/VdeParserFuzzHarness.cs"
      provides: "VDE superblock/inode parser fuzz harness"
      min_lines: 50
    - path: "DataWarehouse.Hardening.Tests/Fuzz/S3ParserFuzzHarness.cs"
      provides: "S3 request parser fuzz harness"
      min_lines: 50
    - path: "DataWarehouse.Hardening.Tests/Fuzz/SqlParserFuzzHarness.cs"
      provides: "SQL query parser fuzz harness"
      min_lines: 50
    - path: "DataWarehouse.Hardening.Tests/Fuzz/CompressionFuzzHarness.cs"
      provides: "Compression codec fuzz harness"
      min_lines: 50
    - path: "DataWarehouse.Hardening.Tests/Fuzz/EncryptionFuzzHarness.cs"
      provides: "Encryption envelope fuzz harness"
      min_lines: 50
  key_links:
    - from: "VdeParserFuzzHarness.cs"
      to: "VDE superblock parser"
      via: "SharpFuzz.Fuzzer.Run()"
      pattern: "Fuzzer\\.Run"
---

<objective>
Build SharpFuzz fuzz harnesses for 5 critical parser/codec subsystems: VDE superblock parser, S3 request parser, SQL parser, compression codecs, and encryption envelopes. Run initial fuzzing and invoke /pentest-fuzz for crash analysis.

Purpose: Fuzz testing discovers crashes and memory corruption that structured tests miss. These harnesses are also prerequisites for Phase 141 penetration testing (/pentest-fuzz skill).
Output: 5 SharpFuzz harnesses, initial fuzz run results, crash logs for /pentest-fuzz analysis.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SharpFuzz harnesses for all 5 subsystems</name>
  <files>DataWarehouse.Hardening.Tests/Fuzz/VdeParserFuzzHarness.cs, DataWarehouse.Hardening.Tests/Fuzz/S3ParserFuzzHarness.cs, DataWarehouse.Hardening.Tests/Fuzz/SqlParserFuzzHarness.cs, DataWarehouse.Hardening.Tests/Fuzz/CompressionFuzzHarness.cs, DataWarehouse.Hardening.Tests/Fuzz/EncryptionFuzzHarness.cs</files>
  <action>
    1. SharpFuzz 2.2.0 is already in the test csproj. Create `DataWarehouse.Hardening.Tests/Fuzz/` directory if not exists.
    2. Check if fuzz harnesses already exist from Phase 111.5 (`DataWarehouse.Hardening.Tests/` — search for any existing `[FuzzTest]` attributes or `Fuzzer.Run` calls). Extend existing harnesses, don't duplicate.
    3. Create each harness following SharpFuzz pattern:
       ```csharp
       public class VdeParserFuzzHarness
       {
           [FuzzTest]
           public void FuzzVdeSuperblockParser(ReadOnlySpan<byte> data)
           {
               try { VdeSuperblockParser.Parse(data); }
               catch (Exception) { /* Expected — we're looking for unhandled crashes */ }
           }
       }
       ```
    4. **VDE Parser**: Feed random bytes to superblock parser and inode parser. Target: any code that reads binary headers.
    5. **S3 Parser**: Feed random bytes/strings to S3 request parser (bucket names, keys, auth headers).
    6. **SQL Parser**: Feed random SQL strings to the PostgreSQL wire protocol parser.
    7. **Compression**: Feed random bytes to each compression codec's Decompress method.
    8. **Encryption**: Feed random bytes to encryption envelope's Decrypt method (simulating corrupt ciphertext).
    9. Each harness should catch expected exceptions (FormatException, ArgumentException, etc.) and only let truly unhandled exceptions (StackOverflow, AccessViolation) propagate as failures.
    10. Create `Metadata/fuzz-results/` directory for crash log output.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -3</automated>
  </verify>
  <done>5 SharpFuzz harnesses compile. Each targets a distinct parser/codec subsystem.</done>
</task>

<task type="auto">
  <name>Task 2: Execute initial fuzz runs and analyze with /pentest-fuzz</name>
  <files>Metadata/fuzz-results/fuzz-run-summary.md</files>
  <action>
    1. Run each fuzz harness for a short initial burst (1,000-10,000 iterations depending on performance):
       ```
       dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~FuzzHarness" --no-build
       ```
       If SharpFuzz requires `sharpfuzz` CLI instrument step, run it first on target assemblies.
    2. Collect any crash logs or exception reports to `Metadata/fuzz-results/`.
    3. After harnesses execute, run `/pentest-fuzz` skill to analyze crash logs and generate patches for any memory corruption or binary parsing flaws discovered.
    4. Create `Metadata/fuzz-results/fuzz-run-summary.md` documenting:
       - Harness name, iterations run, crashes found (count + type)
       - Any patches applied from /pentest-fuzz analysis
       - Status: PASS (no unhandled crashes) or FAIL (crashes found and patched)
    5. If crashes are found, fix the underlying parser/codec code to handle the malformed input, then re-run to confirm fix.
  </action>
  <verify>
    <automated>test -f Metadata/fuzz-results/fuzz-run-summary.md && dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~FuzzHarness" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 harnesses ran 1,000+ iterations. Any crashes found are patched. /pentest-fuzz analysis complete. Fuzz summary report generated.</done>
</task>

</tasks>

<verification>
- All 5 harnesses compile and run
- No unhandled crashes after initial fuzz run
- Crash log analysis complete (via /pentest-fuzz)
- Summary report documents results
</verification>

<success_criteria>
SharpFuzz harnesses built for VDE parser, S3 parser, SQL parser, compression, and encryption. Initial fuzz runs complete with zero unhandled crashes. Harnesses ready for Phase 141 pentest reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/139-test-coverage/139-09-SUMMARY.md`
</output>
