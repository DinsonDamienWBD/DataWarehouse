---
phase: 139-test-coverage
plan: 06
type: execute
wave: 3
depends_on: [139-03, 139-04]
files_modified:
  - DataWarehouse.Hardening.Tests/CLI/CliCommandCoverageTests.cs
  - DataWarehouse.Hardening.Tests/GUI/GuiPageCoverageTests.cs
  - DataWarehouse.Hardening.Tests/Dashboard/DashboardControllerCoverageTests.cs
  - DataWarehouse.Hardening.Tests/Launcher/LauncherCoverageTests.cs
autonomous: true
requirements: [TST-01]

must_haves:
  truths:
    - "CLI commands are tested with mock kernel, verifying output format"
    - "GUI Blazor components are tested with bUnit or mock rendering"
    - "Dashboard controllers are tested with mock services"
    - "Launcher startup sequence and health endpoints tested"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/CLI/CliCommandCoverageTests.cs"
      provides: "CLI command unit tests"
      min_lines: 150
    - path: "DataWarehouse.Hardening.Tests/Dashboard/DashboardControllerCoverageTests.cs"
      provides: "Dashboard controller tests"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/Launcher/LauncherCoverageTests.cs"
      provides: "Launcher tests"
      min_lines: 80
  key_links:
    - from: "DataWarehouse.Hardening.Tests/CLI/"
      to: "DataWarehouse.CLI/"
      via: "ProjectReference"
      pattern: "using DataWarehouse\\.CLI"
---

<objective>
Write unit tests for companion projects: CLI command handlers, GUI pages, Dashboard controllers, and Launcher startup.

Purpose: Companion apps are the user-facing layer. Testing them ensures all command groups work, UI pages render, REST endpoints respond, and the launcher bootstraps correctly.
Output: Companion project test files covering CLI, GUI, Dashboard, and Launcher.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-companions.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI and Launcher coverage tests</name>
  <files>DataWarehouse.Hardening.Tests/CLI/CliCommandCoverageTests.cs, DataWarehouse.Hardening.Tests/Launcher/LauncherCoverageTests.cs</files>
  <behavior>
    - Each CLI command group (volume, plugin, config, status, etc.) can be invoked with --help
    - CLI commands with mock kernel produce expected output format
    - Launcher can construct its host builder
    - Launcher health endpoints return expected responses
  </behavior>
  <action>
    1. Read `docs/ai-maps/map-companions.md` to identify all CLI command groups and Launcher entry points.
    2. Check existing tests in `DataWarehouse.Hardening.Tests/CLI/` — extend, don't duplicate.
    3. Create/extend `CliCommandCoverageTests.cs`:
       - Test each command group can be constructed
       - Test command parsing with valid/invalid args
       - Test output formatting with mock kernel responses
       - Use `System.CommandLine` test infrastructure if available, otherwise invoke via `CommandLineParser.Parse()`.
    4. Create/extend `LauncherCoverageTests.cs`:
       - Test `HostBuilder` configuration
       - Test service registration
       - Test health check endpoint returns healthy
       - Test federation-aware startup configuration
    5. Mock the kernel and all external dependencies. No actual kernel boot in unit tests.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~CliCommandCoverage|FullyQualifiedName~LauncherCoverage" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>CLI command groups and Launcher startup have unit tests. All pass with mock kernel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard and GUI coverage tests</name>
  <files>DataWarehouse.Hardening.Tests/Dashboard/DashboardControllerCoverageTests.cs, DataWarehouse.Hardening.Tests/GUI/GuiPageCoverageTests.cs</files>
  <behavior>
    - Dashboard REST controllers return correct status codes for valid/invalid requests
    - Dashboard service layer interacts correctly with mock kernel
    - GUI page components render without errors
    - GUI DynamicCommandRegistry wiring works
  </behavior>
  <action>
    1. Read `docs/ai-maps/map-companions.md` for Dashboard controllers and GUI pages.
    2. Create `DashboardControllerCoverageTests.cs`:
       - For each REST controller: test GET returns 200 with data, POST validates input, unauthorized returns 401.
       - Mock all service dependencies.
       - Test SignalR hub can be constructed (if applicable).
    3. Create `GuiPageCoverageTests.cs`:
       - If Blazor (bUnit available): render each page component with mock services, assert no render errors.
       - If bUnit not in csproj, test the page view models / code-behind classes directly.
       - Test DynamicCommandRegistry returns expected command list.
    4. Add bUnit package reference to test csproj if needed: `<PackageReference Include="bunit" Version="1.*" />`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~DashboardControllerCoverage|FullyQualifiedName~GuiPageCoverage" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard controllers and GUI pages have unit tests covering happy path and error cases.</done>
</task>

</tasks>

<verification>
- All companion tests pass
- CLI, Dashboard, GUI, Launcher all have test coverage
- No external dependencies required (all mocked)
</verification>

<success_criteria>
Companion projects (CLI, GUI, Dashboard, Launcher) have unit test coverage. All command groups, controllers, pages, and startup paths tested.
</success_criteria>

<output>
After completion, create `.planning/phases/139-test-coverage/139-06-SUMMARY.md`
</output>
