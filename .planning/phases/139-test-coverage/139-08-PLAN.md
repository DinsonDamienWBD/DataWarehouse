---
phase: 139-test-coverage
plan: 08
type: execute
wave: 3
depends_on: [139-03, 139-04]
files_modified:
  - DataWarehouse.Hardening.Tests/Adversarial/InvalidInputTests.cs
  - DataWarehouse.Hardening.Tests/Adversarial/CorruptDataTests.cs
  - DataWarehouse.Hardening.Tests/Adversarial/BoundaryTests.cs
autonomous: true
requirements: [TST-04]

must_haves:
  truths:
    - "Invalid inputs (nulls, empty, oversized) are handled without crashes"
    - "Corrupt data (truncated, bit-flipped, wrong magic bytes) produces clear errors"
    - "Unicode edge cases (RTL, zero-width, surrogate pairs) handled safely"
    - "Integer overflow and boundary values handled correctly"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Adversarial/InvalidInputTests.cs"
      provides: "Null injection, empty input, oversized payload tests"
      min_lines: 200
    - path: "DataWarehouse.Hardening.Tests/Adversarial/CorruptDataTests.cs"
      provides: "Data corruption and malformed input tests"
      min_lines: 150
    - path: "DataWarehouse.Hardening.Tests/Adversarial/BoundaryTests.cs"
      provides: "Boundary value and edge case tests"
      min_lines: 150
  key_links:
    - from: "InvalidInputTests.cs"
      to: "DataWarehouse.SDK/StrategyBase"
      via: "Execute with adversarial inputs"
      pattern: "Assert\\.Throws|Should.*Throw"
---

<objective>
Write negative and adversarial tests: invalid inputs, corrupt data, oversized payloads, null injections, unicode edge cases, integer overflow, and concurrent access races.

Purpose: Production systems face hostile inputs. These tests prove the system fails gracefully (clear errors, no crashes, no data corruption) when given adversarial inputs.
Output: Adversarial test suite covering all major attack surfaces.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invalid input and null injection tests</name>
  <files>DataWarehouse.Hardening.Tests/Adversarial/InvalidInputTests.cs</files>
  <behavior>
    - Null arguments to public SDK/Kernel methods throw ArgumentNullException, not NullReferenceException
    - Empty strings/arrays handled gracefully (not silent corruption)
    - Oversized payloads (>1GB) rejected with clear error, no OOM
    - Whitespace-only strings treated as invalid where content is required
  </behavior>
  <action>
    1. Create `DataWarehouse.Hardening.Tests/Adversarial/` directory if not exists.
    2. Create `InvalidInputTests.cs` with test categories:
       - **Null injection**: Pass null to every public method that accepts reference types on StrategyBase, MessageBus, KnowledgeLake, PipelineOrchestrator. Assert `ArgumentNullException` (not `NullReferenceException`).
       - **Empty inputs**: Pass `Array.Empty<byte>()`, `string.Empty`, empty collections. Assert either valid empty result or clear validation error.
       - **Oversized payloads**: Create a 100MB+ byte array, pass to compression/encryption strategies. Assert either processing succeeds or rejects with meaningful error (not OOM crash).
       - **Type confusion**: Pass wrong generic types to typed APIs. Assert `InvalidCastException` or similar, not silent data loss.
    3. Use `[Theory]` with `[InlineData]` for parameterized null/empty tests.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~InvalidInput" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All invalid input tests pass. Null/empty/oversized inputs produce correct exceptions, never crashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corrupt data and boundary value tests</name>
  <files>DataWarehouse.Hardening.Tests/Adversarial/CorruptDataTests.cs, DataWarehouse.Hardening.Tests/Adversarial/BoundaryTests.cs</files>
  <behavior>
    - Truncated VDE superblock detected and reported
    - Bit-flipped encrypted data detected by integrity check
    - Wrong magic bytes rejected at parse time
    - Int32.MaxValue, Int64.MaxValue handled without overflow
    - Unicode: RTL markers, zero-width joiners, surrogate pairs, null bytes in strings
    - Concurrent access: two threads writing same key produce defined behavior
  </behavior>
  <action>
    1. Create `CorruptDataTests.cs`:
       - **Truncated data**: Take valid VDE superblock bytes, truncate to 50%, pass to parser. Assert parsing fails with clear error.
       - **Bit-flip**: Take valid encrypted payload, flip random bit, pass to decryptor. Assert integrity check catches it.
       - **Wrong magic bytes**: Replace first 4 bytes of VDE header with 0xDEADBEEF. Assert parser rejects with "invalid magic" error.
       - **Malformed JSON/XML**: Pass `{broken json` to config parsers. Assert parse error, not crash.
       - **Zero-byte injection**: Embed `\0` in file paths. Assert path validation catches it.
    2. Create `BoundaryTests.cs`:
       - **Integer boundaries**: Test with `int.MaxValue`, `long.MaxValue`, `0`, `-1` for size/count parameters.
       - **Unicode edge cases**: Strings with RTL markers (U+200F), zero-width joiners (U+200D), emoji (surrogate pairs), combining characters. Assert string handling preserves or safely normalizes.
       - **Concurrent writes**: Use `Task.WhenAll` to write same key from 10 threads simultaneously. Assert no data corruption (last-writer-wins or proper conflict detection).
       - **Boundary sizes**: Files of exactly 0, 1, block_size-1, block_size, block_size+1 bytes.
    3. Each test must complete in <5 seconds.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~CorruptData|FullyQualifiedName~Boundary" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>Corrupt data detected and rejected. Boundary values handled. Unicode edge cases safe. Concurrent access produces defined behavior.</done>
</task>

</tasks>

<verification>
- All adversarial tests pass
- No test causes process crash or OOM
- All exceptions are expected types (ArgumentNullException, etc.), never NullReferenceException or unhandled
</verification>

<success_criteria>
System handles all adversarial inputs gracefully: null injection, corrupt data, oversized payloads, unicode edge cases, integer overflow, and concurrent races. Clear error messages, no crashes, no data corruption.
</success_criteria>

<output>
After completion, create `.planning/phases/139-test-coverage/139-08-SUMMARY.md`
</output>
