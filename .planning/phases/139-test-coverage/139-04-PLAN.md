---
phase: 139-test-coverage
plan: 04
type: execute
wave: 2
depends_on: [139-01, 139-02]
files_modified:
  - DataWarehouse.Hardening.Tests/Plugin/StrategyContractComplianceTests.cs
  - DataWarehouse.Hardening.Tests/Plugin/StrategyContractGenerator.cs
autonomous: true
requirements: [TST-02]

must_haves:
  truths:
    - "All 3,036 strategies have contract compliance tests (instantiate, initialize, dispose)"
    - "Auto-generated tests use reflection to discover all strategy types"
    - "Every strategy can be constructed without throwing"
    - "Every strategy implements the required base class lifecycle"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Plugin/StrategyContractComplianceTests.cs"
      provides: "Auto-generated contract compliance tests for all strategies"
      min_lines: 200
    - path: "DataWarehouse.Hardening.Tests/Plugin/StrategyContractGenerator.cs"
      provides: "Reflection-based strategy discovery and test data provider"
      min_lines: 100
  key_links:
    - from: "StrategyContractGenerator.cs"
      to: "Plugins/DataWarehouse.Plugins.*"
      via: "Assembly.GetTypes() reflection"
      pattern: "GetTypes.*StrategyBase"
    - from: "StrategyContractComplianceTests.cs"
      to: "DataWarehouse.SDK/StrategyBase"
      via: "Initialize/Execute/Dispose lifecycle"
      pattern: "Initialize|Execute|Dispose"
---

<objective>
Auto-generate contract compliance tests for all 3,036 strategies across 52 plugins using reflection-based discovery.

Purpose: Ensures every strategy in the system meets the base class contract (can instantiate, initialize, dispose). This is the widest coverage net — catching broken constructors, missing dependencies, and lifecycle violations across the entire plugin ecosystem.
Output: Reflection-based test suite that discovers and validates all strategy types at runtime.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PLUGIN-CATALOG.md
@Metadata/plugin-strategy-map.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strategy discovery and contract compliance test generator</name>
  <files>DataWarehouse.Hardening.Tests/Plugin/StrategyContractGenerator.cs, DataWarehouse.Hardening.Tests/Plugin/StrategyContractComplianceTests.cs</files>
  <behavior>
    - Reflection discovers all types inheriting from StrategyBase across all 52 plugin assemblies
    - Discovery finds >= 3,000 strategy types
    - Each strategy can be instantiated (parameterless or with mock IServiceProvider)
    - Each strategy survives Initialize() call without unhandled exception
    - Each strategy survives Dispose() call without unhandled exception
  </behavior>
  <action>
    1. Read `Metadata/plugin-strategy-map.json` to understand the strategy class inventory (class name, base class, plugin, file path).
    2. Create `StrategyContractGenerator.cs`:
       - Static class that uses reflection to scan all loaded assemblies matching `DataWarehouse.Plugins.*`.
       - Method `GetAllStrategyTypes()` returns `IEnumerable<Type>` of all types descending from the SDK `StrategyBase`.
       - Method `GetStrategyTestData()` returns `IEnumerable<object[]>` for xUnit `[MemberData]` — each entry is `[Type strategyType, string pluginName]`.
       - Handle edge cases: abstract types (skip), generic open types (skip), types requiring constructor args (use Activator.CreateInstance with mock service provider).
    3. Create `StrategyContractComplianceTests.cs`:
       - `[Theory, MemberData(nameof(StrategyContractGenerator.GetStrategyTestData))]`
       - `CanInstantiate(Type strategyType, string pluginName)` — asserts `Activator.CreateInstance` succeeds or constructor with `IServiceProvider` succeeds.
       - `CanInitializeAndDispose(Type strategyType, string pluginName)` — creates instance, calls Initialize (with minimal mock context), calls Dispose. Asserts no unhandled exceptions.
       - `HasCorrectBaseClass(Type strategyType, string pluginName)` — asserts type inherits from `StrategyBase` (not just implements an interface directly, per AD-05).
       - `[Fact] DiscoveredAtLeast3000Strategies()` — asserts `GetAllStrategyTypes().Count() >= 3000`.
    4. The test project already references all 52 plugins (verified in csproj). Ensure all assemblies are loaded before reflection runs — use `Assembly.LoadFrom` if needed for any plugins not auto-loaded.
    5. For strategies that require constructor parameters beyond IServiceProvider, create a minimal mock service provider that returns null/default for unrecognized services. The goal is lifecycle compliance, not functional correctness (that comes in plan 05).
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~StrategyContractCompliance" --no-build 2>&1 | tail -10</automated>
  </verify>
  <done>3,036+ strategies discovered via reflection. All pass instantiation, initialization, and disposal contract tests. No strategy violates base class hierarchy (AD-05).</done>
</task>

</tasks>

<verification>
- `DiscoveredAtLeast3000Strategies` test passes
- All `CanInstantiate` theory cases pass
- All `CanInitializeAndDispose` theory cases pass
- All `HasCorrectBaseClass` theory cases pass
</verification>

<success_criteria>
Every strategy across all 52 plugins passes contract compliance (instantiate, initialize, dispose). Reflection-based discovery confirms >= 3,000 strategy types.
</success_criteria>

<output>
After completion, create `.planning/phases/139-test-coverage/139-04-SUMMARY.md`
</output>
