---
phase: 141-penetration-testing
plan: 04
type: execute
wave: 2
depends_on: [141-01, 141-02]
files_modified:
  - Metadata/pentest/supply-chain-report.md
  - Metadata/pentest/sbom.json
  - DataWarehouse.Hardening.Tests/Security/SupplyChainTests.cs
autonomous: true
requirements: [PEN-05]

must_haves:
  truths:
    - "No leaked secrets in source code (TruffleHog clean)"
    - "No vulnerable dependencies (Snyk clean or risks accepted)"
    - "SBOM (Software Bill of Materials) generated and verified"
    - "Binary reproducibility: same source produces same output"
  artifacts:
    - path: "Metadata/pentest/supply-chain-report.md"
      provides: "Supply chain security audit report"
    - path: "Metadata/pentest/sbom.json"
      provides: "Software Bill of Materials in CycloneDX format"
    - path: "DataWarehouse.Hardening.Tests/Security/SupplyChainTests.cs"
      provides: "Tests for supply chain security gates"
      min_lines: 80
  key_links:
    - from: "TruffleHog"
      to: "Full git history"
      via: "Secret scanning"
      pattern: "trufflehog|secret"
    - from: "Snyk"
      to: "NuGet packages"
      via: "Dependency vulnerability scan"
      pattern: "snyk|vulnerability"
---

<objective>
Supply chain security: TruffleHog secret scanning, Snyk dependency audit, SBOM generation and verification, binary reproducibility check.

Purpose: Supply chain attacks compromise dependencies or leak secrets. This plan ensures no secrets in source/history, no known vulnerable dependencies, a verified bill of materials, and reproducible builds.
Output: Supply chain report + SBOM + validation tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secret scanning and dependency audit</name>
  <files>DataWarehouse.Hardening.Tests/Security/SupplyChainTests.cs</files>
  <action>
    1. **TruffleHog secret scan**:
       - Install if needed: `pip install trufflehog` or download binary
       - Run: `trufflehog filesystem --directory . --only-verified` (current directory)
       - Run: `trufflehog git file://. --only-verified` (git history)
       - If TruffleHog not available, use `gitleaks detect --source .` as alternative
       - Any verified secrets found must be rotated immediately and removed from history
    2. **Snyk dependency audit**:
       - Install if needed: `npm install -g snyk` or download binary
       - Run: `snyk test --file=DataWarehouse.sln --all-projects`
       - If Snyk not available, use `dotnet list package --vulnerable --include-transitive`
       - For each vulnerability: assess severity, check if exploitable in our context, update package or accept risk with documentation
    3. Create `SupplyChainTests.cs`:
       - `[Fact] NoSecretsInSourceCode()` — scan common patterns (API keys, passwords, private keys) in *.cs files
       - `[Fact] NoVulnerableDependencies()` — run `dotnet list package --vulnerable` and assert zero high-severity results
       - `[Fact] AllPackagesHaveLockFiles()` — assert `packages.lock.json` exists for each project
       - `[Fact] NoDeprecatedPackages()` — check for packages marked deprecated
    4. Fix any issues found: rotate secrets, update vulnerable packages, regenerate lock files.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~SupplyChain" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>No secrets in source or history. No high-severity vulnerable dependencies. Supply chain tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: SBOM generation, binary reproducibility, and final report</name>
  <files>Metadata/pentest/sbom.json, Metadata/pentest/supply-chain-report.md</files>
  <action>
    1. **Generate SBOM** (CycloneDX format):
       - Install: `dotnet tool install --global CycloneDX`
       - Run: `dotnet CycloneDX DataWarehouse.sln -o Metadata/pentest/ -f json`
       - This produces `sbom.json` with all NuGet dependencies, versions, licenses, and hashes
    2. **Verify SBOM completeness**:
       - Parse `sbom.json`, assert it contains entries for all direct dependencies
       - Assert all entries have version and hash
       - Check for any dependency with no license (risk assessment)
    3. **Binary reproducibility**:
       - Build the solution twice from same source: `dotnet build -c Release -o build1/` then `dotnet build -c Release -o build2/`
       - Compare DLL hashes: `sha256sum build1/*.dll` vs `sha256sum build2/*.dll`
       - Note: .NET builds may embed timestamps. Use deterministic build settings if not already enabled:
         `<Deterministic>true</Deterministic>` in Directory.Build.props
       - Document any non-reproducible assemblies with explanation
    4. **Generate final report** `Metadata/pentest/supply-chain-report.md`:
       ```
       ## Supply Chain Security Report

       ### Secret Scanning
       | Tool | Scope | Findings | Status |
       |------|-------|----------|--------|
       | TruffleHog | Source + git history | 0 verified | CLEAN |

       ### Dependency Audit
       | Severity | Count | Remediated | Accepted | Status |
       |----------|-------|------------|----------|--------|
       | Critical | 0 | 0 | 0 | CLEAN |
       | High | 0 | 0 | 0 | CLEAN |

       ### SBOM
       - Format: CycloneDX JSON
       - Components: XXX direct + YYY transitive
       - Path: Metadata/pentest/sbom.json

       ### Binary Reproducibility
       | Assembly | Hash Match | Status |
       |----------|-----------|--------|
       | DataWarehouse.SDK.dll | YES | PASS |

       ### Overall Verdict: PASS
       ```
  </action>
  <verify>
    <automated>test -f Metadata/pentest/sbom.json && test -f Metadata/pentest/supply-chain-report.md && grep -c "PASS\|CLEAN" Metadata/pentest/supply-chain-report.md</automated>
  </verify>
  <done>SBOM generated in CycloneDX format. Binary reproducibility verified. Supply chain report shows CLEAN across all categories.</done>
</task>

</tasks>

<verification>
- TruffleHog: zero verified secrets
- Snyk: zero high-severity vulnerabilities (or accepted with documentation)
- SBOM exists with all dependencies
- Binary reproducibility confirmed (or deterministic build enabled)
- Final report with PASS verdict
</verification>

<success_criteria>
Supply chain secure: no leaked secrets, no vulnerable dependencies, SBOM generated and verified, builds are reproducible. Supply chain report confirms clean across all categories.
</success_criteria>

<output>
After completion, create `.planning/phases/141-penetration-testing/141-04-SUMMARY.md`
</output>
