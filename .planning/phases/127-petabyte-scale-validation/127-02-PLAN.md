---
phase: 127-petabyte-scale-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Scale/PbFederationTest.cs
  - DataWarehouse.Hardening.Tests/Scale/FederationTestHarness.cs
autonomous: true
requirements: [PER-05]
must_haves:
  truths:
    - "100 x 10TB federated VDEs form a 1PB logical namespace"
    - "Cross-shard queries complete with measured fan-out latency"
    - "Shard rebalancing under load works without data loss"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Scale/PbFederationTest.cs"
      provides: "PB-scale federation tests with cross-shard queries and rebalancing"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/Scale/FederationTestHarness.cs"
      provides: "Federation test setup with 100-shard topology"
      min_lines: 60
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Scale/PbFederationTest.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine/Federation"
      via: "Federation router and shard catalog"
      pattern: "FederationRouter|ShardCatalog|CrossShard"
---

<objective>
Create PB-scale federation test that validates 100-shard federated namespace with cross-shard operations.

Purpose: Prove DataWarehouse federation handles petabyte-scale distributed storage.
Output: Federation test that creates 100 x 10TB shards, performs cross-shard queries and rebalancing.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Federation Test Harness</name>
  <files>DataWarehouse.Hardening.Tests/Scale/FederationTestHarness.cs</files>
  <action>
Create FederationTestHarness.cs that:
- Creates a simulated federation topology of N shards (default 100) using in-memory VDE stubs
- Each shard has configurable logical size (default 10TB) with metadata tracking (inode count, extent count, used space)
- Uses SDK's federation router types (ShardCatalog, FederationRouter from DataWarehouse.SDK.VirtualDiskEngine.Federation)
- Provides methods: AddShard(), RemoveShard(), GetShardForKey(), WalkNamespace(), RebalanceShards()
- Simulates cross-shard query by routing to correct shard based on key hash
- Tracks query fan-out count (how many shards touched per query)
- SCALE_FACTOR support: at 0.001, uses 100 x 10MB instead of 100 x 10TB
- Uses lightweight shard stubs (not actual VDE files) for metadata-level testing
- Records latency for all operations using LatencyRecorder from TbStressTestHelpers
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>FederationTestHarness compiles with 100-shard topology, cross-shard routing, and rebalancing support</done>
</task>

<task type="auto">
  <name>Task 2: PB-Scale Federation Tests</name>
  <files>DataWarehouse.Hardening.Tests/Scale/PbFederationTest.cs</files>
  <action>
Create PbFederationTest.cs with NUnit tests:

[TestFixture, Category("Scale"), Category("LongRunning")]
- Test_PB_Federation_Setup: Creates 100 x 10TB federated namespace. Verifies global catalog has 100 shards, total logical size = 1PB.
- Test_CrossShard_Query_Latency: Performs 10,000 cross-shard queries. Measures fan-out latency. Reports: avg fan-out count, P50/P99 query latency. Assert P99 < 100ms for single-shard queries.
- Test_Global_Namespace_Walk: Walks entire global namespace (list all shards, count total inodes). Measures time to enumerate 1PB namespace metadata.
- Test_Shard_Rebalancing_Under_Load: While background queries run (100 queries/sec), trigger shard split (split 1 overloaded shard into 2). Verify: no queries fail during rebalance, data accessible from new shard after split, total namespace unchanged.
- Test_Shard_Failure_Recovery: Simulate 1 shard going offline. Verify: queries to remaining 99 shards succeed, queries to failed shard return graceful error (not crash), shard re-join restores full namespace.

All tests use [Explicit] attribute and [Timeout(3600000)] (1 hour). Use FederationTestHarness for setup.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>PbFederationTest compiles with 5 tests covering cross-shard queries, namespace walk, rebalancing, and failure recovery</done>
</task>

</tasks>

<verification>
dotnet build DataWarehouse.Hardening.Tests succeeds.
PbFederationTest.cs has 5 test methods with [Category("Scale")] and [Explicit] attributes.
FederationTestHarness supports configurable shard count and SCALE_FACTOR.
</verification>

<success_criteria>
- Federation harness creates 100-shard topology with cross-shard routing
- Cross-shard query latency measured with P99 assertion
- Shard rebalancing tested under concurrent query load
- Shard failure recovery tested with graceful degradation
- SCALE_FACTOR support for CI
</success_criteria>

<output>
After completion, create `.planning/phases/127-petabyte-scale-validation/127-02-SUMMARY.md`
</output>
