---
phase: 127-petabyte-scale-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Scale/TbStressTest.cs
  - DataWarehouse.Hardening.Tests/Scale/TbStressTestHelpers.cs
autonomous: true
requirements: [PER-05]
must_haves:
  truths:
    - "1TB VDE can be created and populated with 1 billion 1KB + 1 million 1MB files"
    - "Random 4KB reads at 1TB scale achieve less than 10ms P99 latency"
    - "Allocation group utilization, inode table performance, and extent tree depth are measured"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Scale/TbStressTest.cs"
      provides: "TB-scale stress test with 1TB VDE creation and population"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/Scale/TbStressTestHelpers.cs"
      provides: "Helper utilities for large-scale data generation"
      min_lines: 40
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Scale/TbStressTest.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine"
      via: "VDE creation and block I/O"
      pattern: "VirtualDiskEngine|IBlockDevice|AllocationGroup"
---

<objective>
Create TB-scale stress test that validates VDE performance at 1TB with billions of files.

Purpose: Prove DataWarehouse handles TB-scale workloads with acceptable latency.
Output: Stress test that creates 1TB VDE, writes 1B+1M files, measures P99 random read latency.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TB-Scale Stress Test Infrastructure</name>
  <files>DataWarehouse.Hardening.Tests/Scale/TbStressTestHelpers.cs</files>
  <action>
Create TbStressTestHelpers.cs with utilities for TB-scale testing:
- DataGenerator: generates deterministic random byte arrays of specified size using seeded Random (reproducible)
- VdePopulator: bulk-writes files to VDE using batched async I/O (configurable batch size, default 10,000)
- LatencyRecorder: records operation latencies, computes P50/P90/P99/P999 using sorted array percentile method
- AllocationGroupMonitor: tracks allocation group utilization (used/free blocks per group)
- ExtentTreeDepthChecker: measures maximum extent tree depth after population
- ProgressReporter: logs progress every N operations (default every 100,000)
- All helpers designed for memory efficiency: reuse byte[] buffers via ArrayPool, never hold all data in memory
- Use [Category("Scale")] attribute convention so these tests can be filtered in CI
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>TbStressTestHelpers compiles with DataGenerator, VdePopulator, LatencyRecorder, and monitoring utilities</done>
</task>

<task type="auto">
  <name>Task 2: 1TB VDE Stress Test</name>
  <files>DataWarehouse.Hardening.Tests/Scale/TbStressTest.cs</files>
  <action>
Create TbStressTest.cs with NUnit tests:

[TestFixture, Category("Scale"), Category("LongRunning")]
- Test_1TB_VDE_Creation: Creates 1TB VDE on FileBlockDevice, verifies superblock valid
- Test_BillionSmallFiles: Writes 1 billion 1KB files to 1TB VDE (batched, progress every 1M files). Measures: total time, files/sec throughput, allocation group fill ratio. Uses VdePopulator with 10K batch size.
- Test_MillionMediumFiles: Writes 1 million 1MB files. Measures: MB/s throughput, extent fragmentation.
- Test_Random4KB_P99_Under10ms: After population, performs 100,000 random 4KB reads at random offsets. Uses LatencyRecorder. Asserts P99 < 10ms. Reports P50/P90/P99/P999.
- Test_AllocationGroupUtilization: Reports per-group utilization using AllocationGroupMonitor. Asserts no group >95% while others <50% (balanced distribution).
- Test_ExtentTreeDepth: Measures max extent tree depth. Reports depth. Warns if depth > 10 (performance cliff).

All tests use [Explicit] attribute (only run when explicitly requested, not in normal CI). Add [Timeout(7200000)] (2 hours) for long-running tests. Use try/finally to clean up 1TB temp files.

Tests designed so they CAN run on smaller scale (env var SCALE_FACTOR defaults to 1.0, set to 0.001 for CI = 1GB instead of 1TB).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>TbStressTest compiles with all 6 tests, supports SCALE_FACTOR for CI, measures P99 latency</done>
</task>

</tasks>

<verification>
dotnet build DataWarehouse.Hardening.Tests succeeds.
TbStressTest.cs has 6 test methods with [Category("Scale")] and [Explicit] attributes.
SCALE_FACTOR env var support allows CI to run at reduced scale.
</verification>

<success_criteria>
- 1TB VDE creation test exists with FileBlockDevice
- 1B x 1KB file write test measures throughput and allocation group balance
- Random 4KB read P99 assertion < 10ms after population
- SCALE_FACTOR support for CI (0.001 = 1GB scale)
- All tests use [Explicit] to avoid accidental CI runs
</success_criteria>

<output>
After completion, create `.planning/phases/127-petabyte-scale-validation/127-01-SUMMARY.md`
</output>
