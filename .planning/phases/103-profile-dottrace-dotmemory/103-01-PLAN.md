---
phase: 103-profile-dottrace-dotmemory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Profiling/DotTracePerformanceTests.cs
  - Metadata/profile-hardening-report/dottrace-performance-report.md
autonomous: true
requirements: [PROF-01]

must_haves:
  truths:
    - "dotTrace profiling completes on all xUnit hardening tests (excluding Coyote tests)"
    - "No new hot-path lock contention introduced by hardening fixes"
    - "Performance profile report exported with quantified findings"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Profiling/DotTracePerformanceTests.cs"
      provides: "Performance profiling test class using dotTrace CLI"
      min_lines: 50
    - path: "Metadata/profile-hardening-report/dottrace-performance-report.md"
      provides: "Stage 1 - Step 4 - dotTrace Performance Profile report"
      contains: "Lock Contention"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Profiling/DotTracePerformanceTests.cs"
      to: "DataWarehouse.Hardening.Tests/**Tests.cs"
      via: "profiling xUnit test execution"
      pattern: "dotTrace.*profile"
---

<objective>
Run dotTrace performance profiling on all xUnit hardening tests to verify hardening fixes did not introduce lock contention, context-switching delays, or hot-path regressions.

Purpose: PROF-01 requires proof that no new hot-path lock contention was introduced by hardening fixes.
Output: dotTrace profiling test class + performance profile report in Metadata/profile-hardening-report/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dotTrace performance profiling test and analysis</name>
  <files>DataWarehouse.Hardening.Tests/Profiling/DotTracePerformanceTests.cs, DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj</files>
  <action>
1. Add `JetBrains.dotTrace.CommandLineTools` NuGet package to the hardening test project (if not already present; the project already has JetBrains.dotMemory.Console.windows-x64).

2. Create `DataWarehouse.Hardening.Tests/Profiling/DotTracePerformanceTests.cs`:
   - A test class that uses `JetBrains.Profiler.Api` (or CLI invocation via `Process.Start`) to run dotTrace timeline profiling on the standard xUnit test assembly.
   - IMPORTANT: Exclude any test class with "Coyote" in its name — Coyote tests add intentional scheduler overhead that pollutes profiling data.
   - Profile the test run capturing: method-level CPU time, thread contention events, context switches.
   - After profiling, parse the snapshot for:
     a. Lock contention hotspots (Monitor.Enter, SpinLock, SemaphoreSlim waits > 1ms)
     b. Methods consuming > 5% of total CPU time
     c. Thread context switches per second
   - Assert: no lock contention event exceeds baseline threshold (define a reasonable threshold, e.g., 10ms cumulative contention per method).
   - If dotTrace CLI is not available in the test environment, fall back to using `System.Diagnostics` event counters for contention monitoring:
     - Use `EventListener` to capture `System.Threading` contention events
     - Use `Stopwatch` for hot-path timing
     - Report contention count and duration per critical code path

3. The test must be runnable via `dotnet test --filter "FullyQualifiedName~DotTracePerformanceTests"`.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse &amp;&amp; dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&amp;1 | tail -5</automated>
  </verify>
  <done>DotTracePerformanceTests.cs compiles. Test class profiles xUnit tests (excluding Coyote), checks for lock contention hotspots, and asserts no regression.</done>
</task>

<task type="auto">
  <name>Task 2: Run profiling and generate dotTrace performance report</name>
  <files>Metadata/profile-hardening-report/dottrace-performance-report.md</files>
  <action>
1. Create directory `Metadata/profile-hardening-report/` if not exists.

2. Run the dotTrace performance tests:
   ```
   dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~DotTracePerformanceTests" --logger "console;verbosity=detailed"
   ```

3. Capture test output and generate `Metadata/profile-hardening-report/dottrace-performance-report.md` with format:
   ```markdown
   # Stage 1 - Step 4 - dotTrace Performance Profile

   ## Summary
   - Date: {date}
   - Test assembly: DataWarehouse.Hardening.Tests
   - Tests profiled: {count} (Coyote tests excluded)
   - Result: PASS / FAIL

   ## Lock Contention Analysis
   | Method | Contention Count | Total Wait (ms) | Status |
   |--------|-----------------|------------------|--------|
   | ... | ... | ... | OK/WARN/FAIL |

   ## Hot Path Analysis
   | Method | CPU % | Category |
   |--------|-------|----------|
   | ... | ... | Expected/Unexpected |

   ## Context Switch Analysis
   - Switches/sec: {value}
   - Assessment: {within bounds / concerning}

   ## Conclusion
   {Summary of whether PROF-01 is satisfied}
   ```

4. If any tests fail (lock contention detected), document the specific methods and contention patterns in the report but do NOT mark the overall phase as blocked — the report is informational for subsequent remediation.
  </action>
  <verify>
    <automated>test -f "C:/Temp/DataWarehouse/DataWarehouse/Metadata/profile-hardening-report/dottrace-performance-report.md" &amp;&amp; echo "Report exists" || echo "MISSING"</automated>
  </verify>
  <done>dotTrace performance report exists at Metadata/profile-hardening-report/dottrace-performance-report.md with lock contention analysis, hot path analysis, and PROF-01 assessment.</done>
</task>

</tasks>

<verification>
- DotTracePerformanceTests.cs compiles and is discoverable by test runner
- Performance report generated with structured lock contention and hot-path data
- PROF-01 assessment clearly stated in report conclusion
</verification>

<success_criteria>
- dotTrace profiling ran on all non-Coyote hardening tests
- Lock contention analysis complete with per-method breakdown
- Report exported to Metadata/profile-hardening-report/dottrace-performance-report.md
- No new hot-path lock contention exceeding threshold
</success_criteria>

<output>
After completion, create `.planning/phases/103-profile-dottrace-dotmemory/103-01-SUMMARY.md`
</output>
