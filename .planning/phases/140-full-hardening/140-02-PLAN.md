---
phase: 140-full-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Profiling/DotTraceProfileTests.cs
  - Metadata/profiling/dottrace-hotspot-report.md
autonomous: true
requirements: [TST-07]

must_haves:
  truths:
    - "No single method consumes >5% of total CPU in any benchmark"
    - "Call tree analysis identifies all hot paths"
    - "Hot spot remediation applied where thresholds exceeded"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Profiling/DotTraceProfileTests.cs"
      provides: "dotTrace profiling tests for critical paths"
      min_lines: 100
    - path: "Metadata/profiling/dottrace-hotspot-report.md"
      provides: "Hot spot analysis report with remediation"
  key_links:
    - from: "DotTraceProfileTests.cs"
      to: "Critical v8.0 code paths"
      via: "JetBrains.Profiler.Api"
      pattern: "MeasureProfiler|PerformanceProfiler"
---

<objective>
Profile all critical v8.0 code paths with dotTrace. Identify hot spots where any single method exceeds 5% of total CPU. Remediate and document.

Purpose: Performance profiling ensures no single method becomes a bottleneck. The 5% CPU threshold prevents concentrated hot spots that degrade system throughput.
Output: dotTrace profiling tests + hot spot report with remediation status.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dotTrace profiling tests for critical paths</name>
  <files>DataWarehouse.Hardening.Tests/Profiling/DotTraceProfileTests.cs</files>
  <action>
    1. Check existing profiling tests in `DataWarehouse.Hardening.Tests/Profiling/`. Extend, don't duplicate.
    2. Create/extend `DotTraceProfileTests.cs` with profiled benchmarks for:
       - **VDE I/O path**: Write 1MB data through full decorator chain (Cache-Integrity-Compression-Dedup-Encryption-WAL-RAID-File). Profile CPU per method.
       - **Message bus throughput**: Publish 10,000 messages, measure CPU distribution across bus internals.
       - **Plugin loading**: Load all 52 plugins, measure CPU per phase (discovery, assembly load, registration).
       - **Pipeline orchestration**: Execute 5-stage pipeline with 1,000 items, profile per-stage CPU.
       - **Encryption hot path**: AES-256-GCM encrypt/decrypt 10MB, profile crypto operations.
       - **Compression hot path**: LZ4/Zstd compress/decompress 10MB, profile codec CPU.
    3. Use `JetBrains.Profiler.Api` if available, or run via `dotnet-trace` CLI:
       ```
       dotnet trace collect --providers Microsoft-DotNETCore-SampleProfiler -- dotnet test --filter "Category=Profile"
       ```
    4. Each test should annotate expected hot methods and assert no method > 5% via call tree analysis.
    5. If JetBrains.Profiler.Api not in csproj, add: `<PackageReference Include="JetBrains.Profiler.Api" Version="*" />`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~DotTraceProfile" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All critical paths profiled. No method exceeds 5% CPU threshold.</done>
</task>

<task type="auto">
  <name>Task 2: Hot spot analysis report and remediation</name>
  <files>Metadata/profiling/dottrace-hotspot-report.md</files>
  <action>
    1. Run profiling tests from Task 1 and collect CPU distribution data.
    2. Generate `Metadata/profiling/dottrace-hotspot-report.md`:
       - Table of all methods consuming >2% CPU (even below threshold, for visibility):
         ```
         | Method | CPU % | Subsystem | Status |
         |--------|-------|-----------|--------|
         | VdeCache.Lookup | 3.2% | VDE | OK (below 5%) |
         ```
       - Call tree summary for each profiled benchmark.
       - Any methods that exceeded 5% during initial run: document the issue, the fix applied, and post-fix CPU %.
    3. If any method exceeds 5%, apply remediation:
       - Caching for repeated computations
       - Algorithm optimization (O(n^2) -> O(n log n))
       - Buffer pooling for allocation-heavy methods
       - Inlining for small hot methods
       Then re-profile to confirm fix.
    4. Final report should show all methods below 5% with PASS verdict.
  </action>
  <verify>
    <automated>test -f Metadata/profiling/dottrace-hotspot-report.md && grep -c "PASS\|OK" Metadata/profiling/dottrace-hotspot-report.md</automated>
  </verify>
  <done>Hot spot report generated. All methods below 5% CPU threshold. Any initial violations remediated and re-profiled.</done>
</task>

</tasks>

<verification>
- All profiling tests pass
- No method exceeds 5% CPU in any benchmark
- Hot spot report documents all methods >2% with status
</verification>

<success_criteria>
dotTrace confirms no single method exceeds 5% of total CPU for any benchmark. Call tree analysis complete. Hot spot report documents all findings with PASS verdicts.
</success_criteria>

<output>
After completion, create `.planning/phases/140-full-hardening/140-02-SUMMARY.md`
</output>
