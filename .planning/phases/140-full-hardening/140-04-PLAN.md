---
phase: 140-full-hardening
plan: 04
type: execute
wave: 2
depends_on: [140-01, 140-02]
files_modified:
  - DataWarehouse.Hardening.Tests/Mutation/StrykerConfiguration.cs
  - Metadata/mutation/stryker-final-report.md
  - stryker-config.json
autonomous: true
requirements: [TST-08]

must_haves:
  truths:
    - "Stryker mutation score >=90% across the full solution"
    - "Per-project mutation score >=85%"
    - "Surviving mutants identified and killed with targeted assertions"
    - "Mutation report documents all survivors and remediation"
  artifacts:
    - path: "stryker-config.json"
      provides: "Stryker configuration for full solution"
      min_lines: 20
    - path: "Metadata/mutation/stryker-final-report.md"
      provides: "Mutation testing final report"
    - path: "DataWarehouse.Hardening.Tests/Mutation/StrykerConfiguration.cs"
      provides: "Test validating Stryker configuration and scores"
      min_lines: 50
  key_links:
    - from: "stryker-config.json"
      to: "DataWarehouse.sln"
      via: "dotnet stryker CLI"
      pattern: "dotnet.*stryker"
---

<objective>
Run Stryker mutation testing on all v8.0 code. Achieve >=90% mutation score overall, >=85% per-project. Kill surviving mutants with targeted assertions.

Purpose: Mutation testing measures test QUALITY, not just coverage. A mutant that survives means tests do not detect a code change — indicating weak assertions or missing test cases.
Output: Stryker config + mutation score report + targeted tests to kill survivors.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure and run Stryker mutation testing</name>
  <files>stryker-config.json, DataWarehouse.Hardening.Tests/Mutation/StrykerConfiguration.cs</files>
  <action>
    1. Check if `stryker-config.json` already exists at solution root. If so, read and extend. If not, create it.
    2. Create/update `stryker-config.json`:
       ```json
       {
         "stryker-config": {
           "project": "DataWarehouse.sln",
           "test-projects": [
             "DataWarehouse.Tests/DataWarehouse.Tests.csproj",
             "DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj"
           ],
           "threshold-high": 95,
           "threshold-low": 90,
           "threshold-break": 85,
           "reporters": ["html", "json", "progress"],
           "mutate": [
             "DataWarehouse.SDK/**/*.cs",
             "DataWarehouse.Kernel/**/*.cs",
             "Plugins/**/*.cs",
             "!**/*Test*.cs",
             "!**/obj/**",
             "!**/bin/**"
           ],
           "concurrency": 4,
           "language-version": "preview"
         }
       }
       ```
    3. Create `StrykerConfiguration.cs`:
       - `[Fact] StrykerConfigExists()` — asserts `stryker-config.json` exists and is valid JSON.
       - `[Fact] StrykerThresholdsConfigured()` — asserts threshold-break >= 85.
    4. Run initial Stryker: `dotnet stryker --config-file stryker-config.json`.
       This may take significant time (hours for 3,036 strategies). For initial run, consider limiting scope to SDK + Kernel first, then plugins in batches.
    5. If Stryker not installed: `dotnet tool install --global dotnet-stryker`.
  </action>
  <verify>
    <automated>test -f stryker-config.json && dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~StrykerConfiguration" --no-build 2>&1 | tail -3</automated>
  </verify>
  <done>Stryker configured with thresholds. Initial mutation run completes for SDK and Kernel.</done>
</task>

<task type="auto">
  <name>Task 2: Kill surviving mutants and generate final report</name>
  <files>Metadata/mutation/stryker-final-report.md</files>
  <action>
    1. After Stryker run from Task 1, parse the JSON report to identify surviving mutants.
    2. For each surviving mutant:
       - Identify the mutation (e.g., `if (x > 0)` mutated to `if (x >= 0)` and tests still pass).
       - Add targeted assertion to existing test or create new test that specifically catches the mutation.
       - Example: if `>` vs `>=` survives, add a test with the boundary value.
    3. Group survivors by project and priority:
       - SDK/Kernel survivors: HIGH priority (fix immediately)
       - Plugin survivors: MEDIUM priority (fix top 20 by impact)
       - Companion app survivors: LOW priority (document for future)
    4. Re-run Stryker after adding targeted tests to confirm survivors are killed.
    5. Generate `Metadata/mutation/stryker-final-report.md`:
       ```
       ## Stryker Mutation Testing Report

       | Project | Mutants | Killed | Survived | Score | Status |
       |---------|---------|--------|----------|-------|--------|
       | SDK | 500 | 475 | 25 | 95% | PASS |

       ### Survivors Remediated
       - SDK: 25 survivors → 23 killed by targeted assertions, 2 equivalent mutants

       ### Final Score: XX% (target: >=90%)
       ```
    6. Equivalent mutants (mutations that don't change behavior) should be documented but excluded from score calculation.
  </action>
  <verify>
    <automated>test -f Metadata/mutation/stryker-final-report.md && grep -c "Score\|PASS" Metadata/mutation/stryker-final-report.md</automated>
  </verify>
  <done>Stryker score >=90% overall, >=85% per-project. All non-equivalent surviving mutants killed. Final report generated.</done>
</task>

</tasks>

<verification>
- Stryker mutation score >= 90% overall
- Per-project score >= 85%
- No surviving mutants unaddressed (killed or documented as equivalent)
- Final report with per-project breakdown
</verification>

<success_criteria>
Stryker mutation testing achieves >=90% mutation score across solution, >=85% per-project. Surviving mutants killed with targeted assertions. Equivalent mutants documented. Report confirms quality of test assertions, not just coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/140-full-hardening/140-04-SUMMARY.md`
</output>
