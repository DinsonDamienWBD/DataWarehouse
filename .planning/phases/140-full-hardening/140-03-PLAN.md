---
phase: 140-full-hardening
plan: 03
type: execute
wave: 2
depends_on: [140-01, 140-02]
files_modified:
  - DataWarehouse.Hardening.Tests/Profiling/DotMemoryProfileTests.cs
  - Metadata/profiling/dotmemory-leak-report.md
autonomous: true
requirements: [TST-07]

must_haves:
  truths:
    - "No memory leaks during 1-hour sustained operation"
    - "LOH allocations minimized (no unnecessary large object allocations)"
    - "GC Gen2 collections <1/minute under normal load"
    - "Working set plateaus (no unbounded growth)"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Profiling/DotMemoryProfileTests.cs"
      provides: "dotMemory profiling tests for leak detection"
      min_lines: 150
    - path: "Metadata/profiling/dotmemory-leak-report.md"
      provides: "Memory leak analysis report"
  key_links:
    - from: "DotMemoryProfileTests.cs"
      to: "JetBrains.dotMemory.Console"
      via: "Heap snapshot analysis"
      pattern: "dotMemory|MemoryProfiler"
---

<objective>
Profile memory with dotMemory: heap snapshot analysis, 1-hour soak test for leak detection, LOH minimization, GC Gen2 rate verification.

Purpose: Memory leaks in long-running systems cause OOM crashes. This plan verifies the system maintains stable memory under sustained load with no leaks and minimal GC pressure.
Output: dotMemory profiling tests + memory leak analysis report.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dotMemory profiling tests for leak detection and GC analysis</name>
  <files>DataWarehouse.Hardening.Tests/Profiling/DotMemoryProfileTests.cs</files>
  <action>
    1. JetBrains.dotMemory.Console is already in the test csproj. Check existing profiling tests in `DataWarehouse.Hardening.Tests/Profiling/` and `DataWarehouse.Hardening.Tests/Soak/`.
    2. Create/extend `DotMemoryProfileTests.cs`:
       - **Leak detection (short soak)**: Run 10 minutes of continuous operations (message publish, VDE read/write, plugin reload). Take heap snapshots at 1min, 5min, 10min. Assert no growing object count for any single type.
       - **LOH analysis**: Profile a VDE write workload. Count LOH allocations. Assert no byte[] allocations >85KB unless absolutely necessary (VDE block operations). Document any LOH allocations with justification.
       - **Gen2 collection rate**: Monitor GC events during 5 minutes of load. Assert Gen2 collections < 1 per minute. Use existing `GcEventMonitor.cs` from Soak/ if available.
       - **Working set plateau**: Run workload for 10 minutes. Assert working set at minute 10 is within 110% of minute 5 (plateaued, not growing).
    3. Use `dotMemory` CLI for heap snapshots:
       ```
       dotnet-dotMemory start --target-executable=dotnet --target-arguments="test DataWarehouse.Hardening.Tests --filter Category=Memory"
       ```
       Or use `JetBrains.dotMemory.Unit` API if available.
    4. For the 1-hour full soak (mentioned in requirements), extend the short soak to 60 minutes but with a `[Trait("Category", "LongRunning")]` tag so CI can run it separately.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~DotMemoryProfile" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>Memory profiling tests pass. No leaks detected in short soak. LOH allocations justified. Gen2 < 1/min. Working set plateaus.</done>
</task>

<task type="auto">
  <name>Task 2: Memory leak analysis report and remediation</name>
  <files>Metadata/profiling/dotmemory-leak-report.md</files>
  <action>
    1. Run memory profiling tests from Task 1 and collect results.
    2. Generate `Metadata/profiling/dotmemory-leak-report.md`:
       - **Heap snapshot comparison** (minute 1 vs minute 10):
         ```
         | Type | Count@1min | Count@10min | Delta | Status |
         |------|-----------|------------|-------|--------|
         | byte[] | 1,234 | 1,240 | +6 | OK (stable) |
         ```
       - **LOH allocation inventory**: List all types allocated on LOH, size, frequency, justification.
       - **GC statistics**: Gen0/Gen1/Gen2 counts per minute, average pause time.
       - **Working set timeline**: Memory at 1, 2, 5, 10 minutes.
       - **Leak candidates**: Any type growing >10% between snapshots.
    3. If leaks found:
       - Identify root cause (event handler not unsubscribed, collection not cleared, stream not disposed).
       - Apply fix in production code.
       - Re-run profiling to confirm leak is closed.
       - Document fix in report.
    4. Final verdict: PASS (no leaks, Gen2 <1/min, working set plateau) or FAIL (with remediation plan).
  </action>
  <verify>
    <automated>test -f Metadata/profiling/dotmemory-leak-report.md && grep -c "PASS\|OK\|stable" Metadata/profiling/dotmemory-leak-report.md</automated>
  </verify>
  <done>Memory leak report generated. No leaks detected. LOH minimized. Gen2 < 1/min. Working set plateaus within 10 minutes.</done>
</task>

</tasks>

<verification>
- Memory profiling tests pass
- No memory leaks in 10-minute soak
- Gen2 collections < 1/minute
- LOH allocations documented and justified
- Working set plateaus
</verification>

<success_criteria>
dotMemory confirms no memory leaks during sustained operation. LOH allocations minimized. GC Gen2 collections under 1/minute. Working set plateaus within 10 minutes of operation.
</success_criteria>

<output>
After completion, create `.planning/phases/140-full-hardening/140-03-SUMMARY.md`
</output>
