---
phase: 140-full-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Concurrency/CoyoteSpdkTests.cs
  - DataWarehouse.Hardening.Tests/Concurrency/CoyoteGroupCommitTests.cs
  - DataWarehouse.Hardening.Tests/Concurrency/CoyoteIpcRdmaTests.cs
  - DataWarehouse.Hardening.Tests/Concurrency/CoyoteClusterTests.cs
autonomous: true
requirements: [TST-06]

must_haves:
  truths:
    - "All concurrent code paths tested with Coyote for 10,000 iterations"
    - "No deadlocks detected across SPDK I/O, group commit, IPC, RDMA, cluster"
    - "No race conditions that corrupt shared state"
    - "No livelocks where threads spin without progress"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Concurrency/CoyoteSpdkTests.cs"
      provides: "SPDK I/O concurrency tests"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/Concurrency/CoyoteGroupCommitTests.cs"
      provides: "WAL group commit concurrency tests"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/Concurrency/CoyoteIpcRdmaTests.cs"
      provides: "Multi-process IPC and RDMA concurrency tests"
      min_lines: 100
    - path: "DataWarehouse.Hardening.Tests/Concurrency/CoyoteClusterTests.cs"
      provides: "Cluster bootstrapping concurrency tests"
      min_lines: 100
  key_links:
    - from: "CoyoteSpdkTests.cs"
      to: "SPDK block device implementation"
      via: "Coyote TestingEngine.Create()"
      pattern: "TestingEngine\\.Create|CoyoteTest"
---

<objective>
Run Coyote concurrency testing on all v8.0 concurrent code paths: SPDK I/O, WAL group commit, multi-process IPC, RDMA transport, and cluster bootstrapping. 10,000 iterations per test.

Purpose: Coyote systematically explores thread interleavings to find deadlocks, race conditions, and livelocks that random testing would miss. 10,000 iterations provides high confidence in concurrent correctness.
Output: Coyote test suite covering all concurrent subsystems with 10,000-iteration passing runs.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coyote tests for SPDK I/O and WAL group commit</name>
  <files>DataWarehouse.Hardening.Tests/Concurrency/CoyoteSpdkTests.cs, DataWarehouse.Hardening.Tests/Concurrency/CoyoteGroupCommitTests.cs</files>
  <action>
    1. Microsoft.Coyote.Test 1.7.11 is already in the test csproj.
    2. Check existing concurrency tests in `DataWarehouse.Hardening.Tests/` (ConcurrencyHardeningTests.cs exists). Extend with new v8.0 code paths.
    3. Create `CoyoteSpdkTests.cs`:
       - Test concurrent queue-per-thread I/O submissions (4 threads submitting to same SPDK device).
       - Test zero-copy DMA buffer lifecycle under concurrent access.
       - Test vfio-pci device binding/unbinding during active I/O.
       - Use `[Fact] [Trait("Category", "Coyote")]` and configure `Configuration.Create().WithTestingIterations(10000)`.
    4. Create `CoyoteGroupCommitTests.cs`:
       - Test concurrent WAL writes being batched into group commit.
       - Test group commit flush under concurrent writers (10 writers, verify all committed or none).
       - Test crash during group commit (torn write detection).
       - Test dual-ring WAL (Ring A crash + Ring B replication) concurrent access.
    5. Each test should use Coyote's `TestingEngine` to run 10,000 iterations exploring different schedules.
    6. If tests find bugs, fix the underlying concurrent code and re-run until 10,000 iterations pass clean.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~CoyoteSpdk|FullyQualifiedName~CoyoteGroupCommit" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>SPDK I/O and WAL group commit pass 10,000 Coyote iterations each. No deadlocks, races, or livelocks.</done>
</task>

<task type="auto">
  <name>Task 2: Coyote tests for IPC, RDMA, and cluster bootstrapping</name>
  <files>DataWarehouse.Hardening.Tests/Concurrency/CoyoteIpcRdmaTests.cs, DataWarehouse.Hardening.Tests/Concurrency/CoyoteClusterTests.cs</files>
  <action>
    1. Create `CoyoteIpcRdmaTests.cs`:
       - Test multi-process shared-nothing architecture: process A and process B accessing disjoint data partitions.
       - Test RDMA transport handshake under concurrent connection attempts.
       - Test TCP fallback when RDMA is unavailable during active transfer.
       - Test message ordering guarantees across transport modes.
       - 10,000 iterations per test.
    2. Create `CoyoteClusterTests.cs`:
       - Test cluster bootstrap: 3 nodes joining simultaneously.
       - Test split-brain protection: network partition between 2 nodes during leader election.
       - Test automatic failover: leader dies, follower promotes, client reconnects.
       - Test node rejoin after partition heals.
       - 10,000 iterations per test.
    3. Use Coyote's controlled concurrency primitives (ControlledTask, etc.) for deterministic exploration.
    4. Fix any bugs found and re-run until clean.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~CoyoteIpcRdma|FullyQualifiedName~CoyoteCluster" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>IPC, RDMA, and cluster bootstrapping pass 10,000 Coyote iterations. All concurrent code paths verified deadlock-free and race-free.</done>
</task>

</tasks>

<verification>
- All Coyote tests pass with 10,000 iterations
- No deadlocks, livelocks, or race conditions reported
- Coverage of SPDK, group commit, IPC, RDMA, and cluster subsystems
</verification>

<success_criteria>
All v8.0 concurrent code paths pass 10,000 Coyote iterations without deadlocks, race conditions, or livelocks. Concurrent correctness verified for SPDK I/O, WAL group commit, multi-process IPC, RDMA transport, and cluster bootstrapping.
</success_criteria>

<output>
After completion, create `.planning/phases/140-full-hardening/140-01-SUMMARY.md`
</output>
