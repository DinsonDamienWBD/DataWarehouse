---
phase: 121-secure-erase
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Security/SecureErase/SecureDisposeExtensions.cs
  - DataWarehouse.SDK/Security/SecureErase/DisposeAuditReport.cs
  - DataWarehouse.Hardening.Tests/Security/SecureErase/SecureDisposeTests.cs
autonomous: true
requirements: [SEC-04]

must_haves:
  truths:
    - "SecureDispose extension method zeroes internal buffers, caches, and state"
    - "All IAsyncDisposable implementations audited for buffer zeroization"
    - "DisposeAuditReport identifies classes with unprotected dispose patterns"
    - "DisposeAsync hardening covers all SDK types with internal byte buffers"
  artifacts:
    - path: "DataWarehouse.SDK/Security/SecureErase/SecureDisposeExtensions.cs"
      provides: "SecureDispose extension methods for common dispose patterns"
      exports: ["SecureDisposeExtensions"]
    - path: "DataWarehouse.SDK/Security/SecureErase/DisposeAuditReport.cs"
      provides: "Audit report for IAsyncDisposable implementations"
      exports: ["DisposeAuditReport", "DisposeAuditEntry"]
    - path: "DataWarehouse.Hardening.Tests/Security/SecureErase/SecureDisposeTests.cs"
      provides: "Tests for secure dispose extensions"
  key_links:
    - from: "DataWarehouse.SDK/Security/SecureErase/SecureDisposeExtensions.cs"
      to: "System.Security.Cryptography.CryptographicOperations"
      via: "CryptographicOperations.ZeroMemory for buffer zeroization"
      pattern: "CryptographicOperations\\.ZeroMemory"
---

<objective>
Create SecureDispose extension methods and audit IAsyncDisposable implementations for buffer zeroization gaps.

Purpose: DisposeAsync implementations must zero internal buffers, caches, and state to prevent sensitive data lingering in managed memory after disposal.
Output: SecureDisposeExtensions, DisposeAuditReport, and tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Security/SecureErase/IMemoryProtector.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SecureDispose extensions and dispose audit report</name>
  <files>DataWarehouse.SDK/Security/SecureErase/SecureDisposeExtensions.cs, DataWarehouse.SDK/Security/SecureErase/DisposeAuditReport.cs, DataWarehouse.Hardening.Tests/Security/SecureErase/SecureDisposeTests.cs</files>
  <behavior>
    - SecureDispose(byte[]) zeroes array via CryptographicOperations.ZeroMemory
    - SecureDispose(Memory of byte) zeroes memory span
    - SecureDispose(ArraySegment of byte) zeroes underlying array segment
    - SecureClear(Dictionary) clears and nulls values for reference types
    - SecureClear(List of byte[]) zeroes each array then clears list
    - DisposeAuditReport.Scan(Assembly) returns audit entries for types with byte[] fields lacking zeroization
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/SecureErase/SecureDisposeExtensions.cs`:
       - Static class `SecureDisposeExtensions`:
         - `SecureDispose(this byte[]? buffer)` — null-safe, calls `CryptographicOperations.ZeroMemory`
         - `SecureDispose(this Memory<byte> memory)` — zeroes span
         - `SecureDispose(this ArraySegment<byte> segment)` — zeroes segment's array region
         - `SecureClear<TKey, TValue>(this Dictionary<TKey, TValue>? dict)` — zeroes byte[] values if TValue is byte[], then clears
         - `SecureClear(this List<byte[]>? list)` — zeroes each entry, then clears
         - `SecureDisposeFields(this object obj)` — reflection-based: finds all byte[] fields, zeroes them. Use ONLY in dispose paths (not hot paths). Caches field info per type via `ConcurrentDictionary`.
    2. Create `DataWarehouse.SDK/Security/SecureErase/DisposeAuditReport.cs`:
       - `DisposeAuditEntry` record: `string TypeName`, `string FieldName`, `string FieldType`, `bool HasZeroization`, `string? DisposeMethodName`
       - `DisposeAuditReport` static class:
         - `Scan(Assembly assembly)` -> `IReadOnlyList<DisposeAuditEntry>`:
           - Finds all types implementing IDisposable or IAsyncDisposable
           - For each, checks fields of type byte[], Memory<byte>, ArraySegment<byte>
           - Marks `HasZeroization = true` if Dispose/DisposeAsync method body references CryptographicOperations.ZeroMemory or Array.Clear (via IL inspection or attribute marker)
           - Returns entries for types with sensitive fields
         - `GetUnprotectedTypes(Assembly assembly)` -> filtered list where HasZeroization = false
    3. Create tests:
       - Test: SecureDispose zeroes byte array
       - Test: SecureDispose handles null byte array safely
       - Test: SecureDispose zeroes Memory<byte>
       - Test: SecureClear zeroes dictionary byte[] values
       - Test: SecureClear zeroes list of byte[] entries
       - Test: SecureDisposeFields finds and zeroes byte[] fields via reflection
       - Test: DisposeAuditReport.Scan finds types with byte[] fields
       - Test: DisposeAuditReport identifies unprotected types
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 121: DisposeAsync hardening")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~SecureDispose" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>SecureDispose extensions zero all common buffer types. DisposeAuditReport scans assemblies for unprotected dispose patterns. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "SecureDispose"` — all green
</verification>

<success_criteria>
- SecureDispose zeroes byte[], Memory<byte>, ArraySegment<byte>
- Reflection-based field zeroization for dispose paths
- Audit report identifies unprotected IDisposable types
</success_criteria>

<output>
After completion, create `.planning/phases/121-secure-erase/121-03-SUMMARY.md`
</output>
