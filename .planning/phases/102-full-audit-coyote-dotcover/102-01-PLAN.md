---
phase: 102-full-audit-coyote-dotcover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/
  - Metadata/coyote-audit-report/
autonomous: true
requirements:
  - AUDT-01

must_haves:
  truths:
    - "Coyote systematic testing runs 1000 iterations on all hardening tests"
    - "Zero new concurrency bugs discovered (deadlocks, races, livelocks)"
    - "Audit report documents iterations completed, bugs found, and pass/fail status"
  artifacts:
    - path: "Metadata/coyote-audit-report/coyote-results.md"
      provides: "Coyote concurrency audit report"
      contains: "Iterations: 1000"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/*.cs"
      to: "coyote test CLI"
      via: "coyote test command with --iterations 1000"
      pattern: "coyote test.*--iterations 1000"
---

<objective>
Run Coyote systematic concurrency testing on the full DataWarehouse.Hardening.Tests suite with 1000 iterations to prove no remaining concurrency issues exist.

Purpose: Verify that all hardening fixes from Phases 96-101 have eliminated concurrency bugs (races, deadlocks, livelocks) using Microsoft Coyote's systematic exploration of task interleavings.
Output: Coyote audit report in Metadata/coyote-audit-report/ documenting 0 new bugs.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj
@DataWarehouse.Hardening.Tests/VdeConcurrencyTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Coyote systematic testing with 1000 iterations</name>
  <files>DataWarehouse.Hardening.Tests/, Metadata/coyote-audit-report/</files>
  <action>
1. Build the hardening test project in Release mode:
   ```
   dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj -c Release
   ```

2. Rewrite the test assembly for Coyote instrumentation:
   ```
   coyote rewrite DataWarehouse.Hardening.Tests/bin/Release/net10.0/DataWarehouse.Hardening.Tests.dll
   ```

3. Run Coyote systematic testing with 1000 iterations:
   ```
   coyote test DataWarehouse.Hardening.Tests/bin/Release/net10.0/DataWarehouse.Hardening.Tests.dll --iterations 1000 --verbosity info
   ```
   If Coyote's `test` command requires a specific test method, run it per test class discovered in the assembly. Use `--method` flag for each [Fact] or Coyote-compatible test method.

4. Capture all output (stdout + stderr) to a log file for analysis.

5. If Coyote discovers bugs:
   - Record each bug with its reproduction trace
   - Note the failing test, iteration number, and bug type (deadlock/race/livelock)
   - These become blockers that must be fixed before Phase 102 can pass

6. If `coyote test` does not work directly on the xunit DLL (Coyote requires `[Microsoft.Coyote.SystematicTesting.Test]` attribute or a `TestingEngine` entry point), then use the TestingEngine API approach instead:
   - The existing VdeConcurrencyTests.cs already uses `TestingEngine.Create(configuration, ...)` pattern
   - Build and run via `dotnet test` with the Coyote configuration set to 1000 iterations programmatically
   - Check VdeConcurrencyTests.cs for the existing Configuration setup and ensure iterations are set to 1000
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj -c Release --logger "console;verbosity=normal" 2>&1 | tail -20</automated>
  </verify>
  <done>All Coyote tests pass with 1000 iterations. Zero bugs found. Output captured for report.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Coyote concurrency audit report</name>
  <files>Metadata/coyote-audit-report/coyote-results.md</files>
  <action>
1. Create directory: `mkdir -p Metadata/coyote-audit-report/`

2. Create `Metadata/coyote-audit-report/coyote-results.md` with the following structure:
   ```markdown
   # Stage 1 - Step 2 - Coyote Concurrency Audit

   **Date:** {current date}
   **Phase:** 102
   **Tool:** Microsoft Coyote v1.7.11
   **Configuration:** 1000 iterations per test

   ## Summary

   | Metric | Value |
   |--------|-------|
   | Total test methods | {count} |
   | Iterations per test | 1000 |
   | Total interleavings explored | {count * 1000} |
   | Bugs found | 0 |
   | Deadlocks | 0 |
   | Race conditions | 0 |
   | Livelocks | 0 |

   ## Test Results

   | Test Method | Iterations | Status | Notes |
   |-------------|-----------|--------|-------|
   | {method name} | 1000 | PASS | {notes} |
   ...

   ## Conclusion

   Coyote systematic testing with 1000 iterations per test confirmed
   zero remaining concurrency issues in the hardened codebase.
   All task interleavings explored without discovering deadlocks,
   race conditions, or livelocks.
   ```

3. If any bugs WERE found, document each with:
   - Bug type (deadlock/race/livelock)
   - Reproduction trace file path
   - Affected code path
   - Severity assessment
   - Mark the report as FAILED and list required fixes
  </action>
  <verify>
    <automated>test -f Metadata/coyote-audit-report/coyote-results.md && grep -c "Iterations.*1000" Metadata/coyote-audit-report/coyote-results.md</automated>
  </verify>
  <done>Coyote audit report exists at Metadata/coyote-audit-report/coyote-results.md with iteration count, bug count, and per-test results documented.</done>
</task>

</tasks>

<verification>
1. All Coyote tests pass: `dotnet test DataWarehouse.Hardening.Tests/ -c Release` exits 0
2. Report exists: `Metadata/coyote-audit-report/coyote-results.md`
3. Report shows 1000 iterations and 0 bugs
</verification>

<success_criteria>
- Coyote finds 0 new concurrency bugs across all DataWarehouse.Hardening.Tests
- Audit report documents all test methods, iteration counts, and pass/fail status
- Report saved to Metadata/coyote-audit-report/coyote-results.md
</success_criteria>

<output>
After completion, create `.planning/phases/102-full-audit-coyote-dotcover/102-01-SUMMARY.md`
</output>
