---
phase: 135-todo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/IO/BlockDeviceFactory.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleReader.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleUpdater.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleIntegrityVerifier.cs
  - DataWarehouse.SDK/Contracts/Ecosystem/TerraformProviderSpecification.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Pipeline/Stages/IntegrityCalculatorStage.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Identity/NamespaceAuthority.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Integrity/EpochBatchedMerkleUpdater.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Federation/Tests/ShardLifecycleIntegrationTests.cs
  - DataWarehouse.SDK/Infrastructure/InMemory/InMemoryP2PNetwork.cs
  - DataWarehouse.SDK/Infrastructure/InMemory/InMemoryClusterMembership.cs
  - DataWarehouse.SDK/Infrastructure/DeveloperExperience.cs
  - DataWarehouse.Launcher/Program.cs
  - DataWarehouse.Launcher/Integration/ServiceHost.cs
  - Plugins/DataWarehouse.Plugins.UltimateMultiCloud/Strategies/Portability/CloudPortabilityStrategies.cs
  - Plugins/DataWarehouse.Plugins.UltimateStorage/Strategies/Network/FcStrategy.cs
  - Plugins/DataWarehouse.Plugins.UltimateIntelligence/Strategies/Memory/Embeddings/EmbeddingCache.cs
  - Plugins/DataWarehouse.Plugins.UltimateConsensus/UltimateConsensusPlugin.cs
autonomous: true
requirements: [ADV-06]

must_haves:
  truths:
    - "All 36 TODOs resolved: implemented, removed, or converted to documented design decisions"
    - "BlockDeviceFactory 9 TODOs resolved with platform detection stubs for IoUring, IoRing, WindowsOverlapped, Kqueue"
    - "PreambleReader 4 TODOs resolved with BLAKE3 strategy or documented as deferred to .NET BCL"
    - "TerraformProviderSpecification 4 TODOs resolved with actual client API call generation"
    - "Zero TODO comments remaining in codebase (excluding test expectations and legitimate future work markers)"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/IO/BlockDeviceFactory.cs"
      provides: "Platform detection for all 8 block device implementations"
      contains: "IoUringBlockDevice|IoRingBlockDevice|WindowsOverlappedBlockDevice|KqueueBlockDevice"
  key_links:
    - from: "BlockDeviceFactory.cs"
      to: "IoUringBlockDevice / IoRingBlockDevice / WindowsOverlappedBlockDevice / KqueueBlockDevice"
      via: "platform probe and cascade"
      pattern: "TryCreate|IsAvailable"
---

<objective>
Resolve all 36 remaining TODOs across 20 files in the codebase.

Purpose: ADV-06 requires zero unresolved TODOs. These are remnants from earlier phases where implementation was deferred. Major clusters: BlockDeviceFactory (9 TODOs for block device cascade — partially addressed by Phase 113), PreambleReader (4 TODOs for BLAKE3 hash upgrade), TerraformProviderSpecification (4 TODOs for client API code generation). Each TODO is either implemented, removed, or converted to a documented design decision with rationale.

Output: All TODO comments resolved across all files.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve BlockDeviceFactory TODOs (9) and PreambleReader TODOs (4)</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/IO/BlockDeviceFactory.cs,
    DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleReader.cs,
    DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleUpdater.cs,
    DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleIntegrityVerifier.cs,
    DataWarehouse.SDK/VirtualDiskEngine/Pipeline/Stages/IntegrityCalculatorStage.cs
  </files>
  <action>
**BlockDeviceFactory.cs (9 TODOs):**
1. TODOs 87-57 through 87-60 (4 cascade entries + 4 probe checks + 1 SPDK probe):
   - These reference IoUringBlockDevice, IoRingBlockDevice, WindowsOverlappedBlockDevice, KqueueBlockDevice
   - If Phase 113 (BlockDeviceFactory cascade) has already implemented these: remove TODO comments, they're already resolved
   - If not yet implemented: replace TODO with actual platform probe code:
     - IoUring (Linux): `try { io_uring_queue_init } catch { not available }` via P/Invoke to liburing
     - IoRing (Windows 11+): `try { CreateIoRing } catch { not available }` via P/Invoke to KernelBase.dll
     - WindowsOverlapped (Windows): always available on Windows, `RuntimeInformation.IsOSPlatform(OSPlatform.Windows)`
     - Kqueue (macOS/BSD): `RuntimeInformation.IsOSPlatform(OSPlatform.OSX)`
   - SPDK probe TODO: implement `NativeLibrary.TryLoad("libspdk")` probe, return false if not available
   - Each probe returns bool, cascade tries from best to worst: SPDK > IoUring/IoRing > Kqueue > Overlapped > FileStream

**PreambleReader.cs + PreambleUpdater.cs + PreambleIntegrityVerifier.cs (4 TODOs):**
2. All 4 TODOs say "Replace SHA-256 with BLAKE3 when it ships in the .NET BCL"
   - BLAKE3 is not yet in .NET BCL (as of .NET 10)
   - Resolution: Convert TODO to design decision comment:
     ```
     // DESIGN DECISION: Using SHA-256 until BLAKE3 ships in .NET BCL.
     // BLAKE3 NuGet (Blake3.NET) exists but adds native dependency.
     // SHA-256 is FIPS-approved and sufficient for integrity checking.
     // Revisit when System.Security.Cryptography.Blake3 ships.
     ```
   - Remove "TODO" prefix — this is now a documented decision, not an action item

**IntegrityCalculatorStage.cs:**
3. Check for TODO, apply same BLAKE3 decision pattern if present.
  </action>
  <verify>
    <automated>grep -c "TODO" DataWarehouse.SDK/VirtualDiskEngine/IO/BlockDeviceFactory.cs DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleReader.cs DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleUpdater.cs DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleIntegrityVerifier.cs 2>/dev/null | grep -v ":0$" || echo "All TODOs resolved in target files"</automated>
  </verify>
  <done>
    - BlockDeviceFactory: all 9 TODOs resolved with platform probes or confirmed already implemented
    - PreambleReader/Updater/Verifier: 4 TODOs converted to DESIGN DECISION comments
    - Zero TODO comments in these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Resolve remaining TODOs across all other files</name>
  <files>
    DataWarehouse.SDK/Contracts/Ecosystem/TerraformProviderSpecification.cs,
    DataWarehouse.SDK/VirtualDiskEngine/Identity/NamespaceAuthority.cs,
    DataWarehouse.SDK/VirtualDiskEngine/Integrity/EpochBatchedMerkleUpdater.cs,
    DataWarehouse.SDK/VirtualDiskEngine/Federation/Tests/ShardLifecycleIntegrationTests.cs,
    DataWarehouse.SDK/Infrastructure/InMemory/InMemoryP2PNetwork.cs,
    DataWarehouse.SDK/Infrastructure/InMemory/InMemoryClusterMembership.cs,
    DataWarehouse.SDK/Infrastructure/DeveloperExperience.cs,
    DataWarehouse.Launcher/Program.cs,
    DataWarehouse.Launcher/Integration/ServiceHost.cs,
    Plugins/DataWarehouse.Plugins.UltimateMultiCloud/Strategies/Portability/CloudPortabilityStrategies.cs,
    Plugins/DataWarehouse.Plugins.UltimateStorage/Strategies/Network/FcStrategy.cs,
    Plugins/DataWarehouse.Plugins.UltimateIntelligence/Strategies/Memory/Embeddings/EmbeddingCache.cs,
    Plugins/DataWarehouse.Plugins.UltimateConsensus/UltimateConsensusPlugin.cs
  </files>
  <action>
For each file with remaining TODOs, apply the resolution decision tree:

**TerraformProviderSpecification.cs (4 TODOs):**
- TODOs say "Call client API to create/delete/read/operate resource"
- These are inside Terraform provider code generation templates — the TODO IS the generated output
- Resolution: These are legitimate generated code markers, not action items for our codebase. Replace `// TODO: Call client API to create resource` with `// GENERATED: Replace with actual provider implementation` to make clear these are template placeholders for Terraform provider consumers, not our action items.

**Each remaining file (scan and resolve):**
1. Read the file, find each TODO
2. For each TODO, determine resolution:
   a. **Implement**: If the TODO describes something that can be coded now — implement it
   b. **Design Decision**: If it describes a deferred choice that has been decided — convert to DESIGN DECISION comment with rationale
   c. **Not Applicable**: If referring to work done in a later phase (e.g., Phase 112/113) — verify it's done and remove
   d. **Remove**: If the TODO is outdated or the surrounding code already handles it — remove
3. Apply resolution, ensure no bare TODO remains

**Final sweep:**
- Run `grep -rn "TODO" --include="*.cs" .` across entire codebase
- Exclude `obj/` and `bin/` directories
- Verify count is 0 (or document any legitimate exceptions like test assertions that check for TODO handling)
  </action>
  <verify>
    <automated>grep -rn "TODO" --include="*.cs" DataWarehouse.SDK/ Plugins/ DataWarehouse.Launcher/ DataWarehouse.CLI/ DataWarehouse.Dashboard/ | grep -v "/obj/" | grep -v "/bin/" | wc -l</automated>
  </verify>
  <done>
    - All 36 TODOs across 20 files resolved
    - TerraformProviderSpecification TODOs converted to GENERATED markers
    - Final grep shows 0 remaining TODO comments (excluding obj/bin)
  </done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.sln` succeeds (no regressions from TODO resolutions)
- `grep -rn "TODO" --include="*.cs" . | grep -v "/obj/" | grep -v "/bin/"` returns 0 results
</verification>

<success_criteria>
All 36 TODOs resolved. Each resolved by implementation, design decision documentation, or removal. Zero TODO comments remain in production codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/135-todo-cleanup/135-01-SUMMARY.md`
</output>
