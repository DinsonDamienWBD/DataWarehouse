---
phase: 135-todo-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Contracts/Persistence/IPersistentBackingStore.cs
  - DataWarehouse.SDK/Contracts/Persistence/DefaultPluginStateStore.cs
  - DataWarehouse.SDK/Contracts/StrategyBase.cs
  - DataWarehouse.Hardening.Tests/Persistence/StrategyStateStoreAuditTests.cs
autonomous: true
requirements: [ADV-06]

must_haves:
  truths:
    - "Every strategy using BoundedDictionary for state is classified as persistent or ephemeral"
    - "Persistent strategies wired to IPersistentBackingStore survive restart"
    - "Ephemeral strategies documented as intentionally transient"
    - "Audit report lists all strategies and their state classification"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Persistence/StrategyStateStoreAuditTests.cs"
      provides: "Audit test verifying state store classification"
      min_lines: 60
  key_links:
    - from: "StrategyBase.cs"
      to: "BoundedDictionary"
      via: "counter state storage"
      pattern: "BoundedDictionary"
    - from: "IPersistentBackingStore.cs"
      to: "DefaultPluginStateStore.cs"
      via: "persistence implementation"
      pattern: "IPersistentBackingStore"
---

<objective>
Audit strategy state stores — classify each as persistent or ephemeral and wire persistent ones to IPersistentBackingStore.

Purpose: ADV-06 requires strategy state store audit. StrategyBase uses `BoundedDictionary<string, long>` for counters. Some strategies also use BoundedDictionary for business state that should survive restarts. This plan audits all usages, classifies them, and wires persistent ones to the IPersistentBackingStore from the scaling infrastructure.

Output: Audit test, state classification, persistence wiring for strategies that need it.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DataWarehouse.SDK/Contracts/StrategyBase.cs
@DataWarehouse.SDK/Contracts/Persistence/IPersistentBackingStore.cs
@DataWarehouse.SDK/Contracts/Persistence/DefaultPluginStateStore.cs

<interfaces>
From DataWarehouse.SDK/Contracts/StrategyBase.cs:
- BoundedDictionary<string, long> _counters = new BoundedDictionary<string, long>(1000)
- IncrementCounter(string key), GetCounter(string key)

From DataWarehouse.SDK/Contracts/Persistence/IPersistentBackingStore.cs:
- Interface for persistent key-value storage
- SaveAsync, LoadAsync, DeleteAsync

From DataWarehouse.SDK/Contracts/Persistence/DefaultPluginStateStore.cs:
- Default implementation using file-based storage

BoundedDictionary usage in SDK:
- StrategyBase._counters (performance counters — ephemeral)
- Various strategies may use for business state
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit BoundedDictionary usage and classify persistent vs ephemeral</name>
  <files>
    DataWarehouse.Hardening.Tests/Persistence/StrategyStateStoreAuditTests.cs
  </files>
  <behavior>
    - Test: Audit scans all strategy classes for BoundedDictionary usage
    - Test: Each usage classified as Ephemeral (counters, caches) or Persistent (business state)
    - Test: No unclassified BoundedDictionary usage exists
    - Test: All persistent usages have IPersistentBackingStore wiring or documented exemption
  </behavior>
  <action>
1. Create audit test that uses reflection to find all BoundedDictionary field declarations:
   - Load all assemblies in solution
   - Find all classes inheriting StrategyBase
   - For each, find fields of type `BoundedDictionary<,>`
   - Classify based on field name and context:
     - `_counters` (from StrategyBase): Ephemeral — performance counters, lose on restart is OK
     - `_cache`, `_buffer`, `_pool`: Ephemeral — caches rebuild from source
     - `_state`, `_config`, `_mapping`, `_registry`: Potentially Persistent — need review
   - Generate classification report as test output

2. For each **potentially persistent** usage found:
   - Read the strategy code to determine if the state represents:
     a. **Ephemeral cache**: rebuilds from external source -> classify as Ephemeral
     b. **Derived state**: computed from configuration -> classify as Ephemeral
     c. **Business state**: user data, learned preferences, accumulated metrics -> classify as Persistent
   - Document classification in test with rationale comment

3. The audit test asserts: every BoundedDictionary usage has an explicit classification. No "unknown" entries.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~StrategyStateStoreAudit" --no-restore -v minimal</automated>
  </verify>
  <done>
    - All BoundedDictionary usages classified
    - Classification documented with rationale
    - Audit test passes with zero unclassified entries
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire persistent strategies to IPersistentBackingStore</name>
  <files>
    DataWarehouse.SDK/Contracts/StrategyBase.cs,
    DataWarehouse.SDK/Contracts/Persistence/IPersistentBackingStore.cs,
    DataWarehouse.SDK/Contracts/Persistence/DefaultPluginStateStore.cs
  </files>
  <action>
1. Based on audit results from Task 1, for each strategy with **Persistent** classification:
   - Add `IPersistentBackingStore` injection point (the owning plugin provides this during strategy init)
   - Implement `SaveStateAsync` / `LoadStateAsync` methods that serialize BoundedDictionary contents to the backing store
   - Call `LoadStateAsync` during `InitializeAsyncCore` to restore state on startup
   - Call `SaveStateAsync` periodically (every 60s) and during `ShutdownAsyncCore`

2. Enhance `StrategyBase` with optional persistence support:
   - Add `protected IPersistentBackingStore? StateStore { get; private set; }`
   - Add `internal void InjectStateStore(IPersistentBackingStore store)` (called by owning plugin)
   - Add `protected virtual bool RequiresPersistentState => false;` (strategies opt-in by overriding)
   - If `RequiresPersistentState` and `StateStore != null`:
     - Override `InitializeAsyncCore` to call `LoadStateAsync` first
     - Background timer saves state every 60s
     - `ShutdownAsyncCore` calls `SaveStateAsync`
   - If `RequiresPersistentState` but `StateStore == null`: log warning, operate in ephemeral mode

3. Update persistent strategies to override `RequiresPersistentState => true`.

4. Verify: `DefaultPluginStateStore` (file-based) already implements `IPersistentBackingStore` — no changes needed to the persistence layer itself.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.SDK --no-restore -v minimal && dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~StrategyStateStore" --no-restore -v minimal</automated>
  </verify>
  <done>
    - StrategyBase supports optional persistent state via IPersistentBackingStore
    - Persistent strategies load state on init, save on shutdown
    - Ephemeral strategies unchanged
    - All builds and tests pass
  </done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.sln` succeeds
- Audit test confirms all BoundedDictionary usages classified
- Persistent strategies load/save state correctly
</verification>

<success_criteria>
Every strategy's state storage classified as persistent or ephemeral. Persistent strategies wired to IPersistentBackingStore with auto-save/restore. Audit test enforces classification completeness.
</success_criteria>

<output>
After completion, create `.planning/phases/135-todo-cleanup/135-02-SUMMARY.md`
</output>
