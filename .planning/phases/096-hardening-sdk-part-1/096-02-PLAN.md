---
phase: 096-hardening-sdk-part-1
plan: 02
type: execute
wave: 2
depends_on: ["096-01"]
files_modified:
  - DataWarehouse.Hardening.Tests/SDK/
  - DataWarehouse.SDK/
autonomous: true
requirements:
  - HARD-01
  - HARD-02
  - HARD-03

must_haves:
  truths:
    - "Every SDK finding 219-467 has a failing test proving the vulnerability"
    - "Every failing test has a production fix making it pass"
    - "Solution builds with 0 errors after all fixes"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/SDK/"
      provides: "Hardening tests for SDK findings 219-467"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/SDK/"
      to: "DataWarehouse.SDK/"
      via: "Project reference — tests exercise production code"
      pattern: "\\[Fact\\]|\\[Theory\\]"
---

<objective>
Stage 1 - Steps 1-3 - SDK Hardening: After ColumnarRegionEngine through Contracts/Spatial/ (Findings 219-467)

Process every SDK finding from #219 through #467 using the TDD hardening loop. Covers files alphabetically from after ColumnarRegionEngine.cs through Contracts/Spatial/ directory.

Purpose: Continue eradicating SDK vulnerabilities. This batch includes Contracts/ subdirectories covering core abstractions, strategy bases, orchestrator bases, and spatial contracts — high-value targets with CRITICAL and HIGH severity findings including broken double-checked locking, security contract lies, TOCTOU races, and hardcoded health checks.

Output: Test files in DataWarehouse.Hardening.Tests/SDK/, fixed production code in DataWarehouse.SDK/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/096-hardening-sdk-part-1/096-01-SUMMARY.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 1101-1350 — findings 219-467)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD hardening for SDK findings 219-350 (Post-ColumnarRegionEngine through Contracts/IReplication)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1101-1232 (findings 219-350) sequentially, file by file.

For EACH finding, follow the TDD loop:
1. Read finding from CONSOLIDATED-FINDINGS.md
2. Write failing test (Coyote for concurrency, xUnit for logic/memory)
3. Confirm RED
4. Fix production code
5. Confirm GREEN via `dotnet test`

Key findings in this batch include Contracts/ directory files with critical abstractions:
- Strategy bases with broken initialization patterns (double-checked locking)
- Orchestrator bases with hardcoded health checks
- TOCTOU races in concurrent data structures
- CRITICAL severity findings on security contracts (Blake3 returning SHA512)

Test organization: group by production file, class name `{ClassName}HardeningTests`, method `Finding{N}_{BriefDescription}`.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All findings 219-350 have tests, all pass GREEN, solution builds</done>
</task>

<task type="auto">
  <name>Task 2: TDD hardening for SDK findings 351-467 (Contracts/IReplication through Contracts/Spatial/)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1232-1350 (findings 351-467) sequentially, file by file.

Same TDD workflow. Key files in this batch:
- Remaining Contracts/ interfaces and base classes
- Contracts/Spatial/ — SpatialAnchor, VisualFeatureSignature (DateTime vs DateTimeOffset issues)
- StorageOrchestratorBase — multiple HIGH findings (volatile, CanSeek, partial failure swallow, health lie)
- StrategyBase — broken DCL initialization
- StrategyRegistry — silent failure on discovery

After completing all findings 219-467, commit with message:
"test+fix(096-02): SDK hardening findings 219-467 (Contracts through Contracts/Spatial)"

Then run `dotnet test` as post-commit sanity check.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All findings 219-467 have tests, all pass GREEN, commit created, post-commit dotnet test passes</done>
</task>

</tasks>

<verification>
1. `dotnet build DataWarehouse.slnx` completes with 0 errors
2. `dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK"` — all tests pass
3. Every finding 219-467 has a corresponding test method
4. Post-commit `dotnet test` passes
</verification>

<success_criteria>
- 249 findings (219-467) processed with TDD loop
- All tests pass GREEN
- Solution builds with 0 errors
- 1 commit created
- No finding skipped
</success_criteria>

<output>
After completion, create `.planning/phases/096-hardening-sdk-part-1/096-02-SUMMARY.md`
</output>
