---
phase: 096-hardening-sdk-part-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/SDK/
  - DataWarehouse.SDK/
autonomous: true
requirements:
  - HARD-01
  - HARD-02
  - HARD-03

must_haves:
  truths:
    - "Every SDK finding 1-218 has a failing test proving the vulnerability"
    - "Every failing test has a production fix making it pass"
    - "Solution builds with 0 errors after all fixes"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/SDK/"
      provides: "Hardening tests for SDK findings 1-218"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/SDK/"
      to: "DataWarehouse.SDK/"
      via: "Project reference — tests exercise production code"
      pattern: "\\[Fact\\]|\\[Theory\\]"
---

<objective>
Stage 1 - Steps 1-3 - SDK Hardening: Accelerators through ColumnarRegionEngine.cs (Findings 1-218)

Process every SDK finding from #1 through #218 using the TDD hardening loop: write test -> confirm RED -> fix production code -> confirm GREEN. Covers files alphabetically from accelerator files through ColumnarRegionEngine.cs.

Purpose: Eradicate the first batch of SDK vulnerabilities via proven TDD methodology. This batch covers accelerator files, AccessLogEntry, ActiveStoragePluginBases, AdaptiveIndexEngine, AdvancedMessageBus, AedsPluginBases, AI/MlPipeline, AiAutonomyConfiguration, AlexModel, AlignedMemoryOwner, AllocationGroupDescriptorTable, ArcCache files, ArrowColumnarBridge, ArtNode, AuditLogRegion, AuthenticatedMessageBusDecorator, AuthorityContextPropagator, AutonomousRebalancer, cloud providers (Aws/Azure/Gcp), BadBlockManager, BalloonDriver, BareMetalOptimizer, BeTree files, BillingTypes, BlockDeviceFactory, BlockTypeTags, BloomFilterSkipIndex, BoundedCache/Dictionary/List/Queue, BTree/BTreeNode, BusControllerFactory, BwTree, CameraSettings, CannInterop, CarbonTypes, CheckClassification, ChecksumTable, CloudKmsProvider, CoApClient/Request, and ColumnarRegionEngine.

Output: Test files in DataWarehouse.Hardening.Tests/SDK/, fixed production code in DataWarehouse.SDK/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 876-1101 — findings 1-218)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD hardening for SDK findings 1-110 (Accelerators through BlockTypeTags.cs)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 876-994 (findings 1-110) sequentially, file by file.

For EACH finding:
1. Read the finding from CONSOLIDATED-FINDINGS.md (finding number, severity, file, line, description)
2. Locate the production file in DataWarehouse.SDK/
3. Write a test in DataWarehouse.Hardening.Tests/SDK/ that proves the vulnerability:
   - Use Microsoft.Coyote for concurrency findings (race conditions, TOCTOU, unsynchronized fields)
   - Use xUnit [Fact]/[Theory] for logic/memory findings (nullable, naming, unused fields, empty catch, etc.)
   - Test class name: `{ProductionClassName}HardeningTests`
   - Test method name: `Finding{N}_{BriefDescription}`
4. Run the test — confirm it FAILS (RED) proving the vulnerability
5. Fix the production code:
   - Concurrency: add volatile, locks, Interlocked, or atomic operations
   - Nullable: fix null checks or remove redundant ones
   - Naming: rename to match conventions (PascalCase enums, camelCase locals, etc.)
   - Unused fields: remove or utilize them
   - Empty catch: add proper exception handling or logging
   - CRITICAL findings (cloud providers): implement proper abstractions or throw PlatformNotSupportedException with actionable message
   - HIGH findings (TOCTOU, sync): make operations atomic
6. Run `dotnet test` — confirm GREEN

Group tests by production file for organization. Multiple findings on the same file share a test class.

Finding types in this batch:
- MEDIUM: O(n^3) matrix multiply (findings 1), hardcoded stats (2), nullable always-false (3-5, 25, 35-37, 46-49, 57, 66, 68-69)
- LOW: Naming conventions (6-8, 15-18, 23-24, 26-28, 31-32, 38-45, 51-52, 54, 58, 61, 65, 67, 71-78, 82-110)
- HIGH: Concurrency races (12-13, 29-30, 33-34, 50, 53, 60, 62-64), empty catch (62-63), async void lambda (53)
- CRITICAL: Cloud provider simulations (55-56, 59, 79), mesh simulation (79)

Do NOT skip any finding regardless of severity. Every finding gets a test.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All 110 findings (1-110) have tests, all tests pass GREEN, solution builds with 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: TDD hardening for SDK findings 111-218 (BloomFilterSkipIndex through ColumnarRegionEngine.cs)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 995-1101 (findings 111-218) sequentially, file by file.

Same TDD workflow as Task 1. For EACH finding:
1. Read finding details from CONSOLIDATED-FINDINGS.md
2. Write failing test (Coyote for concurrency, xUnit for logic/memory)
3. Confirm RED
4. Fix production code
5. Confirm GREEN via `dotnet test`

Finding types in this batch:
- LOW: Naming (111-121, 122-125, 132, 145-146, 149-152, 153-188, 189-201, 212-218), unused assignments (126-127, 129-130, 133-134, 147), unused fields (181, 202)
- MEDIUM: Async overload available (128, 131, 135, 210), nullable (136-144, 148), object initializer in using (204-209), cancellation support (211)
- LOW: Collection never queried (203, 216)

After completing all 218 findings, commit with message:
"test+fix(096-01): SDK hardening findings 1-218 (Accelerators through ColumnarRegionEngine)"

Then run `dotnet test` as post-commit sanity check.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All 218 findings (1-218) have tests, all tests pass GREEN, solution builds with 0 errors, commit created</done>
</task>

</tasks>

<verification>
1. `dotnet build DataWarehouse.slnx` completes with 0 errors
2. `dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK"` — all tests pass
3. Every finding 1-218 has a corresponding test method (grep for Finding1_ through Finding218_)
4. Post-commit `dotnet test` passes
</verification>

<success_criteria>
- 218 findings processed with TDD loop (test -> red -> fix -> green)
- All tests in DataWarehouse.Hardening.Tests/SDK/ pass
- Solution builds with 0 errors
- 1 commit created at natural file boundary
- No finding skipped regardless of severity
</success_criteria>

<output>
After completion, create `.planning/phases/096-hardening-sdk-part-1/096-01-SUMMARY.md`
</output>
