---
phase: 096-hardening-sdk-part-1
plan: 05
type: execute
wave: 5
depends_on: ["096-04"]
files_modified:
  - DataWarehouse.Hardening.Tests/SDK/
  - DataWarehouse.SDK/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "Every SDK finding 955-1249 has a failing test proving the vulnerability"
    - "Every failing test has a production fix making it pass"
    - "Solution builds with 0 errors after all fixes"
    - "dotnet test passes after commit"
    - "All 1249 SDK Part 1 findings are complete end-to-end"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/SDK/"
      provides: "Hardening tests for SDK findings 955-1249"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/SDK/"
      to: "DataWarehouse.SDK/"
      via: "Project reference — tests exercise production code"
      pattern: "\\[Fact\\]|\\[Theory\\]"
---

<objective>
Stage 1 - Steps 1-3 - SDK Hardening: After ICacheableStorage through end of SDK Part 1 (Findings 955-1249)

Process every SDK finding from #955 through #1249 using the TDD hardening loop. This is the final plan for Phase 96. Covers files alphabetically from after ICacheableStorage.cs through the end of SDK Part 1 (IoUringBindings.cs, finding 1249).

Purpose: Complete the first half of SDK hardening. This batch covers ICarbonAwareStorage, ICarbonBudget, and the remaining interface/infrastructure files through IoUringBindings. Heavy concentration of naming convention findings (enum members, properties, constants) plus interface contract findings. IoUringBindings has a dense block of OS-level constant naming findings.

Output: Test files in DataWarehouse.Hardening.Tests/SDK/, fixed production code in DataWarehouse.SDK/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/096-hardening-sdk-part-1/096-04-SUMMARY.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 1851-2148 — findings 955-1249)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD hardening for SDK findings 955-1100 (ICarbonAwareStorage through mid-range interfaces)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1851-1996 (findings 955-1100) sequentially, file by file.

For EACH finding, follow the TDD loop:
1. Read finding from CONSOLIDATED-FINDINGS.md
2. Write failing test (Coyote for concurrency, xUnit for logic/memory)
3. Confirm RED
4. Fix production code
5. Confirm GREEN via `dotnet test`

Key areas in this batch:
- ICarbonAwareStorage — CO2/carbon naming conventions (GramsCO2PerKwh, VCS, ACR, CAR enums)
- ICarbonBudget — parameter naming (budgetGramsCO2e)
- Remaining interface files with naming/contract findings
- Infrastructure files

Test organization: group by production file, `{ClassName}HardeningTests`, method `Finding{N}_{Brief}`.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All findings 955-1100 have tests, all pass GREEN, solution builds</done>
</task>

<task type="auto">
  <name>Task 2: TDD hardening for SDK findings 1101-1249 (Remaining interfaces through IoUringBindings.cs)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1996-2148 (findings 1101-1249) sequentially, file by file.

Same TDD workflow. Key files in this batch:
- Remaining interface/infrastructure files
- IoUringBindings.cs — dense block of OS-level constant naming findings (IORING_OP_READ_FIXED, IORING_SETUP_SQPOLL, etc.)
- IPluginCapabilityRegistry — AI/RAID enum naming
- IRaftLogStore — namespace mismatch

After completing all findings 955-1249, commit with message:
"test+fix(096-05): SDK hardening findings 955-1249 (ICarbonAwareStorage through IoUringBindings)"

Then run `dotnet test` as post-commit sanity check.

FINAL PHASE VERIFICATION: After this plan completes, the entire Phase 96 is done. Verify:
1. Total test count covers all 1249 findings
2. `dotnet test DataWarehouse.Hardening.Tests/` passes with 0 failures
3. `dotnet build DataWarehouse.slnx` succeeds with 0 errors
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All 1249 findings (1-1249) have tests across plans 01-05, all pass GREEN, commit created, Phase 96 complete</done>
</task>

</tasks>

<verification>
1. `dotnet build DataWarehouse.slnx` completes with 0 errors
2. `dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK"` — all tests pass
3. Every finding 955-1249 has a corresponding test method
4. Post-commit `dotnet test` passes
5. PHASE COMPLETE: All 1249 findings across plans 01-05 have tests and fixes
</verification>

<success_criteria>
- 295 findings (955-1249) processed with TDD loop
- All tests pass GREEN
- Solution builds with 0 errors
- 1 commit created
- No finding skipped
- Phase 96 fully complete — all 1249 SDK Part 1 findings hardened
</success_criteria>

<output>
After completion, create `.planning/phases/096-hardening-sdk-part-1/096-05-SUMMARY.md`
</output>
