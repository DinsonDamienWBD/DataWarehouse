---
phase: 096-hardening-sdk-part-1
plan: 03
type: execute
wave: 3
depends_on: ["096-02"]
files_modified:
  - DataWarehouse.Hardening.Tests/SDK/
  - DataWarehouse.SDK/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "Every SDK finding 468-710 has a failing test proving the vulnerability"
    - "Every failing test has a production fix making it pass"
    - "Solution builds with 0 errors after all fixes"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/SDK/"
      provides: "Hardening tests for SDK findings 468-710"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/SDK/"
      to: "DataWarehouse.SDK/"
      via: "Project reference — tests exercise production code"
      pattern: "\\[Fact\\]|\\[Theory\\]"
---

<objective>
Stage 1 - Steps 1-3 - SDK Hardening: After Contracts/Spatial through ExtentTree.cs (Findings 468-710)

Process every SDK finding from #468 through #710 using the TDD hardening loop. Covers files alphabetically from after Contracts/Spatial/ through ExtentTree.cs.

Purpose: Continue eradicating SDK vulnerabilities. This batch covers StorageOrchestratorBase continuation, StrategyBase, StrategyRegistry, TamperProof contracts, plus data structure implementations (Dict, DiskIO, DWVD format internals, Encryption providers, Erasure coding, Event sourcing, Extent management). Includes high-density areas of concurrency and correctness findings.

Output: Test files in DataWarehouse.Hardening.Tests/SDK/, fixed production code in DataWarehouse.SDK/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/096-hardening-sdk-part-1/096-02-SUMMARY.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 1351-1600 — findings 468-710)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD hardening for SDK findings 468-590 (Post-Contracts/Spatial through DiskIO/Encryption)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1351-1473 (findings 468-590) sequentially, file by file.

For EACH finding, follow the TDD loop:
1. Read finding from CONSOLIDATED-FINDINGS.md
2. Write failing test (Coyote for concurrency, xUnit for logic/memory)
3. Confirm RED
4. Fix production code
5. Confirm GREEN via `dotnet test`

Key areas in this batch:
- VisualFeatureSignature DateTime issues
- StorageOrchestratorBase volatile/CanSeek/health-lie fixes
- StrategyBase broken DCL initialization
- StrategyRegistry silent discovery failures
- TamperProof AccessLogEntry Blake3/SHA512 contract lie (CRITICAL)
- Data structure files (ConcurrentDictionary patterns, DiskIO)
- DWVD format internals
- Encryption provider implementations

Test organization: group by production file, `{ClassName}HardeningTests`, method `Finding{N}_{Brief}`.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All findings 468-590 have tests, all pass GREEN, solution builds</done>
</task>

<task type="auto">
  <name>Task 2: TDD hardening for SDK findings 591-710 (Erasure coding through ExtentTree.cs)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1473-1600 (findings 591-710) sequentially, file by file.

Same TDD workflow. Key files in this batch:
- Erasure coding implementations
- Event sourcing infrastructure
- Extent management (ExtentAllocator, ExtentTree — HIGH semantic-search findings)
- Various naming/nullable/unused field findings

After completing all findings 468-710, commit with message:
"test+fix(096-03): SDK hardening findings 468-710 (Post-Contracts through ExtentTree)"

Then run `dotnet test` as post-commit sanity check.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All findings 468-710 have tests, all pass GREEN, commit created, post-commit dotnet test passes</done>
</task>

</tasks>

<verification>
1. `dotnet build DataWarehouse.slnx` completes with 0 errors
2. `dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK"` — all tests pass
3. Every finding 468-710 has a corresponding test method
4. Post-commit `dotnet test` passes
</verification>

<success_criteria>
- 243 findings (468-710) processed with TDD loop
- All tests pass GREEN
- Solution builds with 0 errors
- 1 commit created
- No finding skipped
</success_criteria>

<output>
After completion, create `.planning/phases/096-hardening-sdk-part-1/096-03-SUMMARY.md`
</output>
