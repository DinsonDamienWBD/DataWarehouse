---
phase: 096-hardening-sdk-part-1
plan: 04
type: execute
wave: 4
depends_on: ["096-03"]
files_modified:
  - DataWarehouse.Hardening.Tests/SDK/
  - DataWarehouse.SDK/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "Every SDK finding 711-954 has a failing test proving the vulnerability"
    - "Every failing test has a production fix making it pass"
    - "Solution builds with 0 errors after all fixes"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/SDK/"
      provides: "Hardening tests for SDK findings 711-954"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/SDK/"
      to: "DataWarehouse.SDK/"
      via: "Project reference — tests exercise production code"
      pattern: "\\[Fact\\]|\\[Theory\\]"
---

<objective>
Stage 1 - Steps 1-3 - SDK Hardening: After ExtentTree through ICacheableStorage.cs (Findings 711-954)

Process every SDK finding from #711 through #954 using the TDD hardening loop. Covers files alphabetically from after ExtentTree.cs through ICacheableStorage.cs.

Purpose: Continue eradicating SDK vulnerabilities. This batch covers FaultToleranceConfig, Federation types, FileBlockDevice, Format storage profiles, GeoHash, Hardware abstraction, HealthCheck, HotRegion, HyperConverged types, plus interface files (IAccessControl, IBilling, ICacheableStorage). Mix of naming conventions, expression analysis, and interface contract findings.

Output: Test files in DataWarehouse.Hardening.Tests/SDK/, fixed production code in DataWarehouse.SDK/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/096-hardening-sdk-part-1/096-03-SUMMARY.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 1601-1850 — findings 711-954)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD hardening for SDK findings 711-835 (FaultToleranceConfig through HotRegion/HyperConverged)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1601-1725 (findings 711-835) sequentially, file by file.

For EACH finding, follow the TDD loop:
1. Read finding from CONSOLIDATED-FINDINGS.md
2. Write failing test (Coyote for concurrency, xUnit for logic/memory)
3. Confirm RED
4. Fix production code
5. Confirm GREEN via `dotnet test`

Key areas in this batch:
- FaultToleranceConfig — naming (RAID6, RAIDZ3, SMB), always-true expressions, collection never queried
- Federation types — distributed system contracts
- FileBlockDevice — I/O abstractions
- Format storage profiles — data format handling
- GeoHash — spatial indexing
- Hardware abstraction layer
- HealthCheck infrastructure
- HotRegion — performance-critical region management
- HyperConverged types — converged infrastructure

Test organization: group by production file, `{ClassName}HardeningTests`, method `Finding{N}_{Brief}`.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All findings 711-835 have tests, all pass GREEN, solution builds</done>
</task>

<task type="auto">
  <name>Task 2: TDD hardening for SDK findings 836-954 (IAccessControl through ICacheableStorage.cs)</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/ (new test files)
    DataWarehouse.SDK/ (production fixes)
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 1725-1850 (findings 836-954) sequentially, file by file.

Same TDD workflow. Key files in this batch:
- IAccessControl — access control interface contracts
- IBilling — billing interface naming conventions
- ICacheableStorage — cache eviction policy naming (FIFO, TTL, LRU)
- Various SDK interface files with naming convention findings

After completing all findings 711-954, commit with message:
"test+fix(096-04): SDK hardening findings 711-954 (FaultToleranceConfig through ICacheableStorage)"

Then run `dotnet test` as post-commit sanity check.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK" --no-restore -v minimal 2>&1 | tail -20</automated>
  </verify>
  <done>All findings 711-954 have tests, all pass GREEN, commit created, post-commit dotnet test passes</done>
</task>

</tasks>

<verification>
1. `dotnet build DataWarehouse.slnx` completes with 0 errors
2. `dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~SDK"` — all tests pass
3. Every finding 711-954 has a corresponding test method
4. Post-commit `dotnet test` passes
</verification>

<success_criteria>
- 244 findings (711-954) processed with TDD loop
- All tests pass GREEN
- Solution builds with 0 errors
- 1 commit created
- No finding skipped
</success_criteria>

<output>
After completion, create `.planning/phases/096-hardening-sdk-part-1/096-04-SUMMARY.md`
</output>
