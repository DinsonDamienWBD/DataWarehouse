---
phase: 132-formal-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/FormalVerification/WalConsistencyInvariantTests.cs
  - DataWarehouse.Hardening.Tests/FormalVerification/RaidParityInvariantTests.cs
  - DataWarehouse.Hardening.Tests/FormalVerification/RaftSafetyInvariantTests.cs
  - DataWarehouse.Hardening.Tests/FormalVerification/FederationCatalogInvariantTests.cs
autonomous: true
requirements: [ADV-03]

must_haves:
  truths:
    - "Property-based tests translate each TLA+ invariant to executable C# assertions"
    - "Coyote explores 10,000 iterations per invariant for concurrent interleavings"
    - "WAL NoPartialCommits holds across crash injection scenarios"
    - "Raft ElectionSafety holds across all explored interleavings"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/FormalVerification/WalConsistencyInvariantTests.cs"
      provides: "Property-based WAL crash consistency tests"
      min_lines: 80
    - path: "DataWarehouse.Hardening.Tests/FormalVerification/RaidParityInvariantTests.cs"
      provides: "Property-based RAID parity tests"
      min_lines: 60
    - path: "DataWarehouse.Hardening.Tests/FormalVerification/RaftSafetyInvariantTests.cs"
      provides: "Property-based Raft consensus tests"
      min_lines: 80
    - path: "DataWarehouse.Hardening.Tests/FormalVerification/FederationCatalogInvariantTests.cs"
      provides: "Property-based federation catalog tests"
      min_lines: 60
  key_links:
    - from: "WalConsistencyInvariantTests.cs"
      to: "Metadata/formal-verification/WalConsistency.tla"
      via: "translates TLA+ invariants"
      pattern: "NoPartialCommits|WalDurability"
    - from: "RaftSafetyInvariantTests.cs"
      to: "Metadata/formal-verification/RaftSafety.tla"
      via: "translates TLA+ invariants"
      pattern: "ElectionSafety|LogMatching"
---

<objective>
Translate TLA+ invariants into executable property-based tests using Coyote with 10,000-iteration random exploration per invariant.

Purpose: ADV-03 bridges formal specification to runtime verification. TLA+ proves design correctness; these tests prove the C# implementation matches the design. Coyote's systematic exploration of concurrent interleavings catches bugs that random testing misses.

Output: Four invariant test files with Coyote-driven property-based tests, all passing.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DataWarehouse.Hardening.Tests/VdeConcurrencyTests.cs
@DataWarehouse.Hardening.Tests/Chaos/TornWrite/TornWriteRecoveryTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WAL and RAID invariant test suites</name>
  <files>
    DataWarehouse.Hardening.Tests/FormalVerification/WalConsistencyInvariantTests.cs,
    DataWarehouse.Hardening.Tests/FormalVerification/RaidParityInvariantTests.cs
  </files>
  <behavior>
    - Test: NoPartialCommits — after crash at any point during multi-write txn, recovery yields all-or-nothing
    - Test: WalDurability — committed txn entries present after any single crash
    - Test: CommittedTxnEventuallyFlushed — committed txn data reaches disk within bounded time
    - Test: ParityCorrect — after any sequence of writes, XOR(data) == parity for every stripe
    - Test: SingleFailureRecoverable — after one drive failure, rebuild restores all data
    - All tests: 10,000 Coyote iterations exploring concurrent interleavings
  </behavior>
  <action>
1. **WalConsistencyInvariantTests.cs**:
   - Use `Microsoft.Coyote.SystematicTesting` configuration with 10,000 iterations
   - Create in-memory WAL mock that models Ring A (crash log) behavior
   - `NoPartialCommits_HoldsAcrossCrash`: Start 5 concurrent transactions, each writing 3 entries. Inject crash via `Task.Delay` + cancellation at random point. After "recovery" (replay WAL), assert: for each txn, either all 3 writes visible or none visible.
   - `WalDurability_CommittedEntrySurvivesCrash`: Write txn, get commit ack, crash, recover, assert entries present.
   - Use `SchedulingPoint.Interleave()` at each WAL append, flush, and commit point
   - Reference TLA+ spec in comments: "// Corresponds to WalConsistency.tla INVARIANT NoPartialCommits"

2. **RaidParityInvariantTests.cs**:
   - Model 4+1 RAID-5 stripe set in memory
   - `ParityCorrect_AfterConcurrentWrites`: 10 concurrent write operations to different stripes, after all complete, verify XOR parity for every stripe
   - `SingleFailureRecoverable_Rebuild`: Write data, mark one drive failed, rebuild from parity + remaining, verify data matches original
   - Use Coyote 10,000 iterations for concurrent write interleaving exploration
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~FormalVerification.Wal|FullyQualifiedName~FormalVerification.Raid" --no-restore -v minimal</automated>
  </verify>
  <done>
    - WAL NoPartialCommits and WalDurability invariants verified across 10K iterations
    - RAID ParityCorrect and SingleFailureRecoverable verified across 10K iterations
    - All tests pass with Coyote systematic testing
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raft and Federation invariant test suites</name>
  <files>
    DataWarehouse.Hardening.Tests/FormalVerification/RaftSafetyInvariantTests.cs,
    DataWarehouse.Hardening.Tests/FormalVerification/FederationCatalogInvariantTests.cs
  </files>
  <behavior>
    - Test: ElectionSafety — no two leaders in same term across 5-node cluster, 10K iterations
    - Test: LogMatching — matching index+term implies matching prefix
    - Test: StateMachineSafety — no two nodes apply different entries at same index
    - Test: ShardOwnership — every shard has exactly one primary after any operation sequence
    - Test: CatalogConvergence — after partition heal, all catalogs match
    - Test: NoDataLoss — shard migration preserves all data
  </behavior>
  <action>
1. **RaftSafetyInvariantTests.cs**:
   - Model 5-node Raft cluster with in-memory message passing
   - `ElectionSafety_AtMostOneLeaderPerTerm`: Run election rounds with random timeouts and message delays. After each round, assert at most one node with state=Leader for any given term.
   - `LogMatching_MatchingEntries`: Append entries with concurrent leader changes, verify log matching property.
   - `StateMachineSafety_ConsistentApplication`: Apply committed entries, verify no divergence.
   - Use Coyote `SchedulingPoint.Interleave()` at message send/receive, election timeout, and heartbeat points
   - 10,000 iterations per test

2. **FederationCatalogInvariantTests.cs**:
   - Model 3-region federation with in-memory CRDT catalog
   - `ShardOwnership_SinglePrimary`: Create shards, migrate between regions, assert exactly one primary region per shard at all times.
   - `CatalogConvergence_AfterPartitionHeal`: Partition two regions, make independent changes, heal partition, merge CRDTs, assert catalogs converge.
   - `NoDataLoss_MigrationPreservesData`: Start migration, inject failure mid-migration, complete/retry, verify shard data intact.
   - 10,000 Coyote iterations
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~FormalVerification.Raft|FullyQualifiedName~FormalVerification.Federation" --no-restore -v minimal</automated>
  </verify>
  <done>
    - Raft ElectionSafety, LogMatching, StateMachineSafety verified across 10K iterations
    - Federation ShardOwnership, CatalogConvergence, NoDataLoss verified across 10K iterations
    - All tests pass with Coyote systematic testing
  </done>
</task>

</tasks>

<verification>
- `dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~FormalVerification"` all green
- Each test class references corresponding TLA+ invariant in comments
</verification>

<success_criteria>
All TLA+ invariants translated to executable C# property-based tests. Coyote explores 10,000 interleavings per invariant. All invariants hold.
</success_criteria>

<output>
After completion, create `.planning/phases/132-formal-verification/132-02-SUMMARY.md`
</output>
