---
phase: 123-cloud-provider-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Deployment/CloudProviders/AzureProvider.cs
  - DataWarehouse.SDK/DataWarehouse.SDK.csproj
  - DataWarehouse.Hardening.Tests/SDK/CloudProviderHardeningTests.cs
autonomous: true
requirements: [PER-01]

must_haves:
  truths:
    - "AzureProvider.ProvisionVmAsync creates an Azure VM and returns resource ID"
    - "AzureProvider.ProvisionStorageAsync creates Managed Disk or Blob container and returns resource ID"
    - "AzureProvider.DeprovisionAsync deletes Azure resource by ID"
    - "AzureProvider.GetMetricsAsync returns Azure Monitor metrics for a resource"
    - "AzureProvider.ListManagedResourcesAsync returns resources tagged ManagedBy:DataWarehouse"
  artifacts:
    - path: "DataWarehouse.SDK/Deployment/CloudProviders/AzureProvider.cs"
      provides: "Full Azure ICloudProvider implementation"
      min_lines: 200
  key_links:
    - from: "DataWarehouse.SDK/Deployment/CloudProviders/AzureProvider.cs"
      to: "ICloudProvider"
      via: "interface implementation"
      pattern: "class AzureProvider : ICloudProvider"
    - from: "DataWarehouse.SDK/Deployment/CloudProviders/AzureProvider.cs"
      to: "Azure.ResourceManager, Azure.Storage.Blobs, Azure.Monitor.Query"
      via: "NuGet package references"
      pattern: "Azure\\.ResourceManager|Azure\\.Storage|Azure\\.Monitor"
---

<objective>
Implement the Azure cloud provider with full Azure SDK integration, replacing all NotSupportedException stubs.

Purpose: Close Gap A.4 for Azure — AzureProvider currently throws NotSupportedException on all 5 ICloudProvider methods.
Output: Working AzureProvider.cs with Azure SDK NuGet references and unit tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Same ICloudProvider contract as 123-01 -->

From DataWarehouse.SDK/Deployment/CloudProviders/ICloudProvider.cs:
```csharp
public interface ICloudProvider : IDisposable
{
    string Name { get; }
    Task<string> ProvisionVmAsync(VmSpec spec, CancellationToken ct = default);
    Task<string> ProvisionStorageAsync(StorageSpec spec, CancellationToken ct = default);
    Task<bool> DeprovisionAsync(string resourceId, CancellationToken ct = default);
    Task<CloudResourceMetrics?> GetMetricsAsync(string resourceId, CancellationToken ct = default);
    Task<IReadOnlyList<string>> ListManagedResourcesAsync(CancellationToken ct = default);
}
```

VmSpec: ImageId, InstanceType, StorageGb, Region, Tags, SecurityGroup, SubnetId
StorageSpec: StorageType (block/object), SizeGb, PerformanceTier, Encrypted, AvailabilityZone, Tags
CloudResourceMetrics: ResourceId, CpuUtilizationPercent, MemoryUtilizationPercent, StorageUtilizationPercent, NetworkIn/OutBytesPerSec, DiskRead/WriteBytesPerSec, Timestamp
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Azure SDK NuGet packages and write Azure provider tests</name>
  <files>DataWarehouse.SDK/DataWarehouse.SDK.csproj, DataWarehouse.Hardening.Tests/SDK/CloudProviderHardeningTests.cs</files>
  <behavior>
    - Test: AzureProvider_ProvisionVmAsync_CreatesVm — verify ArmClient creates VM resource with correct size/image/network config
    - Test: AzureProvider_ProvisionStorageAsync_BlockType_CreatesManagedDisk — verify DiskCollection.CreateOrUpdateAsync called with SizeGb, DiskSku mapped from PerformanceTier
    - Test: AzureProvider_ProvisionStorageAsync_ObjectType_CreatesBlobContainer — verify BlobServiceClient creates container
    - Test: AzureProvider_DeprovisionAsync_DeletesResource — verify resource deletion by parsing ARM resource ID
    - Test: AzureProvider_DeprovisionAsync_NotFound_ReturnsFalse — verify RequestFailedException with 404 returns false
    - Test: AzureProvider_GetMetricsAsync_ReturnsMonitorMetrics — verify MetricsQueryClient queries correct metrics
    - Test: AzureProvider_ListManagedResourcesAsync_FiltersByTag — verify resource graph query filters ManagedBy tag
    - Test: AzureProvider_Dispose_DisposesClients
  </behavior>
  <action>
    Add NuGet packages to DataWarehouse.SDK.csproj: Azure.ResourceManager, Azure.ResourceManager.Compute, Azure.ResourceManager.Storage, Azure.Storage.Blobs, Azure.Monitor.Query, Azure.Identity (for DefaultAzureCredential).

    Add Azure-specific test methods to CloudProviderHardeningTests.cs. Mock Azure SDK types (ArmClient, BlobServiceClient, MetricsQueryClient). Tests must fail initially against stub AzureProvider.

    NOTE: Azure SDK uses a factory/ARM pattern — mock the ArmClient and resource collections, not individual service clients. Use wrapper interfaces if needed for testability.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~AzureProvider" --no-build 2>&1 | head -50</automated>
  </verify>
  <done>Azure SDK packages referenced, 8+ tests written and confirmed RED</done>
</task>

<task type="auto">
  <name>Task 2: Implement AzureProvider with full Azure SDK integration</name>
  <files>DataWarehouse.SDK/Deployment/CloudProviders/AzureProvider.cs</files>
  <action>
    Replace all NotSupportedException bodies with real Azure SDK calls. Add constructor parameters for ArmClient, BlobServiceClient, MetricsQueryClient (injectable for testing). If null, create using DefaultAzureCredential.

    Store configurable resourceGroupName and subscriptionId as constructor parameters (required for Azure ARM operations). Default from environment: AZURE_SUBSCRIPTION_ID, DW_RESOURCE_GROUP.

    **ProvisionVmAsync(VmSpec):**
    - Map InstanceType: "general-purpose"→"Standard_D2s_v3", "compute-optimized"→"Standard_F2s_v2", "memory-optimized"→"Standard_E2s_v3" (pass through if already Standard_*)
    - Create VM via VirtualMachineCollection.CreateOrUpdateAsync: location from Region, VM size, OS disk from ImageId, network interface from SubnetId
    - Tag with ManagedBy=DataWarehouse plus VmSpec.Tags
    - Wait for provisioning to complete (WaitForCompletion)
    - Return ARM resource ID

    **ProvisionStorageAsync(StorageSpec):**
    - block: Create Managed Disk via DiskCollection.CreateOrUpdateAsync, size=SizeGb, sku from PerformanceTier ("standard"→StandardSSD_LRS, "premium"→Premium_LRS, "ultra"→UltraSSD_LRS), encryption=EncryptionAtRestWithPlatformKey if Encrypted. Return disk resource ID.
    - object: Create Blob container via BlobServiceClient.GetBlobContainerClient("dw-{guid}").CreateIfNotExistsAsync. Return container name.

    **DeprovisionAsync(resourceId):**
    - Parse ARM resource ID to determine resource type. Call appropriate Delete method.
    - Catch RequestFailedException with Status=404 → return false. Return true on success.

    **GetMetricsAsync(resourceId):**
    - Query Azure Monitor via MetricsQueryClient.QueryResourceAsync for: Percentage CPU, Available Memory Bytes (compute inverse), Disk Read Bytes/sec, Disk Write Bytes/sec, Network In Total, Network Out Total. Timespan: last 5 minutes.
    - Map to CloudResourceMetrics. Return null if resource not found.

    **ListManagedResourcesAsync():**
    - Use ResourceGroupResource.GetGenericResourcesAsync with tag filter "ManagedBy eq 'DataWarehouse'"
    - Return list of resource IDs

    **Dispose:** Dispose BlobServiceClient and any disposable clients.

    All operations: respect CancellationToken, log via ILogger, handle throttling (429) with retry.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~AzureProvider" --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 8+ Azure provider tests pass GREEN. Zero NotSupportedException in AzureProvider.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK` compiles with zero errors
- `dotnet test DataWarehouse.Hardening.Tests --filter "FullyQualifiedName~AzureProvider"` — all tests pass
- `grep -c "NotSupportedException" DataWarehouse.SDK/Deployment/CloudProviders/AzureProvider.cs` returns 0
</verification>

<success_criteria>
- AzureProvider implements all 5 ICloudProvider methods with real Azure SDK calls
- Azure.ResourceManager.*, Azure.Storage.Blobs, Azure.Monitor.Query, Azure.Identity NuGet packages referenced
- Unit tests cover all 5 methods including 404 error paths
- Zero NotSupportedException in AzureProvider
</success_criteria>

<output>
After completion, create `.planning/phases/123-cloud-provider-implementation/123-02-SUMMARY.md`
</output>
