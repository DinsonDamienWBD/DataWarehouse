---
phase: 91.5-vde-v2.1-format-completion
plan: 47
type: execute
wave: 6
depends_on: ["87-45"]
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/Preamble/StrippedKernelBuildSpec.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Preamble/KernelBuildScriptGenerator.cs
autonomous: true

must_haves:
  truths:
    - "KernelBuildScriptGenerator produces a shell script for building a stripped Linux kernel <8MB"
    - "StrippedKernelBuildSpec defines the kernel config (USB+NIC+display+vfio-pci, nothing else)"
    - "Generated script is deterministic and reproducible given the same spec"
    - "This is a C# helper that generates build scripts, NOT an actual build pipeline"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Preamble/StrippedKernelBuildSpec.cs"
      provides: "Kernel build configuration spec for preamble"
      contains: "class StrippedKernelBuildSpec"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Preamble/KernelBuildScriptGenerator.cs"
      provides: "Generates shell scripts for stripped kernel builds"
      contains: "class KernelBuildScriptGenerator"
  key_links:
    - from: "KernelBuildScriptGenerator.cs"
      to: "StrippedKernelBuildSpec.cs"
      via: "reads spec to emit kernel config and build commands"
      pattern: "StrippedKernelBuildSpec"
    - from: "KernelBuildScriptGenerator.cs"
      to: "PreambleHeader"
      via: "output bzImage size validated against uint32 KernelSize limit"
      pattern: "uint.MaxValue"
---

<objective>
Implement the stripped Linux kernel build pipeline helper classes (VOPT-64): C# classes that generate deterministic build scripts for a minimal bzImage <8MB with only USB+NIC+display+vfio-pci drivers.

Purpose: The preamble bundles a stripped Linux kernel for bare-metal boot. These helper classes generate the build scripts and kernel config — they are DevOps artifacts, not actual build pipelines.
Output: 2 files in `DataWarehouse.SDK/VirtualDiskEngine/Preamble/`.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v6.0-VDE-FORMAT-v2.0-SPEC.md
@DataWarehouse.SDK/VirtualDiskEngine/Preamble/PreambleHeader.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: StrippedKernelBuildSpec and KernelBuildScriptGenerator</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/Preamble/StrippedKernelBuildSpec.cs
    DataWarehouse.SDK/VirtualDiskEngine/Preamble/KernelBuildScriptGenerator.cs
  </files>
  <action>
1. Create `StrippedKernelBuildSpec.cs` with `[SdkCompatibility("6.0.0")]`:
   - Namespace: `DataWarehouse.SDK.VirtualDiskEngine.Preamble`
   - `sealed class StrippedKernelBuildSpec`
   - Properties for kernel configuration:
     - `string KernelVersion { get; init; } = "6.6"` — LTS kernel version
     - `TargetArchitecture Architecture { get; init; } = TargetArchitecture.X86_64` — reuse enum from PreambleHeader
     - `long MaxBzImageBytes { get; init; } = 8 * 1024 * 1024` — 8MB target
     - `bool IncludeUsbDrivers { get; init; } = true`
     - `bool IncludeNicDrivers { get; init; } = true`
     - `bool IncludeDisplayDrivers { get; init; } = true`
     - `bool IncludeVfioPci { get; init; } = true` — required for SPDK handoff
     - `bool IncludeNvmeDriver { get; init; } = false` — NVMe handled by SPDK, not kernel
     - `IReadOnlyList<string> AdditionalModules { get; init; }` — optional extra modules
     - `IReadOnlyList<string> DisabledSubsystems { get; init; }` — subsystems to explicitly disable (sound, bluetooth, wireless by default)
   - Static factory methods:
     - `static StrippedKernelBuildSpec ServerFull()` — USB+NIC+display+vfio-pci (default)
     - `static StrippedKernelBuildSpec EmbeddedMinimal()` — no display, no USB (serial console only)
     - `static StrippedKernelBuildSpec AirgapReadonly()` — no NIC, USB+display+vfio-pci
   - `IReadOnlyList<string> GenerateKconfigEntries()` — returns list of CONFIG_xxx=y/n lines:
     - Always enabled: CONFIG_64BIT=y, CONFIG_SMP=y, CONFIG_VFIO=y, CONFIG_VFIO_PCI=y, CONFIG_IOMMU_SUPPORT=y, CONFIG_EFI=y, CONFIG_EFI_STUB=y, CONFIG_BLK_DEV=y, CONFIG_BLOCK=y
     - Conditional on flags: CONFIG_USB_SUPPORT, CONFIG_NET, CONFIG_DRM (display), CONFIG_FRAMEBUFFER_CONSOLE
     - Always disabled: CONFIG_SOUND=n, CONFIG_WIRELESS=n, CONFIG_BLUETOOTH=n, CONFIG_WLAN=n, CONFIG_DEBUG_INFO=n (saves ~2MB), CONFIG_MODULES=n (monolithic for simplicity)
     - Architecture-specific: CONFIG_X86_64 or CONFIG_ARM64

2. Create `KernelBuildScriptGenerator.cs` with `[SdkCompatibility("6.0.0")]`:
   - `sealed class KernelBuildScriptGenerator`
   - `string GenerateBuildScript(StrippedKernelBuildSpec spec)`:
     Returns a complete bash script string that:
     a. Downloads kernel source tarball (kernel.org URL with version)
     b. Extracts and enters source directory
     c. Writes .config from spec.GenerateKconfigEntries() via heredoc
     d. Runs `make olddefconfig` to resolve dependencies
     e. Runs `make -j$(nproc) bzImage` (x86_64) or `make -j$(nproc) Image.gz` (aarch64)
     f. Validates output size <= spec.MaxBzImageBytes, errors if exceeded
     g. Copies output to a known path for preamble assembly
     h. Prints SHA256 checksum of the output
   - `string GenerateDockerBuildScript(StrippedKernelBuildSpec spec)`:
     Returns a Dockerfile + build script for reproducible builds:
     a. FROM ubuntu:22.04, installs build-essential, flex, bison, libelf-dev, bc, libssl-dev
     b. Copies .config
     c. Builds kernel in container
     d. Multi-stage: copies only bzImage to output
   - `void ValidateSpec(StrippedKernelBuildSpec spec)`:
     - Throws if Architecture is reserved value
     - Throws if VfioPci is disabled (required for SPDK handoff)
     - Warns (via returned list of warnings) if both NIC and USB are disabled (no I/O path)
  </action>
  <verify>Build compiles: `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5`</verify>
  <done>StrippedKernelBuildSpec defines kernel configs for server/embedded/airgap profiles. KernelBuildScriptGenerator produces deterministic bash and Docker build scripts targeting <8MB bzImage.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore` succeeds with zero errors
- Generated kconfig entries include vfio-pci and EFI stub
- Three factory profiles produce distinct kernel configs
</verification>

<success_criteria>
- VOPT-64 satisfied: C# helper classes that generate stripped Linux kernel build scripts for preamble
</success_criteria>

<output>
After completion, create `.planning/phases/91.5-vde-v2.1-format-completion/87-47-SUMMARY.md`
</output>
