---
phase: 91.5-vde-v2.1-format-completion
plan: 87-26
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/Format/SpatioTemporalModule.cs
  - DataWarehouse.SDK/VirtualDiskEngine/Format/SpatioTemporalExtent.cs
autonomous: true

must_haves:
  truths:
    - "STEX inode module stores CoordinateSystem:2, Precision:2, HilbertOrder:2 (6B in DataOsModuleArea)"
    - "6x32B standard extents reinterpreted as 3x64B spatiotemporal extents when SPATIOTEMPORAL flag set"
    - "Each 64B extent carries Geohash:16, TimeStart:8, TimeEnd:8, StartBlock:8, BlockCount:4, Flags:4, ExpectedHash:16"
    - "Hilbert curve ordering clusters spatially adjacent data on physically adjacent blocks"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Format/SpatioTemporalModule.cs"
      provides: "STEX module data structure (6B) at inode fixed-offset 0x1FA with serialization"
      contains: "struct SpatioTemporalModule"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/Format/SpatioTemporalExtent.cs"
      provides: "64B spatiotemporal extent pointer with Geohash and time range"
      contains: "struct SpatioTemporalExtent"
  key_links:
    - from: "SpatioTemporalModule.cs"
      to: "ExtendedInode512"
      via: "Fixed-offset at inode byte 506 (0x1FA) in DataOsModuleArea"
      pattern: "DataOsModuleArea"
    - from: "SpatioTemporalExtent.cs"
      to: "InodeExtent"
      via: "Reinterprets standard 32B extent slots as 64B spatiotemporal extents"
      pattern: "SpatioTemporalExtent"
---

<objective>
Implement Spatiotemporal 4D Extent Addressing (STEX, Module bit 24, VOPT-39): Geohash+time extent addressing for IoT/LiDAR/drone telemetry with Hilbert curve spatial clustering.

Purpose: When SPATIOTEMPORAL flag is set, the inode's extent array is reinterpreted for 4D addressing. 16-byte Geohash provides ~10cm global resolution. Hilbert curve ordering ensures spatially adjacent data lands on physically adjacent blocks, enabling NVMe scatter-gather DMA prefetch for bounding box queries.
Output: Two files in `DataWarehouse.SDK/VirtualDiskEngine/Format/`.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v6.0-VDE-FORMAT-v2.0-SPEC.md
@DataWarehouse.SDK/VirtualDiskEngine/Format/InodeExtent.cs
@DataWarehouse.SDK/VirtualDiskEngine/Format/ExtendedInode512.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: SpatioTemporalModule and SpatioTemporalExtent</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/Format/SpatioTemporalModule.cs
    DataWarehouse.SDK/VirtualDiskEngine/Format/SpatioTemporalExtent.cs
  </files>
  <action>
Create `SpatioTemporalModule` readonly struct with `[SdkCompatibility("6.0.0")]` in namespace `DataWarehouse.SDK.VirtualDiskEngine.Format`:

Per spec: 6B at inode fixed-offset 0x1FA in DataOsModuleArea: [CoordinateSystem:2][Precision:2][HilbertOrder:2]

- `SpatioCoordinateSystem CoordinateSystem` -- enum (ushort): Wgs84=0 (GPS lat/lon), Ecef=1 (Earth-centered), LocalCartesian=2, UtmZone=3
- `ushort Precision` -- geohash precision level (bits of resolution, higher = finer). 64 bits = ~10cm
- `ushort HilbertOrder` -- Hilbert curve order for spatial clustering (typical: 16-32)
- `const int SerializedSize = 6`
- `const byte ModuleBitPosition = 24` -- STEX in ModuleManifest
- `const int InodeOffset = 0x1FA` -- fixed offset in 512B inode DataOsModuleArea (byte 506)
- `static void Serialize(in SpatioTemporalModule module, Span<byte> buffer)` -- 6B LE
- `static SpatioTemporalModule Deserialize(ReadOnlySpan<byte> buffer)` -- 6B LE
- `static SpatioTemporalModule DefaultWgs84` -- WGS84, precision=64, HilbertOrder=16

Create `SpatioTemporalExtent` readonly struct with `[SdkCompatibility("6.0.0")]` in namespace `DataWarehouse.SDK.VirtualDiskEngine.Format`:

Per spec: 64B spatiotemporal extent (reinterpretation of 2x32B standard extents):
[SpatialGeohash:16][TimeEpochStart:8][TimeEpochEnd:8][StartBlock:8][BlockCount:4][Flags:4][ExpectedHash:16]

- `byte[] SpatialGeohash` -- 16-byte geohash (~10cm global resolution)
- `ulong TimeEpochStart` -- start of temporal range (UTC nanoseconds since epoch)
- `ulong TimeEpochEnd` -- end of temporal range (UTC nanoseconds since epoch)
- `long StartBlock` -- block address in Data Region
- `uint BlockCount` -- number of contiguous blocks
- `uint Flags` -- extent flags (same 32-bit flags as standard extents)
- `byte[] ExpectedHash` -- BLAKE3 truncated to 16 bytes
- `const int SerializedSize = 64`
- `const int MaxDirectExtents = 3` -- 6x32B = 3x64B direct slots
- `const int ExtentsPerIndirectBlock = 63` -- floor(4080 / 64) per spec
- `static void Serialize(in SpatioTemporalExtent extent, Span<byte> buffer)` -- 64B LE
- `static SpatioTemporalExtent Deserialize(ReadOnlySpan<byte> buffer)` -- 64B LE

Hilbert curve utilities (static methods in SpatioTemporalExtent):
- `static byte[] EncodeGeohash(double latitude, double longitude, int precisionBits)` -- encodes lat/lon to 16-byte geohash
- `static (double Latitude, double Longitude) DecodeGeohash(ReadOnlySpan<byte> geohash, int precisionBits)` -- decodes geohash to lat/lon
- `static ulong ComputeHilbertIndex(ReadOnlySpan<byte> geohash, int hilbertOrder)` -- maps 2D geohash to 1D Hilbert curve index for spatial clustering
- `bool ContainsPoint(double latitude, double longitude, int precisionBits)` -- returns true if geohash covers the given point
- `bool OverlapsTimeRange(ulong queryStart, ulong queryEnd)` -- returns true if extent's time range overlaps query range
- `bool OverlapsBoundingBox(ReadOnlySpan<byte> minGeohash, ReadOnlySpan<byte> maxGeohash, ulong timeStart, ulong timeEnd, int precisionBits)` -- 4D bounding box intersection test

Use `System.Buffers.Binary.BinaryPrimitives` for serialization. Use `DataWarehouse.SDK.Contracts` for SdkCompatibility.
  </action>
  <verify>Build compiles: `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5`</verify>
  <done>SpatioTemporalModule provides 6B inode module at fixed-offset with coordinate system and Hilbert curve params, SpatioTemporalExtent provides 64B extent pointer with Geohash, time range, and bounding box query support.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore` succeeds with zero errors
- SpatioTemporalModule serializes to exactly 6 bytes at inode offset 0x1FA
- SpatioTemporalExtent serializes to exactly 64 bytes (2x standard 32B extent slots)
- MaxDirectExtents = 3 matches spec (6x32B -> 3x64B reinterpretation)
</verification>

<success_criteria>
- VOPT-39 satisfied: STEX module (bit 24), 4D extent addressing, Geohash encoding/decoding, Hilbert curve clustering, bounding box query support
</success_criteria>

<output>
After completion, create `.planning/phases/91.5-vde-v2.1-format-completion/87-26-SUMMARY.md`
</output>
