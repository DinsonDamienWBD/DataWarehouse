---
phase: 65.5-production-readiness
plan: 07
type: execute
wave: 2
depends_on: ["65.5-01", "65.5-02", "65.5-03", "65.5-04"]
autonomous: true

must_haves:
  truths:
    - "All 5 plugins' message handlers actually invoke the resolved strategy's method"
    - "UltimateDataGovernance compliance handler evaluates real rules (not always returns true)"
    - "All 5 plugins compile with zero errors"
---

<objective>
Fix no-op message handlers in Batch 1 (5 plugins). Each handler currently locates a strategy but never calls its method — fix them to actually invoke the resolved strategy.

1. **UltimateDataPrivacy**: Wire 8 handlers (anonymize, pseudonymize, tokenize, mask, differential, compliance, metrics) to call strategy.ExecuteAsync
2. **UltimateDataCatalog**: Wire 4 handlers (discover, document, access, schema) to call strategy methods
3. **UltimateDataGovernance**: Wire 5 handlers (stewardship, lineage, retention, compliance, audit) — fix compliance to evaluate real rules
4. **UltimateDataMesh**: Wire GetStrategyOrThrow to actually dispatch in all handlers
5. **UltimateDataManagement**: Wire 7 handlers (dedup, version, tier, shard, lifecycle, cache, index)
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.5-production-readiness-plan.md
@.planning/PLUGIN-CATALOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire no-op message handlers in 5 plugins (Batch 1)</name>
  <action>
    For each of the 5 plugins:
    1. Read the plugin main file and handler files
    2. Find each message handler that resolves a strategy
    3. Ensure the handler actually calls the strategy's method with the correct arguments
    4. For UltimateDataGovernance: ensure compliance handler evaluates real rules, not always-true
    5. Set response payload from strategy result
    Build verify: `dotnet build` all 5 plugin projects.
  </action>
  <verify>All handlers call strategy methods and populate responses.</verify>
  <done>All 5 Batch 1 plugins have functional message handlers.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/65.5-production-readiness/65.5-07-SUMMARY.md`
</output>
