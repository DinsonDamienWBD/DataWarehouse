---
phase: 65.5-production-readiness
plan: 08
type: execute
wave: 2
depends_on: ["65.5-01", "65.5-02", "65.5-03", "65.5-04"]
autonomous: true

must_haves:
  truths:
    - "All 5 plugins' message handlers dispatch to strategies and return real results"
    - "UltimateMicroservices HandleInvokeAsync routes to strategy (not always success)"
    - "UltimateMultiCloud handlers dispatch to strategies"
    - "UltimateDataProtection implements all 5 bus handlers"
    - "UltimateDatabaseStorage list/get strategy handlers return real data"
    - "All 5 plugins compile with zero errors"
---

<objective>
Fix no-op message handlers in Batch 2 (5 plugins).

1. **UltimateDataQuality**: Wire 8 handlers (validate, profile, cleanse, detect-duplicates, standardize, score, monitor, report)
2. **UltimateMicroservices**: Fix HandleInvokeAsync to route to strategy instead of always returning success
3. **UltimateMultiCloud**: Fix HandleExecuteAsync, HandleReplicateAsync, HandleMigrateAsync, HandleOptimizeCostAsync to dispatch to strategies
4. **UltimateDataProtection**: Implement all 5 message bus handlers (list, select, backup, restore, statistics)
5. **UltimateDatabaseStorage**: Fix HandleListStrategiesAsync/HandleGetStrategyAsync to populate response payload
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.5-production-readiness-plan.md
@.planning/PLUGIN-CATALOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire no-op message handlers in 5 plugins (Batch 2)</name>
  <action>
    For each of the 5 plugins:
    1. Read the plugin main file and handler files
    2. Find each no-op or stub message handler
    3. Wire each handler to dispatch to the correct strategy and return real results
    4. For UltimateDataProtection: implement all 5 bus handlers
    5. For UltimateDatabaseStorage: populate response payload
    Build verify: `dotnet build` all 5 plugin projects.
  </action>
  <verify>All handlers dispatch to strategies and return real results.</verify>
  <done>All 5 Batch 2 plugins have functional message handlers.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/65.5-production-readiness/65.5-08-SUMMARY.md`
</output>
