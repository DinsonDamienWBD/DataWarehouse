---
phase: 65.5-production-readiness
plan: 13
type: execute
wave: 4
depends_on: ["65.5-09", "65.5-10"]
autonomous: true

must_haves:
  truths:
    - "KubernetesCsi merged into UltimateStorage as KubernetesCsiStorageStrategy"
    - "SqlOverObject merged into UltimateDatabaseProtocol as SqlOverObjectProtocolStrategy"
    - "AppPlatform merged into UltimateDeployment as AppHostingStrategy + AppRuntimeStrategy"
    - "3 plugin project directories deleted and removed from .slnx"
    - "Solution compiles with zero errors"
---

<objective>
Consolidate Storage + Protocol + Deployment Plugins (3 plugins merged, net -3).

1. Merge KubernetesCsi into UltimateStorage as KubernetesCsiStorageStrategy
2. Merge SqlOverObject into UltimateDatabaseProtocol as SqlOverObjectProtocolStrategy
3. Merge AppPlatform into UltimateDeployment as AppHostingStrategy + AppRuntimeStrategy

For each merge: move strategy classes, update namespaces, register in target plugin, delete source project, remove from .slnx.
Build verify: `dotnet build DataWarehouse.slnx` — zero errors.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.5-production-readiness-plan.md
@.planning/PLUGIN-CATALOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Consolidate 3 plugins into existing targets</name>
  <action>
    For each of the 3 merges:
    1. Read source plugin strategies
    2. Move to target plugin's Strategies/ folder with correct namespace
    3. Register in target plugin
    4. Delete source project directory and remove from .slnx
    Build verify: `dotnet build DataWarehouse.slnx`
  </action>
  <verify>3 plugins merged, 3 project directories deleted, solution compiles.</verify>
  <done>Storage + Protocol + Deployment plugins consolidated.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/65.5-production-readiness/65.5-13-SUMMARY.md`
</output>
