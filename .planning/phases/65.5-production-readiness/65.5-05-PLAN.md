---
phase: 65.5-production-readiness
plan: 05
type: execute
wave: 2
depends_on: ["65.5-01", "65.5-02", "65.5-03", "65.5-04"]
autonomous: true

must_haves:
  truths:
    - "All 8 plugins have base-class strategy registration alongside local registry"
    - "All 8 plugins compile with zero errors"
---

<objective>
Complete dual-registration for Batch 1 (8 plugins). For each plugin, add base-class strategy registration alongside the local registry.

1. UltimateServerless: Add RegisterComputeStrategy() calls after discovery
2. UltimateDocGen: Add base PlatformPluginBase registration
3. UltimateSDKPorts: Add base PlatformPluginBase registration
4. UltimateSustainability: Consolidate two parallel registries into one + base registration
5. UltimateMicroservices: Add base PlatformPluginBase registration
6. UltimateMultiCloud: Add base InfrastructurePluginBase registration
7. UltimateStreamingData: Add base StreamingPluginBase registration
8. UltimateCompliance: Add base SecurityPluginBase registration

For each plugin: read the current file, find the strategy discovery/registration code, and add the corresponding base-class registration call. Build verify.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.5-production-readiness-plan.md
@.planning/PLUGIN-CATALOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add dual-registration for 8 plugins (Batch 1)</name>
  <action>
    For each of the 8 plugins:
    1. Read the plugin main file
    2. Find the existing strategy registration code (usually in OnStartCoreAsync or DiscoverAndRegister)
    3. Add the base-class registration call (e.g., RegisterComputeStrategy, RegisterStorageStrategy, etc.)
    4. Ensure both local and base registries are populated
    Build verify: `dotnet build` all 8 plugin projects.
  </action>
  <verify>All 8 plugins have dual registration and compile cleanly.</verify>
  <done>All 8 Batch 1 plugins have base-class strategy registration.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/65.5-production-readiness/65.5-05-SUMMARY.md`
</output>
