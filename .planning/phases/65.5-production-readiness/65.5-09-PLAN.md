---
phase: 65.5-production-readiness
plan: 09
type: execute
wave: 3
depends_on: ["65.5-05", "65.5-06", "65.5-07", "65.5-08"]
autonomous: true

must_haves:
  truths:
    - "UltimateDataTransit is public (not internal sealed)"
    - "UltimateRAID subscribes to RaidTopics.Initialize"
    - "UltimateWorkflow uses correct key 'AIOptimizedWorkflow'"
    - "UltimateCompression list handler returns strategy list in payload"
    - "AirGapBridge persists _masterKey via SaveStateAsync"
    - "UltimateEncryption persists FIPS mode and default strategy"
    - "All 7 fixes compile with zero errors"
---

<objective>
Fix critical P0 functional breakage issues.

1. **UltimateDataTransit**: Change `internal sealed class` to `public sealed class` (P0: plugin invisible to kernel)
2. **UltimateRAID**: Add `RaidTopics.Initialize` constant + subscribe in OnStartCoreAsync (P0: init unreachable)
3. **UltimateWorkflow**: Fix key mismatch `"AIOptimized"` to `"AIOptimizedWorkflow"` (P0: runtime exception)
4. **UltimateDataIntegrity**: Implement HMAC algorithms or remove from SupportedAlgorithms (P2: advertised but throws)
5. **UltimateCompression**: Fix `compression.ultimate.list` handler to return strategy list in payload
6. **AirGapBridge**: Persist `_masterKey` via SaveStateAsync (P1: exported packages unreadable after restart)
7. **UltimateEncryption**: Persist FIPS mode and default strategy via SaveStateAsync
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.5-production-readiness-plan.md
@.planning/PLUGIN-CATALOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fix P0 functional breakage across 7 plugins</name>
  <action>
    Apply each fix:
    1. UltimateDataTransit: Change class visibility to public
    2. UltimateRAID: Add initialization topic subscription
    3. UltimateWorkflow: Fix strategy key mismatch
    4. UltimateDataIntegrity: Implement or remove advertised HMAC algorithms
    5. UltimateCompression: Fix list handler payload
    6. AirGapBridge: Add _masterKey persistence
    7. UltimateEncryption: Add FIPS mode and default strategy persistence
    Build verify: `dotnet build` all affected projects.
  </action>
  <verify>All P0/P1 issues resolved, all plugins compile cleanly.</verify>
  <done>All critical functional breakage issues fixed.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/65.5-production-readiness/65.5-09-SUMMARY.md`
</output>
