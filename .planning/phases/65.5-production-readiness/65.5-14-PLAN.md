---
phase: 65.5-production-readiness
plan: 14
type: execute
wave: 4
depends_on: ["65.5-09", "65.5-10"]
autonomous: true

must_haves:
  truths:
    - "DataMarketplace merged into UltimateDataCatalog as DataMarketplaceStrategy + DataListingStrategy"
    - "UltimateDataFabric merged into UltimateDataManagement as 13 fabric strategies"
    - "2 plugin project directories deleted and removed from .slnx"
    - "Solution compiles with zero errors"
---

<objective>
Consolidate Catalog + DataManagement Plugins (2 plugins merged, net -2).

1. Merge DataMarketplace into UltimateDataCatalog as DataMarketplaceStrategy + DataListingStrategy
2. Merge UltimateDataFabric into UltimateDataManagement as 13 fabric strategies (topology: Star/Mesh/Federated, virtualization: View/Materialized/Cached, semantic layer, etc.)

For each merge: move strategy classes, update namespaces, register in target plugin, delete source project, remove from .slnx.
Build verify: `dotnet build DataWarehouse.slnx` — zero errors.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.5-production-readiness-plan.md
@.planning/PLUGIN-CATALOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Consolidate 2 plugins into existing targets</name>
  <action>
    For each of the 2 merges:
    1. Read source plugin strategies
    2. Move to target plugin's Strategies/ folder with correct namespace
    3. Register in target plugin
    4. Delete source project directory and remove from .slnx
    Build verify: `dotnet build DataWarehouse.slnx`
  </action>
  <verify>2 plugins merged, 2 project directories deleted, solution compiles.</verify>
  <done>Catalog + DataManagement plugins consolidated.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/65.5-production-readiness/65.5-14-SUMMARY.md`
</output>
