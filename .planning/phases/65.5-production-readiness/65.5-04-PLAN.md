---
phase: 65.5-production-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/DataWarehouse.Plugins.ChaosVaccination/**
  - Plugins/DataWarehouse.Plugins.TamperProof/**
  - Plugins/DataWarehouse.Plugins.UltimateKeyManagement/**
  - Plugins/DataWarehouse.Plugins.UltimateEdgeComputing/**
  - Plugins/DataWarehouse.Plugins.UltimateDataIntegration/**
  - Plugins/DataWarehouse.Plugins.UltimateStreamingData/**
  - Plugins/DataWarehouse.Plugins.FuseDriver/**
  - Plugins/DataWarehouse.Plugins.UltimateAccessControl/**
autonomous: true

must_haves:
  truths:
    - "Zero `new void` or `new async` keyword shadowing in any plugin file"
    - "Zero sync-over-async (.GetAwaiter().GetResult()) in any modified file"
    - "Zero dual _messageBus field declarations"
    - "UltimateAccessControl uses thread-safe collection (not List<T>) for audit log"
    - "All modified plugins compile with zero errors"
---

<objective>
Fix `new` keyword shadowing and deadlock risks across all affected plugins.

1. **ChaosVaccination**: Replace `new void Dispose()` with `override Dispose(bool)` calling base; replace `GetMetadata()` sync-over-async
2. **TamperProof**: Replace `GetMetadata()` sync-over-async with proper async pattern
3. **UltimateKeyManagement**: Replace `Dispose(bool)` sync-over-async with DisposeAsyncCore; remove dual `_messageBus` field
4. **UltimateEdgeComputing**: Replace `new async Task ShutdownAsync()` with proper override
5. **UltimateDataIntegration**: Replace `new void ThrowIfDisposed()` with proper override
6. **UltimateStreamingData**: Fix `StreamingDataStrategyBase` `new bool _initialized` shadowing
7. **FuseDriver**: Remove dual `_messageBus` field
8. **UltimateAccessControl**: Replace `_auditLog List<T>` with thread-safe ConcurrentQueue or BoundedList
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/65.5-production-readiness-plan.md
@.planning/PLUGIN-CATALOG.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fix keyword shadowing and deadlock risks</name>
  <action>
    For each affected plugin:
    1. Read the current file(s)
    2. Identify `new` keyword shadowing, sync-over-async, dual fields, non-thread-safe collections
    3. Apply the specific fix described in the objective
    4. Ensure all overrides use `override` keyword (not `new`)
    5. Replace .GetAwaiter().GetResult() with proper async patterns
    Build verify: `dotnet build` all affected projects.
  </action>
  <verify>No `new void`, `new async`, `new bool` shadowing. No sync-over-async. No dual fields.</verify>
  <done>All keyword shadowing and deadlock risks eliminated across 8+ plugins.</done>
</task>
</tasks>

<verification>
- Zero `new` keyword shadowing in modified files
- Zero .GetAwaiter().GetResult() in modified files
- Thread-safe collections used for shared state
- All plugins compile cleanly
</verification>

<success_criteria>
No keyword shadowing, no deadlock risks, thread-safe shared state in all modified plugins.
</success_criteria>

<output>
After completion, create `.planning/phases/65.5-production-readiness/65.5-04-SUMMARY.md`
</output>
