---
phase: 61-chaos-vaccination
plan: 07
type: execute
wave: 5
depends_on: ["61-06"]
files_modified:
  - DataWarehouse.slnx
autonomous: true
must_haves:
  truths:
    - "Full solution builds with zero errors after adding ChaosVaccination plugin"
    - "ChaosVaccination plugin is listed in the solution file"
    - "No cross-plugin references exist -- only SDK references"
  artifacts:
    - path: "DataWarehouse.slnx"
      provides: "Solution file including ChaosVaccination plugin"
      contains: "DataWarehouse.Plugins.ChaosVaccination"
  key_links:
    - from: "Plugins/DataWarehouse.Plugins.ChaosVaccination/ChaosVaccinationPlugin.csproj"
      to: "DataWarehouse.SDK/DataWarehouse.SDK.csproj"
      via: "ProjectReference"
      pattern: "ProjectReference.*DataWarehouse.SDK"
---

<objective>
Add ChaosVaccination plugin to solution and verify full build.

Purpose: Final gate -- ensure the new plugin integrates cleanly with the entire 71+ project solution. No regressions, no cross-plugin references, no build warnings introduced.

Output: Updated solution file, clean build.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-chaos-vaccination/61-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add plugin to solution and verify full build</name>
  <files>
    DataWarehouse.slnx
  </files>
  <action>
1. Add ChaosVaccination plugin to DataWarehouse.slnx:
   - Run: `dotnet sln DataWarehouse.slnx add Plugins/DataWarehouse.Plugins.ChaosVaccination/DataWarehouse.Plugins.ChaosVaccination.csproj`
   - If slnx format requires manual edit, add the project entry following the existing Plugins/ pattern

2. Full solution build:
   - Run: `dotnet build DataWarehouse.slnx`
   - Target: 0 errors across all 72+ projects
   - Check warnings: no new warnings introduced by ChaosVaccination plugin

3. Verify plugin isolation:
   - Grep ChaosVaccination.csproj for ProjectReference -- must contain ONLY DataWarehouse.SDK
   - Grep all other .csproj files -- none should reference ChaosVaccination
   - Verify no `using DataWarehouse.Plugins.UltimateResilience` in ChaosVaccination code (must use SDK types only)

4. Verify all files exist:
   - SDK contracts: 6 files in DataWarehouse.SDK/Contracts/ChaosVaccination/
   - Plugin: ChaosVaccinationPlugin.cs, Engine/ (2 files), Engine/FaultInjectors/ (6 files), BlastRadius/ (3 files), ImmuneResponse/ (3 files), Scheduling/ (2 files), Storage/ (1 file), Integration/ (2 files), Strategies/ (1 file)
   - Total: ~20 new .cs files + 1 .csproj

5. Run existing tests to verify no regressions:
   - `dotnet test` across the solution (if test projects exist and pass currently)
  </action>
  <verify>
`dotnet build DataWarehouse.slnx` completes with 0 errors.
`grep -r "ProjectReference" Plugins/DataWarehouse.Plugins.ChaosVaccination/*.csproj` shows only DataWarehouse.SDK.
`grep -r "DataWarehouse.Plugins.ChaosVaccination" Plugins/*/DataWarehouse.Plugins.*.csproj` returns zero results (no other plugin references it).
  </verify>
  <done>Full solution builds cleanly with ChaosVaccination plugin. Zero regressions. Plugin references only SDK. No other plugin references ChaosVaccination.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.slnx` -- 0 errors
- ChaosVaccination plugin in solution file
- Plugin isolation verified (SDK-only references)
- No regressions in existing tests
</verification>

<success_criteria>
- 72+ projects build with 0 errors
- ChaosVaccination is a first-class plugin in the solution
- Plugin architecture rules enforced: SDK-only references, message bus communication
</success_criteria>

<output>
After completion, create `.planning/phases/61-chaos-vaccination/61-07-SUMMARY.md`
</output>
