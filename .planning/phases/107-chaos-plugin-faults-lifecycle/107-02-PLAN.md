---
phase: 107-chaos-plugin-faults-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["107-01"]
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/PluginFault/ConcurrentLifecycleTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/PluginFault/LifecycleStressPlugin.cs
autonomous: true
requirements:
  - CHAOS-02

must_haves:
  truths:
    - "100+ concurrent load/unload cycles with active I/O produce no torn state"
    - "No orphaned message bus subscriptions after rapid load/unload"
    - "No dangling event handlers or leaked AssemblyLoadContexts"
    - "Read/write operations in-flight complete or fail cleanly during unload"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/PluginFault/ConcurrentLifecycleTests.cs"
      provides: "Concurrent lifecycle chaos tests"
      min_lines: 200
    - path: "DataWarehouse.Hardening.Tests/Chaos/PluginFault/LifecycleStressPlugin.cs"
      provides: "Plugin designed for rapid load/unload stress testing"
      min_lines: 40
  key_links:
    - from: "ConcurrentLifecycleTests.cs"
      to: "Kernel AssemblyLoadContext"
      via: "parallel load/unload calls"
      pattern: "Task\\.Run.*Load|Unload"
    - from: "ConcurrentLifecycleTests.cs"
      to: "Message bus"
      via: "subscription count verification"
      pattern: "Subscription.*Count|GetSubscriptions"
---

<objective>
Concurrent plugin lifecycle chaos testing — prove load/unload during active I/O produces no torn state or leaked resources.

Purpose: Validate that the Kernel's plugin lifecycle management is thread-safe under extreme concurrent pressure.
Output: Test suite proving 100+ load/unload cycles are safe. Report: "Stage 3 - Steps 1-2 - Concurrent Plugin Lifecycle"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-chaos-plugin-faults-lifecycle/107-01-SUMMARY.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lifecycle stress plugin and test infrastructure</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/PluginFault/LifecycleStressPlugin.cs
  </files>
  <action>
1. Read 107-01-SUMMARY.md to understand the test plugin pattern established in Plan 01.
2. Read Kernel source for AssemblyLoadContext lifecycle — how plugins register/deregister, how in-flight operations are drained.
3. Create `LifecycleStressPlugin.cs` — a lightweight plugin that:
   - Extends SDK PluginBase
   - Registers multiple message bus subscriptions during Initialize
   - Performs a slow async operation (configurable delay) in its Execute method to simulate in-flight I/O
   - Tracks its own lifecycle state (Loading/Loaded/Unloading/Unloaded) with atomic flags
   - Exposes counters: operations started, operations completed, operations interrupted
   - Properly implements IDisposable/IAsyncDisposable to track cleanup
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>LifecycleStressPlugin compiles, tracks lifecycle state, counts operations, registers bus subscriptions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement concurrent lifecycle chaos tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/PluginFault/ConcurrentLifecycleTests.cs
  </files>
  <behavior>
    - Test: 100 sequential load/unload cycles with no active I/O → AssemblyLoadContext count returns to baseline
    - Test: 100 load/unload cycles with concurrent read/write → no torn state (operations either complete or throw clean OperationCanceledException)
    - Test: Parallel load of 10 plugin instances + parallel unload → subscription count returns to zero
    - Test: Unload during active message publish → no orphaned handlers, publish completes or fails cleanly
    - Test: GC.Collect after unload cycles → WeakReference to unloaded AssemblyLoadContext is dead (no leak)
  </behavior>
  <action>
1. Create `ConcurrentLifecycleTests.cs` with xUnit test class:
   - `[Fact] SequentialLoadUnload_100Cycles_NoLeaks` — load/unload LifecycleStressPlugin 100 times, after each cycle verify subscription count and AssemblyLoadContext cleanup
   - `[Fact] ConcurrentLoadUnload_WithActiveIO_NoTornState` — start background read/write tasks, simultaneously load/unload 100 times. Assert all operations either complete successfully or throw clean cancellation. No partial/corrupt results.
   - `[Fact] ParallelLoad10_ParallelUnload_SubscriptionsClean` — load 10 instances in parallel, then unload all in parallel. Verify message bus subscription count returns to pre-test baseline.
   - `[Fact] UnloadDuringPublish_NoOrphanedHandlers` — start a message bus publish storm, unload plugin mid-storm. Assert handler count correct after unload.
   - `[Fact] AssemblyLoadContextGC_AfterUnload_NoLeak` — hold WeakReference to unloaded context, GC.Collect, assert WeakReference.IsAlive == false
2. Use `Parallel.ForEachAsync` and `Task.WhenAll` for concurrent pressure. Use Coyote `SystematicTestingEngine` for non-determinism coverage where applicable.
3. Each test should have a timeout of 60 seconds to catch deadlocks.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~ConcurrentLifecycle" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 5+ concurrent lifecycle tests pass. 100+ load/unload cycles proven safe with active I/O. No subscription leaks, no torn state, no AssemblyLoadContext memory leaks.</done>
</task>

</tasks>

<verification>
All tests in Chaos/PluginFault/ pass (both Plan 01 and Plan 02 tests):
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~PluginFault" -c Release -v normal
```
</verification>

<success_criteria>
- 5+ concurrent lifecycle tests passing
- 100+ load/unload cycles without torn state
- Zero orphaned message bus subscriptions
- AssemblyLoadContext properly garbage collected after unload
- Report: "Stage 3 - Steps 1-2 - Concurrent Plugin Lifecycle"
</success_criteria>

<output>
After completion, create `.planning/phases/107-chaos-plugin-faults-lifecycle/107-02-SUMMARY.md`
</output>
