---
phase: 107-chaos-plugin-faults-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Hardening.Tests/Chaos/PluginFault/PluginFaultInjectionTests.cs
  - DataWarehouse.Hardening.Tests/Chaos/PluginFault/FaultyTestPlugin.cs
  - DataWarehouse.Hardening.Tests/Chaos/PluginFault/HealthyTestPlugin.cs
  - DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj
autonomous: true
requirements:
  - CHAOS-01

must_haves:
  truths:
    - "Kernel catches fatal plugin exceptions (OOM, StackOverflow) without crashing"
    - "Faulted plugin is unloaded via AssemblyLoadContext"
    - "Other plugins continue operating after one plugin faults"
    - "Message bus subscriptions from faulted plugin are cleaned up"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Chaos/PluginFault/PluginFaultInjectionTests.cs"
      provides: "Fault injection test cases"
      min_lines: 150
    - path: "DataWarehouse.Hardening.Tests/Chaos/PluginFault/FaultyTestPlugin.cs"
      provides: "Test plugin that throws fatal exceptions on command"
      min_lines: 50
    - path: "DataWarehouse.Hardening.Tests/Chaos/PluginFault/HealthyTestPlugin.cs"
      provides: "Control plugin that must survive peer faults"
      min_lines: 30
  key_links:
    - from: "PluginFaultInjectionTests.cs"
      to: "DataWarehouse.SDK Kernel"
      via: "Kernel.LoadPlugin / Kernel.UnloadPlugin"
      pattern: "AssemblyLoadContext"
    - from: "FaultyTestPlugin.cs"
      to: "SDK PluginBase"
      via: "extends PluginBase, throws in overridden methods"
      pattern: "throw new OutOfMemoryException|StackOverflowException"
---

<objective>
Plugin fault injection chaos testing — prove the Kernel survives fatal exceptions thrown inside plugin methods mid-operation.

Purpose: Validate Kernel isolation guarantees. A single misbehaving plugin must not bring down the entire system.
Output: Test suite in DataWarehouse.Hardening.Tests/Chaos/PluginFault/ with report "Stage 3 - Steps 1-2 - Plugin Fault Injection"
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-core.md
@DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fault injection test plugins and test infrastructure</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/PluginFault/FaultyTestPlugin.cs,
    DataWarehouse.Hardening.Tests/Chaos/PluginFault/HealthyTestPlugin.cs,
    DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj
  </files>
  <action>
1. Read `docs/ai-maps/map-core.md` to find the Kernel plugin loading contracts (PluginBase, IPlugin, AssemblyLoadContext usage).
2. Read actual Kernel source for plugin load/unload methods and how it catches exceptions.
3. Create `FaultyTestPlugin.cs` — a minimal plugin extending SDK PluginBase that:
   - Has a configurable fault mode (OOM, StackOverflow, AccessViolation, TaskCanceled)
   - Throws the configured fatal exception inside its Initialize(), Execute(), or message handler methods
   - Uses a static trigger flag so tests can control WHEN the fault fires (not during load, but mid-operation)
4. Create `HealthyTestPlugin.cs` — a minimal plugin extending SDK PluginBase that:
   - Increments an atomic counter on each successful operation
   - Subscribes to message bus events and tracks received messages
   - Provides a `IsHealthy` property tests can assert against
5. Update `.csproj` if needed to add ProjectReferences for any SDK types used. The existing ProjectReference to DataWarehouse.SDK should suffice. Add `<ProjectReference>` to the Kernel project if needed for integration testing.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Hardening.Tests/ -c Release 2>&1 | tail -5</automated>
  </verify>
  <done>Both test plugins compile, extend correct SDK base classes, FaultyTestPlugin can throw configurable fatal exceptions on demand</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement plugin fault injection tests</name>
  <files>
    DataWarehouse.Hardening.Tests/Chaos/PluginFault/PluginFaultInjectionTests.cs
  </files>
  <behavior>
    - Test: Kernel loads FaultyTestPlugin + HealthyTestPlugin, FaultyTestPlugin throws OOM mid-operation → Kernel catches, unloads faulted plugin, HealthyTestPlugin continues
    - Test: Kernel loads FaultyTestPlugin throwing StackOverflowException → Kernel isolates without process crash
    - Test: After fault + unload, message bus subscription count equals only HealthyTestPlugin's subscriptions (no orphans)
    - Test: Multiple sequential faults (load faulty, fault, unload, reload, fault again) — Kernel handles all without degradation
    - Test: Fault during active message bus publish — message still delivered to healthy subscribers
  </behavior>
  <action>
1. Read Kernel source to understand exact plugin lifecycle (Load → Initialize → Execute → Unload) and how AssemblyLoadContext isolation works.
2. Create `PluginFaultInjectionTests.cs` with xUnit test class:
   - `[Fact] KernelIsolates_OomFault_OtherPluginsContinue` — boot Kernel, load both plugins, trigger OOM in faulty plugin, assert healthy plugin still processes operations
   - `[Fact] KernelIsolates_StackOverflow_NoProcessCrash` — trigger StackOverflow, assert Kernel still responsive
   - `[Fact] MessageBusCleanup_AfterFault_NoOrphanedSubscriptions` — after fault+unload, verify subscription count matches expected
   - `[Fact] RepeatedFaults_KernelRemainsStable` — 10 cycles of load→fault→unload with healthy plugin running throughout
   - `[Fact] FaultDuringPublish_HealthySubscribersStillReceive` — publish message where one subscriber faults, others still get it
3. Each test must set up a real (not mocked) Kernel instance with AssemblyLoadContext isolation. Use Coyote `SystematicTestingEngine` if available for non-determinism exploration.
4. Add XML doc comments on each test explaining the chaos scenario.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~PluginFault" -c Release --no-build 2>&1 | tail -20</automated>
  </verify>
  <done>All 5+ plugin fault injection tests pass. Kernel proven to isolate faulted plugins without crashing, clean up subscriptions, and keep healthy plugins running.</done>
</task>

</tasks>

<verification>
All tests in Chaos/PluginFault/ pass:
```
dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~PluginFault" -c Release -v normal
```
</verification>

<success_criteria>
- 5+ fault injection tests passing
- Kernel catches OOM and StackOverflow without process crash
- Healthy plugin continues operating after peer fault
- Message bus subscriptions cleaned up after plugin unload
- Report: "Stage 3 - Steps 1-2 - Plugin Fault Injection"
</success_criteria>

<output>
After completion, create `.planning/phases/107-chaos-plugin-faults-lifecycle/107-01-SUMMARY.md`
</output>
