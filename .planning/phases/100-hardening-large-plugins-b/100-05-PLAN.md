---
phase: 100-hardening-large-plugins-b
plan: 05
type: execute
wave: 5
depends_on: ["100-04"]
files_modified:
  - DataWarehouse.Hardening.Tests/UltimateRAID/
  - Plugins/DataWarehouse.Plugins.UltimateRAID/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "Findings 1-190 of UltimateRAID have corresponding failing tests before fixes"
    - "Findings 1-190 of UltimateRAID have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/UltimateRAID/"
      provides: "Hardening tests for UltimateRAID findings 1-190"
    - path: "Plugins/DataWarehouse.Plugins.UltimateRAID/"
      provides: "Production fixes for UltimateRAID findings 1-190"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/UltimateRAID/"
      to: "Plugins/DataWarehouse.Plugins.UltimateRAID/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*UltimateRAID"
---

<objective>
Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateRAID [findings 1-190]

Fix findings 1-190 (of 380 total) in UltimateRAID project using TDD methodology.
Severity breakdown across full project: 19 critical, 21 high, 229 medium, 111 low.
Source: CONSOLIDATED-FINDINGS.md lines 8803-9002.

This first half covers strategy-level and feature findings including:
- RAID 5/6 writes throw NotSupportedException (CRITICAL #203)
- WriteToDiskAsync with no WAL transaction (CRITICAL #232-233, #236, #242, #245, #247)
- Deduplication using DateTime.UtcNow.Ticks for storage addresses (CRITICAL #264)
- Snapshot stubs — RestoreBlockAsync, CompressBlock, EncryptBlock all no-ops (CRITICAL #288)
- RAID 10 health check incorrect per-pair logic (HIGH #55)
- BadBlockRemapping returning fake data (HIGH #262)
- Deduplication unsynchronized ReferenceCount (HIGH #263)
- GeoRaid no real parity computation (HIGH #267)
- Monitoring returning hardcoded strings (HIGH #268)
- PerformanceOptimization fire-and-forget prefetch (HIGH #274)
- RaidLevelMigration all stubs (HIGH #282)
- RaidPluginMigration stubs (HIGH #285)
- Snapshots unsynchronized CoW counters (HIGH #286)
- Massive PossibleMultipleEnumeration across AdaptiveRaid, ErasureCoding, ExtendedRaid, NestedRaid, VendorRaid strategies
- Naming convention fixes (LBA, DiskIO, etc.)

Purpose: Harden the first half of UltimateRAID — adaptive, erasure coding, extended, nested RAID strategies, features, and base classes — against all audit-identified vulnerabilities.
Output: ~190 hardening tests + ~190 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 8803-9002 — UltimateRAID findings 1-190)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop — write tests, confirm RED, fix production code, confirm GREEN for UltimateRAID findings 1-190</name>
  <files>
    DataWarehouse.Hardening.Tests/UltimateRAID/*.cs
    Plugins/DataWarehouse.Plugins.UltimateRAID/**/*.cs
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 8803-9002 sequentially (findings 1-190 of UltimateRAID section).

For EACH finding, follow the TDD loop:
1. Write a test that proves the vulnerability exists
2. Run dotnet test on the new test — confirm it FAILS (RED)
3. Fix the production code
4. Run dotnet test — confirm it PASSES (GREEN)
5. Move to next finding

Key fix patterns for this batch:
- CRITICAL #203: Implement RAID 5/6 write operations (not NotSupportedException)
- CRITICAL #232-247: Add WAL transaction wrapping around WriteToDiskAsync, use FileOptions.WriteThrough
- CRITICAL #264: Replace DateTime.UtcNow.Ticks with proper address allocation (atomic counter)
- CRITICAL #288: Implement real RestoreBlockAsync, CompressBlock, EncryptBlock
- HIGH #55: Fix RAID 10 health check to verify per-mirror-pair, not just total count
- HIGH #262: Implement real block I/O for BadBlockRemapping
- HIGH #263: Add synchronization for DedupEntry mutation
- HIGH #267: Implement real XOR parity computation for GeoRaid
- HIGH #268: Connect Monitoring to actual RAID state (not hardcoded strings)
- HIGH #274: Observe prefetch Task results, add error logging
- HIGH #282, #285: Implement real migration data movement or throw NotSupportedException
- HIGH #286: Add locking for snapshot CoW block counters
- MEDIUM PossibleMultipleEnumeration (bulk): Materialize IEnumerable with .ToList() — this is the dominant pattern (~100+ findings)
- MEDIUM ConditionAlwaysTrue/False: Fix null-check logic
- LOW naming: Rename LBA->Lba, DiskIO->DiskIo, etc.
- LOW unused fields/collections: Remove or implement

After all 190 findings are fixed, run full test suite.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~UltimateRAID" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 190 findings have tests and fixes. Tests pass. Solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Post-commit sanity — full solution build and test</name>
  <files>DataWarehouse.Hardening.Tests/UltimateRAID/</files>
  <action>
1. Run dotnet build on the full solution to verify 0 errors
2. Run dotnet test on the full test suite to verify no regressions
3. Commit all changes with message: "fix(100-05): harden UltimateRAID findings 1-190 — TDD loop complete"
4. Report: "Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateRAID [1-190]"
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3 && dotnet test DataWarehouse.Hardening.Tests/ --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>Solution builds with 0 errors. All tests pass. Changes committed.</done>
</task>

</tasks>

<verification>
- All 190 UltimateRAID findings (1-190) have hardening tests
- All hardening tests pass after production fixes
- Solution builds with 0 errors
- Full test suite passes (no regressions)
</verification>

<success_criteria>
- 190 findings processed via TDD loop (test -> red -> fix -> green)
- 0 build errors
- 0 test failures
- 1 commit for this batch
</success_criteria>

<output>
After completion, create `.planning/phases/100-hardening-large-plugins-b/100-05-SUMMARY.md`
</output>
