---
phase: 100-hardening-large-plugins-b
plan: 07
type: execute
wave: 7
depends_on: ["100-06"]
files_modified:
  - DataWarehouse.Hardening.Tests/UltimateDataManagement/
  - Plugins/DataWarehouse.Plugins.UltimateDataManagement/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "Findings 1-143 of UltimateDataManagement have corresponding failing tests before fixes"
    - "Findings 1-143 of UltimateDataManagement have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/UltimateDataManagement/"
      provides: "Hardening tests for UltimateDataManagement findings 1-143"
    - path: "Plugins/DataWarehouse.Plugins.UltimateDataManagement/"
      provides: "Production fixes for UltimateDataManagement findings 1-143"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/UltimateDataManagement/"
      to: "Plugins/DataWarehouse.Plugins.UltimateDataManagement/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*UltimateDataManagement"
---

<objective>
Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateDataManagement [findings 1-143]

Fix findings 1-143 (of 285 total) in UltimateDataManagement project using TDD methodology.
Severity breakdown across full project: 2 critical, 57 high, 107 medium, 119 low.
Source: CONSOLIDATED-FINDINGS.md lines 6202-6350.

This first half covers strategy-level findings including:
- DistributedCacheStrategy sync-over-async deadlock in constructor (CRITICAL #67)
- EventSourcingStrategies nested-lock deadlock (CRITICAL #73)
- Numerous Inconsistent synchronization on field (HIGH #62, #70, #82-83, #94-95, #104-105, #108-109, #113-114, #117, #119-123)
- FullTextIndex duplicate keys in HashSet init (HIGH #78)
- DirectoryShardingStrategy async void lambda (HIGH #64)
- DataWarehouseWriteFanOutOrchestrator field sync issues (HIGH #62)
- DistributedCacheStrategy raw NetworkStream without sync (HIGH #68)
- EventSourcingStrategies non-thread-safe List in ConcurrentDictionary (HIGH #71)
- TamperProofFanOutStrategy rollback failure swallowed (HIGH #135)
- AiEnhancedStrategyBase subscription silently swallowed (HIGH #150)
- 10+ stub strategies without IsProductionReady flag (MEDIUM #1)
- ~35 bare catch blocks across management strategies (MEDIUM #2)
- PossibleMultipleEnumeration across multiple files
- Naming convention fixes (TTL, PII, CO2, etc.)

Purpose: Harden the first half of UltimateDataManagement — caching, event sourcing, deduplication, indexing, sharding, and fan-out strategies — against all audit-identified vulnerabilities.
Output: ~143 hardening tests + ~143 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 6202-6350 — UltimateDataManagement findings 1-143)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop — write tests, confirm RED, fix production code, confirm GREEN for UltimateDataManagement findings 1-143</name>
  <files>
    DataWarehouse.Hardening.Tests/UltimateDataManagement/*.cs
    Plugins/DataWarehouse.Plugins.UltimateDataManagement/**/*.cs
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 6202-6350 sequentially (findings 1-143 of UltimateDataManagement section).

For EACH finding, follow the TDD loop:
1. Write a test that proves the vulnerability exists
2. Run dotnet test on the new test — confirm it FAILS (RED)
3. Fix the production code
4. Run dotnet test — confirm it PASSES (GREEN)
5. Move to next finding

Key fix patterns for this batch:
- CRITICAL #67: Replace GetAwaiter().GetResult() in constructor with async factory pattern
- CRITICAL #73: Fix lock ordering in EventSourcingStrategies to prevent deadlock
- HIGH sync issues: Add proper locking/ConcurrentDictionary for all inconsistent synchronization findings
- HIGH #78: Fix duplicate "the" key in FullTextIndex HashSet initialization
- HIGH #64: Convert async void lambda to async Task with error handling
- HIGH #68: Add synchronization for NetworkStream access in DistributedCache
- HIGH #71: Replace List with ConcurrentBag/ConcurrentQueue in ConcurrentDictionary values
- HIGH #135: Add proper error handling for rollback failures
- MEDIUM bare catches: Add specific exception handling and logging
- MEDIUM PossibleMultipleEnumeration: Materialize with .ToList()
- MEDIUM MemberHidesStaticFromOuterClass: Rename or qualify
- LOW naming: TTL->Ttl, PII->Pii, CO2->Co2, etc.
- LOW unused fields: Remove or implement

After all 143 findings are fixed, run full test suite.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~UltimateDataManagement" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 143 findings have tests and fixes. Tests pass. Solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Post-commit sanity — full solution build and test</name>
  <files>DataWarehouse.Hardening.Tests/UltimateDataManagement/</files>
  <action>
1. Run dotnet build on the full solution to verify 0 errors
2. Run dotnet test on the full test suite to verify no regressions
3. Commit all changes with message: "fix(100-07): harden UltimateDataManagement findings 1-143 — TDD loop complete"
4. Report: "Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateDataManagement [1-143]"
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3 && dotnet test DataWarehouse.Hardening.Tests/ --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>Solution builds with 0 errors. All tests pass. Changes committed.</done>
</task>

</tasks>

<verification>
- All 143 UltimateDataManagement findings (1-143) have hardening tests
- All hardening tests pass after production fixes
- Solution builds with 0 errors
- Full test suite passes (no regressions)
</verification>

<success_criteria>
- 143 findings processed via TDD loop (test -> red -> fix -> green)
- 0 build errors
- 0 test failures
- 1 commit for this batch
</success_criteria>

<output>
After completion, create `.planning/phases/100-hardening-large-plugins-b/100-07-SUMMARY.md`
</output>
