---
phase: 100-hardening-large-plugins-b
plan: 03
type: execute
wave: 3
depends_on: ["100-02"]
files_modified:
  - DataWarehouse.Hardening.Tests/UltimateKeyManagement/
  - Plugins/DataWarehouse.Plugins.UltimateKeyManagement/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]

must_haves:
  truths:
    - "Findings 1-190 of UltimateKeyManagement have corresponding failing tests before fixes"
    - "Findings 1-190 of UltimateKeyManagement have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/UltimateKeyManagement/"
      provides: "Hardening tests for UltimateKeyManagement findings 1-190"
    - path: "Plugins/DataWarehouse.Plugins.UltimateKeyManagement/"
      provides: "Production fixes for UltimateKeyManagement findings 1-190"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/UltimateKeyManagement/"
      to: "Plugins/DataWarehouse.Plugins.UltimateKeyManagement/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*UltimateKeyManagement"
---

<objective>
Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateKeyManagement [findings 1-190]

Fix findings 1-190 (of 380 total) in UltimateKeyManagement project using TDD methodology.
Severity breakdown across full project: 14 critical, 87 high, 114 medium, 165 low.
Source: CONSOLIDATED-FINDINGS.md lines 8285-8481.

This first half covers strategy-level and infrastructure findings including:
- DeviceAuthenticationStrategy using Guid.NewGuid() for auth tokens (CRITICAL #144-148)
- Bare catch on deserialization of persisted key state (MEDIUM #1)
- AnalyticsStrategies returning hardcoded fake data (HIGH #12-14)
- ConvergenceDetector thread-unsafe reads (HIGH #30-31)
- DifferentialPrivacyIntegration budget violation via unsync'd double (HIGH #37-38)
- PluginMigrationHelper disposed variable access (HIGH #128-129)
- ProtocolStrategies returning Random values for industrial systems (HIGH #135-136)
- SensorIngestionStrategies unbounded channels (HIGH #153)
- Numerous naming convention issues (FrostStrategy, SmpcVaultStrategy, OnlyKeyStrategy)
- HttpClient creation per-call patterns (MEDIUM #8)
- NotSupportedException throws needing proper handling (MEDIUM #18, #154, #197, #202)

Purpose: Harden the first half of UltimateKeyManagement — core strategies, analytics, security, protocols, and sensor ingestion — against all audit-identified vulnerabilities.
Output: ~190 hardening tests + ~190 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 8285-8481 — UltimateKeyManagement findings 1-190)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop — write tests, confirm RED, fix production code, confirm GREEN for UltimateKeyManagement findings 1-190</name>
  <files>
    DataWarehouse.Hardening.Tests/UltimateKeyManagement/*.cs
    Plugins/DataWarehouse.Plugins.UltimateKeyManagement/**/*.cs
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 8285-8481 sequentially (findings 1-190 of UltimateKeyManagement section).

For EACH finding, follow the TDD loop:
1. Write a test that proves the vulnerability exists
2. Run dotnet test on the new test — confirm it FAILS (RED)
3. Fix the production code
4. Run dotnet test — confirm it PASSES (GREEN)
5. Move to next finding

Key fix patterns for this batch:
- CRITICAL #144-148: Replace Guid.NewGuid().ToByteArray() with RandomNumberGenerator.GetBytes() for auth tokens
- HIGH #12-14: Replace hardcoded analytics data with real calculations or throw NotSupportedException
- HIGH #30-31, #37-38: Add proper locking/Interlocked for thread-unsafe field access
- HIGH #128-129: Fix disposed variable capture in lambda closures
- HIGH #135-136: Replace Random register/node values with proper protocol handling
- HIGH #153: Use Channel.CreateBounded instead of Unbounded to prevent OOM
- MEDIUM #1: Replace bare catch with specific exception handling and logging
- MEDIUM #8: Use static HttpClient or IHttpClientFactory pattern
- MEDIUM naming: Rename to match C# conventions (FrostStrategy variables, OnlyKey constants, etc.)
- LOW unused fields: Remove or implement

After all 190 findings are fixed, run full test suite.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~UltimateKeyManagement" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 190 findings have tests and fixes. Tests pass. Solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Post-commit sanity — full solution build and test</name>
  <files>DataWarehouse.Hardening.Tests/UltimateKeyManagement/</files>
  <action>
1. Run dotnet build on the full solution to verify 0 errors
2. Run dotnet test on the full test suite to verify no regressions
3. Commit all changes with message: "fix(100-03): harden UltimateKeyManagement findings 1-190 — TDD loop complete"
4. Report: "Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateKeyManagement [1-190]"
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3 && dotnet test DataWarehouse.Hardening.Tests/ --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>Solution builds with 0 errors. All tests pass. Changes committed.</done>
</task>

</tasks>

<verification>
- All 190 UltimateKeyManagement findings (1-190) have hardening tests
- All hardening tests pass after production fixes
- Solution builds with 0 errors
- Full test suite passes (no regressions)
</verification>

<success_criteria>
- 190 findings processed via TDD loop (test -> red -> fix -> green)
- 0 build errors
- 0 test failures
- 1 commit for this batch
</success_criteria>

<output>
After completion, create `.planning/phases/100-hardening-large-plugins-b/100-03-SUMMARY.md`
</output>
