---
phase: 100-hardening-large-plugins-b
plan: 01
type: execute
wave: 1
depends_on: ["099-11"]
files_modified:
  - DataWarehouse.Hardening.Tests/UltimateAccessControl/
  - Plugins/DataWarehouse.Plugins.UltimateAccessControl/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]

must_haves:
  truths:
    - "Findings 1-205 of UltimateAccessControl have corresponding failing tests before fixes"
    - "Findings 1-205 of UltimateAccessControl have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/UltimateAccessControl/"
      provides: "Hardening tests for UltimateAccessControl findings 1-205"
    - path: "Plugins/DataWarehouse.Plugins.UltimateAccessControl/"
      provides: "Production fixes for UltimateAccessControl findings 1-205"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/UltimateAccessControl/"
      to: "Plugins/DataWarehouse.Plugins.UltimateAccessControl/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*UltimateAccessControl"
---

<objective>
Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateAccessControl [findings 1-205]

Fix findings 1-205 (of 409 total) in UltimateAccessControl project using TDD methodology.
Severity breakdown across full project: 25 critical, 139 high, 109 medium, 136 low.
Source: CONSOLIDATED-FINDINGS.md lines 3865-4083.

This first half covers strategy-level findings including:
- Sync-over-async deadlocks in AccessAudit and PolicyBasedAC (CRITICAL #1)
- lock(this) anti-pattern in AccessControlStrategyBase (CRITICAL #93)
- Timing-unsafe MFA code comparison (CRITICAL #163)
- Fail-open clearance validation (CRITICAL #191)
- 15+ strategies throwing NotSupportedException from EvaluateAccessAsync (HIGH #2)
- Fire-and-forget Task.Run without error handling (HIGH #3)
- Inconsistent field synchronization (HIGH #74, #149)
- Hardcoded/stub security evaluations (HIGH #153-186)
- Thread-unsafe collections in concurrent strategies (HIGH #157, #177, #182-183)
- Numerous PossibleMultipleEnumeration, naming, and null-check issues

Purpose: Harden the first half of UltimateAccessControl — core access control strategies, clearance, steganography, biometrics, and feature classes — against all audit-identified vulnerabilities.
Output: ~205 hardening tests + ~205 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 3865-4083 — UltimateAccessControl findings 1-205)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop — write tests, confirm RED, fix production code, confirm GREEN for UltimateAccessControl findings 1-205</name>
  <files>
    DataWarehouse.Hardening.Tests/UltimateAccessControl/*.cs
    Plugins/DataWarehouse.Plugins.UltimateAccessControl/**/*.cs
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 3865-4083 sequentially (findings 1-205 of UltimateAccessControl section).

For EACH finding, follow the TDD loop:
1. Write a test in DataWarehouse.Hardening.Tests/UltimateAccessControl/ that proves the vulnerability exists (e.g., test that sync-over-async deadlocks, test that lock(this) allows external deadlock, test that MFA comparison is timing-unsafe, test that clearance validation fails open)
2. Run dotnet test on the new test — confirm it FAILS (RED)
3. Fix the production code in Plugins/DataWarehouse.Plugins.UltimateAccessControl/
4. Run dotnet test — confirm it PASSES (GREEN)
5. Move to next finding

Group tests by source file for organization (e.g., AccessAuditLoggingStrategyTests.cs, MfaOrchestratorTests.cs, AclScalingManagerTests.cs).

Key fix patterns for this batch:
- CRITICAL #1: Replace sync-over-async with proper async/await
- CRITICAL #93: Replace lock(this) with private readonly object _lock
- CRITICAL #163: Replace string.Equals with CryptographicOperations.FixedTimeEquals for MFA codes
- CRITICAL #191: Change fail-open to fail-closed when _clearanceFrameworks is empty
- HIGH async void/lambda: Convert to proper async Task with error handling
- HIGH thread safety: Add locks or use ConcurrentDictionary where concurrent access detected
- HIGH stubs: Implement real logic or throw PlatformNotSupportedException with clear message
- MEDIUM PossibleMultipleEnumeration: Materialize IEnumerable with .ToList() before multiple iterations
- MEDIUM ConditionAlwaysTrue/False: Fix null-check logic
- LOW naming: Rename to match C# conventions
- LOW unused fields: Remove or implement usage

After all 205 findings are fixed, run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~UltimateAccessControl" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 205 findings have tests and fixes. Tests pass. Solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Post-commit sanity — full solution build and test</name>
  <files>DataWarehouse.Hardening.Tests/UltimateAccessControl/</files>
  <action>
1. Run dotnet build on the full solution to verify 0 errors
2. Run dotnet test on the full test suite to verify no regressions
3. Commit all changes with message: "fix(100-01): harden UltimateAccessControl findings 1-205 — TDD loop complete"
4. Report: "Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateAccessControl [1-205]"
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3 && dotnet test DataWarehouse.Hardening.Tests/ --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>Solution builds with 0 errors. All tests pass. Changes committed.</done>
</task>

</tasks>

<verification>
- All 205 UltimateAccessControl findings (1-205) have hardening tests
- All hardening tests pass after production fixes
- Solution builds with 0 errors
- Full test suite passes (no regressions)
</verification>

<success_criteria>
- 205 findings processed via TDD loop (test -> red -> fix -> green)
- 0 build errors
- 0 test failures
- 1 commit for this batch
</success_criteria>

<output>
After completion, create `.planning/phases/100-hardening-large-plugins-b/100-01-SUMMARY.md`
</output>
