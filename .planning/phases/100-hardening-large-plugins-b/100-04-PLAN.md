---
phase: 100-hardening-large-plugins-b
plan: 04
type: execute
wave: 4
depends_on: ["100-03"]
files_modified:
  - DataWarehouse.Hardening.Tests/UltimateKeyManagement/
  - Plugins/DataWarehouse.Plugins.UltimateKeyManagement/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "Findings 191-380 of UltimateKeyManagement have corresponding failing tests before fixes"
    - "Findings 191-380 of UltimateKeyManagement have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/UltimateKeyManagement/"
      provides: "Hardening tests for UltimateKeyManagement findings 191-380"
    - path: "Plugins/DataWarehouse.Plugins.UltimateKeyManagement/"
      provides: "Production fixes for UltimateKeyManagement findings 191-380"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/UltimateKeyManagement/"
      to: "Plugins/DataWarehouse.Plugins.UltimateKeyManagement/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*UltimateKeyManagement"
---

<objective>
Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateKeyManagement [findings 191-380]

Fix findings 191-380 (of 380 total) in UltimateKeyManagement project using TDD methodology.
Source: CONSOLIDATED-FINDINGS.md lines 8482-8671.

This second half covers Features, Hardware, HSM, Cloud KMS, Container, Password-Derived, Platform, Privacy, Threshold, and Plugin-level findings including:
- DigitalOceanVault no-ops — keys never persisted (CRITICAL #239)
- BiometricDerivedKey BCH parity completely broken (CRITICAL #290)
- ThresholdBls12381 signature verification always returns true (CRITICAL #354)
- Password stored as plain string field (CRITICAL #323)
- Plaintext key persistence across multiple strategies (HIGH #263, #302, #307, #316)
- Silent catches losing key data at startup (HIGH #289, #326, #349, #352, #355-356)
- BreakGlassAccess audit trail eviction (HIGH #218)
- KeyDerivationHierarchy ignoring caller-supplied salt (HIGH #223)
- KeyEscrowRecovery storing unencrypted shares (HIGH #224-225)
- PassStrategy path traversal (HIGH #258)
- Plugin-level silent catches and fire-and-forget (HIGH #358-359)
- Token refresh races, SSRF, thread safety across cloud/HSM strategies

Purpose: Complete UltimateKeyManagement hardening — all 380 findings covered across plans 03 and 04.
Output: ~190 hardening tests + ~190 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 8482-8671 — UltimateKeyManagement findings 191-380)
@.planning/phases/100-hardening-large-plugins-b/100-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop — write tests, confirm RED, fix production code, confirm GREEN for UltimateKeyManagement findings 191-380</name>
  <files>
    DataWarehouse.Hardening.Tests/UltimateKeyManagement/*.cs
    Plugins/DataWarehouse.Plugins.UltimateKeyManagement/**/*.cs
  </files>
  <action>
Process CONSOLIDATED-FINDINGS.md lines 8482-8671 sequentially (findings 191-380 of UltimateKeyManagement section).

For EACH finding, follow the TDD loop:
1. Write a test that proves the vulnerability exists
2. Run dotnet test on the new test — confirm it FAILS (RED)
3. Fix the production code
4. Run dotnet test — confirm it PASSES (GREEN)
5. Move to next finding

Key fix patterns for this batch:
- CRITICAL #239: Implement actual key persistence for DigitalOceanVault (not no-ops)
- CRITICAL #290: Implement proper BCH generator polynomial (not SHA256 hash)
- CRITICAL #323: Replace string password storage with SecureString or byte[] with zeroing
- CRITICAL #354: Implement real BLS12-381 signature verification (not length-check-only)
- HIGH plaintext keys: Encrypt key material before persisting to disk
- HIGH silent catches: Add proper exception logging and re-throw for fatal exceptions
- HIGH path traversal: Sanitize keyId/path inputs against directory traversal
- HIGH token races: Add synchronization for token refresh across cloud KMS strategies
- HIGH fire-and-forget: Observe Task results, add error logging
- MEDIUM empty catches: Add logging, distinguish error types
- MEDIUM using-variable init: Move property init inside using block
- LOW naming: Rename to match C# conventions

After all findings are fixed, run full test suite.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~UltimateKeyManagement" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 190 findings have tests and fixes. Tests pass. Solution builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Post-commit sanity — full solution build and test</name>
  <files>DataWarehouse.Hardening.Tests/UltimateKeyManagement/</files>
  <action>
1. Run dotnet build on the full solution to verify 0 errors
2. Run dotnet test on the full test suite to verify no regressions
3. Commit all changes with message: "fix(100-04): harden UltimateKeyManagement findings 191-380 — TDD loop complete"
4. Report: "Stage 1 - Step 1-3 - Large Plugins B Hardening: UltimateKeyManagement [191-380]"
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3 && dotnet test DataWarehouse.Hardening.Tests/ --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>Solution builds with 0 errors. All tests pass. Changes committed. UltimateKeyManagement fully hardened (380/380).</done>
</task>

</tasks>

<verification>
- All 190 UltimateKeyManagement findings (191-380) have hardening tests
- All hardening tests pass after production fixes
- Solution builds with 0 errors
- Full test suite passes (no regressions)
- Combined with Plan 03: all 380 UltimateKeyManagement findings complete
</verification>

<success_criteria>
- 190 findings processed via TDD loop (test -> red -> fix -> green)
- 0 build errors
- 0 test failures
- 1 commit for this batch
</success_criteria>

<output>
After completion, create `.planning/phases/100-hardening-large-plugins-b/100-04-SUMMARY.md`
</output>
