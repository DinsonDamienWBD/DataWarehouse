---
phase: 113-blockdevicefactory-cascade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBlockDevice.cs
  - DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBindings.cs
  - DataWarehouse.Hardening.Tests/SDK/BlockDevice/IoUringBlockDeviceTests.cs
autonomous: true
requirements: [SIO-02]

must_haves:
  truths:
    - "IoUringBlockDevice lives in IO/Linux namespace alongside its bindings, consistent with IO/Windows and IO/Unix layout"
    - "IoUringBlockDevice implements IBlockDevice with full read/write/flush/dispose contract"
    - "SQPoll mode and registered buffers are configurable via constructor parameters"
    - "Ring-per-thread design uses ThreadStatic with shared-ring overflow fallback"
    - "Non-Linux platforms get graceful fallback via FileBlockDevice (no PlatformNotSupportedException)"
    - "Tests verify contract compliance and fallback behavior on Windows"
  artifacts:
    - path: "DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBlockDevice.cs"
      provides: "Linux io_uring block device in correct IO namespace"
      contains: "class IoUringBlockDevice : IBlockDevice"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBindings.cs"
      provides: "LibUring P/Invoke bindings in correct IO namespace"
      contains: "class IoUringBindings"
    - path: "DataWarehouse.Hardening.Tests/SDK/BlockDevice/IoUringBlockDeviceTests.cs"
      provides: "Unit tests for IoUringBlockDevice contract and fallback"
      contains: "class IoUringBlockDeviceTests"
  key_links:
    - from: "DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBlockDevice.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBindings.cs"
      via: "IoUringBindings.IsAvailable and P/Invoke calls"
      pattern: "IoUringBindings\\."
    - from: "DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBlockDevice.cs"
      to: "DataWarehouse.SDK/VirtualDiskEngine/IBlockDevice.cs"
      via: "implements IBlockDevice"
      pattern: "IBlockDevice"
---

<objective>
Relocate IoUringBlockDevice from AdaptiveIndex namespace to IO/Linux namespace and ensure it is production-ready for factory cascade integration.

Purpose: The existing IoUringBlockDevice lives in AdaptiveIndex namespace (wrong location). Plan 113-04 needs it in IO/Linux/ so BlockDeviceFactory can reference it alongside the Windows and Unix implementations. This plan moves and validates the implementation.

Output: Production-ready IoUringBlockDevice + IoUringBindings in IO/Linux/ namespace with tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Source files to reference:
@DataWarehouse.SDK/VirtualDiskEngine/AdaptiveIndex/IoUringBlockDevice.cs
@DataWarehouse.SDK/VirtualDiskEngine/AdaptiveIndex/IoUringBindings.cs
@DataWarehouse.SDK/VirtualDiskEngine/IBlockDevice.cs
@DataWarehouse.SDK/VirtualDiskEngine/IO/BlockDeviceOptions.cs

<interfaces>
<!-- Key types and contracts the executor needs -->

From DataWarehouse.SDK/VirtualDiskEngine/IBlockDevice.cs:
```csharp
public interface IBlockDevice : IAsyncDisposable
{
    int BlockSize { get; }
    long BlockCount { get; }
    Task ReadBlockAsync(long blockNumber, Memory<byte> buffer, CancellationToken ct = default);
    Task WriteBlockAsync(long blockNumber, ReadOnlyMemory<byte> data, CancellationToken ct = default);
    Task FlushAsync(CancellationToken ct = default);
}
```

From DataWarehouse.SDK/VirtualDiskEngine/IO/BlockDeviceOptions.cs:
```csharp
public sealed class BlockDeviceOptions
{
    public bool DirectIo { get; set; }
    public BlockDeviceImplementation? ForceImplementation { get; set; }
    public int QueueDepth { get; set; } = 64;
    public bool UseSqPoll { get; set; }
    public int RegisteredBufferCount { get; set; } = 256;
    public bool AllowNonDwvd { get; set; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move IoUringBlockDevice + IoUringBindings to IO/Linux namespace</name>
  <files>
    DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBlockDevice.cs
    DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/IoUringBindings.cs
    DataWarehouse.SDK/VirtualDiskEngine/AdaptiveIndex/IoUringBlockDevice.cs
    DataWarehouse.SDK/VirtualDiskEngine/AdaptiveIndex/IoUringBindings.cs
  </files>
  <action>
    1. Create directory `DataWarehouse.SDK/VirtualDiskEngine/IO/Linux/`.

    2. Copy `AdaptiveIndex/IoUringBindings.cs` to `IO/Linux/IoUringBindings.cs`:
       - Change namespace from `DataWarehouse.SDK.VirtualDiskEngine.AdaptiveIndex` to `DataWarehouse.SDK.VirtualDiskEngine.IO.Linux`.
       - Keep all P/Invoke signatures, structs, and `IsAvailable` property exactly as-is. This is mature code -- no logic changes.
       - Add `[SupportedOSPlatform("linux")]` attribute on the class.

    3. Copy `AdaptiveIndex/IoUringBlockDevice.cs` to `IO/Linux/IoUringBlockDevice.cs`:
       - Change namespace from `DataWarehouse.SDK.VirtualDiskEngine.AdaptiveIndex` to `DataWarehouse.SDK.VirtualDiskEngine.IO.Linux`.
       - Add `using DataWarehouse.SDK.VirtualDiskEngine.IO.Linux;` if needed (for IoUringBindings).
       - Add `[SupportedOSPlatform("linux")]` attribute on the class.
       - Update `[SdkCompatibility]` notes to reference Phase 113 instead of Phase 86.
       - Keep ALL existing logic: ring-per-thread, registered buffers, SQPoll, NVMe passthrough, batched ReadBlocksAsync, fallback methods, dispose. Zero logic changes.
       - Remove the `Create` static factory method (that responsibility moves to BlockDeviceFactory in plan 113-04).
       - Add constructor overload that accepts `BlockDeviceOptions` to read `QueueDepth`, `UseSqPoll`, and `RegisteredBufferCount` from options:
         ```csharp
         public IoUringBlockDevice(string filePath, int blockSize, long blockCount, BlockDeviceOptions options)
             : this(filePath, blockSize, blockCount, options.QueueDepth, options.UseSqPoll)
         { }
         ```

    4. Update the OLD files in AdaptiveIndex/ to become thin forwarding wrappers:
       - `AdaptiveIndex/IoUringBlockDevice.cs`: Change the class to inherit from `IO.Linux.IoUringBlockDevice` (or make it a `[Obsolete]` type alias using `using IoUringBlockDevice = DataWarehouse.SDK.VirtualDiskEngine.IO.Linux.IoUringBlockDevice;`) to avoid breaking AdaptiveIndex code that references the old location. Simplest: keep the old file but make the class body just `[Obsolete("Use DataWarehouse.SDK.VirtualDiskEngine.IO.Linux.IoUringBlockDevice")] internal sealed class IoUringBlockDevice_Deprecated {}` and add a using alias at the namespace level. Actually simplest approach: delete old files entirely and fix any using references in AdaptiveIndex code that referenced them.
       - Search for ALL references to `AdaptiveIndex.IoUringBlockDevice` and `AdaptiveIndex.IoUringBindings` in the codebase and update imports to `IO.Linux.*`. Key files: `AdaptiveIndexEngine.cs` if it uses IoUringBlockDevice.Create().

    5. Grep for `AdaptiveIndex.IoUringB` across ALL .cs files to find every reference. Update each. Common locations:
       - `AdaptiveIndexEngine.cs` may call `IoUringBlockDevice.Create()` -- replace with `new FileBlockDevice(...)` or remove (factory handles this now).
       - Test files in Hardening.Tests that reference the old namespace.

    Do NOT modify any other block device implementation files (IoRing, WindowsOverlapped, Kqueue).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>
    IoUringBlockDevice.cs and IoUringBindings.cs exist in IO/Linux/ namespace.
    Old AdaptiveIndex files removed or deprecated.
    All codebase references updated.
    SDK project compiles with zero errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IoUringBlockDevice contract compliance tests</name>
  <files>
    DataWarehouse.Hardening.Tests/SDK/BlockDevice/IoUringBlockDeviceTests.cs
  </files>
  <behavior>
    - Test 1: Constructor validates null path throws ArgumentNullException
    - Test 2: Constructor validates blockSize < 512 throws ArgumentOutOfRangeException
    - Test 3: Constructor validates blockCount <= 0 throws ArgumentOutOfRangeException
    - Test 4: On non-Linux (Windows), ReadBlockAsync falls back to FileBlockDevice behavior (reads from temp file)
    - Test 5: On non-Linux (Windows), WriteBlockAsync falls back to FileBlockDevice behavior (writes to temp file, reads back correctly)
    - Test 6: On non-Linux (Windows), FlushAsync completes without exception
    - Test 7: DisposeAsync is idempotent (double dispose does not throw)
    - Test 8: BlockSize and BlockCount properties return constructor values
    - Test 9: ReadBlockAsync after DisposeAsync throws ObjectDisposedException
    - Test 10: Constructor with BlockDeviceOptions reads QueueDepth and UseSqPoll from options
  </behavior>
  <action>
    Create test class `IoUringBlockDeviceTests` in `DataWarehouse.Hardening.Tests/SDK/BlockDevice/` directory. Use xUnit. Since we're on Windows, all tests exercise the fallback path (IoUringBindings.IsAvailable returns false). This validates the contract compliance and graceful degradation.

    Pattern: Follow existing test conventions in DataWarehouse.Hardening.Tests. Use `[Fact]` for individual tests. Use temp files via `Path.GetTempFileName()` with cleanup in `IAsyncDisposable` test fixtures.

    Create a helper method `CreateTempDevice(int blockSize = 4096, int blockCount = 10)` that creates a temp file, pre-allocates it, and returns `(IoUringBlockDevice device, string path)`.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~IoUringBlockDeviceTests" --no-restore 2>&1 | tail -10</automated>
  </verify>
  <done>
    All 10 tests pass on Windows (fallback path).
    IoUringBlockDevice contract compliance verified.
  </done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds with zero errors
- `dotnet test --filter IoUringBlockDeviceTests` -- all tests green
- No remaining references to `AdaptiveIndex.IoUringBlockDevice` or `AdaptiveIndex.IoUringBindings` except deprecated markers
- `IoUringBlockDevice` exists in `DataWarehouse.SDK.VirtualDiskEngine.IO.Linux` namespace
</verification>

<success_criteria>
IoUringBlockDevice is relocated to IO/Linux/ namespace with full contract compliance tests passing on Windows via fallback path. Ready for BlockDeviceFactory integration in plan 113-04.
</success_criteria>

<output>
After completion, create `.planning/phases/113-blockdevicefactory-cascade/113-01-SUMMARY.md`
</output>
