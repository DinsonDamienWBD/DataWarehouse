---
phase: 52-clean-house
plan: 07
type: execute
wave: 3
depends_on: ["52-04", "52-05", "52-06"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "Zero TODO/FIXME/HACK/STUB/PLACEHOLDER comment markers in any production .cs file"
    - "Zero Assert.True(true) placeholder tests in any test file"
    - "Zero MemoryStream(0) stubs in any storage strategy"
    - "Zero placeholder WASM modules"
    - "Full solution builds with 0 errors, 0 warnings"
    - "All tests pass"
  artifacts: []
  key_links: []
---

<objective>
Final verification sweep of entire codebase to confirm Phase 52 success criteria are met.

Purpose: After Plans 01-06 complete the cleanup work, this plan does a comprehensive verification sweep to confirm zero markers remain and identifies any stragglers that were missed.

Output: Verification report confirming all Phase 52 success criteria are met, or fixes for any remaining issues.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/52-clean-house/52-01-SUMMARY.md
@.planning/phases/52-clean-house/52-02-SUMMARY.md
@.planning/phases/52-clean-house/52-03-SUMMARY.md
@.planning/phases/52-clean-house/52-04-SUMMARY.md
@.planning/phases/52-clean-house/52-05-SUMMARY.md
@.planning/phases/52-clean-house/52-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive marker sweep and straggler fix</name>
  <files></files>
  <action>
    Run a comprehensive sweep across the entire codebase for remaining markers:

    1. TODO/FIXME/HACK markers:
       `grep -rn "// TODO\|// FIXME\|// HACK" --include="*.cs" Plugins/ DataWarehouse.SDK/ DataWarehouse.Kernel/ DataWarehouse.CLI/ DataWarehouse.Shared/ DataWarehouse.Launcher/`
       Any results = fix them (implement the TODO or remove if obsolete).

    2. STUB/PLACEHOLDER markers:
       `grep -rni "// STUB\|// PLACEHOLDER\|placeholder" --include="*.cs" Plugins/ DataWarehouse.SDK/ DataWarehouse.Kernel/`
       Exclude legitimate uses (e.g., "placeholder" as a variable name in template logic). Fix any remaining stubs.

    3. MemoryStream(0) stubs:
       `grep -rn "new MemoryStream(0)" --include="*.cs" Plugins/ DataWarehouse.SDK/ DataWarehouse.Kernel/`
       Note: UltimateMultiCloud null-guard patterns `if (_client == null) return new MemoryStream(0)` are LEGITIMATE - these handle the case where cloud SDK client is not configured. Only fix if they appear in non-null-guard contexts.

    4. Assert.True(true) placeholder tests:
       `grep -rn "Assert.True(true)" --include="*.cs" DataWarehouse.Tests/`
       Any results = replace with real tests.

    5. GeneratePlaceholder references:
       `grep -rn "Placeholder\|placeholder" --include="*.cs" Plugins/DataWarehouse.Plugins.SelfEmulatingObjects/`
       Any results = fix.

    6. Full build:
       `dotnet build DataWarehouse.slnx`
       Must show 0 errors, 0 warnings.

    7. Full test run:
       `dotnet test DataWarehouse.Tests/ --no-restore`
       All tests must pass.

    If any stragglers are found, fix them in-place. Document all findings and fixes.

    EXCLUDE from this sweep (Phase 53 scope, NOT Phase 52):
    - TLS certificate bypass (`ServerCertificateCustomValidationCallback => true`)
    - Security/pentest findings
    - NotSupportedException (these are legitimate "operation not supported" for specific strategies)
  </action>
  <verify>
    dotnet build DataWarehouse.slnx 2>&1 | tail -3
    # Must show 0 errors, 0 warnings
    dotnet test DataWarehouse.Tests/ --no-restore 2>&1 | tail -5
    # All tests must pass
    grep -rn "// TODO\|// FIXME\|// HACK" --include="*.cs" Plugins/ DataWarehouse.SDK/ DataWarehouse.Kernel/ DataWarehouse.CLI/ | wc -l
    # Must be 0
    grep -rn "Assert.True(true)" --include="*.cs" DataWarehouse.Tests/ | wc -l
    # Must be 0
  </verify>
  <done>Zero TODO/FIXME/HACK markers in production code. Zero placeholder tests. Zero stub storage implementations. Build: 0 errors, 0 warnings. All tests pass. Phase 52 success criteria fully met.</done>
</task>

</tasks>

<verification>
# All Phase 52 success criteria verified:
# 1. Zero TODO/FIXME/HACK/STUB/PLACEHOLDER markers in production code
# 2. Zero placeholder test files
# 3. Zero mock/simulation implementations (MemoryStream(0) stubs)
# 4. Zero stub cloud SDKs (already resolved - NuGet packages exist)
# 5. PNG compression uses DEFLATE (already verified - was never HMAC)
# 6. Build: 0 errors, 0 warnings, all tests passing
</verification>

<success_criteria>
- Comprehensive sweep confirms zero remaining issues
- Any stragglers found are fixed
- Full build: 0 errors, 0 warnings
- Full test suite passes
- Phase 52 can be marked COMPLETE
</success_criteria>

<output>
After completion, create `.planning/phases/52-clean-house/52-07-SUMMARY.md`
</output>
