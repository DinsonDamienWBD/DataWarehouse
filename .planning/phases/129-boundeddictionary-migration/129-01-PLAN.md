---
phase: 129-boundeddictionary-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Metadata/boundeddictionary-audit.json
  - DataWarehouse.Hardening.Tests/Migration/BoundedDictionaryAuditTests.cs
autonomous: true
requirements: [PER-07]
must_haves:
  truths:
    - "Every BoundedDictionary instance in codebase is cataloged with plugin, file, usage pattern"
    - "Instances categorized as hot-path cache, session store, index, or metadata cache"
    - "Audit JSON enables automated migration tracking"
  artifacts:
    - path: "Metadata/boundeddictionary-audit.json"
      provides: "Complete audit of all BoundedDictionary instances"
      min_lines: 50
    - path: "DataWarehouse.Hardening.Tests/Migration/BoundedDictionaryAuditTests.cs"
      provides: "Test that verifies audit completeness against grep count"
      min_lines: 30
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Migration/BoundedDictionaryAuditTests.cs"
      to: "Metadata/boundeddictionary-audit.json"
      via: "Validates audit completeness"
      pattern: "boundeddictionary-audit\\.json"
---

<objective>
Audit all BoundedDictionary instances across the codebase and categorize by plugin and usage pattern.

Purpose: Create migration plan by understanding where and how BoundedDictionary is used before replacing with BoundedCache.
Output: Comprehensive audit JSON and validation test.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Utilities/BoundedDictionary.cs
@DataWarehouse.SDK/Contracts/Scaling/BoundedCache.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit All BoundedDictionary Instances</name>
  <files>Metadata/boundeddictionary-audit.json</files>
  <action>
Search the entire codebase for all BoundedDictionary usage using grep/ripgrep. For each instance, record:

{
  "audit_date": "2026-03-08",
  "total_instances": N,
  "instances": [
    {
      "file": "relative/path/to/file.cs",
      "line": N,
      "plugin": "PluginName or SDK",
      "class": "ContainingClassName",
      "field_name": "fieldName",
      "key_type": "string",
      "value_type": "SomeType",
      "capacity": 1000,
      "category": "hot-path-cache|session-store|index|metadata-cache|lookup-table",
      "usage_pattern": "brief description of what it caches",
      "migration_priority": "wave1|wave2",
      "migration_notes": "any special considerations"
    }
  ],
  "summary": {
    "by_plugin": { "PluginName": N, ... },
    "by_category": { "hot-path-cache": N, "session-store": N, ... },
    "by_priority": { "wave1": N, "wave2": N }
  }
}

Migration priority assignment:
- wave1 (top 20): Intelligence, Storage, Compression, Encryption, AccessControl plugins (hot path)
- wave2 (remaining): all other plugins and SDK internals

Category assignment heuristics:
- "cache" or "Cache" in field name -> hot-path-cache
- "session" or "Session" -> session-store
- "index" or "Index" -> index
- "metadata" or "Metadata" -> metadata-cache
- Default -> lookup-table

Write to Metadata/boundeddictionary-audit.json with pretty-printed JSON.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && test -f Metadata/boundeddictionary-audit.json && python3 -c "import json; d=json.load(open('Metadata/boundeddictionary-audit.json')); print(f'Instances: {d[\"total_instances\"]}, Plugins: {len(d[\"summary\"][\"by_plugin\"])}')" 2>/dev/null || node -e "const d=require('./Metadata/boundeddictionary-audit.json'); console.log('Instances:', d.total_instances, 'Plugins:', Object.keys(d.summary.by_plugin).length)"</automated>
  </verify>
  <done>Audit JSON lists every BoundedDictionary instance with plugin, category, and migration priority</done>
</task>

<task type="auto">
  <name>Task 2: Audit Completeness Validation Test</name>
  <files>DataWarehouse.Hardening.Tests/Migration/BoundedDictionaryAuditTests.cs</files>
  <action>
Create BoundedDictionaryAuditTests.cs with NUnit:

- Test_Audit_Matches_Grep_Count: Run ripgrep/grep for "BoundedDictionary" in *.cs files (excluding test files and the BoundedDictionary.cs definition itself). Count occurrences. Load audit JSON. Assert audit instance count >= grep count (audit may have more due to multi-line usage). If mismatch > 10%, fail with message listing files not in audit.

- Test_Audit_Schema_Valid: Load audit JSON. Assert all required fields present on every instance. Assert category is one of the allowed values. Assert migration_priority is wave1 or wave2.

- Test_Wave1_Contains_Hot_Path_Plugins: Assert wave1 instances include at least some from Intelligence, Storage, Compression, Encryption, AccessControl plugins.

- Test_All_Plugins_Categorized: Assert every unique plugin name in instances appears in summary.by_plugin.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Audit validation tests compile, verifying completeness and schema correctness</done>
</task>

</tasks>

<verification>
Metadata/boundeddictionary-audit.json exists with valid JSON schema.
Audit instance count matches grep results within 10%.
All instances categorized and prioritized.
</verification>

<success_criteria>
- Every BoundedDictionary instance cataloged with file, plugin, category
- Wave 1 priority assigned to top 20 hot-path caches
- Wave 2 covers all remaining instances
- Validation test confirms audit completeness
</success_criteria>

<output>
After completion, create `.planning/phases/129-boundeddictionary-migration/129-01-SUMMARY.md`
</output>
