---
phase: 129-boundeddictionary-migration
plan: 03
type: execute
wave: 2
depends_on: [129-01, 129-02]
files_modified:
  - Plugins/DataWarehouse.Plugins.*/**/*.cs
  - DataWarehouse.SDK/**/*.cs
  - DataWarehouse.Hardening.Tests/Migration/BoundedCacheMigrationWave2Tests.cs
autonomous: true
requirements: [PER-07]
must_haves:
  truths:
    - "All remaining BoundedDictionary instances migrated to BoundedCache"
    - "Default policy: ARC eviction, auto-sized, 0.1% RAM per cache"
    - "Zero BoundedDictionary usage anywhere except BoundedDictionary.cs definition"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Migration/BoundedCacheMigrationWave2Tests.cs"
      provides: "Tests verifying complete migration across all plugins and SDK"
      min_lines: 40
  key_links:
    - from: "Plugins/**/*.cs"
      to: "DataWarehouse.SDK/Contracts/Scaling/BoundedCache.cs"
      via: "All caches now use BoundedCache"
      pattern: "BoundedCache<|AutoSizeFromRam"
---

<objective>
Migrate all remaining BoundedDictionary instances to BoundedCache across the entire codebase.

Purpose: Complete the migration so zero BoundedDictionary usage remains outside the definition file.
Output: All remaining caches migrated with ARC eviction and auto-sizing.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Contracts/Scaling/BoundedCache.cs
@.planning/phases/129-boundeddictionary-migration/129-01-SUMMARY.md
@.planning/phases/129-boundeddictionary-migration/129-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate All Remaining Plugin Caches</name>
  <files>Plugins/DataWarehouse.Plugins.*/**/*.cs</files>
  <action>
Using the audit JSON and wave1 summary, identify all remaining wave2 BoundedDictionary instances in plugin directories (all plugins NOT already migrated in wave1).

For each remaining instance, apply the same migration pattern:
1. Replace `new BoundedDictionary<TKey, TValue>(N)` with `new BoundedCache<TKey, TValue>(new BoundedCacheOptions<TKey, TValue> { EvictionMode = CacheEvictionMode.Arc, AutoSizeFromRam = true, RamFractionPerMille = 1 })`
2. Update method calls to match BoundedCache API (confirmed in wave1)
3. Add using statement if needed
4. Ensure disposal if class is IDisposable

Work plugin by plugin, alphabetically. Build each plugin after migration to catch errors early. Expect ~30-50 plugins to touch.

For SDK internal files (StrategyBase.cs, IKeyStore.cs, UserConfigurationSystem.cs, TcpP2PNetwork.cs, etc.), apply same migration. Be extra careful with SDK files as they affect all plugins.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>All remaining BoundedDictionary instances in plugins and SDK migrated to BoundedCache</done>
</task>

<task type="auto">
  <name>Task 2: Wave 2 Validation Tests</name>
  <files>DataWarehouse.Hardening.Tests/Migration/BoundedCacheMigrationWave2Tests.cs</files>
  <action>
Create BoundedCacheMigrationWave2Tests.cs with NUnit:

- Test_Zero_BoundedDictionary_Usage_Codebase: Grep entire codebase for "new BoundedDictionary" in *.cs files. Exclude BoundedDictionary.cs definition, test files that test BoundedDictionary itself, and comments. Assert count == 0.

- Test_Zero_BoundedDictionary_Field_Declarations: Grep for "BoundedDictionary<" (field/variable declarations). Exclude definition file and tests. Assert count == 0.

- Test_All_BoundedCache_AutoSized: Grep for "new BoundedCache" in *.cs files. For each, grep same file for "AutoSizeFromRam = true" or "AutoSizeFromRam=true". Assert all instances have auto-sizing enabled.

- Test_Solution_Builds_Clean: Build entire solution. Assert zero errors. (This catches any missed API changes.)

- Test_Audit_Fully_Migrated: Load audit JSON. Assert all instances have status "migrated" (update audit JSON during migration to track progress).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Wave 2 tests compile, verifying zero BoundedDictionary usage outside definition</done>
</task>

</tasks>

<verification>
dotnet build entire solution succeeds.
Zero "new BoundedDictionary" or "BoundedDictionary<" in production code (excluding definition).
All BoundedCache instances use AutoSizeFromRam.
</verification>

<success_criteria>
- Zero BoundedDictionary usage in production code
- All caches use BoundedCache with ARC eviction and AutoSizeFromRam
- Solution builds clean
- Audit JSON shows all instances migrated
</success_criteria>

<output>
After completion, create `.planning/phases/129-boundeddictionary-migration/129-03-SUMMARY.md`
</output>
