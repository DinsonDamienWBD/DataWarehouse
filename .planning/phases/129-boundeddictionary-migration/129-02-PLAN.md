---
phase: 129-boundeddictionary-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/DataWarehouse.Plugins.Intelligence/**/*.cs
  - Plugins/DataWarehouse.Plugins.Storage/**/*.cs
  - Plugins/DataWarehouse.Plugins.Compression/**/*.cs
  - Plugins/DataWarehouse.Plugins.Encryption/**/*.cs
  - Plugins/DataWarehouse.Plugins.AccessControl/**/*.cs
  - DataWarehouse.Hardening.Tests/Migration/BoundedCacheMigrationWave1Tests.cs
autonomous: true
requirements: [PER-07]
must_haves:
  truths:
    - "Top 20 hot-path BoundedDictionary instances replaced with BoundedCache"
    - "All migrated caches use AutoSizeFromRam for dynamic sizing"
    - "No behavioral regression -- migrated caches function identically"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Migration/BoundedCacheMigrationWave1Tests.cs"
      provides: "Tests verifying wave 1 migration correctness"
      min_lines: 50
  key_links:
    - from: "Plugins/**/**.cs"
      to: "DataWarehouse.SDK/Contracts/Scaling/BoundedCache.cs"
      via: "Using BoundedCache instead of BoundedDictionary"
      pattern: "BoundedCache<|AutoSizeFromRam"
---

<objective>
Replace top 20 hot-path BoundedDictionary instances with BoundedCache using AutoSizeFromRam.

Purpose: Eliminate hardcoded cache sizes in the highest-traffic code paths for dynamic sizing.
Output: 20 hot-path caches migrated to BoundedCache with tests proving no regression.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Contracts/Scaling/BoundedCache.cs
@.planning/phases/129-boundeddictionary-migration/129-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Intelligence, Storage, Compression Caches</name>
  <files>Plugins/DataWarehouse.Plugins.Intelligence/**/*.cs, Plugins/DataWarehouse.Plugins.Storage/**/*.cs, Plugins/DataWarehouse.Plugins.Compression/**/*.cs</files>
  <action>
Using the audit from 129-01-SUMMARY.md, migrate wave1 BoundedDictionary instances in Intelligence, Storage, and Compression plugins to BoundedCache.

For each instance:
1. Replace `new BoundedDictionary<TKey, TValue>(1000)` with:
   ```csharp
   new BoundedCache<TKey, TValue>(new BoundedCacheOptions<TKey, TValue>
   {
       EvictionMode = CacheEvictionMode.Arc,
       AutoSizeFromRam = true,
       RamFractionPerMille = 1  // 0.1% of available RAM
   })
   ```
2. Replace `.TryAdd(key, value)` with `.Set(key, value)` if BoundedCache uses Set instead of TryAdd
3. Replace `.TryGetValue(key, out var value)` with `.TryGet(key, out var value)` if method name differs
4. Keep the field type as the concrete BoundedCache type (not IReadOnlyDictionary -- BoundedCache may have different interface)
5. Add `using DataWarehouse.SDK.Contracts.Scaling;` if not present
6. If the class implements IDisposable, ensure BoundedCache is disposed in Dispose() method

Check BoundedCache.cs API surface first to confirm exact method names. Do NOT guess -- read the actual class.

Target: ~10-12 instances across these 3 plugins (the highest traffic caches).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build Plugins/DataWarehouse.Plugins.Intelligence/DataWarehouse.Plugins.Intelligence.csproj Plugins/DataWarehouse.Plugins.Storage/DataWarehouse.Plugins.Storage.csproj Plugins/DataWarehouse.Plugins.Compression/DataWarehouse.Plugins.Compression.csproj --no-restore 2>&1 | tail -10</automated>
  </verify>
  <done>10-12 hot-path caches in Intelligence/Storage/Compression migrated to BoundedCache with AutoSizeFromRam</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Encryption, AccessControl Caches and Wave 1 Tests</name>
  <files>Plugins/DataWarehouse.Plugins.Encryption/**/*.cs, Plugins/DataWarehouse.Plugins.AccessControl/**/*.cs, DataWarehouse.Hardening.Tests/Migration/BoundedCacheMigrationWave1Tests.cs</files>
  <action>
Migrate remaining wave1 instances (~8-10) in Encryption and AccessControl plugins using same pattern as Task 1.

Create BoundedCacheMigrationWave1Tests.cs with NUnit:
- Test_No_BoundedDictionary_In_Wave1_Plugins: Grep Intelligence, Storage, Compression, Encryption, AccessControl plugin directories. Assert zero BoundedDictionary references (excluding comments and using statements).
- Test_BoundedCache_AutoSizeFromRam_Used: Grep same plugins for BoundedCache. Assert AutoSizeFromRam = true appears for each instance.
- Test_BoundedCache_Compiles_All_Wave1_Plugins: Build all 5 plugins. Assert zero errors.
- Test_Migration_Count_Matches_Audit: Load audit JSON. Count wave1 instances in these 5 plugins. Assert all migrated (zero BoundedDictionary remaining).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build Plugins/DataWarehouse.Plugins.Encryption/DataWarehouse.Plugins.Encryption.csproj Plugins/DataWarehouse.Plugins.AccessControl/DataWarehouse.Plugins.AccessControl.csproj DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -10</automated>
  </verify>
  <done>All wave1 caches migrated, validation tests compile confirming zero BoundedDictionary in wave1 plugins</done>
</task>

</tasks>

<verification>
dotnet build succeeds for all 5 wave1 plugins and test project.
Zero BoundedDictionary references in wave1 plugins (excluding comments).
All replacements use BoundedCache with AutoSizeFromRam = true.
</verification>

<success_criteria>
- Top 20 hot-path BoundedDictionary instances replaced with BoundedCache
- All use AutoSizeFromRam = true with ARC eviction
- Zero BoundedDictionary in Intelligence, Storage, Compression, Encryption, AccessControl plugins
- Validation tests confirm migration completeness
</success_criteria>

<output>
After completion, create `.planning/phases/129-boundeddictionary-migration/129-02-SUMMARY.md`
</output>
