---
phase: 129-boundeddictionary-migration
plan: 04
type: execute
wave: 2
depends_on: [129-03]
files_modified:
  - DataWarehouse.SDK/Utilities/BoundedDictionary.cs
  - DataWarehouse.SDK/Analyzers/BoundedDictionaryAnalyzer.cs
  - DataWarehouse.Hardening.Tests/Migration/BoundedDictionaryDeprecationTests.cs
autonomous: true
requirements: [PER-07]
must_haves:
  truths:
    - "BoundedDictionary class marked [Obsolete] with migration message"
    - "Roslyn analyzer warns on new BoundedDictionary usage"
    - "Migration guide documents the BoundedDictionary to BoundedCache transition"
  artifacts:
    - path: "DataWarehouse.SDK/Utilities/BoundedDictionary.cs"
      provides: "BoundedDictionary with [Obsolete] attribute"
      contains: "[Obsolete"
    - path: "DataWarehouse.SDK/Analyzers/BoundedDictionaryAnalyzer.cs"
      provides: "Roslyn analyzer that warns on BoundedDictionary usage"
      min_lines: 60
    - path: "DataWarehouse.Hardening.Tests/Migration/BoundedDictionaryDeprecationTests.cs"
      provides: "Tests verifying deprecation and analyzer behavior"
      min_lines: 40
  key_links:
    - from: "BoundedDictionaryAnalyzer.cs"
      to: "BoundedDictionary.cs"
      via: "Detects usage of deprecated type"
      pattern: "BoundedDictionary|DiagnosticDescriptor"
---

<objective>
Deprecate BoundedDictionary with [Obsolete] attribute and Roslyn analyzer to prevent future usage.

Purpose: Ensure no new BoundedDictionary instances are introduced after migration.
Output: Deprecated class, Roslyn analyzer, and deprecation tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Utilities/BoundedDictionary.cs
@.planning/phases/129-boundeddictionary-migration/129-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark BoundedDictionary Obsolete</name>
  <files>DataWarehouse.SDK/Utilities/BoundedDictionary.cs</files>
  <action>
Add [Obsolete] attribute to BoundedDictionary class:

```csharp
[Obsolete("BoundedDictionary is deprecated. Use BoundedCache<TKey, TValue> from DataWarehouse.SDK.Contracts.Scaling instead. " +
          "BoundedCache provides auto-sizing from RAM, ARC/LRU/TTL eviction, and better memory efficiency. " +
          "See migration guide: replace new BoundedDictionary<K,V>(N) with new BoundedCache<K,V>(new BoundedCacheOptions<K,V> { AutoSizeFromRam = true }). " +
          "This type will be removed in v9.0.", false)]
```

Use `false` (warning, not error) to allow a grace period. Will become `true` (error) in v9.0.

Add XML doc comment noting deprecation date, replacement, and v9.0 removal target.

Suppress the obsolete warnings in the BoundedDictionary.cs file itself using #pragma warning disable CS0618 around internal usage, if any self-references exist.

Do NOT delete the class -- it must remain for backward compatibility until v9.0.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>BoundedDictionary marked [Obsolete] with detailed migration message, SDK builds clean</done>
</task>

<task type="auto">
  <name>Task 2: Roslyn Analyzer and Deprecation Tests</name>
  <files>DataWarehouse.SDK/Analyzers/BoundedDictionaryAnalyzer.cs, DataWarehouse.Hardening.Tests/Migration/BoundedDictionaryDeprecationTests.cs</files>
  <action>
Create BoundedDictionaryAnalyzer.cs as a DiagnosticAnalyzer:
- Diagnostic ID: DW9001
- Title: "BoundedDictionary usage detected"
- Message: "Replace BoundedDictionary<{0}, {1}> with BoundedCache<{0}, {1}>. Use BoundedCacheOptions with AutoSizeFromRam = true."
- Category: "Migration"
- Severity: DiagnosticSeverity.Warning
- Analyzer logic: register SyntaxNodeAction for ObjectCreationExpressionSyntax. Check if type name is "BoundedDictionary". Report diagnostic with key and value type arguments.
- Also detect field/property declarations of type BoundedDictionary.
- Use Microsoft.CodeAnalysis.Diagnostics namespace.
- [DiagnosticAnalyzer(LanguageNames.CSharp)] attribute.

If the project does not have a Roslyn analyzer infrastructure yet, create the analyzer as a standalone class that can be registered later. Include the DiagnosticDescriptor and analysis logic but note in comments that it requires Microsoft.CodeAnalysis.CSharp NuGet package.

Create BoundedDictionaryDeprecationTests.cs:
- Test_BoundedDictionary_Has_Obsolete_Attribute: Use reflection to check [Obsolete] on BoundedDictionary class. Assert present. Assert message contains "BoundedCache".
- Test_Obsolete_Is_Warning_Not_Error: Assert Obsolete attribute IsError = false.
- Test_Analyzer_Descriptor_Correct: Instantiate BoundedDictionaryAnalyzer. Assert DiagnosticId = "DW9001". Assert severity = Warning.
- Test_No_Production_Code_Uses_BoundedDictionary: Grep production code (exclude tests, exclude BoundedDictionary.cs). Assert zero matches for "new BoundedDictionary".
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Roslyn analyzer DW9001 warns on BoundedDictionary usage, deprecation tests verify [Obsolete] attribute</done>
</task>

</tasks>

<verification>
dotnet build DataWarehouse.SDK succeeds with BoundedDictionary [Obsolete].
Analyzer class exists with DW9001 diagnostic descriptor.
Deprecation tests compile and verify attribute presence.
</verification>

<success_criteria>
- BoundedDictionary has [Obsolete] with detailed migration message
- Obsolete is warning (not error) for v8.0 grace period
- Roslyn analyzer DW9001 detects new BoundedDictionary usage
- Tests verify deprecation via reflection
- No production code uses BoundedDictionary
</success_criteria>

<output>
After completion, create `.planning/phases/129-boundeddictionary-migration/129-04-SUMMARY.md`
</output>
