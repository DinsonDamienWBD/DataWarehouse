---
phase: 142-final-production-certification
plan: 03
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - DataWarehouse.Tests/Integration/**/*.cs
  - DataWarehouse.Tests/DataWarehouse.Tests.csproj
autonomous: true
requirements:
  - CRT-01

must_haves:
  truths:
    - "Cross-boundary interactions between plugins via message bus are tested end-to-end"
    - "VDE decorator chain with all v8.0 additions is tested as a complete pipeline"
    - "Federation with MLS labels and FIPS mode are tested end-to-end"
  artifacts:
    - path: "DataWarehouse.Tests/Integration/"
      provides: "Integration tests for cross-boundary interactions"
      contains: "[Fact]"
  key_links:
    - from: "DataWarehouse.Tests/Integration/"
      to: "DataWarehouse.Kernel/"
      via: "Kernel bootstrap + message bus E2E"
      pattern: "MessageBus|KernelBootstrap"
    - from: "DataWarehouse.Tests/Integration/"
      to: "DataWarehouse.SDK/VirtualDiskEngine/"
      via: "VDE decorator chain integration"
      pattern: "VdeMount|DecoratorChain"
---

<objective>
Generate missing integration tests for cross-boundary interactions not covered by prior phases.

Purpose: Unit tests verify individual methods; integration tests verify that components work together correctly across project boundaries.
Output: Integration test classes covering message bus flows, VDE chains, federation, and FIPS mode.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-final-production-certification/142-01-SUMMARY.md
@Metadata/coverage-gap-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Message bus and plugin interaction integration tests</name>
  <files>DataWarehouse.Tests/Integration/MessageBusIntegrationTests.cs, DataWarehouse.Tests/Integration/PluginInteractionTests.cs</files>
  <action>
Read `Metadata/coverage-gap-analysis.md` Section 4 "Integration tests needed" for cross-boundary gaps.

Create integration tests in `DataWarehouse.Tests/Integration/` covering:

1. **Message bus E2E flow**: Publish a message from one plugin domain, verify it routes through the kernel message bus, and is received by subscriber(s). Test with: command messages, event messages, query messages. Verify message ordering, delivery guarantees, and error propagation.

2. **Plugin-to-plugin via bus**: Test scenarios where Plugin A publishes an event that Plugin B subscribes to. Use at least 3 representative plugin pairs from different domains (e.g., Storage->Replication, Security->Compliance, Intelligence->Observability). Verify the full publish->route->handle cycle.

3. **Kernel bootstrap integration**: Boot the kernel with all 52 plugins via reflection (extend existing `DataWarehouse.Hardening.Tests/` pattern). Verify all plugins register their message handlers. Send a test message and verify routing.

4. **Strategy resolution integration**: Test that the kernel correctly resolves strategies from plugins based on configuration, applicability (VDE-level vs data-level per StrategyApplicability enum), and priority.

Follow existing test patterns. Use `[Trait("Category", "Integration")]` to categorize.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Tests/ --filter "Category=Integration" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>Message bus E2E, plugin interaction, kernel bootstrap, and strategy resolution integration tests pass</done>
</task>

<task type="auto">
  <name>Task 2: VDE chain, federation, and FIPS integration tests</name>
  <files>DataWarehouse.Tests/Integration/VdeChainIntegrationTests.cs, DataWarehouse.Tests/Integration/FederationMlsTests.cs, DataWarehouse.Tests/Integration/FipsModeTests.cs</files>
  <action>
Create integration tests for the remaining cross-boundary scenarios:

1. **VDE decorator chain E2E**: Test the full decorator chain (Cache->Integrity->Compression->Dedup->Encryption->WAL->RAID->File) with v8.0 additions:
   - SPDK block device integration (mock if hardware unavailable, but test the wiring)
   - S3 epoch-flush block device path
   - WAL group commit batching
   - Erasure coding at VDE level
   - Write data through full chain, read back, verify checksums match.

2. **Federation with MLS labels**: Test federated VDE operations where MLS security labels (Bell-LaPadula + Biba) are enforced across federation boundaries. Verify: data with higher classification cannot flow to lower-classified nodes, write integrity levels are enforced.

3. **FIPS mode E2E**: Boot the system with `--fips strict` equivalent configuration. Verify: only FIPS-approved algorithms are available, non-FIPS algorithms throw at selection time, VDE operations complete successfully using only FIPS algorithms, CNSA 2.0 suite is enforced when configured.

4. **Live filesystem resize**: Test VDE online grow and offline shrink operations through the full decorator chain. Verify data integrity before and after resize.

Use `[Trait("Category", "Integration")]` on all tests.
Run `dotnet build` and `dotnet test` to verify compilation and passage.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Tests/ --filter "Category=Integration" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>VDE chain, federation MLS, FIPS mode, and resize integration tests compile and pass</done>
</task>

</tasks>

<verification>
- All integration test files exist in DataWarehouse.Tests/Integration/
- dotnet test with Integration filter passes all tests
- Message bus, VDE chain, federation, FIPS scenarios all covered
</verification>

<success_criteria>
All cross-boundary integration gaps from coverage analysis are closed. Integration tests exercise real multi-component paths, not mocked stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/142-final-production-certification/142-03-SUMMARY.md`
</output>
