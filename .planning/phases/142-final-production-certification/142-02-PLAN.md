---
phase: 142-final-production-certification
plan: 02
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - DataWarehouse.Tests/**/*.cs
  - DataWarehouse.Tests/DataWarehouse.Tests.csproj
autonomous: true
requirements:
  - CRT-01

must_haves:
  truths:
    - "Every public API gap identified in 142-01 has a corresponding unit test"
    - "Tests cover constructor validation, happy path, edge cases, error paths, and disposal"
    - "All new tests compile and pass"
  artifacts:
    - path: "DataWarehouse.Tests/"
      provides: "Unit tests for all coverage gaps"
      contains: "[Fact]"
    - path: "DataWarehouse.Tests/"
      provides: "Parameterized tests for edge cases"
      contains: "[Theory]"
  key_links:
    - from: "DataWarehouse.Tests/"
      to: "DataWarehouse.SDK/"
      via: "Direct method invocation of previously untested APIs"
      pattern: "new.*\\(|\\.[A-Z]\\w+\\("
    - from: "DataWarehouse.Tests/"
      to: "Plugins/DataWarehouse.Plugins.*/"
      via: "Strategy instantiation and execution tests"
      pattern: "new.*Strategy|Execute"
---

<objective>
Generate missing unit tests for every gap identified in the coverage analysis.

Purpose: Close all unit-level test coverage gaps to achieve 100% public API coverage.
Output: xUnit [Fact]/[Theory] tests in DataWarehouse.Tests/ for every untested public API.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-final-production-certification/142-01-SUMMARY.md
@Metadata/coverage-gap-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate P0 unit tests (security and data integrity APIs)</name>
  <files>DataWarehouse.Tests/**/*.cs</files>
  <action>
Read `Metadata/coverage-gap-analysis.md` Section 2 for P0 (security/data-integrity) untested APIs.

For each P0 gap, generate xUnit tests in `DataWarehouse.Tests/` organized by namespace:
- **Constructor validation**: Verify null-argument checks throw `ArgumentNullException`, invalid ranges throw `ArgumentOutOfRangeException`.
- **Happy path**: Call with valid inputs, assert expected return value or state change.
- **Error paths**: Invalid inputs, corrupt data, boundary conditions — assert correct exception type.
- **Disposal**: For `IDisposable` types, verify `Dispose()` releases resources and subsequent calls throw `ObjectDisposedException`.

Follow existing test conventions in `DataWarehouse.Tests/` (naming: `{ClassName}Tests.cs`, structure: Arrange-Act-Assert).

For crypto/security classes: test that key material is zeroed on dispose, that invalid keys are rejected, that algorithm selection matches FIPS mode.

For VDE data integrity classes: test that corrupt checksums are detected, that invalid block addresses are rejected, that ref count underflow is prevented.

Update `DataWarehouse.Tests.csproj` if new project references are needed to access the types under test.

Priority: Start with security-critical paths (encryption, access control, integrity verification) before data-handling paths.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Tests/ --filter "Category=P0Coverage" --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All P0 untested APIs have unit tests. Tests compile and pass. Zero security-critical code paths remain untested.</done>
</task>

<task type="auto">
  <name>Task 2: Generate P1/P2 unit tests (core logic and utility APIs)</name>
  <files>DataWarehouse.Tests/**/*.cs</files>
  <action>
Read `Metadata/coverage-gap-analysis.md` Section 2 for P1 (core-logic) and P2 (utility) untested APIs.

For each gap, generate xUnit tests following the same patterns as Task 1:
- **P1 core logic**: Strategy execute methods, pipeline stages, message bus handlers, factory methods, configuration validators. Use `[Theory]` with `[InlineData]` for parameterized testing where input/output pairs are deterministic.
- **P2 utilities**: Helper methods, extension methods, formatters, converters. These often have simple input/output contracts ideal for `[Theory]` tests.

For strategy classes specifically:
- Test `InitializeAsync()` with valid and invalid configuration.
- Test `ExecuteAsync()` with representative inputs per strategy domain.
- Test `Dispose()` cleanup.
- Reference `Metadata/plugin-strategy-map.json` for the full list of strategies.

Do NOT duplicate tests that already exist. Check existing test files before creating new ones.

Run `dotnet build DataWarehouse.Tests/` after generating tests to verify compilation. Fix any compilation errors before proceeding.

Run `dotnet test DataWarehouse.Tests/` to verify all tests pass. Fix any failures.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Tests/ --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All P1 and P2 untested APIs have unit tests. Full test suite compiles and passes. 100% public API coverage at the unit test level.</done>
</task>

</tasks>

<verification>
- dotnet build DataWarehouse.Tests/ succeeds with zero errors
- dotnet test DataWarehouse.Tests/ passes all tests (new + existing)
- Every gap from Metadata/coverage-gap-analysis.md Section 2 "Unit tests needed" has a corresponding test file
</verification>

<success_criteria>
Every untested public API identified in 142-01 has a unit test. All tests compile and pass. No regressions in existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/142-final-production-certification/142-02-SUMMARY.md`
</output>
