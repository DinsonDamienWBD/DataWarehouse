---
phase: 142-final-production-certification
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/audit.yml
autonomous: true
requirements:
  - CRT-02

must_haves:
  truths:
    - "audit.yml includes all v8.0 test projects in the test matrix"
    - "Semgrep SAST gate blocks on P0/P1 findings"
    - "SecurityCodeScan gate blocks on critical findings"
    - "dotCover coverage gate enforces >=95% line coverage"
    - "Stryker mutation gate enforces >=90% mutation score"
    - "BenchmarkDotNet regression gate enforces P99 <=10% degradation"
    - "Matrix build runs on both Linux and Windows"
  artifacts:
    - path: ".github/workflows/audit.yml"
      provides: "Complete CI/CD pipeline with all quality gates"
      contains: "semgrep"
  key_links:
    - from: ".github/workflows/audit.yml"
      to: "DataWarehouse.Tests/"
      via: "dotnet test step references all test projects"
      pattern: "dotnet test"
    - from: ".github/workflows/audit.yml"
      to: "DataWarehouse.Fuzz.Tests/"
      via: "Fuzz test execution step"
      pattern: "Fuzz"
---

<objective>
Expand CI/CD audit.yml to include all v8.0 test projects and quality gates.

Purpose: The CI/CD pipeline must enforce all quality standards automatically — coverage, mutation testing, SAST, benchmarks — so no regression can slip through.
Output: Updated .github/workflows/audit.yml with comprehensive gates and Linux+Windows matrix.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/audit.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all test projects and SAST/coverage gates</name>
  <files>.github/workflows/audit.yml</files>
  <action>
Read the existing `.github/workflows/audit.yml` to understand current structure.

Update the workflow to include:

1. **Matrix strategy**: Add `os: [ubuntu-latest, windows-latest]` matrix so all steps run on both Linux and Windows.

2. **Test projects**: Ensure ALL test projects are included in `dotnet test` steps:
   - `DataWarehouse.Tests/`
   - `DataWarehouse.Hardening.Tests/`
   - `DataWarehouse.Benchmarks/` (benchmark verification mode, not full benchmark)
   - `DataWarehouse.Runtime.Tests/` (if exists from Phase 111.5)
   - `DataWarehouse.Fuzz.Tests/` (if exists)
   - Any other `*.Tests` projects added during v8.0

3. **Semgrep SAST gate**: Add step to run `semgrep --config auto --error --severity ERROR --severity WARNING` on the solution. Gate: zero P0/P1 findings (exit code 0).

4. **SecurityCodeScan gate**: Add step to run SecurityCodeScan as part of `dotnet build` with `/p:SecurityCodeScan=true` or via dotnet tool. Gate: zero critical findings.

5. **dotCover coverage gate**: Add step to run `dotnet dotcover test` (or JetBrains CLI) with threshold: `--dcAttributeThreshold=95`. Gate: fail if line coverage < 95%.

6. **Stryker mutation gate**: Add step to run `dotnet stryker` with threshold configuration: `--threshold-high 90 --threshold-low 85 --threshold-break 85`. Gate: fail if mutation score < 90% solution-wide.

Preserve all existing workflow steps. Add new steps after existing ones.
  </action>
  <verify>
    <automated>grep -c "semgrep\|SecurityCodeScan\|dotcover\|stryker\|ubuntu-latest\|windows-latest" .github/workflows/audit.yml</automated>
  </verify>
  <done>audit.yml has Semgrep, SecurityCodeScan, dotCover >=95%, Stryker >=90% gates on Linux+Windows matrix</done>
</task>

<task type="auto">
  <name>Task 2: Add benchmark regression gate and validate workflow syntax</name>
  <files>.github/workflows/audit.yml</files>
  <action>
Continue updating `.github/workflows/audit.yml`:

1. **BenchmarkDotNet regression gate**: Add step to run benchmarks in CI mode:
   - Execute `dotnet run --project DataWarehouse.Benchmarks/ -c Release -- --filter * --exporters json`
   - Compare P99 latencies against baseline in `Metadata/benchmark-baseline-v8.0.json` (if exists) or establish baseline on first run.
   - Gate: fail if any P99 degrades by more than 10% from baseline.
   - Use `BenchmarkDotNet.Diagnostics.Windows` for Windows-specific metrics on Windows matrix.

2. **Fuzz test step**: Add step to run `dotnet test DataWarehouse.Fuzz.Tests/` specifically, with a timeout guard (max 10 minutes in CI).

3. **Artifact upload**: Upload test results, coverage reports, and benchmark results as GitHub Actions artifacts for review.

4. **Workflow syntax validation**: Ensure the YAML is valid by checking:
   - All `uses:` actions reference valid versions
   - Environment variables are properly quoted
   - Matrix strategy is correctly structured
   - Conditional steps use proper `if:` syntax

5. **Caching**: Add NuGet cache step (`actions/cache`) to speed up CI runs.

Validate the complete workflow by checking YAML syntax (no tabs, proper indentation).
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/audit.yml'))" 2>&1 || echo "YAML validation requires PyYAML"</automated>
  </verify>
  <done>audit.yml has BenchmarkDotNet P99 <=10% regression gate, fuzz step, artifact upload, NuGet cache, valid YAML syntax</done>
</task>

</tasks>

<verification>
- .github/workflows/audit.yml is valid YAML
- Contains matrix: [ubuntu-latest, windows-latest]
- Contains gates: Semgrep, SecurityCodeScan, dotCover >=95%, Stryker >=90%, BenchmarkDotNet P99 <=10%
- All v8.0 test projects referenced
- Artifact upload configured
</verification>

<success_criteria>
CI/CD pipeline enforces all v8.0 quality standards on every PR. No quality regression can bypass the gates.
</success_criteria>

<output>
After completion, create `.planning/phases/142-final-production-certification/142-05-SUMMARY.md`
</output>
