---
phase: 142-final-production-certification
plan: 04
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - DataWarehouse.Fuzz.Tests/**/*.cs
  - DataWarehouse.Fuzz.Tests/DataWarehouse.Fuzz.Tests.csproj
autonomous: true
requirements:
  - CRT-01

must_haves:
  truths:
    - "SharpFuzz harnesses cover all v8.0 binary parsers not covered by Phase 111.5"
    - "SPDK command buffers, RDMA message frames, HDF5/NetCDF headers, and cluster protocol messages are fuzz-tested"
    - "Zero unhandled exceptions escape fuzz harnesses"
  artifacts:
    - path: "DataWarehouse.Fuzz.Tests/"
      provides: "Fuzz harnesses for v8.0 binary parsers"
      contains: "[FuzzTest]"
  key_links:
    - from: "DataWarehouse.Fuzz.Tests/"
      to: "DataWarehouse.SDK/"
      via: "Direct invocation of binary parsing methods"
      pattern: "Deserialize|Parse|Decode"
---

<objective>
Extend SharpFuzz harnesses to cover v8.0 binary parsers not already fuzz-tested.

Purpose: Binary parsers processing untrusted input are the highest-risk attack surface. Every parser must withstand adversarial input without crashing.
Output: New SharpFuzz [FuzzTest] harnesses for SPDK, RDMA, HDF5/NetCDF, and cluster protocol parsers.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/142-final-production-certification/142-01-SUMMARY.md
@Metadata/coverage-gap-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SPDK and RDMA fuzz harnesses</name>
  <files>DataWarehouse.Fuzz.Tests/SpdkFuzzTests.cs, DataWarehouse.Fuzz.Tests/RdmaFuzzTests.cs</files>
  <action>
Read `Metadata/coverage-gap-analysis.md` Section 4 "Fuzz tests needed" for binary parser gaps.

Create SharpFuzz harnesses in `DataWarehouse.Fuzz.Tests/`:

1. **SPDK command buffer fuzzing** (`SpdkFuzzTests.cs`):
   - Locate SPDK command parsing classes in the SDK (likely `DataWarehouse.SDK/BlockDevices/Spdk/` or similar path from ai-maps).
   - Create `[FuzzTest]` methods that feed corrupt byte arrays to: NVMe command deserialization, completion queue entry parsing, scatter-gather list parsing, admin command parsing.
   - Seed initial corpus from valid SPDK command structures.
   - Assert: no unhandled exceptions, no buffer overruns, all corrupt input returns error result or throws documented exception type.

2. **RDMA message frame fuzzing** (`RdmaFuzzTests.cs`):
   - Locate RDMA/DPDK message frame parsing classes (likely `DataWarehouse.SDK/Networking/` or `KernelBypass/`).
   - Create `[FuzzTest]` methods for: RDMA connection setup messages, data transfer frames, completion notifications, TCP fallback protocol headers.
   - Assert: no crashes on truncated frames, no infinite loops on circular references, working set bounded.

3. **Corpus generation**: For each harness, create a `Corpus/` subdirectory with 5-10 valid serialized structures as seed inputs. Generate these by calling the production serialization methods with valid data.

If `DataWarehouse.Fuzz.Tests/` project does not exist yet (may have been created in Phase 111.5), create it with SharpFuzz NuGet reference and project references to SDK.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.Fuzz.Tests/ 2>&1 | tail -5</automated>
  </verify>
  <done>SPDK and RDMA fuzz harnesses compile. Initial corpus seeded. Zero crashes on valid inputs.</done>
</task>

<task type="auto">
  <name>Task 2: HDF5/NetCDF and cluster protocol fuzz harnesses + run /pentest-fuzz</name>
  <files>DataWarehouse.Fuzz.Tests/ScientificFormatFuzzTests.cs, DataWarehouse.Fuzz.Tests/ClusterProtocolFuzzTests.cs</files>
  <action>
Continue extending fuzz harnesses:

1. **HDF5/NetCDF header fuzzing** (`ScientificFormatFuzzTests.cs`):
   - Locate HDF5/NetCDF parsing classes (likely in `Plugins/DataWarehouse.Plugins.UltimateDataFormat/` or SDK format handlers).
   - Create `[FuzzTest]` methods for: HDF5 superblock parsing, dataset header parsing, B-tree node parsing, NetCDF dimension/variable header parsing.
   - Feed corrupt headers with: invalid magic bytes, truncated headers, absurd dimension counts (OOM prevention), circular group references.
   - Assert: all corruption caught gracefully, no OOM from inflated sizes, working set < 500MB.

2. **Cluster protocol message fuzzing** (`ClusterProtocolFuzzTests.cs`):
   - Locate cluster/federation protocol message parsers (Raft messages, federation router frames, HA heartbeat packets).
   - Create `[FuzzTest]` methods for: leader election messages, log replication entries, snapshot transfer headers, split-brain detection packets.
   - Assert: no state corruption from malformed messages, no infinite retries.

3. **Run all fuzz harnesses**: Execute `dotnet test DataWarehouse.Fuzz.Tests/` to run the initial fuzz pass. This verifies harnesses work on seed corpus.

4. **Run /pentest-fuzz**: If any SharpFuzz crash logs exist in output, run the `/pentest-fuzz` skill to analyze crashes and generate patches. Apply patches if any crashes found.

5. **Coverage report**: Generate a brief summary of fuzz-tested code paths appended to `Metadata/coverage-gap-analysis.md` Section 5 "Fuzz Coverage".
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Fuzz.Tests/ --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>All v8.0 binary parsers have fuzz harnesses. /pentest-fuzz ran on results. Zero unhandled crashes.</done>
</task>

</tasks>

<verification>
- DataWarehouse.Fuzz.Tests/ builds successfully
- All fuzz harnesses run on seed corpus without crashes
- SPDK, RDMA, HDF5/NetCDF, cluster protocol parsers all covered
- /pentest-fuzz skill output reviewed (if crash logs exist)
</verification>

<success_criteria>
Every v8.0 binary parser has a SharpFuzz harness. Seed corpus runs clean. Any crashes are patched.
</success_criteria>

<output>
After completion, create `.planning/phases/142-final-production-certification/142-04-SUMMARY.md`
</output>
