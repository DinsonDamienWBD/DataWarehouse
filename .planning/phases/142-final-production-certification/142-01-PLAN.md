---
phase: 142-final-production-certification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Metadata/coverage-gap-analysis.md
autonomous: true
requirements:
  - CRT-01

must_haves:
  truths:
    - "Every public method across SDK, Kernel, 52 plugins, and companions is cataloged"
    - "Every untested public API is identified with file path and method signature"
    - "Gap analysis cross-references dotCover report to identify coverage holes"
  artifacts:
    - path: "Metadata/coverage-gap-analysis.md"
      provides: "Exhaustive list of untested public APIs"
      contains: "## Untested Public APIs"
  key_links:
    - from: "Metadata/coverage-gap-analysis.md"
      to: "DataWarehouse.Tests/"
      via: "Gap list drives test generation in plans 142-02, 142-03, 142-04"
      pattern: "Untested.*Public"
---

<objective>
Semantic codebase analysis to identify every untested public method, strategy, and code path.

Purpose: Before generating missing tests, we must know exactly what is missing. This plan produces the gap analysis that drives plans 02-04.
Output: Metadata/coverage-gap-analysis.md — complete catalog of untested public APIs.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/ai-maps/map-structure.md
@docs/ai-maps/map-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog all public APIs across solution</name>
  <files>Metadata/coverage-gap-analysis.md</files>
  <action>
Use Grep/Glob to enumerate every public method, property, and type across the entire solution:

1. Scan all `.cs` files in SDK (`DataWarehouse.SDK/`), Kernel (`DataWarehouse.Kernel/`), all 52 plugin projects (`Plugins/DataWarehouse.Plugins.*/`), and companion apps (`DataWarehouse.CLI/`, `DataWarehouse.GUI/`, `DataWarehouse.Dashboard/`, `DataWarehouse.Launcher/`).
2. Extract every `public` method signature using pattern: `public\s+(static\s+)?(override\s+)?(async\s+)?[\w<>\[\],\s]+\s+\w+\s*\(`.
3. Extract every `public` class/interface/struct using pattern: `public\s+(abstract\s+|sealed\s+|static\s+)?class|interface|struct|record`.
4. For strategies specifically, cross-reference `Metadata/plugin-strategy-map.json` to ensure all 3,036 strategies are accounted for.
5. Group results by project/namespace and count total public surface area.

Output the raw catalog as Section 1 of `Metadata/coverage-gap-analysis.md` with columns: Project | Namespace | Type | Method | File:Line.
  </action>
  <verify>
    <automated>grep -c "public" Metadata/coverage-gap-analysis.md</automated>
  </verify>
  <done>Metadata/coverage-gap-analysis.md Section 1 contains catalog of all public APIs grouped by project</done>
</task>

<task type="auto">
  <name>Task 2: Cross-reference against test coverage and identify gaps</name>
  <files>Metadata/coverage-gap-analysis.md</files>
  <action>
Cross-reference the public API catalog (Task 1) against existing tests:

1. Scan all test projects (`DataWarehouse.Tests/`, `DataWarehouse.Hardening.Tests/`, `DataWarehouse.Benchmarks/`, any `DataWarehouse.Runtime.Tests/`, `DataWarehouse.Fuzz.Tests/`) for test methods that exercise each public API.
2. For each test file, extract the types being tested by analyzing: `using` statements, constructor calls, method invocations, and `[InlineData]`/`[Theory]` patterns.
3. Match test coverage to public API catalog. Mark each public method as: TESTED (has direct test), INDIRECT (tested via integration/higher-level test), or UNTESTED.
4. If a dotCover report exists at `Metadata/` or `TestResults/`, parse its XML/HTML for line-level coverage data to validate findings.
5. Add Section 2 "Untested Public APIs" to `Metadata/coverage-gap-analysis.md` listing every UNTESTED method with: Project | Type | Method | File:Line | Priority (P0=security/data-integrity, P1=core-logic, P2=utility).
6. Add Section 3 "Summary Statistics" with: total public APIs, tested count, untested count, coverage percentage by project.
7. Add Section 4 "Test Generation Recommendations" grouping gaps into: unit tests needed (142-02), integration tests needed (142-03), fuzz tests needed (142-04).

Do NOT generate any test code in this plan — only the analysis.
  </action>
  <verify>
    <automated>grep -c "UNTESTED" Metadata/coverage-gap-analysis.md</automated>
  </verify>
  <done>Gap analysis complete with every untested public API identified, prioritized, and categorized for test generation</done>
</task>

</tasks>

<verification>
- Metadata/coverage-gap-analysis.md exists and contains Sections 1-4
- Every SDK, Kernel, plugin, and companion project appears in the catalog
- Untested APIs are prioritized P0/P1/P2
- Recommendations map to plans 142-02, 142-03, 142-04
</verification>

<success_criteria>
100% of public APIs cataloged. All coverage gaps identified with file:line precision. Gap analysis is actionable for downstream test generation plans.
</success_criteria>

<output>
After completion, create `.planning/phases/142-final-production-certification/142-01-SUMMARY.md`
</output>
