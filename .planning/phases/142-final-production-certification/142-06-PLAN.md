---
phase: 142-final-production-certification
plan: 06
type: execute
wave: 2
depends_on: ["142-01"]
files_modified:
  - Metadata/final-audit-report.md
  - DataWarehouse.SDK/**/*.cs
  - Plugins/**/*.cs
autonomous: true
requirements:
  - CRT-03

must_haves:
  truths:
    - "/audit skill produces zero P0/P1 findings"
    - "/pentest-sast produces zero P0/P1 findings"
    - "/pentest-dast produces zero P0/P1 findings against Dashboard and S3 API"
    - "/profile confirms no single method >5% CPU and no memory leaks"
  artifacts:
    - path: "Metadata/final-audit-report.md"
      provides: "Consolidated final audit results with evidence"
      contains: "## Audit Results"
  key_links:
    - from: "Metadata/final-audit-report.md"
      to: "DataWarehouse.SDK/"
      via: "Audit findings reference specific files and lines"
      pattern: "Finding|P0|P1"
---

<objective>
Run comprehensive final audit using all available tooling and fix every finding.

Purpose: The final audit ensures zero known vulnerabilities, performance issues, or code quality problems remain before certification.
Output: Metadata/final-audit-report.md with evidence of clean results from all tools.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run /audit and /pentest-sast, fix all P0/P1 findings</name>
  <files>Metadata/final-audit-report.md, DataWarehouse.SDK/**/*.cs, Plugins/**/*.cs</files>
  <action>
Execute the full audit and SAST sweep:

1. **Run /audit** on every project in the solution. This performs code quality analysis across all 52 plugins, SDK, Kernel, and companions. Record all findings.

2. **Run /pentest-sast** (Semgrep + SecurityCodeScan) on the full solution. This checks for:
   - SQL injection patterns
   - Path traversal vulnerabilities
   - Hardcoded secrets/credentials
   - Unsafe deserialization
   - Buffer overflow patterns
   - Crypto misuse (weak algorithms, IV reuse, padding oracle)
   - Missing input validation

3. **Triage findings**: Categorize each finding as P0 (critical security), P1 (high security/reliability), P2 (medium quality), P3 (informational).

4. **Fix ALL P0 and P1 findings**: For each finding:
   - Read the specific file and line
   - Apply the fix following existing code conventions
   - Verify the fix does not break tests with `dotnet build` + `dotnet test`

5. **Re-run /audit and /pentest-sast** to confirm zero P0/P1 remaining.

6. **Document results** in `Metadata/final-audit-report.md` Section 1 "SAST Results" with: tool, findings count by severity, all findings fixed, re-scan confirmation.

Any P2/P3 findings may be documented as accepted risk with justification.
  </action>
  <verify>
    <automated>grep -c "P0.*REMAINING\|P1.*REMAINING" Metadata/final-audit-report.md | grep "^0$" || echo "Check final-audit-report.md for remaining findings"</automated>
  </verify>
  <done>Zero P0/P1 findings from /audit and /pentest-sast. All fixes applied and verified. Results documented.</done>
</task>

<task type="auto">
  <name>Task 2: Run /pentest-dast and /profile, fix all findings</name>
  <files>Metadata/final-audit-report.md, DataWarehouse.SDK/**/*.cs</files>
  <action>
Execute DAST and profiling sweeps:

1. **Run /pentest-dast** (RESTler + Nuclei) against:
   - Dashboard REST API endpoints (if Dashboard is runnable)
   - S3-compatible API surface
   - Any other HTTP-exposed endpoints

   If Dashboard cannot be started locally, document as "requires live deployment for DAST" and focus on static analysis of the HTTP handlers for common DAST-category vulnerabilities (XSS, CSRF, CORS misconfiguration, auth bypass).

2. **Run /profile** (dotTrace + dotMemory analysis) on critical code paths:
   - VDE write + read through full decorator chain
   - Kernel boot with 52 plugins
   - Message bus publish/subscribe under load
   - Strategy resolution with 3,036 strategies

   Verify: no single method consumes >5% CPU, no memory leaks (Gen2 collections bounded), no excessive allocations on hot paths.

3. **Fix ALL P0/P1 findings** from DAST:
   - Missing auth on endpoints
   - XSS/injection vulnerabilities
   - CORS misconfiguration
   - Information disclosure

4. **Fix performance issues** from profiling:
   - Methods exceeding 5% CPU → optimize or cache
   - Memory leaks → fix disposal, add pooling
   - Hot-path allocations → use Span<T>, stackalloc, ArrayPool

5. **Re-run tools** to confirm fixes.

6. **Document** in `Metadata/final-audit-report.md` Section 2 "DAST Results" and Section 3 "Profile Results".
  </action>
  <verify>
    <automated>grep -c "DAST Results\|Profile Results" Metadata/final-audit-report.md</automated>
  </verify>
  <done>Zero P0/P1 from DAST. Profile confirms no method >5% CPU and no memory leaks. All documented.</done>
</task>

</tasks>

<verification>
- Metadata/final-audit-report.md exists with Sections 1-3
- Zero P0/P1 findings across all tools
- Profile results confirm performance targets met
- All fixes verified with dotnet build + dotnet test
</verification>

<success_criteria>
Clean audit across all tools. Zero P0/P1 findings remaining. Performance profile meets targets. Evidence documented.
</success_criteria>

<output>
After completion, create `.planning/phases/142-final-production-certification/142-06-SUMMARY.md`
</output>
