---
phase: 098-hardening-core-infrastructure
plan: 04
type: execute
wave: 4
depends_on: ["098-03"]
files_modified:
  - DataWarehouse.Hardening.Tests/Shared/
  - DataWarehouse.Shared/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]

must_haves:
  truths:
    - "All 61 Shared findings have corresponding failing tests before fixes"
    - "All 61 Shared findings have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Shared/"
      provides: "Hardening tests for all 61 Shared findings"
    - path: "DataWarehouse.Shared/"
      provides: "Production fixes for all 61 Shared findings"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Shared/"
      to: "DataWarehouse.Shared/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*Shared"
---

<objective>
Stage 1 - Step 1-3 - Core Infrastructure Hardening: Shared

Fix all 61 findings (4 critical, 18 medium, 39 low) in the Shared project using TDD methodology.
Source: CONSOLIDATED-FINDINGS.md lines 3470-3535.

Purpose: Harden DataWarehouse.Shared — CLI commands, service managers, models, and utilities — against all audit-identified vulnerabilities including command injection, race conditions, sensitive data exposure, and naming violations.
Output: 61 hardening tests + 61 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 3470-3535 — Shared section, 61 findings)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop for all 61 Shared findings</name>
  <files>
    DataWarehouse.Hardening.Tests/Shared/ (new test files),
    DataWarehouse.Shared/AdapterFactory.cs,
    DataWarehouse.Shared/AICredential.cs,
    DataWarehouse.Shared/BackupCommands.cs,
    DataWarehouse.Shared/CapabilityManager.cs,
    DataWarehouse.Shared/CLILearningStore.cs,
    DataWarehouse.Shared/CliScriptingEngine.cs,
    DataWarehouse.Shared/CommandRecorder.cs,
    DataWarehouse.Shared/ComplianceReportService.cs,
    DataWarehouse.Shared/ConnectCommand.cs,
    DataWarehouse.Shared/ContinuousComplianceMonitor.cs,
    DataWarehouse.Shared/DeveloperToolsModels.cs,
    DataWarehouse.Shared/DeveloperToolsService.cs,
    DataWarehouse.Shared/DynamicEndpointGenerator.cs,
    DataWarehouse.Shared/InstallCommands.cs,
    DataWarehouse.Shared/InstallFromUsbCommand.cs,
    DataWarehouse.Shared/InstanceManager.cs,
    DataWarehouse.Shared/IServerHost.cs,
    DataWarehouse.Shared/Message.cs,
    DataWarehouse.Shared/MessageBridge.cs,
    DataWarehouse.Shared/NaturalLanguageProcessor.cs,
    DataWarehouse.Shared/NlpMessageBusRouter.cs,
    DataWarehouse.Shared/OutputFormatter.cs,
    DataWarehouse.Shared/PlatformServiceManager.cs,
    DataWarehouse.Shared/PortableMediaDetector.cs,
    DataWarehouse.Shared/Program.cs,
    DataWarehouse.Shared/RaidCommands.cs,
    DataWarehouse.Shared/ServerCommands.cs,
    DataWarehouse.Shared/UndoManager.cs,
    DataWarehouse.Shared/UsbInstaller.cs,
    DataWarehouse.Shared/UserQuota.cs,
    DataWarehouse.Shared/UserSession.cs,
    DataWarehouse.Shared/VdeCommands.cs
  </files>
  <action>
    Read CONSOLIDATED-FINDINGS.md lines 3470-3535 for all 61 Shared findings.
    Process EVERY finding sequentially (finding #1 through #61) using TDD:

    For EACH finding:
    1. Write a test in `DataWarehouse.Hardening.Tests/Shared/` that exposes the vulnerability/issue
       - Use Coyote for concurrency findings (race conditions on static fields, check-and-set races)
       - Use xUnit for logic/memory findings (command injection, naming, unused fields, sensitive data)
       - Test name format: `Finding{N}_{BriefDescription}`
    2. Run test — confirm RED
    3. Fix the production code
    4. Run test — confirm GREEN
    5. Move to next finding

    Group tests by file into test classes:
    - `AdapterFactoryTests.cs` — finding 1 (thread-safety)
    - `AICredentialTests.cs` — findings 2-4 (naming + ApiKey serialization leak)
    - `CLILearningStoreTests.cs` — findings 7-10
    - `ConnectCommandTests.cs` — findings 14-16 (cancellation support)
    - `ContinuousComplianceMonitorTests.cs` — finding 17 (race condition)
    - `DeveloperToolsTests.cs` — findings 18-27
    - `InstanceManagerTests.cs` — finding 31 (race condition)
    - `IServerHostTests.cs` — finding 32 (synchronization)
    - `MessageBridgeTests.cs` — findings 34-35
    - `NaturalLanguageProcessorTests.cs` — findings 36-44 (naming)
    - `PlatformServiceManagerTests.cs` — findings 48-49 (CRITICAL: command injection)
    - `ServerCommandsTests.cs` — finding 54 (static synchronization)
    - `UserQuotaTests.cs` — findings 58-59 (CRITICAL: RecordUsage race condition)
    - `VdeCommandsTests.cs` — finding 61 (CRITICAL: uint truncates ulong)

    Key critical findings requiring special attention:
    - #3 (CRITICAL): AICredential ApiKey serializable to logs — add [JsonIgnore] or secure serialization
    - #48 (CRITICAL): PlatformServiceManager command injection via service names — sanitize all Process.Start arguments
    - #59 (CRITICAL): UserQuota RecordUsage race condition — use Interlocked or lock
    - #61 (CRITICAL): VdeCommands uint cast truncates ulong — fix to preserve full 64-bit value

    For LOW-severity naming findings, fix the naming AND ensure no public API breaks (add [Obsolete] if needed for backward compat, or confirm internal-only usage).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Shared" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 61 Shared findings have passing hardening tests; production code fixed; solution builds with 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Commit and verify full solution build</name>
  <files>
    DataWarehouse.Hardening.Tests/Shared/,
    DataWarehouse.Shared/
  </files>
  <action>
    1. Run `dotnet build` on full solution to confirm 0 errors
    2. Run `dotnet test` on full test suite to confirm no regressions
    3. Commit all changes with message: "test+fix(098-04): Stage 1 - Step 1-3 - Core Infrastructure Hardening: Shared (61 findings)"
    4. Report summary: findings fixed by severity (4 crit, 18 med, 39 low), files modified, test count
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>1 commit created with all 61 Shared fixes; solution builds; all tests pass</done>
</task>

</tasks>

<verification>
- All 61 findings from CONSOLIDATED-FINDINGS.md lines 3470-3535 have corresponding tests
- All tests in DataWarehouse.Hardening.Tests/Shared/ pass (GREEN)
- Full solution builds with 0 errors
- Full `dotnet test` passes with 0 failures
</verification>

<success_criteria>
- 61 hardening tests exist in DataWarehouse.Hardening.Tests/Shared/
- All 61 tests pass
- All 61 production code issues fixed
- 1 commit with descriptive message
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/098-hardening-core-infrastructure/098-04-SUMMARY.md`
</output>
