---
phase: 098-hardening-core-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["098-01"]
files_modified:
  - DataWarehouse.Hardening.Tests/Kernel/
  - Kernel/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03]

must_haves:
  truths:
    - "All 148 Kernel findings have corresponding failing tests before fixes"
    - "All 148 Kernel findings have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Kernel/"
      provides: "Hardening tests for all 148 Kernel findings"
    - path: "Kernel/"
      provides: "Production fixes for all 148 Kernel findings"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Kernel/"
      to: "Kernel/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*Kernel"
---

<objective>
Stage 1 - Step 1-3 - Core Infrastructure Hardening: Kernel

Fix all 148 findings (14 critical, 28 high, 80 medium, 26 low) in Kernel project using TDD methodology.
Source: CONSOLIDATED-FINDINGS.md lines 479-631.

Purpose: Harden the DataWarehouse Kernel — the message bus, pipeline orchestrator, storage service, plugin loader, and core runtime — against all audit-identified vulnerabilities.
Output: 148 hardening tests + 148 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 479-631 — Kernel section, 148 findings)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop for all 148 Kernel findings</name>
  <files>
    DataWarehouse.Hardening.Tests/Kernel/ (new test files),
    Kernel/AdvancedMessageBus.cs,
    Kernel/AuthenticatedMessageBusDecorator.cs,
    Kernel/ContainerManager.cs,
    Kernel/DataWarehouseKernel.cs,
    Kernel/EnhancedPipelineOrchestrator.cs,
    Kernel/InMemoryStoragePlugin.cs,
    Kernel/KernelLogger.cs,
    Kernel/KernelStorageService.cs,
    Kernel/KnowledgeLake.cs,
    Kernel/MemoryPressureMonitor.cs,
    Kernel/MessageBus.cs,
    Kernel/PipelineMigrationEngine.cs,
    Kernel/PipelineOrchestrator.cs,
    Kernel/PipelinePluginIntegration.cs,
    Kernel/PipelinePolicyManager.cs,
    Kernel/PipelineTransaction.cs,
    Kernel/PluginCapabilityRegistry.cs,
    Kernel/PluginLoader.cs
  </files>
  <action>
    Read CONSOLIDATED-FINDINGS.md lines 479-631 for all 148 Kernel findings.
    Process EVERY finding sequentially (finding #1 through #148) using TDD:

    For EACH finding:
    1. Write a test in `DataWarehouse.Hardening.Tests/Kernel/` that exposes the vulnerability/issue
       - Use Coyote for concurrency findings (race conditions, TOCTOU, thread pool storms, non-atomic ops)
       - Use xUnit for logic/memory findings (missing disposal, stubs, fire-and-forget, suspicious casts)
       - Test name format: `Finding{N}_{BriefDescription}`
    2. Run test — confirm RED
    3. Fix the production code
    4. Run test — confirm GREEN
    5. Move to next finding

    Group tests by file into test classes:
    - `AdvancedMessageBusHardeningTests.cs` — findings 1-11
    - `AuthenticatedMessageBusDecoratorTests.cs` — findings 12-21
    - `ContainerManagerTests.cs` — findings 22-27
    - `DataWarehouseKernelTests.cs` — findings 27-43
    - `EnhancedPipelineOrchestratorTests.cs` — findings 44-54
    - `InMemoryStoragePluginTests.cs` — findings 55-56
    - `KernelLoggerTests.cs` — findings 57-65
    - `KernelStorageServiceTests.cs` — findings 66-73
    - `KnowledgeLakeTests.cs` — findings 74-81
    - `MemoryPressureMonitorTests.cs` — findings 82-90
    - `MessageBusTests.cs` — findings 91-98
    - `PipelineMigrationEngineTests.cs` — findings 99-111
    - `PipelineOrchestratorTests.cs` — findings 112-122
    - `PipelinePluginIntegrationTests.cs` — findings 123-129
    - `PipelinePolicyManagerTests.cs` — findings 130-132
    - `PipelineTransactionTests.cs` — finding 133
    - `PluginCapabilityRegistryTests.cs` — findings 134-138
    - `PluginLoaderTests.cs` — findings 139-148

    Key critical findings requiring special attention:
    - #4/#98: PublishAndWaitAsync Task.WhenAll without per-handler deadline — add configurable timeouts
    - #12: AuthenticatedMessageBusDecorator Timer leak — implement IDisposable
    - #22: ContainerManager missing ACL verification — add permission checks
    - #30/#39/#40: Fire-and-forget StartAsync — observe and escalate fatal exceptions
    - #91: SlidingWindowRateLimiter TOCTOU — make check-and-increment atomic
    - #106-107: PipelineMigrationEngine ReverseStageAsync stub — implement rollback
    - #130-131: PipelinePolicyManager null securityContext allows group admin — fix security inconsistency
    - #143: PluginLoader HandshakeRequest.KernelId wrong value — use actual kernel ID

    For duplicate/near-duplicate findings, write ONE test covering both, note dedup in comments.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Kernel" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 148 Kernel findings have passing hardening tests; production code fixed; solution builds with 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Commit and verify full solution build</name>
  <files>
    DataWarehouse.Hardening.Tests/Kernel/,
    Kernel/
  </files>
  <action>
    1. Run `dotnet build` on full solution to confirm 0 errors
    2. Run `dotnet test` on full test suite to confirm no regressions
    3. Commit all changes with message: "test+fix(098-02): Stage 1 - Step 1-3 - Core Infrastructure Hardening: Kernel (148 findings)"
    4. Report summary: findings fixed by severity, files modified, test count
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>1 commit created with all 148 Kernel fixes; solution builds; all tests pass</done>
</task>

</tasks>

<verification>
- All 148 findings from CONSOLIDATED-FINDINGS.md lines 479-631 have corresponding tests
- All tests in DataWarehouse.Hardening.Tests/Kernel/ pass (GREEN)
- Full solution builds with 0 errors
- Full `dotnet test` passes with 0 failures
</verification>

<success_criteria>
- 148 hardening tests exist in DataWarehouse.Hardening.Tests/Kernel/
- All 148 tests pass
- All 148 production code issues fixed
- 1 commit with descriptive message
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/098-hardening-core-infrastructure/098-02-SUMMARY.md`
</output>
