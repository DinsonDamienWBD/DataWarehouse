---
phase: 098-hardening-core-infrastructure
plan: 01
type: execute
wave: 1
depends_on: ["097-05"]
files_modified:
  - DataWarehouse.Hardening.Tests/AedsCore/
  - AedsCore/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "All 139 AedsCore findings have corresponding failing tests before fixes"
    - "All 139 AedsCore findings have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/AedsCore/"
      provides: "Hardening tests for all 139 AedsCore findings"
    - path: "AedsCore/"
      provides: "Production fixes for all 139 AedsCore findings"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/AedsCore/"
      to: "AedsCore/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*AedsCore"
---

<objective>
Stage 1 - Step 1-3 - Core Infrastructure Hardening: AedsCore

Fix all 139 findings (7 critical, 35 high, 70 medium, 27 low) in AedsCore project using TDD methodology.
Source: CONSOLIDATED-FINDINGS.md lines 113-256.

Purpose: Harden AedsCore — the AEDS system core including control plane, data plane, extensions, and scaling — against all audit-identified vulnerabilities.
Output: 139 hardening tests + 139 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 113-256 — AedsCore section, 139 findings)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop for all 139 AedsCore findings</name>
  <files>
    DataWarehouse.Hardening.Tests/AedsCore/ (new test files),
    AedsCore/Adapters/MeshNetworkAdapter.cs,
    AedsCore/AedsCorePlugin.cs,
    AedsCore/ClientCourierPlugin.cs,
    AedsCore/ControlPlane/GrpcControlPlanePlugin.cs,
    AedsCore/ControlPlane/WebSocketControlPlanePlugin.cs,
    AedsCore/DataPlane/Http3DataPlanePlugin.cs,
    AedsCore/Extensions/CodeSigningPlugin.cs,
    AedsCore/Extensions/DeltaSyncPlugin.cs,
    AedsCore/Extensions/GlobalDeduplicationPlugin.cs,
    AedsCore/Extensions/MulePlugin.cs,
    AedsCore/Extensions/NotificationPlugin.cs,
    AedsCore/Extensions/PolicyEnginePlugin.cs,
    AedsCore/Extensions/PreCogPlugin.cs,
    AedsCore/Extensions/SwarmIntelligencePlugin.cs,
    AedsCore/Extensions/ZeroTrustPairingPlugin.cs,
    AedsCore/Http2DataPlanePlugin.cs,
    AedsCore/Scaling/AedsScalingManager.cs,
    AedsCore/ServerDispatcherPlugin.cs,
    AedsCore/RateLimitingMiddleware.cs,
    AedsCore/PluginMarketplacePlugin.cs,
    AedsCore/IntentManifestSignerPlugin.cs,
    AedsCore/MqttControlPlanePlugin.cs,
    AedsCore/QuicDataPlanePlugin.cs,
    AedsCore/WebTransportDataPlanePlugin.cs,
    AedsCore/AuthController.cs,
    AedsCore/ConfigurationController.cs
  </files>
  <action>
    Read CONSOLIDATED-FINDINGS.md lines 113-256 for all 139 AedsCore findings.
    Process EVERY finding sequentially (finding #1 through #139) using TDD:

    For EACH finding:
    1. Write a test in `DataWarehouse.Hardening.Tests/AedsCore/` that exposes the vulnerability/issue
       - Use Coyote (`Microsoft.Coyote.Test`) for concurrency findings (race conditions, torn reads, non-atomic ops)
       - Use xUnit for logic/memory findings (stubs, missing disposal, empty catches, injection)
       - Test name format: `Finding{N}_{BriefDescription}` e.g. `Finding003_ClientCourierFireAndForgetLoops`
    2. Run test — confirm RED (test fails, proving the finding exists)
    3. Fix the production code to address the finding
    4. Run test — confirm GREEN (test passes, proving the fix works)
    5. Move to next finding

    Group tests by file into test classes:
    - `MeshNetworkAdapterTests.cs` — findings 1, 86-89
    - `AedsCorePluginTests.cs` — findings 2, 35-44
    - `ClientCourierPluginTests.cs` — findings 3-7, 50-61
    - `GrpcControlPlanePluginTests.cs` — findings 8, 66-71
    - `WebSocketControlPlanePluginTests.cs` — findings 9, 126-130
    - `Http3DataPlanePluginTests.cs` — findings 10-11, 78-83
    - `CodeSigningPluginTests.cs` — findings 12, 62-64
    - `DeltaSyncPluginTests.cs` — findings 13-16
    - `GlobalDeduplicationPluginTests.cs` — finding 17
    - `MulePluginTests.cs` — finding 18
    - `NotificationPluginTests.cs` — findings 19-21, 94
    - `PolicyEnginePluginTests.cs` — findings 22-23, 100-105
    - `PreCogPluginTests.cs` — findings 24-25, 106-107
    - `SwarmIntelligencePluginTests.cs` — findings 26, 122-125
    - `ZeroTrustPairingPluginTests.cs` — findings 27-30, 133-139
    - `Http2DataPlanePluginTests.cs` — findings 31, 72-77
    - `AedsScalingManagerTests.cs` — findings 32-33, 45-47
    - `ServerDispatcherPluginTests.cs` — findings 34, 118-121
    - `AuthControllerTests.cs` — findings 48-49
    - `RateLimitingMiddlewareTests.cs` — finding 117
    - `PluginMarketplacePluginTests.cs` — findings 95-99
    - `IntentManifestSignerPluginTests.cs` — findings 84-85
    - `MqttControlPlanePluginTests.cs` — findings 90-93
    - `QuicDataPlanePluginTests.cs` — findings 108-116
    - `WebTransportDataPlanePluginTests.cs` — findings 131-132
    - `ConfigurationControllerTests.cs` — finding 65

    Key critical findings requiring special attention:
    - #3: Fire-and-forget background loops in ClientCourier — observe faults, track tasks
    - #22: PolicyEngine LoadPolicyAsync stub — implement real JSON policy loading
    - #47: ComputeHitRate params by value with Interlocked.Read — fix to ref parameters
    - #48: AuthController zero users — implement proper user seeding
    - #56-57: Signature check only verifies non-empty — implement cryptographic verification

    For duplicate/near-duplicate findings (e.g., 36/37, 51/50, 66/67, etc.), write ONE test covering both, note the dedup in test comments.

    Ensure DataWarehouse.Hardening.Tests/AedsCore/ project references are set up (add to .csproj if needed).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~AedsCore" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 139 AedsCore findings have passing hardening tests; production code fixed; solution builds with 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Commit and verify full solution build</name>
  <files>
    DataWarehouse.Hardening.Tests/AedsCore/,
    AedsCore/
  </files>
  <action>
    1. Run `dotnet build` on full solution to confirm 0 errors
    2. Run `dotnet test` on full test suite to confirm no regressions
    3. Commit all changes with message: "test+fix(098-01): Stage 1 - Step 1-3 - Core Infrastructure Hardening: AedsCore (139 findings)"
    4. Report summary: findings fixed by severity, files modified, test count
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>1 commit created with all 139 AedsCore fixes; solution builds; all tests pass</done>
</task>

</tasks>

<verification>
- All 139 findings from CONSOLIDATED-FINDINGS.md lines 113-256 have corresponding tests
- All tests in DataWarehouse.Hardening.Tests/AedsCore/ pass (GREEN)
- Full solution builds with 0 errors
- Full `dotnet test` passes with 0 failures
</verification>

<success_criteria>
- 139 hardening tests exist in DataWarehouse.Hardening.Tests/AedsCore/
- All 139 tests pass
- All 139 production code issues fixed
- 1 commit with descriptive message
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/098-hardening-core-infrastructure/098-01-SUMMARY.md`
</output>
