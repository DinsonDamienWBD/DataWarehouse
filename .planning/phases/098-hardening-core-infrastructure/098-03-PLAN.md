---
phase: 098-hardening-core-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["098-02"]
files_modified:
  - DataWarehouse.Hardening.Tests/Plugin/
  - Plugins/
autonomous: true
requirements: [HARD-01, HARD-02, HARD-03, HARD-04, HARD-05]

must_haves:
  truths:
    - "All 195 Plugin findings have corresponding failing tests before fixes"
    - "All 195 Plugin findings have production code fixes that make tests pass"
    - "Solution builds with 0 errors after commit"
    - "dotnet test passes after commit"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Plugin/"
      provides: "Hardening tests for all 195 Plugin findings"
    - path: "Plugins/"
      provides: "Production fixes for all 195 Plugin findings"
  key_links:
    - from: "DataWarehouse.Hardening.Tests/Plugin/"
      to: "Plugins/"
      via: "Test references production code, confirms each finding is fixed"
      pattern: "using.*Plugin"
---

<objective>
Stage 1 - Step 1-3 - Core Infrastructure Hardening: Plugin

Fix all 195 findings (3 critical, 96 high, 72 medium, 24 low) in the Plugin project using TDD methodology.
Source: CONSOLIDATED-FINDINGS.md lines 650-849.

Purpose: Harden Plugin-level connector strategies — systemic issues across 50+ connector files including fake auth, HttpClient leaks, socket exhaustion, IPv6 parsing, health check inefficiency, and missing cancellation support.
Output: 195 hardening tests + 195 production fixes, 1 commit.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Metadata/production-audit-2026-03-05/CONSOLIDATED-FINDINGS.md (lines 650-849 — Plugin section, 195 findings)
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD loop for all 195 Plugin findings</name>
  <files>
    DataWarehouse.Hardening.Tests/Plugin/ (new test files),
    Plugins/ (multiple connector strategy files across UltimateConnector and other plugin projects)
  </files>
  <action>
    Read CONSOLIDATED-FINDINGS.md lines 650-849 for all 195 Plugin findings.
    Process EVERY finding sequentially (finding #1 through #195) using TDD:

    For EACH finding:
    1. Write a test in `DataWarehouse.Hardening.Tests/Plugin/` that exposes the vulnerability/issue
       - Use Coyote for concurrency findings (race conditions, socket exhaustion under load)
       - Use xUnit for logic/memory findings (fake auth, missing disposal, IPv6 parsing, empty catches)
       - Test name format: `Finding{N}_{BriefDescription}`
    2. Run test — confirm RED
    3. Fix the production code
    4. Run test — confirm GREEN
    5. Move to next finding

    CRITICAL — SYSTEMIC FINDINGS (findings 1-10):
    These are cross-cutting issues affecting MANY files simultaneously. Handle as follows:
    - Finding #1 (CRITICAL): AuthenticateAsync returns fake Guid.NewGuid token in ALL 11 simple strategies — fix ALL 11 to implement real credential exchange or throw NotConfiguredException with clear message
    - Findings #2-3 (HIGH): new HttpClient per-connection in ALL 15+ files — replace with IHttpClientFactory pattern everywhere
    - Finding #4 (MEDIUM): Split(':') fails for IPv6 in ALL TCP/UDP strategies — fix to use Uri.TryCreate or IPEndPoint.TryParse
    - Finding #5 (HIGH): DisconnectCoreAsync only disposes HttpClient — fix to dispose all handle resources
    - Finding #6 (MEDIUM): Full HTTP round-trip per health check — add cached health status with TTL
    - Findings #7-10: Various systemic patterns (cancellation, error handling, metrics)

    For systemic findings, create one parameterized test class per systemic pattern that validates ALL affected files, then fix all affected files.

    Remaining findings #11-195 are per-file issues — group test classes by strategy file or plugin.

    Ensure DataWarehouse.Hardening.Tests/Plugin/ project references are set up correctly for all affected plugin projects.
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet test DataWarehouse.Hardening.Tests/ --filter "FullyQualifiedName~Plugin" --no-restore -v minimal 2>&1 | tail -5</automated>
  </verify>
  <done>All 195 Plugin findings have passing hardening tests; production code fixed; solution builds with 0 errors</done>
</task>

<task type="auto">
  <name>Task 2: Commit and verify full solution build</name>
  <files>
    DataWarehouse.Hardening.Tests/Plugin/,
    Plugins/
  </files>
  <action>
    1. Run `dotnet build` on full solution to confirm 0 errors
    2. Run `dotnet test` on full test suite to confirm no regressions
    3. Commit all changes with message: "test+fix(098-03): Stage 1 - Step 1-3 - Core Infrastructure Hardening: Plugin (195 findings)"
    4. Report summary: findings fixed by severity (3 crit, 96 high, 72 med, 24 low), files modified, test count
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.slnx --no-restore -v minimal 2>&1 | tail -3</automated>
  </verify>
  <done>1 commit created with all 195 Plugin fixes; solution builds; all tests pass</done>
</task>

</tasks>

<verification>
- All 195 findings from CONSOLIDATED-FINDINGS.md lines 650-849 have corresponding tests
- All tests in DataWarehouse.Hardening.Tests/Plugin/ pass (GREEN)
- Full solution builds with 0 errors
- Full `dotnet test` passes with 0 failures
</verification>

<success_criteria>
- 195 hardening tests exist in DataWarehouse.Hardening.Tests/Plugin/
- All 195 tests pass
- All 195 production code issues fixed
- 1 commit with descriptive message
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/098-hardening-core-infrastructure/098-03-SUMMARY.md`
</output>
