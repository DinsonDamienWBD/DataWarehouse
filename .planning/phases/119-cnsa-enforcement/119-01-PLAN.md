---
phase: 119-cnsa-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Security/CnsaSuitePolicy.cs
  - DataWarehouse.SDK/Security/CnsaSuiteEnforcer.cs
  - DataWarehouse.Hardening.Tests/Security/CnsaSuiteEnforcerTests.cs
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "CNSA 2.0 approved algorithms are enumerated and selectable via CryptoSuitePolicy"
    - "Non-approved algorithms are rejected at runtime when CNSA enforcement is active"
    - "Suite.CnsaClassified and Suite.CnsaTopSecret presets select correct algorithm sets"
  artifacts:
    - path: "DataWarehouse.SDK/Security/CnsaSuitePolicy.cs"
      provides: "CNSA 2.0 algorithm list, CryptoSuitePolicy, Suite enum"
      exports: ["CryptoSuitePolicy", "CnsaSuite", "CnsaAlgorithmList"]
    - path: "DataWarehouse.SDK/Security/CnsaSuiteEnforcer.cs"
      provides: "Runtime enforcer that validates algorithm selection against CNSA policy"
      exports: ["CnsaSuiteEnforcer", "ICnsaSuiteEnforcer"]
    - path: "DataWarehouse.Hardening.Tests/Security/CnsaSuiteEnforcerTests.cs"
      provides: "Tests for CNSA enforcement"
  key_links:
    - from: "DataWarehouse.SDK/Security/CnsaSuiteEnforcer.cs"
      to: "DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs"
      via: "ICryptographicAlgorithmRegistry integration"
      pattern: "ICryptographicAlgorithmRegistry"
    - from: "DataWarehouse.SDK/Security/CnsaSuitePolicy.cs"
      to: "DataWarehouse.SDK/Contracts/Encryption/CryptoAgilityTypes.cs"
      via: "AlgorithmCategory and AlgorithmProfile references"
      pattern: "AlgorithmCategory|AlgorithmProfile"
---

<objective>
Create CNSA 2.0 algorithm suite enforcer with CryptoSuitePolicy and Suite presets.

Purpose: NSA CNSA 2.0 requires specific algorithm suites for Classified and Top Secret data. The crypto registry exists but doesn't enforce military-grade algorithm selection.
Output: CnsaSuitePolicy enum/class, CnsaSuiteEnforcer, and hardening tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs
@DataWarehouse.SDK/Contracts/Encryption/CryptoAgilityTypes.cs
@Plugins/DataWarehouse.Plugins.UltimateEncryption/Features/CipherPresets.cs

<interfaces>
<!-- Key types and contracts the executor needs -->

From DataWarehouse.SDK/Security/CryptographicAlgorithmRegistry.cs:
```csharp
public interface ICryptographicAlgorithmRegistry
{
    HashAlgorithmName DefaultHashAlgorithm { get; }
    HashAlgorithmName DefaultHmacAlgorithm { get; }
    IReadOnlyList<HashAlgorithmName> SupportedHashAlgorithms { get; }
    bool IsFipsApproved(HashAlgorithmName algorithmName);
}
```

From DataWarehouse.SDK/Contracts/Encryption/CryptoAgilityTypes.cs:
```csharp
public enum AlgorithmCategory { SymmetricEncryption, AsymmetricEncryption, KeyExchange, DigitalSignature, HashFunction, KeyDerivation }
public record AlgorithmProfile { public required string AlgorithmId { get; init; } public required string AlgorithmName { get; init; } }
```

From Plugins/.../Features/CipherPresets.cs:
```csharp
public enum CipherPreset { ..., Classified, TopSecret, ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define CnsaSuitePolicy and CNSA 2.0 algorithm list</name>
  <files>DataWarehouse.SDK/Security/CnsaSuitePolicy.cs</files>
  <behavior>
    - CnsaSuite.CnsaClassified selects: AES-256-GCM, SHA-384, ECDH P-384, ECDSA P-384, RSA-3072+, ML-KEM-768 (hybrid)
    - CnsaSuite.CnsaTopSecret selects: AES-256-GCM, SHA-512, ECDH P-384, ECDSA P-384, RSA-4096, ML-KEM-1024
    - CnsaAlgorithmList.IsApproved(algorithmId, suite) returns true only for CNSA-approved algorithms
    - CnsaAlgorithmList.GetApprovedAlgorithms(suite, category) returns filtered list by AlgorithmCategory
  </behavior>
  <action>
    Create `DataWarehouse.SDK/Security/CnsaSuitePolicy.cs` with:
    1. `CnsaSuite` enum: `None`, `CnsaClassified`, `CnsaTopSecret`
    2. `CryptoSuitePolicy` record: holds active `CnsaSuite`, minimum key sizes per category, allowed algorithm IDs
    3. `CnsaAlgorithmList` static class with:
       - CNSA 2.0 Classified approved list: AES-256 (symmetric), SHA-384+ (hash), ECDH/ECDSA P-384 (asymmetric/signature), RSA-3072+ (legacy asymmetric), ML-KEM-768 (PQ KEM transition), ML-DSA-65 (PQ signature transition)
       - CNSA 2.0 TopSecret approved list: AES-256 (symmetric), SHA-512 (hash), ECDH/ECDSA P-384 (asymmetric/signature), RSA-4096 (legacy asymmetric), ML-KEM-1024 (PQ KEM), ML-DSA-87 (PQ signature)
       - `IsApproved(string algorithmId, CnsaSuite suite)` method
       - `GetApprovedAlgorithms(CnsaSuite suite, AlgorithmCategory? category = null)` method
       - `GetMinimumKeySize(CnsaSuite suite, AlgorithmCategory category)` method
    4. Reference `AlgorithmCategory` from `DataWarehouse.SDK.Contracts.Encryption`.
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 119: CNSA 2.0 algorithm suite policy")]`.
  </action>
  <verify>
    <automated>dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>CnsaSuite enum, CryptoSuitePolicy, and CnsaAlgorithmList exist with complete CNSA 2.0 algorithm lists for both Classified and TopSecret suites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CnsaSuiteEnforcer runtime enforcement + tests</name>
  <files>DataWarehouse.SDK/Security/CnsaSuiteEnforcer.cs, DataWarehouse.Hardening.Tests/Security/CnsaSuiteEnforcerTests.cs</files>
  <behavior>
    - Enforcer.Validate(algorithmId, suite) throws CryptographicException for non-approved algorithms
    - Enforcer.Validate(algorithmId, CnsaSuite.None) always passes (enforcement disabled)
    - Enforcer validates key size minimums (RSA-2048 rejected under CnsaClassified, RSA-3072 accepted)
    - Enforcer integrates with ICryptographicAlgorithmRegistry for runtime checks
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/CnsaSuiteEnforcer.cs`:
       - `ICnsaSuiteEnforcer` interface with `Validate(string algorithmId, int keySizeBits, CnsaSuite suite)` and `IsCompliant(string algorithmId, int keySizeBits, CnsaSuite suite)`
       - `CnsaSuiteEnforcer` implementation that uses `CnsaAlgorithmList` for validation
       - On violation: throw `CryptographicException($"Algorithm '{algorithmId}' (key={keySizeBits}) is not approved for CNSA 2.0 {suite}")`
       - Thread-safe, stateless — all methods are pure functions against the static approved list
    2. Create `DataWarehouse.Hardening.Tests/Security/CnsaSuiteEnforcerTests.cs`:
       - Test: AES-256 approved for both suites
       - Test: AES-128 rejected for both suites
       - Test: SHA-256 rejected for CnsaClassified (requires SHA-384+)
       - Test: SHA-384 approved for CnsaClassified, rejected for CnsaTopSecret
       - Test: SHA-512 approved for CnsaTopSecret
       - Test: RSA-2048 rejected, RSA-3072 approved for Classified, RSA-4096 required for TopSecret
       - Test: ML-KEM-768 approved for Classified, ML-KEM-1024 required for TopSecret
       - Test: CnsaSuite.None disables all enforcement
       - Test: Unknown algorithm ID rejected
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 119: CNSA 2.0 suite enforcer")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~CnsaSuiteEnforcer" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>CnsaSuiteEnforcer validates algorithm selection at runtime. All CNSA enforcement tests pass. Non-approved algorithms throw CryptographicException.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "CnsaSuiteEnforcer"` — all tests green
</verification>

<success_criteria>
- CNSA 2.0 algorithm lists defined for Classified and TopSecret
- Runtime enforcer rejects non-approved algorithms
- All hardening tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/119-cnsa-enforcement/119-01-SUMMARY.md`
</output>
