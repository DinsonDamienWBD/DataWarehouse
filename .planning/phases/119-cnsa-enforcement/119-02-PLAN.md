---
phase: 119-cnsa-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.SDK/Security/CnsaTransitionTracker.cs
  - DataWarehouse.Hardening.Tests/Security/CnsaTransitionTrackerTests.cs
autonomous: true
requirements: [SEC-02]

must_haves:
  truths:
    - "Phase 1 (now-2030) allows classical + PQ hybrid algorithms"
    - "Phase 2 (2030+) requires PQ-only algorithms"
    - "TransitionPolicy auto-selects algorithm set based on current date"
    - "Algorithm usage is audit-logged with transition phase context"
  artifacts:
    - path: "DataWarehouse.SDK/Security/CnsaTransitionTracker.cs"
      provides: "CNSA 2.0 transition timeline tracker with Phase 1/2 enforcement"
      exports: ["CnsaTransitionTracker", "TransitionPhase", "TransitionPolicy"]
    - path: "DataWarehouse.Hardening.Tests/Security/CnsaTransitionTrackerTests.cs"
      provides: "Tests for transition timeline enforcement"
  key_links:
    - from: "DataWarehouse.SDK/Security/CnsaTransitionTracker.cs"
      to: "DataWarehouse.SDK/Security/CnsaSuitePolicy.cs"
      via: "CnsaSuite and CnsaAlgorithmList references"
      pattern: "CnsaSuite|CnsaAlgorithmList"
---

<objective>
Implement CNSA 2.0 transition timeline tracker with Phase 1 (classical+PQ hybrid) and Phase 2 (PQ-only) enforcement.

Purpose: NSA CNSA 2.0 mandates a phased transition from classical to post-quantum algorithms. Phase 1 (now-2030) permits hybrid; Phase 2 (2030+) requires PQ-only.
Output: TransitionPolicy, CnsaTransitionTracker, audit logging, and tests.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DataWarehouse.SDK/Security/CnsaSuitePolicy.cs
@DataWarehouse.SDK/Contracts/Encryption/CryptoAgilityTypes.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CnsaTransitionTracker with Phase 1/2 timeline</name>
  <files>DataWarehouse.SDK/Security/CnsaTransitionTracker.cs, DataWarehouse.Hardening.Tests/Security/CnsaTransitionTrackerTests.cs</files>
  <behavior>
    - TransitionPhase.Phase1 active when date < 2030-01-01 UTC
    - TransitionPhase.Phase2 active when date >= 2030-01-01 UTC
    - Phase 1: classical algorithms (RSA, ECDSA, ECDH) allowed alongside PQ (ML-KEM, ML-DSA)
    - Phase 2: only PQ algorithms allowed for KEM and signatures; classical RSA/ECDSA rejected
    - Phase 2: AES-256 and SHA-384/512 remain approved (symmetric/hash unchanged)
    - GetRecommendedAlgorithm(category, suite, date) returns best algorithm for current phase
    - LogAlgorithmUsage records algorithm ID, suite, transition phase, timestamp
  </behavior>
  <action>
    1. Create `DataWarehouse.SDK/Security/CnsaTransitionTracker.cs`:
       - `TransitionPhase` enum: `Phase1` (hybrid classical+PQ), `Phase2` (PQ-only)
       - `TransitionPolicy` record: `CnsaSuite Suite`, `DateTimeOffset CutoverDate` (default 2030-01-01 UTC), `bool StrictMode`
       - `CnsaTransitionTracker` class:
         - `GetCurrentPhase(DateTimeOffset? asOf = null)` — returns Phase1 or Phase2
         - `IsAlgorithmAllowed(string algorithmId, AlgorithmCategory category, TransitionPhase phase)` — Phase2 rejects classical asymmetric/signature algorithms
         - `GetRecommendedAlgorithm(AlgorithmCategory category, CnsaSuite suite, DateTimeOffset? asOf = null)` — returns best algorithm for phase
         - `CreateUsageAuditEntry(string algorithmId, CnsaSuite suite, TransitionPhase phase)` — returns `AlgorithmUsageAudit` record
       - `AlgorithmUsageAudit` record: `AlgorithmId`, `Suite`, `Phase`, `Timestamp`, `IsCompliant`, `DeprecationWarning`
       - Phase 2 rejection list: RSA-* (all key sizes), ECDH-P384, ECDSA-P384 for KEM/signature categories
       - Phase 2 approval list: ML-KEM-1024, ML-DSA-87, SLH-DSA-* for KEM/signature categories
    2. Create tests:
       - Test: Phase1 active before 2030-01-01
       - Test: Phase2 active on/after 2030-01-01
       - Test: RSA-3072 allowed in Phase1, rejected in Phase2
       - Test: ML-KEM-1024 allowed in both phases
       - Test: AES-256 allowed in both phases (symmetric unaffected)
       - Test: GetRecommendedAlgorithm returns ML-KEM-1024 in Phase2 for KeyExchange
       - Test: GetRecommendedAlgorithm returns ECDH-P384 in Phase1 for KeyExchange (when not in TopSecret)
       - Test: Audit entry correctly records compliance status
    Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 119: CNSA 2.0 transition timeline tracker")]`.
  </action>
  <verify>
    <automated>dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~CnsaTransitionTracker" --no-restore -v minimal 2>&1 | tail -10</automated>
  </verify>
  <done>CnsaTransitionTracker enforces Phase 1/Phase 2 timeline. Classical algorithms rejected in Phase 2. Audit entries created. All tests pass.</done>
</task>

</tasks>

<verification>
- `dotnet build DataWarehouse.SDK/DataWarehouse.SDK.csproj` succeeds
- `dotnet test DataWarehouse.Hardening.Tests/ --filter "CnsaTransitionTracker"` — all green
</verification>

<success_criteria>
- Phase 1/Phase 2 transition timeline enforced based on date
- Classical algorithms rejected in Phase 2
- Audit logging for algorithm usage with transition context
</success_criteria>

<output>
After completion, create `.planning/phases/119-cnsa-enforcement/119-02-SUMMARY.md`
</output>
