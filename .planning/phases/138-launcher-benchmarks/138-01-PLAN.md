---
phase: 138-launcher-benchmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DataWarehouse.Launcher/Federation/ClusterJoinService.cs
  - DataWarehouse.Launcher/Federation/TopologyAwarePluginLoader.cs
  - DataWarehouse.Launcher/Program.cs
autonomous: true
requirements: [APP-05]

must_haves:
  truths:
    - "Launcher supports --cluster-seed <ip:port> for auto-join"
    - "Plugin loading is topology-aware (server vs edge profile)"
    - "ClusterTopologyManager integration for node discovery"
  artifacts:
    - path: "DataWarehouse.Launcher/Federation/ClusterJoinService.cs"
      provides: "Auto-join cluster on startup"
      exports: ["ClusterJoinService"]
    - path: "DataWarehouse.Launcher/Federation/TopologyAwarePluginLoader.cs"
      provides: "Topology-based plugin selection"
      exports: ["TopologyAwarePluginLoader"]
  key_links:
    - from: "DataWarehouse.Launcher/Program.cs"
      to: "DataWarehouse.Launcher/Federation/ClusterJoinService.cs"
      via: "Startup command-line arg --cluster-seed"
      pattern: "--cluster-seed"
    - from: "DataWarehouse.Launcher/Federation/TopologyAwarePluginLoader.cs"
      to: "DataWarehouse.Launcher/PluginProfileLoader.cs"
      via: "Profile-based plugin selection"
      pattern: "PluginProfileLoader|LoadProfile"
---

<objective>
Add federation support to Launcher: --cluster-seed auto-join, topology-aware plugin loading.

Purpose: Launcher must support clustered deployments. --cluster-seed enables automatic cluster join. Topology-aware loading ensures server nodes load storage plugins while edge nodes load lightweight set.
Output: ClusterJoinService, TopologyAwarePluginLoader, updated Program.cs with --cluster-seed option.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.Launcher/Program.cs
@DataWarehouse.Launcher/PluginProfileLoader.cs
@DataWarehouse.Launcher/Integration/DataWarehouseHost.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClusterJoinService and TopologyAwarePluginLoader</name>
  <files>
    DataWarehouse.Launcher/Federation/ClusterJoinService.cs,
    DataWarehouse.Launcher/Federation/TopologyAwarePluginLoader.cs
  </files>
  <action>
ClusterJoinService.cs:
1. Accept seed address(es) as constructor param
2. On StartAsync: connect to seed node, exchange topology info, register self
3. Support multiple seed addresses for resilience (try in order)
4. Retry logic: 3 attempts with exponential backoff (1s, 5s, 15s)
5. Once joined: receive topology updates via MessageBus subscription
6. Handle network partition: detect via heartbeat timeout (30s), attempt rejoin
7. Graceful leave on shutdown: notify cluster before exit

TopologyAwarePluginLoader.cs:
1. Extend PluginProfileLoader behavior
2. Node roles: Server (full plugin set — storage, compute, security), Edge (lightweight — cache, encryption, compression only), Gateway (interface plugins only), Worker (compute-only)
3. Read role from --profile arg or auto-detect based on hardware capabilities
4. Filter plugin set based on role before loading
5. Log which plugins were skipped due to role: "Skipping UltimateStorage: not needed for Edge role"
6. Support override: --force-plugin <name> to load a plugin regardless of role
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Launcher/DataWarehouse.Launcher.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>ClusterJoinService and TopologyAwarePluginLoader compile with cluster join and role-based plugin filtering.</done>
</task>

<task type="auto">
  <name>Task 2: Wire federation support in Program.cs</name>
  <files>DataWarehouse.Launcher/Program.cs</files>
  <action>
Update Program.cs:
1. Add --cluster-seed option (comma-separated list of ip:port)
2. Add --node-role option (server/edge/gateway/worker/auto)
3. If --cluster-seed provided: instantiate ClusterJoinService, call StartAsync before kernel boot
4. Use TopologyAwarePluginLoader instead of PluginProfileLoader when role is specified
5. On shutdown: call ClusterJoinService.LeaveAsync() for graceful cluster exit
6. Log cluster status: "Joined cluster at {seed}, role: {role}, {n} nodes active"
7. If --cluster-seed fails all retries: log warning but continue as standalone node
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Launcher/DataWarehouse.Launcher.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>Program.cs supports --cluster-seed and --node-role. Federation wired into startup/shutdown lifecycle.</done>
</task>

</tasks>

<verification>
- ClusterJoinService.cs handles cluster join/leave
- TopologyAwarePluginLoader.cs filters plugins by role
- Program.cs has --cluster-seed and --node-role options
- `dotnet build DataWarehouse.Launcher` succeeds
</verification>

<success_criteria>
Launcher supports --cluster-seed for auto-join with retry. Topology-aware plugin loading filters by node role. Graceful cluster leave on shutdown. Launcher project builds.
</success_criteria>

<output>
After completion, create `.planning/phases/138-launcher-benchmarks/138-01-SUMMARY.md`
</output>
