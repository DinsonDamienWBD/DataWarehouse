---
phase: 138-launcher-benchmarks
plan: 04
type: execute
wave: 2
depends_on: [138-01]
files_modified:
  - DataWarehouse.Benchmarks/VdePipelineBenchmarks.cs
  - DataWarehouse.Benchmarks/FederationBenchmarks.cs
  - DataWarehouse.Benchmarks/PolicyEngineBenchmarks.cs
  - DataWarehouse.Benchmarks/RaidParityBenchmarks.cs
  - DataWarehouse.Benchmarks/ShardOperationBenchmarks.cs
  - DataWarehouse.Benchmarks/WalGroupCommitBenchmarks.cs
  - DataWarehouse.Benchmarks/Program.cs
autonomous: true
requirements: [APP-06]

must_haves:
  truths:
    - "VDE pipeline benchmark measures end-to-end decorator chain throughput"
    - "Federation benchmark measures node join, sync, and message latency"
    - "Policy engine benchmark measures evaluation throughput"
    - "RAID parity benchmark measures compute and rebuild throughput"
    - "Shard operations benchmark measures split/merge/migrate latency"
    - "WAL group commit benchmark measures batched write throughput"
    - "All benchmarks report P50/P99/P999 latencies"
  artifacts:
    - path: "DataWarehouse.Benchmarks/VdePipelineBenchmarks.cs"
      provides: "VDE pipeline benchmarks"
      contains: "[Benchmark]"
    - path: "DataWarehouse.Benchmarks/FederationBenchmarks.cs"
      provides: "Federation benchmarks"
      contains: "[Benchmark]"
  key_links:
    - from: "DataWarehouse.Benchmarks/Program.cs"
      to: "DataWarehouse.Benchmarks/*Benchmarks.cs"
      via: "BenchmarkDotNet runner"
      pattern: "BenchmarkRunner\\.Run|BenchmarkSwitcher"
---

<objective>
Expand benchmark suite to cover all v6.0 subsystems with comprehensive P50/P99/P999 measurements.

Purpose: APP-06 requires benchmarks for all v6.0 subsystems. Current suite covers only basic storage, crypto, serialization, concurrency, memory, compression.
Output: 6 new benchmark classes covering VDE pipeline, federation, policy, RAID, shards, WAL.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@DataWarehouse.Benchmarks/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create VDE, Federation, and Policy benchmarks</name>
  <files>
    DataWarehouse.Benchmarks/VdePipelineBenchmarks.cs,
    DataWarehouse.Benchmarks/FederationBenchmarks.cs,
    DataWarehouse.Benchmarks/PolicyEngineBenchmarks.cs
  </files>
  <action>
All benchmarks use [MemoryDiagnoser], [RankColumn], and custom [PercentileColumn] for P50/P99/P999.

VdePipelineBenchmarks.cs:
- [Benchmark] WriteThrough_FullDecoratorChain — write 1MB through Cache->Integrity->Compression->Dedup->Encryption->WAL->RAID->File
- [Benchmark] ReadThrough_FullDecoratorChain — read 1MB through reverse chain
- [Benchmark] WriteThrough_MinimalChain — write 1MB through Cache->File only
- [Benchmark] MountPipeline — measure time to build decorator chain from SuperblockV2
- [Benchmark] WriteThrough_4KB_Sequential — small sequential writes (common workload)
- [Benchmark] WriteThrough_4KB_Random — small random writes

FederationBenchmarks.cs:
- [Benchmark] NodeJoin — time for a node to join cluster (simulated)
- [Benchmark] TopologySync — time to propagate topology change to N nodes
- [Benchmark] CrossNodeMessage — round-trip latency for inter-node messages
- [Benchmark] ConsensusRound — time for Raft consensus round (3/5/7 nodes)
- [Benchmark] ShardRouting — time to resolve shard location for a key

PolicyEngineBenchmarks.cs:
- [Benchmark] EvaluateSimplePolicy — evaluate a single-rule policy
- [Benchmark] EvaluateComplexPolicy — evaluate a 50-rule policy with AND/OR/NOT
- [Benchmark] CascadePolicy — cascade policy across 10 targets
- [Benchmark] DryRunPolicy — dry-run evaluation (no side effects)
- [Benchmark] PolicyDeserialization — parse policy JSON
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>3 benchmark classes compile with P50/P99/P999 measurements for VDE, federation, policy.</done>
</task>

<task type="auto">
  <name>Task 2: Create RAID, Shard, WAL benchmarks and update Program.cs</name>
  <files>
    DataWarehouse.Benchmarks/RaidParityBenchmarks.cs,
    DataWarehouse.Benchmarks/ShardOperationBenchmarks.cs,
    DataWarehouse.Benchmarks/WalGroupCommitBenchmarks.cs,
    DataWarehouse.Benchmarks/Program.cs
  </files>
  <action>
RaidParityBenchmarks.cs:
- [Benchmark] Raid5_ParityCompute_1MB — RAID-5 XOR parity for 1MB stripe
- [Benchmark] Raid6_ParityCompute_1MB — RAID-6 Reed-Solomon parity
- [Benchmark] Raid_Rebuild_1MB — simulate single-disk rebuild for 1MB
- [Benchmark] Raid_Write_Striped — write 1MB across 4-disk RAID-0

ShardOperationBenchmarks.cs:
- [Benchmark] ShardSplit_RangePartition — split shard by range
- [Benchmark] ShardSplit_HashPartition — split shard by hash
- [Benchmark] ShardMerge — merge two adjacent shards
- [Benchmark] ShardMigrate_1MB — migrate 1MB shard to new node (simulated)
- [Benchmark] ShardRebalance — rebalance 10 shards across 3 nodes

WalGroupCommitBenchmarks.cs:
- [Benchmark] SingleWrite — single WAL entry write
- [Benchmark] GroupCommit_10 — batch 10 entries in single fsync
- [Benchmark] GroupCommit_100 — batch 100 entries
- [Benchmark] GroupCommit_1000 — batch 1000 entries
- [Benchmark] WalReplay_1000 — replay 1000 WAL entries

Program.cs:
- Register all new benchmark classes in BenchmarkSwitcher
- Add --subsystem filter: `dotnet run -- --filter VdePipeline*` to run specific benchmarks
- Configure export: JSON + HTML + CSV results
- Configure artifacts directory: Metadata/benchmarks/
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Benchmarks/DataWarehouse.Benchmarks.csproj --no-restore 2>&1 | tail -5</automated>
  </verify>
  <done>3 benchmark classes compile. Program.cs registers all benchmarks with filtering and export config.</done>
</task>

</tasks>

<verification>
- 6 new benchmark files under DataWarehouse.Benchmarks/
- Each uses [MemoryDiagnoser] and reports P50/P99/P999
- Program.cs registers all benchmarks
- `dotnet build DataWarehouse.Benchmarks` succeeds
</verification>

<success_criteria>
6 new benchmark classes covering VDE pipeline, federation, policy engine, RAID parity, shard operations, WAL group commit. All report P50/P99/P999. BenchmarkSwitcher registers all. Project builds.
</success_criteria>

<output>
After completion, create `.planning/phases/138-launcher-benchmarks/138-04-SUMMARY.md`
</output>
