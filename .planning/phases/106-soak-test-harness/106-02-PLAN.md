---
phase: 106-soak-test-harness
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - Metadata/soak-test-report/soak-test-results.md
autonomous: true
requirements: [SOAK-04]

must_haves:
  truths:
    - "10-minute CI soak test runs to completion"
    - "Gen2 collection rate stays below threshold"
    - "Working set does not grow monotonically over soak duration"
    - "No event bus or telemetry sink memory leaks detected"
    - "Soak test results report exported"
  artifacts:
    - path: "Metadata/soak-test-report/soak-test-results.md"
      provides: "Stage 2 - Step 2 - Soak Test Results report"
      contains: "Gen2 Collection Rate"
  key_links:
    - from: "Metadata/soak-test-report/soak-test-results.md"
      to: "DataWarehouse.Hardening.Tests/Soak/SoakTestHarness.cs"
      via: "report generated from soak test execution output"
      pattern: "Soak"
---

<objective>
Run the 10-minute CI soak test, verify stable GC metrics with no monotonic growth, and generate the soak test results report.

Purpose: SOAK-04 requires Gen2 rate below threshold and no monotonic working set growth.
Output: Soak test results report in Metadata/soak-test-report/
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/106-soak-test-harness/106-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run 10-minute CI soak test and generate results report</name>
  <files>Metadata/soak-test-report/soak-test-results.md</files>
  <action>
1. Create directory `Metadata/soak-test-report/` if not exists.

2. Run the CI soak test (10 minutes):
   ```
   dotnet test DataWarehouse.Hardening.Tests/ --filter "Category=Soak&Duration=CI" --logger "console;verbosity=detailed" --timeout 900000
   ```
   (15-minute timeout to allow 10-minute soak + setup/teardown)

3. Capture test output (xUnit ITestOutputHelper output shows in console with detailed verbosity).

4. Generate `Metadata/soak-test-report/soak-test-results.md`:
   ```markdown
   # Stage 2 - Step 2 - Soak Test Results

   ## Summary
   - Date: {date}
   - Duration: 10 minutes (CI mode)
   - Concurrent workers: 4
   - Total operations: {count}
   - Total data processed: {GB}
   - Result: PASS / FAIL

   ## GC Metrics
   | Metric | Value | Threshold | Status |
   |--------|-------|-----------|--------|
   | Gen0 collections | {n} | - | INFO |
   | Gen1 collections | {n} | - | INFO |
   | Gen2 collections | {n} | {threshold} | OK/FAIL |
   | Gen2 rate (per min) | {n} | {threshold}/min | OK/FAIL |
   | Peak alloc rate | {MB/s} | - | INFO |

   ## Working Set Analysis
   | Metric | Value | Status |
   |--------|-------|--------|
   | Initial | {MB} | - |
   | Peak | {MB} | - |
   | Final | {MB} | - |
   | Growth | {pct}% | OK (< 10%) / FAIL |
   | Monotonic trend | Yes/No | OK (No) / FAIL (Yes) |

   ## Workload Summary
   | Metric | Value |
   |--------|-------|
   | Write operations | {count} |
   | Read operations | {count} |
   | Checksum mismatches | {count} (must be 0) |
   | Exceptions | {count} (must be 0) |
   | Deadlocks detected | {count} (must be 0) |

   ## GC Timeline (sampled every 5 seconds)
   | Time (min) | Gen0 | Gen1 | Gen2 | Heap (MB) | WorkingSet (MB) |
   |------------|------|------|------|-----------|-----------------|
   | 0:00 | ... | ... | ... | ... | ... |
   | 0:05 | ... | ... | ... | ... | ... |
   | ... | ... | ... | ... | ... | ... |
   | 10:00 | ... | ... | ... | ... | ... |

   ## Event Bus / Telemetry Leak Check
   - Event bus subscriptions at start: {n}
   - Event bus subscriptions at end: {n}
   - Delta: {n} (must be 0)
   - Telemetry sink buffer size at end: {bytes} (must be bounded)
   - Assessment: No leak / LEAK DETECTED

   ## Conclusion
   {Whether SOAK-03 and SOAK-04 are satisfied}
   {Recommendations for manual 24-72hr soak if needed}
   ```

5. If the soak test fails:
   - Document the specific failure (which metric exceeded threshold)
   - Include the GC timeline data for diagnosis
   - Recommend targeted investigation areas
   - Do NOT retry indefinitely — report the failure accurately

6. If the soak test passes, note that the 24-72hr manual soak is recommended before production certification but not required for CI gate.
  </action>
  <verify>
    <automated>test -f "C:/Temp/DataWarehouse/DataWarehouse/Metadata/soak-test-report/soak-test-results.md" &amp;&amp; grep -c "Gen2 Collection" "C:/Temp/DataWarehouse/DataWarehouse/Metadata/soak-test-report/soak-test-results.md" || echo "0"</automated>
  </verify>
  <done>10-minute soak test completed. Results report at Metadata/soak-test-report/soak-test-results.md with GC metrics, working set analysis, workload summary, and leak check. SOAK-03 and SOAK-04 assessment stated.</done>
</task>

</tasks>

<verification>
- 10-minute CI soak test ran to completion (or failure documented)
- GC metrics captured throughout duration
- Gen2 rate below threshold
- Working set not monotonically growing
- Event bus / telemetry sink leak check performed
- Results report generated with full data
</verification>

<success_criteria>
- Soak test ran for full 10-minute CI duration
- Gen2 collection rate below defined threshold
- Working set stable (no monotonic growth, growth < 10%)
- Zero checksum mismatches, zero exceptions, zero deadlocks
- No event bus or telemetry sink leaks
- Report at Metadata/soak-test-report/soak-test-results.md
</success_criteria>

<output>
After completion, create `.planning/phases/106-soak-test-harness/106-02-SUMMARY.md`
</output>
