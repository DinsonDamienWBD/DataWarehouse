---
phase: 112-spdk-full-implementation
plan: 08
type: execute
wave: 3
depends_on: [112-04, 112-05, 112-06, 112-07]
files_modified:
  - DataWarehouse.Hardening.Tests/Integration/SpdkE2ETests.cs
  - DataWarehouse.Hardening.Tests/Integration/SpdkQemuTestFixture.cs
  - DataWarehouse.SDK/VirtualDiskEngine/IO/Spdk/SpdkTestUtilities.cs
autonomous: true
requirements: [SIO-01]

must_haves:
  truths:
    - "E2E test exercises full VDE decorator chain through SPDK block device"
    - "Test validates Cache -> Integrity -> Compression -> Dedup -> Encryption -> WAL -> RAID -> SPDK path"
    - "QEMU NVMe emulation fixture enables CI execution without hardware"
    - "Tests gracefully skip on environments without QEMU/SPDK"
  artifacts:
    - path: "DataWarehouse.Hardening.Tests/Integration/SpdkE2ETests.cs"
      provides: "End-to-end tests for VDE through SPDK"
      contains: "class SpdkE2ETests"
    - path: "DataWarehouse.Hardening.Tests/Integration/SpdkQemuTestFixture.cs"
      provides: "QEMU NVMe emulation test fixture for CI"
      contains: "class SpdkQemuTestFixture"
    - path: "DataWarehouse.SDK/VirtualDiskEngine/IO/Spdk/SpdkTestUtilities.cs"
      provides: "Helper methods for SPDK test scenarios"
      contains: "class SpdkTestUtilities"
  key_links:
    - from: "SpdkE2ETests.cs"
      to: "SpdkQemuTestFixture.cs"
      via: "IClassFixture for QEMU lifecycle"
      pattern: "IClassFixture<SpdkQemuTestFixture>"
    - from: "SpdkE2ETests.cs"
      to: "BlockDeviceFactory.cs"
      via: "creates SPDK device through factory"
      pattern: "BlockDeviceFactory.Create"
---

<objective>
Create end-to-end tests exercising the full VDE decorator chain through SPDK, with QEMU NVMe emulation fixture for CI environments without physical hardware.

Purpose: The SPDK implementation must work correctly through the full VDE stack (Cache, Integrity, Compression, Dedup, Encryption, WAL, RAID, SPDK). An E2E test validates this integration. QEMU NVMe emulation enables testing in CI without real NVMe hardware.

Output: E2E test suite with QEMU fixture, graceful skip on unsupported environments.
</objective>

<execution_context>
@C:/Users/ddamien/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ddamien/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
From BlockDeviceFactory (after plan 112-04):
```csharp
public static class BlockDeviceFactory
{
    public static IBlockDevice Create(
        string path, int blockSize, long blockCount, bool createNew,
        BlockDeviceOptions? options = null, ILogger? logger = null);
    // Now creates SpdkBlockDevice when SPDK + vfio-pci available
}
```

VDE Decorator Chain order:
Cache -> Integrity -> Compression -> Dedup -> Encryption -> WAL -> RAID -> BlockDevice
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QEMU NVMe test fixture and SPDK test utilities</name>
  <files>
    DataWarehouse.Hardening.Tests/Integration/SpdkQemuTestFixture.cs,
    DataWarehouse.SDK/VirtualDiskEngine/IO/Spdk/SpdkTestUtilities.cs
  </files>
  <action>
1. Create `SpdkTestUtilities.cs` in the SDK:
   - `public static class SpdkTestUtilities`
   - `static bool IsSpdkTestEnvironment()` — returns true when SPDK is available AND either:
     - `SPDK_TEST_PCI_ADDRESS` env var is set (real hardware), or
     - QEMU NVMe emulation is running (check for `SPDK_QEMU_DEVICE` env var)
   - `static string GetTestPciAddress()` — returns env var or QEMU default address
   - `static BlockDeviceOptions CreateTestOptions(string? pciAddress = null)` — builds options with test defaults (small queue depth for testing)
   - Mark with `[SdkCompatibility("8.0.0", Notes = "Phase 112: SPDK test environment utilities")]`

2. Create `SpdkQemuTestFixture.cs`:
   - `public sealed class SpdkQemuTestFixture : IAsyncLifetime`
   - On `InitializeAsync`:
     - Check if QEMU is available (`which qemu-system-x86_64` or `QEMU_PATH` env var)
     - If not available, set `IsAvailable = false` and return (tests will skip)
     - Create a temporary NVMe backing file (e.g., 128MB raw file)
     - Start QEMU with NVMe emulation: `qemu-system-x86_64 -drive file=test.raw,format=raw,if=none,id=nvme0 -device nvme,serial=DWTEST,drive=nvme0 -nographic -no-reboot`
     - Wait for QEMU to start and device to be accessible
     - Bind device to vfio-pci (if in a VM or container with nested virt)
     - Set `PciAddress` and `IsAvailable = true`
   - On `DisposeAsync`:
     - Stop QEMU process
     - Delete temporary backing file
   - Properties: `bool IsAvailable`, `string? PciAddress`, `Process? QemuProcess`
   - Note: On Windows CI, QEMU fixture simply sets `IsAvailable = false` — all tests skip gracefully
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>QEMU test fixture and SPDK test utilities compile. Fixture gracefully reports unavailable on non-Linux CI.</done>
</task>

<task type="auto">
  <name>Task 2: Create SPDK E2E integration tests</name>
  <files>DataWarehouse.Hardening.Tests/Integration/SpdkE2ETests.cs</files>
  <action>
Create `SpdkE2ETests.cs`:

1. Class: `public class SpdkE2ETests : IClassFixture<SpdkQemuTestFixture>`
2. All tests use `Skip` attribute or runtime skip when `!_fixture.IsAvailable` — this means they pass with skip on Windows CI but execute on Linux with QEMU/hardware

3. Test methods:

   a. `SpdkBlockDevice_ReadWriteRoundtrip` — Write 1 block, read it back, verify data matches

   b. `SpdkBlockDevice_BatchReadWrite` — Write 10 blocks in batch, read them back in batch, verify all data

   c. `SpdkBlockDevice_FlushPersistsData` — Write block, flush, read back from new device instance

   d. `SpdkBlockDevice_ViaFactory` — Use `BlockDeviceFactory.Create` with `ForceImplementation = Spdk` and verify correct device type returned

   e. `SpdkBlockDevice_FullDecoratorChain` — Build the full VDE decorator stack:
      - Create SpdkBlockDevice (or QEMU-emulated)
      - Wrap in CompoundBlockDevice (RAID 1 with file-backed mirror)
      - Apply WAL decorator
      - Apply Encryption decorator
      - Apply Dedup decorator
      - Apply Compression decorator
      - Apply Integrity decorator
      - Apply Cache decorator
      - Write data through full stack, read back, verify correctness
      Note: If full decorator stack construction is complex, simplify to the 2-3 most critical decorators (Cache + WAL + SPDK) that validate the integration path

   f. `NvmeNamespaceProbe_DiscoverCapabilities` — Probe the QEMU NVMe device and verify capabilities record is populated

   g. `ZnsSpdkBlockDevice_ZoneOperations` (conditional on ZNS device) — Open zone, write sequentially, report zones, reset zone

4. Each test:
   - Wraps device in `await using` for proper cleanup
   - Uses `Assert.Skip("SPDK not available")` when fixture unavailable
   - Has meaningful test data (not all zeros — use deterministic pattern)

Mark class with `[Collection("SPDK")]` to prevent parallel execution (SPDK is single-process).
  </action>
  <verify>
    <automated>cd C:/Temp/DataWarehouse/DataWarehouse && dotnet build DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --no-restore -v q 2>&1 | tail -5 && dotnet test DataWarehouse.Hardening.Tests/DataWarehouse.Hardening.Tests.csproj --filter "FullyQualifiedName~SpdkE2E" --no-restore -v q 2>&1 | tail -10</automated>
  </verify>
  <done>SPDK E2E tests compile and either pass (with SPDK) or skip gracefully (without SPDK). Full decorator chain test validates integration path.</done>
</task>

</tasks>

<verification>
- All SPDK tests compile
- Tests skip gracefully on Windows CI (no SPDK available)
- On Linux with QEMU/hardware, tests exercise full I/O path
- E2E decorator chain test validates Cache -> ... -> SPDK integration
</verification>

<success_criteria>
Full E2E test coverage for SPDK through VDE decorator chain. QEMU fixture enables CI testing. All tests compile and skip gracefully on non-SPDK environments. Phase 112 (SIO-01) is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/112-spdk-full-implementation/112-08-SUMMARY.md`
</output>
