<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <Description>Geo-distributed consensus plugin with multi-datacenter Raft, hierarchical quorums, witness nodes, leader leasing, flexible Paxos, geo-aware replication, conflict resolution, partition handling, latency optimization, split-brain prevention, hybrid logical clocks, and safe membership reconfiguration</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\DataWarehouse.SDK\DataWarehouse.SDK.csproj" />
  </ItemGroup>

</Project>
